<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="4.3.4">Jekyll</generator><link href="https://hvppycoding.github.io/feed.xml" rel="self" type="application/atom+xml" /><link href="https://hvppycoding.github.io/" rel="alternate" type="text/html" /><updated>2026-07-14T00:06:21+09:00</updated><id>https://hvppycoding.github.io/feed.xml</id><title type="html">ARCHIVE</title><subtitle>An amazing archive.</subtitle><author><name>hvppycoding</name></author><entry><title type="html">OpenCode · Claude Code · Codex · Farad 아키텍처 교차 비교</title><link href="https://hvppycoding.github.io/research/opencode-claude-code-codex-farad-comparison/" rel="alternate" type="text/html" title="OpenCode · Claude Code · Codex · Farad 아키텍처 교차 비교" /><published>2026-07-13T23:55:00+09:00</published><updated>2026-07-13T23:55:00+09:00</updated><id>https://hvppycoding.github.io/research/opencode-claude-code-codex-farad-comparison</id><content type="html" xml:base="https://hvppycoding.github.io/research/opencode-claude-code-codex-farad-comparison/"><![CDATA[<h1 id="opencode--claude-code--codex--farad-아키텍처-교차-비교">OpenCode · Claude Code · Codex · Farad 아키텍처 교차 비교</h1>

<blockquote>
  <p><a href="https://hvppycoding.github.io/research/coding-agent-architecture-comparison/">전체 보고서 인덱스</a></p>
</blockquote>

<blockquote>
  <p>조사 기준일: 2026-07-13<br />
전제: 이 문서는 네 제품을 한 줄짜리 기능표로 환산하지 않는다. 각 제품의 최적화 대상,
state authority, 실행 경계, 저장 비용이 다르므로 “어디에 쓰느냐”를 포함해 판정한다.<br />
세부 근거: <a href="https://hvppycoding.github.io/research/opencode-architecture-analysis/">OpenCode</a> · <a href="https://hvppycoding.github.io/research/claude-code-architecture-analysis/">Claude Code</a> ·
<a href="https://hvppycoding.github.io/research/codex-architecture-analysis/">Codex</a> · <a href="https://hvppycoding.github.io/research/farad-architecture-audit/">Farad</a></p>
</blockquote>

<h2 id="1-최종-요약">1. 최종 요약</h2>

<p>네 제품은 같은 “coding agent”라는 이름 아래 서로 다른 시스템을 만들었다.</p>

<table>
  <thead>
    <tr>
      <th>제품</th>
      <th>실제 정체성</th>
      <th>가장 강한 축</th>
      <th>가장 큰 구조 리스크</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>OpenCode</td>
      <td>provider-neutral HTTP agent server와 여러 client surface</td>
      <td>개방형 provider·server/API·SDK·확장성</td>
      <td>V1/V2 이중 구조, 현 V1의 history read amplification, host-level 격리 부재</td>
    </tr>
    <tr>
      <td>Claude Code</td>
      <td>여러 surface를 가진 Claude 중심 workflow 제품; local CLI/SDK는 process-local runtime</td>
      <td>agent loop 완성도, context/permission/hook, 장기 자율 workflow</td>
      <td>Claude 결합, local surface의 process/file affinity, opt-in sandbox, 공개되지 않은 current 내부</td>
    </tr>
    <tr>
      <td>Codex</td>
      <td>typed thread/turn kernel과 여러 OpenAI client surface</td>
      <td>ordered command/event protocol, OS sandbox, replay/index, subagent lifecycle</td>
      <td>매우 큰 Rust composition/feature matrix, OpenAI 중심, protocol 운영 복잡도</td>
    </tr>
    <tr>
      <td>Farad</td>
      <td>provider-neutral native desktop agent application</td>
      <td>native Qt product와 엄격한 explicit-refresh/HPC/NFS 설계 계약; 구현 위반 존재</td>
      <td>command containment 부재, runtime/UI hotspot, 확인된 문서-실행 drift와 hot-path 위반</td>
    </tr>
  </tbody>
</table>

<p>이 결과는 “Codex 1등, Claude 2등” 같은 단일 순위로 요약할 수 없다.</p>

<ul>
  <li><strong>provider와 embeddable server가 우선</strong>이면 OpenCode가 가장 자연스럽다.</li>
  <li><strong>개발자 개인의 가장 깊은 terminal workflow</strong>는 Claude Code가 강하다.</li>
  <li><strong>지원 플랫폼의 기본 local command containment와 surface-independent runtime protocol</strong>은 Codex가
기준점이다. 이것이 remote MCP나 모든 side effect까지 자동 격리한다는 뜻은 아니다.</li>
  <li><strong>멀티 provider native desktop, scheduler, shared HPC/NFS 운영</strong>은 Farad가 가장 명시적인 설계 대상으로
삼았지만, current hot path가 그 목표를 아직 모두 충족하지 않는다.</li>
</ul>

<p>Farad에 대한 결론도 “다 갈아엎어야 한다”가 아니다. Farad는 이미 callback-inverted async loop,
Qt/async 분리, per-turn snapshot, append-oriented transcript, capability exposure, explicit refresh를 갖고
있다. 부족한 것은 agent loop 자체보다 <strong>OS enforcement, 몇 개 authority 경계의 구현 일치성,
누적 hot path의 실증, 거대 session projection 책임 분해</strong>다.</p>

<h2 id="2-비교-전제와-버전-경계">2. 비교 전제와 버전 경계</h2>

<table>
  <thead>
    <tr>
      <th>제품</th>
      <th>분석 기준</th>
      <th>구현 투명성</th>
      <th>주의할 점</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>OpenCode</td>
      <td><code class="language-plaintext highlighter-rouge">anomalyco/opencode@b3a012c…</code>, package 1.17.18</td>
      <td>공개 MIT 소스</td>
      <td>V1 primary path와 진행 중 V2를 분리</td>
    </tr>
    <tr>
      <td>Claude Code</td>
      <td>조사 당시 npm <code class="language-plaintext highlighter-rouge">latest</code> 2.1.207의 공식 계약 + 로컬 설치 2.1.206 + 로컬 2.1.88 sourcemap 복원</td>
      <td>제품 비공개, 복원본은 관찰 증거</td>
      <td>공식 표면의 동작 계약과 서로 다른 버전의 내부 구현을 동일시하지 않음</td>
    </tr>
    <tr>
      <td>Codex</td>
      <td><code class="language-plaintext highlighter-rouge">openai/codex@2b0b37a…</code> + 공식 문서</td>
      <td>local core/app-server 공개 Apache-2.0</td>
      <td>호스티드 backend를 local core와 동일시하지 않음</td>
    </tr>
    <tr>
      <td>Farad</td>
      <td>2026-07-13 현재 dirty worktree</td>
      <td>현재 소스·테스트·문서 직접 확인</td>
      <td>설계 문서와 실제 반복 경로가 다르면 소스를 우선</td>
    </tr>
  </tbody>
</table>

<p>Claude Code만 구현 증거의 등급이 다르다. 따라서 내부 모듈 품질 비교에서 Claude의 내부 구현 평가는 현재
공개 제품 계약보다 불확실성이 크다. 반대로 OpenCode와 Codex의 공개 소스가 있다는 이유만으로
호스티드 서비스까지 공개됐다고 보지 않는다.</p>

<h2 id="3-네-아키텍처를-한-그림에-놓으면">3. 네 아키텍처를 한 그림에 놓으면</h2>

<pre class="mermaid">
flowchart TB
    subgraph OC[OpenCode]
        OCC[&quot;TUI/Web/Desktop/IDE/SDK&quot;] --&gt; OAPI[&quot;transport-independent HTTP app&lt;br/&gt;Worker direct fetch 또는 real HTTP&quot;]
        OAPI --&gt; OINST[&quot;directory-scoped Effect instance&quot;]
        OINST --&gt; OLOOP[&quot;V1 SessionPrompt — current primary&quot;]
        OINST -.-&gt;|migration| OV2[&quot;V2 typed runner/API — incomplete&quot;]
        OLOOP --&gt; OE[(&quot;shared durable EventV2 log + projector&quot;)]
        OV2 --&gt; OE
        OE --&gt; ODB[(&quot;V1 JSON rows / V2 typed projection&quot;)]
        OLOOP -.-&gt;|high-frequency live delta| OSSE[&quot;legacy bus/SSE — non-durable&quot;]
    end

    subgraph CC[Claude Code]
        CCLOCAL[&quot;CLI/IDE/Remote Control&quot;] --&gt; CCP[&quot;local session process&quot;]
        CCSDK[&quot;Agent SDK host&quot;] --&gt; CCSUB[&quot;CLI subprocess per live SDK session&quot;]
        CCDW[&quot;Desktop/Web&quot;] --&gt; CCUNK[&quot;official behavior contract&lt;br/&gt;internal topology unknown&quot;]
        CCP --&gt; CCLOOP[&quot;query loop&lt;br/&gt;(내부명은 2.1.88 관찰)&quot;]
        CCSUB --&gt; CCLOOP
        CCLOOP --&gt; CCFS[&quot;working tree + JSONL&quot;]
        CCLOOP --&gt; CCT[&quot;tools/hooks/MCP/sandbox&quot;]
    end

    subgraph CX[Codex]
        CXC[&quot;TUI/IDE/custom client/exec&quot;] --&gt; CXRPC[&quot;core queue or experimental app-server RPC&quot;]
        CXRPC --&gt; CXTH[&quot;ThreadManager + Session&quot;]
        CXTH --&gt; CXTURN[&quot;single active turn + ToolRouter&quot;]
        CXTURN --&gt; CXJ[(&quot;canonical JSONL&quot;)]
        CXTURN --&gt; CXS[(&quot;SQLite metadata projection&quot;)]
    end

    subgraph FA[Farad]
        FUI[&quot;PySide6 UI / UiSession&quot;] --&gt;|typed proxy| FAS[&quot;AsyncSession&quot;]
        FAS -.-&gt;|AgentEvent| FUI
        FAS --&gt; FQL[&quot;callback-inverted query_loop&quot;]
        FQL --&gt; FCAP[&quot;CapabilityCatalog / tools / permission / MCP&quot;]
        FAS --&gt; FTR[(&quot;chunked append transcript&quot;)]
        FUI --&gt; FIDX[(&quot;session metadata/index&quot;)]
    end

</pre>

<p>이 그림에서 가장 큰 차이는 UI toolkit이 아니다. 아래의 “권위”는 저장·filesystem까지 포괄하는
전역 권위가 아니라 <strong>실행 중인 한 run/turn을 순서화하는 active-run authority</strong>를 뜻한다.</p>

<ul>
  <li>OpenCode는 active run을 server/session runtime이, conversation을 SQLite가, 실제 side effect를
filesystem/host process가 소유한다.</li>
  <li>Claude Code의 확인 가능한 local CLI/SDK에서는 current turn을 session process가, conversation을
JSONL이, code state를 working tree가 소유한다. Desktop/Web 내부 authority는 판단 보류다.</li>
  <li>Codex는 active turn을 thread/session queue consumer가, conversation을 JSONL rollout이, 목록 조회를
SQLite projection이 소유한다.</li>
  <li>Farad는 async active-run authority와 Qt projection authority를 나누고, transcript와 session index에
서로 다른 영속 책임을 둔다.</li>
</ul>

<h2 id="4-공통-아키텍처-매트릭스">4. 공통 아키텍처 매트릭스</h2>

<p><code class="language-plaintext highlighter-rouge">강/중/약</code>은 공통 evidence에서 도출한 <strong>D급 압축 판정</strong>이다. 메커니즘 존재를 운영 품질로
승격하거나 제품 간 산술 점수로 읽으면 안 된다. <code class="language-plaintext highlighter-rouge">조건부</code>는 기능은 있으나 opt-in, 전환 중, 공개
증거 부족, 또는 운영자가 별도 경계를 제공해야 함을 뜻한다.</p>

<table>
  <thead>
    <tr>
      <th>축</th>
      <th>OpenCode</th>
      <th>Claude Code</th>
      <th>Codex</th>
      <th>Farad</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>주 제품 형태</td>
      <td>server + clients</td>
      <td>multi-surface Claude workflow; local CLI/SDK process runtime 확인</td>
      <td>runtime kernel + clients</td>
      <td>native desktop app</td>
    </tr>
    <tr>
      <td>core/surface 분리</td>
      <td>강</td>
      <td>official behavior 공통, current 내부 split 미공개</td>
      <td>매우 강</td>
      <td>강</td>
    </tr>
    <tr>
      <td>한 session active-run authority</td>
      <td>강</td>
      <td>local CLI/SDK 강, Desktop/Web 미공개</td>
      <td>매우 강</td>
      <td>강</td>
    </tr>
    <tr>
      <td>ordered control command</td>
      <td>중</td>
      <td>local interactive contract, current 내부 ordering 미공개</td>
      <td>매우 강</td>
      <td>중~강</td>
    </tr>
    <tr>
      <td>typed streamed event schema</td>
      <td>강</td>
      <td>강</td>
      <td>매우 강</td>
      <td>강</td>
    </tr>
    <tr>
      <td>event delivery/backpressure</td>
      <td>legacy SSE/PubSub unbounded; V2 replay route 별도</td>
      <td>streamed contract 존재, current queue bound 미공개</td>
      <td>submission 512 bounded, core event unbounded</td>
      <td>Qt queued delivery, end-to-end bound 미감사</td>
    </tr>
    <tr>
      <td>검증된 provider breadth</td>
      <td>매우 강</td>
      <td>중, Claude 배포 경로 중심</td>
      <td>중, OpenAI 중심</td>
      <td>중, GLM/Kimi 중심</td>
    </tr>
    <tr>
      <td>core의 provider semantic 결합도</td>
      <td>낮음</td>
      <td>높음</td>
      <td>중~높음</td>
      <td>낮음</td>
    </tr>
    <tr>
      <td>tool schema/dispatch 분리</td>
      <td>중~강</td>
      <td>중(2.1.88 Tool 결합 관찰)</td>
      <td>매우 강</td>
      <td>강</td>
    </tr>
    <tr>
      <td>deferred capability exposure</td>
      <td>일부/진행</td>
      <td>강</td>
      <td>매우 강</td>
      <td>강</td>
    </tr>
    <tr>
      <td>permission policy</td>
      <td>강한 설정력·기본 permissive</td>
      <td>매우 강</td>
      <td>매우 강</td>
      <td>강</td>
    </tr>
    <tr>
      <td>OS command sandbox</td>
      <td>약</td>
      <td>지원 OS 강·opt-in, native Windows 없음</td>
      <td>강한 기본 구조이나 platform/kernel/profile/full-access 조건부</td>
      <td>약</td>
    </tr>
    <tr>
      <td>canonical conversation replay</td>
      <td>lifecycle durable log 존재; legacy SSE 약, V2 replay API 강</td>
      <td>강</td>
      <td>매우 강</td>
      <td>강</td>
    </tr>
    <tr>
      <td>query index/projection</td>
      <td>event-projected SQLite rows; updated-order index·10k 실증 부족</td>
      <td>약~중</td>
      <td>강한 구조, 실증 필요</td>
      <td>session index 강; transcript rewind·repair gap 확인</td>
    </tr>
    <tr>
      <td>context compaction</td>
      <td>강</td>
      <td>매우 강</td>
      <td>매우 강</td>
      <td>강</td>
    </tr>
    <tr>
      <td>subagent lifecycle</td>
      <td>중~강</td>
      <td>매우 강</td>
      <td>매우 강</td>
      <td>강</td>
    </tr>
    <tr>
      <td>interactive background delegation</td>
      <td>중, experimental/process-local</td>
      <td>강, surface별 제약</td>
      <td>강, child thread lifecycle</td>
      <td>강, 앱 runtime</td>
    </tr>
    <tr>
      <td>durable scheduled execution</td>
      <td>근거 없음</td>
      <td>local <code class="language-plaintext highlighter-rouge">/loop</code>는 process-bound; cloud 별도</td>
      <td>본 조사 증거로 판단 보류</td>
      <td>강한 제품 domain, hot-path 감사 필요</td>
    </tr>
    <tr>
      <td>skill/procedural context</td>
      <td>강</td>
      <td>매우 강</td>
      <td>강</td>
      <td>강, lazy body와 exposure</td>
    </tr>
    <tr>
      <td>plugin/hook execution</td>
      <td>강, host JS trust surface</td>
      <td>매우 강, 넓은 hook trust surface</td>
      <td>중~강, extension별 계약</td>
      <td>조건부, data plugin은 강하나 plugin-hook executable wiring 미확인</td>
    </tr>
    <tr>
      <td>MCP lifecycle</td>
      <td>강</td>
      <td>강</td>
      <td>강</td>
      <td>강, workspace server는 미완</td>
    </tr>
    <tr>
      <td>desktop surface/features·감사 가능성</td>
      <td>Electron beta·public client source</td>
      <td>기능 강, current 내부 비공개</td>
      <td>기능 강, 일부 내부 비공개</td>
      <td>native Qt 기능 강, local source 감사 가능</td>
    </tr>
    <tr>
      <td>headless automation</td>
      <td>매우 강</td>
      <td>강, subprocess SDK</td>
      <td>매우 강</td>
      <td>약~중</td>
    </tr>
    <tr>
      <td>embedding surface 존재</td>
      <td>HTTP/OpenAPI/SDK</td>
      <td>CLI subprocess hosting</td>
      <td>experimental app-server/custom client</td>
      <td>in-process app 중심</td>
    </tr>
    <tr>
      <td>public stability</td>
      <td>강한 surface, V1/V2 drift 조건부</td>
      <td>공식 SDK 계약, process hosting 비용</td>
      <td>app-server experimental</td>
      <td>별도 public application protocol 없음</td>
    </tr>
    <tr>
      <td>local-store topology fit</td>
      <td>SQLite/WAL, benchmark 별도</td>
      <td>JSONL/file affinity, current scale 미공개</td>
      <td>JSONL+SQLite, 10k 미실증</td>
      <td>chunk/index, 실제 hot-path 위반 확인</td>
    </tr>
    <tr>
      <td>NFS/shared HPC 계약</td>
      <td>약</td>
      <td>약</td>
      <td>명시 계약 없음, 별도 검증</td>
      <td>매우 강(설계), 반복 경로 위반 확인</td>
    </tr>
    <tr>
      <td>N=10,000 명시적 계약</td>
      <td>약</td>
      <td>약</td>
      <td>중(구조 유리, 실증 미확인)</td>
      <td>매우 강(설계), 구현 위반 확인</td>
    </tr>
    <tr>
      <td>구현 단순성</td>
      <td>약~중</td>
      <td>current 판단 보류; 2.1.88 복잡도 관찰</td>
      <td>약</td>
      <td>중</td>
    </tr>
    <tr>
      <td>구현 감사 가능성</td>
      <td>매우 강, public</td>
      <td>약, 제품 비공개</td>
      <td>매우 강, local core public</td>
      <td>매우 강, 현재 local source</td>
    </tr>
    <tr>
      <td>배포·license posture</td>
      <td>MIT 공개 소스</td>
      <td>proprietary 제품</td>
      <td>local core Apache-2.0, hosted 별도</td>
      <td>현재 worktree에 배포 license 선언 미확인</td>
    </tr>
  </tbody>
</table>

<h2 id="5-active-run-authority와-threadprocess-ownership">5. Active-run authority와 thread/process ownership</h2>

<p>여기서 active-run owner와 저장 자원을 혼동하면 “세션 10,000개 = 프로세스 10,000개” 같은 잘못된
비교가 생긴다.</p>

<table>
  <thead>
    <tr>
      <th>제품</th>
      <th>historical persisted</th>
      <th>loaded/runtime-resident</th>
      <th>concurrently active execution</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>OpenCode</td>
      <td>SQLite session/message/part</td>
      <td>directory-scoped instance cache와 session state</td>
      <td>같은 session의 run state 하나</td>
    </tr>
    <tr>
      <td>Claude Code</td>
      <td>JSONL session 파일</td>
      <td>재개하거나 열린 session state</td>
      <td>실행 중 Agent SDK/agent-view session별 local process</td>
    </tr>
    <tr>
      <td>Codex</td>
      <td>JSONL rollout + SQLite metadata projection</td>
      <td>ThreadManager가 연 thread/session</td>
      <td>thread별 active task 최대 하나</td>
    </tr>
    <tr>
      <td>Farad</td>
      <td>chunked transcript + session metadata/index</td>
      <td>앱이 연 UiSession/AsyncSession projection</td>
      <td>session별 turn task 최대 하나</td>
    </tr>
  </tbody>
</table>

<p>이 표의 loaded 비용과 historical 목록 비용, active CPU/process 비용은 각각 따로 측정해야 한다.</p>

<h3 id="51-opencode-directory-scoped-server-authority">5.1 OpenCode: directory-scoped server authority</h3>

<p>OpenCode client는 directory/session을 지정하고 server가 해당 normalized directory의 Effect service
graph를 선택한다. 같은 session의 run은 <code class="language-plaintext highlighter-rouge">SessionRunState</code>가 하나로 직렬화한다. 장점은 client를
추가해도 runtime이 하나라는 점이다. 단점은 server credential이 사실상 해당 OS user가 볼 수 있는
directory와 command 전반의 capability가 된다는 점과, long-lived instance cache에 명시적 LRU/TTL이
없다는 점이다.</p>

<h3 id="52-claude-code-local-clisdk-process-isolation으로-단순화">5.2 Claude Code local CLI/SDK: process isolation으로 단순화</h3>

<p>확인 가능한 local CLI는 conversation owner와 cwd, tools, state를 한 local process에 묶는다. SDK도
가벼운 in-process library라기보다 session별 CLI subprocess를 띄우는 방식이다. 이 선택은 singleton과
ambient state를 process boundary로 격리해 개인 workflow를 단순화한다. 하지만 <strong>동시에 실행 중인</strong>
SDK/agent-view session 수만큼 memory, process tree, routing, local disk affinity가 늘어난다. 과거에
저장된 session 수가 process 수로 직결되는 것은 아니다. process-global state의 세부 구현은 2.1.88
sourcemap 관찰(C)이며 현재 2.1.207 내부나 Desktop/Web topology와 같다고 단정하지 않는다.</p>

<h3 id="53-codex-ordered-queue를-소비하는-threadsession">5.3 Codex: ordered queue를 소비하는 thread/session</h3>

<p>Codex의 <code class="language-plaintext highlighter-rouge">Submission in / Event out</code>은 가장 명확한 control plane이다. settings update, input,
interrupt, approval, refresh가 같은 queue ordering을 타며 session에는 active task가 하나뿐이다.
client가 mutable session field를 직접 고치지 않으므로 “어떤 설정으로 이 turn이 시작했는가”를
replay하기 쉽다.</p>

<h3 id="54-farad-ui-projection과-async-execution-분리">5.4 Farad: UI projection과 async execution 분리</h3>

<p>Farad는 <code class="language-plaintext highlighter-rouge">UiSession</code>이 Qt-thread 화면 상태와 signal을, <code class="language-plaintext highlighter-rouge">AsyncSession</code>이 transcript projection,
turn task, permission rendezvous, tool cancellation을 소유한다. proxy와 queued signal이 두 thread를
연결한다. 이는 native GUI에 적합한 명시적 ownership이다. current Claude Code 전체 제품과 내부
응집도를 직접 우열 비교할 공개 근거는 없다. 단, boundary를 우회해 Qt가 async-side mutable object를
직접 읽거나, async loop가 동기 disk I/O를 수행하면 장점이 즉시
사라진다. 따라서 문서가 아니라 각 call path의 thread ownership test가 중요하다.</p>

<h3 id="판정">판정</h3>

<ul>
  <li>가장 명시적 ordering: <strong>Codex</strong></li>
  <li>다중 client 권위: <strong>OpenCode</strong></li>
  <li>확인 가능한 local surface에서 가장 단순한 process isolation: <strong>Claude Code</strong></li>
  <li>rich desktop에 맞는 이중 projection: <strong>Farad</strong></li>
</ul>

<p>Farad는 Codex queue를 그대로 복제할 필요는 없다. 다만 <code class="language-plaintext highlighter-rouge">submit/start/steer/cancel/approve/settings
snapshot</code>의 ordering을 현재 proxy/event contract에 명시하고 테스트할 필요가 있다.</p>

<h2 id="6-agent-loop와-상태-머신">6. Agent loop와 상태 머신</h2>

<p>네 제품의 기본 loop는 모두 <code class="language-plaintext highlighter-rouge">model stream → tool decision → execute → append result → continue</code>다.
차이는 loop가 무엇까지 소유하느냐다.</p>

<table>
  <thead>
    <tr>
      <th>제품</th>
      <th>loop owner</th>
      <th>iteration state</th>
      <th>terminal/recovery 특징</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>OpenCode</td>
      <td><code class="language-plaintext highlighter-rouge">SessionPrompt</code> + <code class="language-plaintext highlighter-rouge">SessionProcessor</code></td>
      <td>DB message/parts + run state</td>
      <td>compaction/subtask/plugin/tool을 중앙 조정; V2로 이전 중</td>
    </tr>
    <tr>
      <td>Claude Code</td>
      <td>query loop (<code class="language-plaintext highlighter-rouge">QueryEngine/queryLoop</code>는 2.1.88 관찰 C)</td>
      <td>message state, compact/recovery/turn/budget</td>
      <td>공식 행동 계약은 세밀, 내부명은 현재 미확인</td>
    </tr>
    <tr>
      <td>Codex</td>
      <td><code class="language-plaintext highlighter-rouge">Session/run_turn</code></td>
      <td>turn-scoped <code class="language-plaintext highlighter-rouge">TurnContext</code> + request-scoped <code class="language-plaintext highlighter-rouge">StepContext</code>, history, active task</td>
      <td>ordered Op, compaction, tool routing, typed events</td>
    </tr>
    <tr>
      <td>Farad</td>
      <td><code class="language-plaintext highlighter-rouge">AsyncSession/query_loop</code></td>
      <td><code class="language-plaintext highlighter-rouge">TurnState</code>, <code class="language-plaintext highlighter-rouge">TurnContext</code>, callbacks</td>
      <td>flat async generator, terminal/transition event, abort invariant</td>
    </tr>
  </tbody>
</table>

<p>Farad와 2.1.88 sourcemap에서 관찰한 Claude Code의 flat async generator는 구조적으로 가깝다(C).
현재 2.1.207이 같은 내부 구현을 유지한다는 뜻은 아니다. Farad의 <code class="language-plaintext highlighter-rouge">query_loop</code>는 app-layer class를
import하지 않고 callback으로 system prompt, model, permission, compaction, tool execution을 받는다.
이것은 테스트성과 SDK/app 경계를 지키는 좋은 선택이다.</p>

<p>OpenCode의 현재 V1은 매 step에서 DB의 compacted view를 다시 materialize한다. DB authority와 client
projection은 좋지만 loop hot path가 누적 history에 비례한다. 진행 중 V2는 context epoch 이후만
읽는 방향으로 이를 고치려 한다.</p>

<p>Codex의 특별한 강점은 loop 내부가 아니라 loop로 들어오는 control operation의 ordering이다.
turn configuration은 <code class="language-plaintext highlighter-rouge">TurnContext</code>가 고정하고, 각 model sampling request에 보이는 environment,
capability root, MCP runtime/tool list, <code class="language-plaintext highlighter-rouge">AGENTS.md</code>는 <code class="language-plaintext highlighter-rouge">StepContext</code>가 같은 request view로 묶는다.
Claude/Farad도 active turn 하나를 지키지만, Codex는 live settings mutation까지 command stream에 넣어
재현성을 높인다. 단 DeferredExecutor가 켜진 Codex는 sampling request 경계에서 filesystem-derived
state를 refresh할 수 있으므로, 이 freshness 정책은 Farad의 explicit-refresh 계약과 같지 않다.</p>

<h2 id="7-context-transcript-compaction-world-state">7. Context, transcript, compaction, world state</h2>

<h3 id="71-공통된-좋은-방향">7.1 공통된 좋은 방향</h3>

<p>네 제품 모두 다음 사실을 인정한다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>raw transcript != active conversation chain != model request context != UI projection
</code></pre></div></div>

<ul>
  <li>raw transcript는 감사와 resume를 위한 장기 기록이다.</li>
  <li>compaction은 raw history를 없애는 대신 model view를 대체한다.</li>
  <li>skills/instructions/tool schema/world state는 request마다 별도 규칙으로 합쳐진다.</li>
  <li>UI streaming placeholder는 durable conversation authority가 아니다.</li>
</ul>

<h3 id="72-제품별-차이">7.2 제품별 차이</h3>

<p><strong>OpenCode</strong>의 현재 primary V1은 SQLite의 mutable message/part JSON projection에 상세 tool lifecycle을
보존하고 compaction summary를 추가한다. lifecycle 변경은 이미 공용 EventV2 durable log에 append되고
같은 transaction에서 projection되며, 고빈도 <code class="language-plaintext highlighter-rouge">message.part.delta</code>와 legacy SSE만 비영속이다. 진행 중인
V2는 durable substrate를 새로 만드는 것이 아니라 typed session message/input/context projection,
context epoch, 새 runner/API를 얹는다. V2는 compaction continuation과 replay-then-live API도 구현했지만
durable execution ownership, bounded retry, plugin/MCP parity 등은 남아 있다. V1은 과거 row를 읽은 뒤
버리는 read amplification이 약점이다.</p>

<p><strong>Claude Code</strong>는 append-oriented JSONL chain, resume/fork, file checkpoint를 제공한다. prompt cache를
위해 stable prefix를 적극 관리하고, snip/microcompact/context collapse/autocompact 및 large output
spill을 조합한다. 하지만 session JSONL과 working tree는 원자적 transaction이 아니며, compaction
summary가 nested instruction이나 오래된 skill 세부를 잃을 수 있다.</p>

<p><strong>Codex</strong>는 rollout item에 response, inter-agent communication, turn context, world state,
compaction을 타입으로 구분한다. JSONL을 durable replay 원본으로 두고 SQLite는 조회 projection이다.
원본과 index authority가 가장 명료하다.</p>

<p><strong>Farad</strong>는 append-only transcript, chunked storage, marker twin, per-turn reminder, runtime metadata의
on-demand tool 분리를 명시한다. <code class="language-plaintext highlighter-rouge">working_folder_meta</code> 같은 byte-stable marker는 변경된 대화성 context만
transcript에 남겨 재현성과 prompt cache를 동시에 지킨다. 중요한 질문은 disk append와 in-memory/UI
projection 사이 failure ordering, 큰 transcript의 active-window read가 실제 반복 경로에서도 bounded인지다.</p>

<h3 id="판정-1">판정</h3>

<ul>
  <li>가장 세밀한 개인 coding context 최적화: <strong>Claude Code</strong></li>
  <li>가장 명시적인 replay/index authority: <strong>Codex</strong></li>
  <li>provider/server DB projection: <strong>OpenCode</strong></li>
  <li>NFS와 대화 재현성을 함께 명세한 방향: <strong>Farad</strong></li>
</ul>

<h2 id="8-tool-architecture와-capability-exposure">8. Tool architecture와 capability exposure</h2>

<h3 id="공통-pipeline">공통 pipeline</h3>

<p>model-visible tool path는 대체로 다음 단계로 수렴한다. hook, permission, spill, persistence는
제품과 tool 종류에 따라 optional이며, local OS containment는 generic execute와 별도 경계다.</p>

<pre class="mermaid">
flowchart LR
    DISC[Discover/load] --&gt; CAT[Catalog]
    CAT --&gt; EXP[Per-turn exposure]
    EXP --&gt; MODEL[Model schema]
    MODEL --&gt; VAL[Validate input]
    VAL --&gt; POLICY[Optional hook + permission]
    POLICY --&gt; ROUTE[Tool-kind adapter]
    ROUTE --&gt; INPROC[In-process built-in]
    ROUTE --&gt; LOCAL[Local child process]
    LOCAL --&gt; CONTAIN[OS containment when available]
    ROUTE --&gt; REMOTE[Remote MCP trust/transport]
    INPROC --&gt; BOUND[Bound/spill result]
    CONTAIN --&gt; BOUND
    REMOTE --&gt; BOUND
    BOUND --&gt; EVENT[Persist + event]

</pre>

<p>차이는 각 칸을 얼마나 명시적으로 분리했는가다.</p>

<ul>
  <li>Codex는 dispatch registry와 <code class="language-plaintext highlighter-rouge">model_visible_specs</code>를 타입상 분리하고
Direct/Deferred/DirectModelOnly/Hidden exposure를 per-step plan으로 만든다.</li>
  <li>Farad는 Registry/Factory/Loader와 CapabilityCatalog, Off/Searchable/Active exposure, run readiness
snapshot을 분리한다.</li>
  <li>Claude Code는 ToolSearch, skill lazy body, streamed tool executor, conservative concurrency flag로
prompt와 wall time을 줄인다.</li>
  <li>OpenCode는 built-in/custom/MCP tool을 provider adapter 위에 조합하고 output truncation을 갖지만,
current V1 resolver와 V2 tool execution이 전환 중이다.</li>
</ul>

<h3 id="병렬성">병렬성</h3>

<ul>
  <li>Claude Code와 Codex는 safe/read tool만 병렬, mutation은 직렬이라는 fail-closed 기본에 가깝다.
Claude의 schema/call/permission/concurrency/result/TUI render 결합은 2.1.88 관찰(C)이고 현재 내부는
공개되지 않았다.</li>
  <li>OpenCode는 underlying AI SDK/tool declaration과 Effect concurrency를 사용하므로 tool별 안전성 및
shared filesystem 충돌 검증이 중요하다.</li>
  <li>Farad도 parallel dispatch와 per-tool cancellation을 갖지만, concurrency declaration과 terminal
event invariant가 모든 built-in/MCP path에서 동일한지 회귀 테스트가 필요하다.</li>
</ul>

<h3 id="판정-2">판정</h3>

<p>schema budget과 dispatch separation의 기준점은 Codex다. Farad는 이 분야에서 뒤처진다기보다 이미
유사한 product-specific 구조를 갖는다. 추가 abstraction보다 catalog/exposure/readiness의 <strong>동일
snapshot 사용</strong>과 국소 invalidation을 입증하는 것이 우선이다.</p>

<h2 id="9-permission과-실제-command-containment">9. Permission과 실제 command containment</h2>

<p>이 축에서는 용어를 엄격히 구분해야 한다.</p>

<table>
  <thead>
    <tr>
      <th>제품</th>
      <th>policy decision</th>
      <th>OS/process enforcement</th>
      <th>기본 함의</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>OpenCode</td>
      <td>wildcard allow/ask/deny, tool-specific parser</td>
      <td>내장 host sandbox 확인 안 됨</td>
      <td>승인은 있으나 shell/plugin은 host user 권한</td>
    </tr>
    <tr>
      <td>Claude Code</td>
      <td>mode/rules/hooks/classifier</td>
      <td>macOS Seatbelt/Linux·WSL bubblewrap sandbox, opt-in; native Windows 미지원</td>
      <td>지원 OS에서 켜면 강함; unavailable 시 fail-open 가능 unless configured</td>
    </tr>
    <tr>
      <td>Codex</td>
      <td>approval/exec rules/tool annotation</td>
      <td>platform-native sandbox가 local 기본</td>
      <td>policy와 기술 경계를 가장 명확히 분리</td>
    </tr>
    <tr>
      <td>Farad</td>
      <td>scoped PermissionManager와 tool checks</td>
      <td>agent command용 OS sandbox 없음</td>
      <td>정책은 강하지만 arbitrary child process containment가 없음</td>
    </tr>
  </tbody>
</table>

<h3 id="왜-이것이-최우선-격차인가">왜 이것이 최우선 격차인가</h3>

<p>permission parser는 model과 user 사이의 의사결정이고 sandbox는 kernel/OS가 child process를 막는
기술 경계다. Bash tool이 <code class="language-plaintext highlighter-rouge">python -c</code>, compiler, git hook, package script를 실행할 수 있다면 개별
Read/Edit tool의 semantic guard는 우회 가능하다. plugin hook과 MCP stdio server도 별도 process
surface다.</p>

<p>OpenCode 내부에서도 기억 범위가 다르다. V1 <code class="language-plaintext highlighter-rouge">always</code>는 directory instance 메모리라 restart에 사라지지만,
V2 <code class="language-plaintext highlighter-rouge">always</code>는 save resource가 있을 때 project-scoped SQLite rule로 영속될 수 있다. 반면 pending approval은
여전히 process-local이다. 따라서 “approval UI가 있다”와 “durable project policy가 있다”도 분리해 봐야 한다.</p>

<p>OpenCode와 Farad는 이 구분에서 가장 취약하다. Claude는 지원 OS에서 sandbox가 있지만 기본 활성화가
아니고 dependency가 없을 때 명시적으로 fail-closed를 설정하지 않으면 unsandboxed fallback이
가능하다. native Windows에서는 같은 sandbox가 동작하지 않으므로 permission만으로 동일한 격리를
얻었다고 보면 안 된다. Codex는 <strong>지원 플랫폼의 기본 local agent-command containment</strong>에서 protected
path와 network policy까지 가장 강하다. <code class="language-plaintext highlighter-rouge">danger-full-access</code>, platform/backend 차이, remote MCP side
effect 때문에 이 판정을 “제품의 모든 보안”으로 확대해서는 안 된다.</p>

<p>Farad가 취할 방향은 “Codex와 동일한 bubblewrap 명령”이 아니다. RHEL 8.10 공유 HPC, user namespace
제한, Windows를 전제로 다음을 먼저 계약해야 한다.</p>

<ul>
  <li>filesystem/network/process-tree/socket별 <code class="language-plaintext highlighter-rouge">enforced / unavailable / disabled</code>를 표시하고, 선택 profile의
required guarantee를 모두 만족할 때만 overall <code class="language-plaintext highlighter-rouge">enforced</code>로 표시</li>
  <li>permission posture와 containment capability를 직교 상태로 유지</li>
  <li>Bash, background shell, user-configured hook, stdio MCP child 중 어느 local process가 경계 대상인지 inventory</li>
  <li>backend가 없을 때 fail-closed가 필요한 managed policy와 사용자 선택형 fallback 구분</li>
  <li>workspace write/read/network/socket/process-tree 규칙과 platform adapter</li>
</ul>

<p>HTTP/SSE MCP의 원격 side effect는 local OS sandbox로 격리할 수 없다. 이 경로는 permission, server trust,
credential scope와 provenance로 통제해야 한다. 현재 Farad skill은 instruction package이고 plugin hook은
metadata discovery와 executable wiring이 분리돼 있으므로 둘을 현행 local spawn surface로 과장하지 않는다.</p>

<h2 id="10-mcp-skills-plugins-hooks">10. MCP, skills, plugins, hooks</h2>

<p>네 제품 모두 이 확장 primitive를 갖지만 의미는 다르다.</p>

<table>
  <thead>
    <tr>
      <th>primitive</th>
      <th>올바른 책임</th>
      <th>잘못된 평탄화의 위험</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>instruction/rule file</td>
      <td>model context</td>
      <td>enforcement로 오해</td>
    </tr>
    <tr>
      <td>skill</td>
      <td>lazy procedural context + assets/scripts</td>
      <td>항상 prompt에 전부 주입</td>
    </tr>
    <tr>
      <td>MCP</td>
      <td>external capability/resource connection</td>
      <td>local built-in tool과 lifecycle을 동일시</td>
    </tr>
    <tr>
      <td>hook</td>
      <td>deterministic lifecycle policy/automation</td>
      <td>core business logic을 hook에 숨김</td>
    </tr>
    <tr>
      <td>plugin</td>
      <td>위 primitive의 배포 단위</td>
      <td>설치가 곧 신뢰라는 가정</td>
    </tr>
  </tbody>
</table>

<p>Claude Code는 이 구분을 가장 풍부한 제품 계약으로 제공하고, Codex는 per-step planner와 typed
extension으로 이를 core에 통합한다. OpenCode는 provider/plugin 생태계와 server extension이 강하지만
arbitrary JS plugin이 host process 권한을 공유한다. Farad는 4-layer Registry/Factory/Loader,
marketplace, exposure, explicit refresh와 delta MCP reconcile을 갖고 <strong>명세상</strong> NFS 비용을 가장 엄격히
제한한다. settings/endpoint/MCP current path가 그 명세를 실제로 위반한다는 감사 결과를 함께 봐야 한다.</p>

<p>Farad의 차별점은 “항상 최신”이 아니라 <strong>명시적 refresh와 write-after-invalidate</strong>다. Claude/OpenCode의
turn-time refresh나 file watch를 무조건 이식하면 NFS file-content round trip과 재현성이 나빠진다.
현재 감사에서는 capability discovery는 이 계약을 대체로 지키지만, settings/endpoint는 5분 mtime timer,
per-turn tier stat, 20분 poller의 endpoint config reconcile이라는 세 freshness 축을 동시에 가지고 있었고
first/dirty MCP turn도 blocking reconcile을 기다렸다. 기본 composition에는 discovery provider가 없어
20분 경로가 Ollama network probe를 실행하지는 않지만, Qt callback의 stat·selection·signal fanout은 남는다.
따라서 이 축에서 Farad의 점수는 “좋은 명세”가 아니라 <strong>확인된 계약-구현 gap</strong>을 반영해야 한다.</p>

<h2 id="11-multi-agent-background-work-scheduler">11. Multi-agent, background work, scheduler</h2>

<table>
  <thead>
    <tr>
      <th>제품</th>
      <th>child 단위</th>
      <th>context 관계</th>
      <th>file isolation</th>
      <th>interactive owner</th>
      <th>durable schedule</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>OpenCode</td>
      <td>child session/task</td>
      <td>parent-linked, foreground/background</td>
      <td>기본 shared directory; 별도 격리 미확인</td>
      <td>server process, experimental job</td>
      <td>근거 없음</td>
    </tr>
    <tr>
      <td>Claude Code</td>
      <td>subagent/session/team member</td>
      <td>기본 fresh context, <code class="language-plaintext highlighter-rouge">fork</code>는 parent 상속 + result/mailbox</td>
      <td>shared FS 또는 worktree</td>
      <td>CLI process/per-user supervisor/cloud</td>
      <td>local <code class="language-plaintext highlighter-rouge">/loop</code>는 process-bound; cloud 별도</td>
    </tr>
    <tr>
      <td>Codex</td>
      <td>child agent thread</td>
      <td>parent rollout flush + fork snapshot</td>
      <td>shared workspace/environment policy</td>
      <td>ThreadManager/agent graph</td>
      <td>본 조사 증거로 판단 보류</td>
    </tr>
    <tr>
      <td>Farad</td>
      <td>subagent/runtime task/scheduled session</td>
      <td>session/task artifact와 event</td>
      <td>product policy에 따름</td>
      <td>app runtime</td>
      <td>persistent scheduler store</td>
    </tr>
  </tbody>
</table>

<p>Claude Code는 subagent, independent agent view, experimental team, worktree를 서로 다른 primitive로
분리한 점이 좋다. 현재 일반 subagent는 최대 depth 5까지 nested delegation을 지원하고, <code class="language-plaintext highlighter-rouge">fork</code>만 다시
fork할 수 없다. 다만 team은 experimental이고 resume/file-conflict 제약이 있으므로 durable team
workflow로 과대평가하지 않는다. Codex는 child를 별도 thread로 만들어 lifecycle과 permission
inheritance를 명시적으로 기록하지만, 이것만으로 장기 durable team workflow가 입증되지는 않는다.
OpenCode는 server session tree가 자연스럽지만 background job은 process-local·non-durable이고 V2
durable multi-node ownership은 아직 진행 중이다.</p>

<p>Farad의 scheduler는 부가 CLI loop가 아니라 앱의 제품 기능이라는 점이 차별점이다. 그러나 scheduler가
Qt timer에서 10,000 item을 주기적으로 stat/sort하거나 UI thread에 I/O를 올리면 이 차별점이 오히려
가장 큰 운영 병목이 된다. scheduler의 authority, due index, executor thread, run idempotency를 별도로
감사해야 한다.</p>

<h2 id="12-provider-abstraction">12. Provider abstraction</h2>

<h3 id="opencode">OpenCode</h3>

<p>가장 폭넓고 제품 정체성 자체가 provider neutral이다. AI SDK/native adapter와 model catalog, provider
config가 runtime의 중심이다. 장점은 선택권이고, 비용은 provider별 tool/reasoning/stream/error
normalization의 거대한 compatibility matrix다.</p>

<h3 id="claude-code">Claude Code</h3>

<p>Bedrock/Vertex/Foundry와 gateway를 지원해도 메시지 의미론과 최적화는 Claude 모델 중심이다. 이는
제품 품질에는 장점이지만 범용 kernel의 중립성은 낮다.</p>

<h3 id="codex">Codex</h3>

<p>model provider interface와 OSS endpoint가 있으나 auth, model catalog, 최신 기능과 hosted surface는
OpenAI 중심이다. OpenCode 수준의 동등 provider experience라고 보기는 어렵다.</p>

<h3 id="farad">Farad</h3>

<p>hvppyai를 HTTP/types library로 제한하고 agent loop를 app 계층에 둔다. GLM/Kimi 같은 near-frontier
provider를 first-class로 취급해야 하므로 이 경계는 유지할 가치가 크다. provider capability 차이를
SDK에 agent policy로 hardcode하지 말고 turn/readiness adapter에서 해석해야 한다.</p>

<h3 id="판정-3">판정</h3>

<p>두 질문을 분리해야 한다.</p>

<ul>
  <li><strong>검증된 provider breadth</strong>: 75+ provider와 adapter matrix를 운영하는 OpenCode가 선두다.</li>
  <li><strong>core semantic coupling과 architecture neutrality</strong>: Farad의 hvppyai/app 경계가 강하다. 다만
GLM/Kimi first-class 목표가 OpenCode급 breadth나 모든 provider parity를 입증하지는 않는다.</li>
</ul>

<h2 id="13-ui와-human-in-the-loop">13. UI와 human-in-the-loop</h2>

<table>
  <thead>
    <tr>
      <th>제품</th>
      <th>UI의 강점</th>
      <th>구조상 주의점</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>OpenCode</td>
      <td>TUI/Web/Electron/IDE가 API를 공유</td>
      <td>SSE gap refetch, remote auth, client/server version</td>
    </tr>
    <tr>
      <td>Claude Code</td>
      <td>terminal workflow와 permission/diff/plan 밀도</td>
      <td>local CLI 및 2.1.88 관찰(C)의 TUI/core render 결합·process-local state; Desktop/Web 내부 미확인</td>
    </tr>
    <tr>
      <td>Codex</td>
      <td>TUI/IDE/App에서 thread/subagent/approval 공유</td>
      <td>app-server protocol evolution과 connection state</td>
    </tr>
    <tr>
      <td>Farad</td>
      <td>persistent sidebar, timeline, scheduler, settings, native terminal</td>
      <td>Qt thread I/O, huge projection class, signal fanout</td>
    </tr>
  </tbody>
</table>

<p>Farad가 가장 잘할 수 있는 것은 Claude/Codex CLI를 픽셀 단위로 복제하는 일이 아니다. long-lived
session, multiple provider, schedule, permission, context usage, subagent 상태를 <strong>저사양 desktop에서
지속적으로 이해 가능하게 투영</strong>하는 것이다. 이 우위는 runtime authority를 UI가 침범하지 않을 때만
유지된다.</p>

<h2 id="14-persistence-nfs-n10000">14. Persistence, NFS, N=10,000</h2>

<h3 id="141-local-ssd와-nfs는-다른-문제다">14.1 local SSD와 NFS는 다른 문제다</h3>

<p>OpenCode의 SQLite WAL, Claude의 JSONL+stat, Codex의 JSONL+SQLite는 local disk에서 합리적이다.
하지만 NFS에서는 SQLite locking, WAL, file-per-item content read, 대량 concurrent stat가 전혀 다른
비용과 실패를 낳는다.</p>

<p>Farad의 공식 비용 모델은 다음을 요구한다.</p>

<ul>
  <li>반복 경로는 누적 entity 수에 대해 O(1) 또는 visible-only</li>
  <li>list data는 단일 derived index와 mtime/size delta 검증</li>
  <li>content read는 changed file에만</li>
  <li>전체 reconcile은 startup/명시 refresh처럼 bounded one-shot</li>
  <li>external change 감지는 polling/watch가 아니라 explicit reconcile</li>
</ul>

<h3 id="142-제품별-판정">14.2 제품별 판정</h3>

<ul>
  <li>OpenCode V1은 한 긴 transcript에서 agent step마다 O(N) history materialization이라 가장 직접적인
hot-path 문제가 있다. session list는 반환 수가 제한돼도 <code class="language-plaintext highlighter-rouge">time_updated</code> compound index가 없어 후보
scan/sort 비용은 10k에서 미검증이다. SQLite WAL도 shared NFS canonical DB로는 별도 검증이 필요하다.
<code class="language-plaintext highlighter-rouge">OPENCODE_DB</code>로 node-local path를 지정할 구현상 완화책은 있으나 공식 배포 계약은 약하다.</li>
  <li>Claude Code는 2.1.88 sourcemap 관찰(C)에서 resume list가 O(N) stat/sort 후 recent item만 content
read했다. local SSD에는 합리적일 수 있지만 현재 2.1.207 구현으로 단정할 수 없고, 그 관찰 경로는
Farad의 10k 반복 계약에는 맞지 않는다.</li>
  <li>Codex는 JSONL canonical과 SQLite query index 분리가 좋은 구조적 참고다. 다만 10,000 thread latency,
fallback/rebuild bound, stale projection repair 비용을 보장하는 증거는 이번 조사에서 확인하지 못했다.
Farad는 SQLite/WAL을 NFS canonical store로 그대로 복제하지 말고 node-local derived projection 또는
자체 index를 써야 한다.</li>
  <li>Farad는 명세가 가장 엄격하지만 실제 source의 scheduler/session-open/turn-start 경로가 이를
지키는지는 별도 성능 gate로 증명해야 한다. 실제로 scheduler의 60초 Qt-thread 전량 stat/sort,
sync transcript append와 cold double scan, rewind 뒤 full projection fallback이 확인됐다. “문서에 O(1)”은
증거가 아니다.</li>
</ul>

<h2 id="15-failure-cancellation-recovery-observability">15. Failure, cancellation, recovery, observability</h2>

<p>성숙한 agent runtime은 실패를 예외 문자열 하나로 만들지 않는다.</p>

<table>
  <thead>
    <tr>
      <th>실패 축</th>
      <th>좋은 계약</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>model</td>
      <td>retryable/terminal, rate limit, output limit, context limit 분리</td>
    </tr>
    <tr>
      <td>tool</td>
      <td>pending/running/completed/error/cancelled와 terminal result 보장</td>
    </tr>
    <tr>
      <td>approval</td>
      <td>request identity, timeout/cancel, noninteractive fail behavior</td>
    </tr>
    <tr>
      <td>process</td>
      <td>process tree kill, late output suppression, cleanup finalizer</td>
    </tr>
    <tr>
      <td>transcript</td>
      <td>append ordering, partial record repair, resume boundary</td>
    </tr>
    <tr>
      <td>client</td>
      <td>disconnect 뒤 authoritative refetch/replay</td>
    </tr>
    <tr>
      <td>MCP</td>
      <td>per-server status, required/optional, reconnect/backoff, delta refresh</td>
    </tr>
    <tr>
      <td>subagent</td>
      <td>child terminal state, orphan cleanup, parent partial result</td>
    </tr>
  </tbody>
</table>

<p>Codex는 typed event/item과 cancellation token, thread lifecycle가 가장 체계적이다. 다만 core submission
channel은 512로 bounded인 반면 event channel은 unbounded라서 느린 consumer와 event burst의 memory
backpressure는 별도 운영 리스크다. Claude Code는 continue/terminal reason, tool state, hook failure,
retry taxonomy가 매우 풍부하다. OpenCode는 DB
projection과 Effect finalizer, 공용 durable event substrate가 강하지만 legacy SSE queue/backpressure와 V2 미완
경계가 있다. Farad는 AgentEvent/TerminalReason, synthetic abort result, UI event dispatcher와 run
measurement를 갖지만 모든 cancellation path에서 terminal event와 disk state가 닫히는지 감사가
중요하다.</p>

<h2 id="16-테스트-가능성과-변경-용이성">16. 테스트 가능성과 변경 용이성</h2>

<h3 id="좋은-경계">좋은 경계</h3>

<ul>
  <li>OpenCode: typed schema/OpenAPI, Effect service/layer, public source/tests</li>
  <li>Claude Code: 공식 SDK conformance surface; async generator·narrow <code class="language-plaintext highlighter-rouge">QueryDeps</code>는 2.1.88 관찰(C)</li>
  <li>Codex: Rust types, storage trait, protocol schema, router/registry separation</li>
  <li>Farad: Protocol/dataclass, query callback inversion, UI proxy, 4-layer contract, fail-on-expensive-fallback tests</li>
</ul>

<h3 id="변경-비용이-집중된-곳">변경 비용이 집중된 곳</h3>

<ul>
  <li>OpenCode: V1 <code class="language-plaintext highlighter-rouge">SessionPrompt</code>와 V2 migration overlap</li>
  <li>Claude Code: 2.1.88에서 관찰한 QueryEngine/query/TUI/global state/message/session hotspot(C);
current 2.1.207 내부는 미확인</li>
  <li>Codex: Session spawn/services/tool spec feature composition</li>
  <li>Farad: <code class="language-plaintext highlighter-rouge">AsyncSession</code>, <code class="language-plaintext highlighter-rouge">UiSession</code>, <code class="language-plaintext highlighter-rouge">CapabilityCatalog</code>, session/scheduler path</li>
</ul>

<p>LOC 자체가 결함은 아니다. 위험은 서로 다른 변경 이유가 한 module에 모여 independent test seam 없이
같이 바뀌는 것이다. Farad의 분해 기준은 “파일이 길다”가 아니라 <strong>authority와 failure boundary가
다른 책임을 추출</strong>하는 것이어야 한다.</p>

<h2 id="17-use-case별-선택">17. Use-case별 선택</h2>

<table>
  <thead>
    <tr>
      <th>use case</th>
      <th>1순위</th>
      <th>이유</th>
      <th>차선</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>다양한 provider를 붙인 headless agent service</td>
      <td>OpenCode</td>
      <td>trusted single-user/service 기준 server/API/SDK/provider breadth; multi-tenant는 ACL·sandbox·bound·durable owner 별도 필요</td>
      <td>Farad engine을 별도 표면으로 만들 경우</td>
    </tr>
    <tr>
      <td>개인 개발자의 깊은 terminal coding</td>
      <td>Claude Code</td>
      <td>workflow·hooks·context·tool 완성도</td>
      <td>Codex</td>
    </tr>
    <tr>
      <td>안전한 local command 실행이 핵심인 coding</td>
      <td>Codex</td>
      <td>지원 플랫폼의 sandbox-enabled default profile + approval 분리; <code class="language-plaintext highlighter-rouge">danger-full-access</code> 제외</td>
      <td>Claude sandbox를 fail-closed로 설정</td>
    </tr>
    <tr>
      <td>IDE/app/CLI를 한 runtime에 붙이는 플랫폼</td>
      <td>Codex/OpenCode</td>
      <td>Codex app-server는 experimental, OpenCode는 V1/V2 API drift 조건부</td>
      <td>Claude SDK subprocess</td>
    </tr>
    <tr>
      <td>GLM/Kimi 중심 native desktop</td>
      <td>Farad</td>
      <td>provider-neutral SDK + rich Qt product</td>
      <td>OpenCode desktop</td>
    </tr>
    <tr>
      <td>RHEL shared HPC/NFS</td>
      <td>Farad(목표)</td>
      <td>explicit NFS/10k/refresh 계약</td>
      <td>별도 container/SSD를 붙인 다른 제품</td>
    </tr>
    <tr>
      <td>inspectable parallel delegated coding</td>
      <td>Codex/Claude Code</td>
      <td>child lifecycle 또는 subagent workflow</td>
      <td>Farad orchestration</td>
    </tr>
    <tr>
      <td>durable long-lived local agent teams</td>
      <td>판단 보류</td>
      <td>네 제품 모두 제약 또는 증거 부족</td>
      <td>실제 durability 요구부터 정의</td>
    </tr>
    <tr>
      <td>persistent scheduled assistant</td>
      <td>Farad</td>
      <td>scheduler가 product domain; 현재 10k/NFS 배포는 due-index 수정 전 release-blocked</td>
      <td>타 제품은 본 조사에서 동등 내구성 미확인</td>
    </tr>
  </tbody>
</table>

<p>“Farad(목표)”라는 표현은 의도적이다. 해당 계약을 문서화했다는 것과 current implementation의 모든
경로가 지킨다는 것은 다르다. 개별 Farad 감사에서 위반이 나오면 release gate로 고쳐야 한다.</p>

<h2 id="18-무엇을-누구에게서-배울-것인가">18. 무엇을 누구에게서 배울 것인가</h2>

<table>
  <thead>
    <tr>
      <th>가져올 pattern</th>
      <th>기준 제품</th>
      <th>Farad식 번역</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>typed client/runtime anti-corruption layer</td>
      <td>Codex, OpenCode</td>
      <td>우선 in-process command/event Protocol; 두 번째 surface 전에는 server 보류</td>
    </tr>
    <tr>
      <td>ordered control plane</td>
      <td>Codex</td>
      <td>turn input/settings/approval/cancel ordering 명문화</td>
    </tr>
    <tr>
      <td>tool pipeline과 lazy disclosure</td>
      <td>Claude, Codex</td>
      <td>기존 catalog/readiness/exposure invariant 강화</td>
    </tr>
    <tr>
      <td>durable event + projection</td>
      <td>Codex, OpenCode EventV2/V2 typed projection</td>
      <td>transcript canonical, bounded derived index와 repair 계약</td>
    </tr>
    <tr>
      <td>context epoch/byte-stable baseline</td>
      <td>OpenCode V2, Farad marker twin</td>
      <td>external context 변경을 transcript에 재현 가능하게 기록</td>
    </tr>
    <tr>
      <td>output spill + preview</td>
      <td>Claude Code</td>
      <td>NFS 비용과 민감정보 정책을 포함한 bounded artifact store</td>
    </tr>
    <tr>
      <td>resource finalizer/scope</td>
      <td>OpenCode Effect, Codex cancellation</td>
      <td>Python async context/lifecycle owner를 명시</td>
    </tr>
    <tr>
      <td>subagent as first-class lifecycle</td>
      <td>Codex, Claude</td>
      <td>parent snapshot, child terminal state, artifact merge</td>
    </tr>
    <tr>
      <td>OS sandbox + separate approval</td>
      <td>Codex, Claude</td>
      <td>RHEL/Windows adapter와 honest capability state</td>
    </tr>
  </tbody>
</table>

<p>Codex의 request-scoped refresh는 이식 대상이 아니다. Farad에서는 <code class="language-plaintext highlighter-rouge">StepContext</code>처럼 한 요청의 tool과
context view를 일치시키는 <strong>snapshot 불변식</strong>만 가져오고, 그 snapshot을 만들기 위해 매 sampling
request마다 filesystem을 refresh하는 정책은 가져오지 않는다.</p>

<h2 id="19-의도적으로-모방하지-않을-것">19. 의도적으로 모방하지 않을 것</h2>

<ul>
  <li>OpenCode의 모든 client/server 복잡도를 단일 Qt 앱에 선제 도입</li>
  <li>Agent SDK의 live-session subprocess topology와 2.1.88에서 관찰한 process-global state</li>
  <li>Codex의 crate 수와 모든 OpenAI product surface</li>
  <li>local SSD 기준 SQLite/JSONL operation을 NFS에 그대로 적용</li>
  <li>per-turn file watch/rescan을 “freshness”라는 이유로 도입</li>
  <li>agent 수·plugin 수·hook event 수를 성숙도 지표로 삼기</li>
  <li>문서상의 read-only/permission을 OS enforcement로 표현</li>
  <li>compaction summary를 raw transcript 대체 원본으로 사용</li>
</ul>

<h2 id="20-farad의-현재-위치">20. Farad의 현재 위치</h2>

<p>Farad는 다른 세 제품과 배포 형태·언어·공개 범위가 달라 단순 규모 순위를 매길 수 없다. 다만 다음
contract는 비교 대상보다 더 엄격하다.</p>

<ul>
  <li>SDK에는 agent loop를 두지 않음</li>
  <li>UI와 async execution authority 분리</li>
  <li>모든 app LLM call streaming</li>
  <li>explicit refresh와 국소 invalidation</li>
  <li>NFS content-read 비용 명시</li>
  <li>누적 10,000 entity 반복 경로 제한</li>
  <li>transcript context와 runtime metadata 분리</li>
  <li>Session 통일, scheduler와 subagent도 session projection에 통합</li>
</ul>

<p>따라서 전략은 feature parity가 아니라 <strong>계약-구현 gap closing</strong>이어야 한다. 특히 다음 네 질문이
release 수준의 판정 기준이다.</p>

<ol>
  <li>agent-generated process가 permission decision 밖의 실제 OS 경계를 갖는가?</li>
  <li>turn/session/scheduler 반복 경로가 10,000 entity에서 정말 bounded인가?</li>
  <li>Qt와 asyncio가 서로의 mutable authority를 직접 읽지 않는가?</li>
  <li>explicit refresh라고 문서화한 capability가 실제 turn start를 불필요하게 block하지 않는가?</li>
</ol>

<p>구체적인 우선순위와 exit criterion은 <a href="https://hvppycoding.github.io/research/farad-architecture-roadmap/">Farad 판정 및 로드맵</a>에 둔다.</p>

<h2 id="21-최종-판정">21. 최종 판정</h2>

<p>네 제품의 architecture를 한 문장씩 줄이면 이렇다.</p>

<ul>
  <li>OpenCode: <strong>server를 중심으로 가장 넓게 열려 있으나 runtime migration과 current hot path의 빚이 크다.</strong></li>
  <li>Claude Code: <strong>가장 깊게 제품화된 workflow이며, 확인 가능한 local CLI/SDK에서는 Claude·process·local-file 결합을 대가로 낸다.</strong></li>
  <li>Codex: <strong>가장 강한 protocol·sandbox·replay kernel이지만 그만큼 composition과 platform 복잡도가 크다.</strong></li>
  <li>Farad: <strong>올바른 desktop/HPC 계약을 상당수 이미 세웠으나, 이제 문서가 아닌 enforcement와 hot-path
evidence로 그 계약을 증명해야 한다.</strong></li>
</ul>

<p>Farad가 승리할 수 있는 경로는 OpenCode의 provider openness, Claude의 workflow semantics, Codex의
runtime/security invariants를 모두 복제하는 것이 아니다. 현재 product differentiator인 native desktop,
provider neutrality, scheduler와 선언된 explicit-refresh/NFS/10k 설계 계약을 바로잡아 유지하면서 <strong>Codex 수준의 실행 경계,
Claude 수준의 tool/failure semantics, OpenCode 수준의 surface-independent application contract</strong>를
필요한 만큼만 흡수하는 것이다.</p>

<style>pre.mermaid{overflow-x:auto;background:transparent;border:0;padding:0}.mermaid svg{max-width:100%;height:auto}</style>

<script type="module">
import mermaid from "https://cdn.jsdelivr.net/npm/mermaid@11.16.0/dist/mermaid.esm.min.mjs";
mermaid.initialize({startOnLoad:true,securityLevel:"strict"});
</script>]]></content><author><name>hvppycoding</name></author><category term="research" /><summary type="html"><![CDATA[OpenCode · Claude Code · Codex · Farad 아키텍처 교차 비교]]></summary></entry><entry><title type="html">Farad 현재 구현 아키텍처 심층 감사</title><link href="https://hvppycoding.github.io/research/farad-architecture-audit/" rel="alternate" type="text/html" title="Farad 현재 구현 아키텍처 심층 감사" /><published>2026-07-13T23:54:00+09:00</published><updated>2026-07-13T23:54:00+09:00</updated><id>https://hvppycoding.github.io/research/farad-architecture-audit</id><content type="html" xml:base="https://hvppycoding.github.io/research/farad-architecture-audit/"><![CDATA[<h1 id="farad-현재-구현-아키텍처-심층-감사">Farad 현재 구현 아키텍처 심층 감사</h1>

<blockquote>
  <p><a href="https://hvppycoding.github.io/research/coding-agent-architecture-comparison/">전체 보고서 인덱스</a></p>
</blockquote>

<p>작성일: 2026-07-13<br />
분석 기준: 현재 작업 트리(HEAD e25b08b14218c85284a687018fb47bcccb497d18 + 미커밋 변경)<br />
분석 대상: packages/farad, packages/hvppyai, packages/hvppyterm 및 관련 문서·테스트<br />
비교 프로젝트: OpenCode, Claude Code, Codex와의 최종 교차 비교를 위한 Farad 단독 보고서</p>

<h2 id="1-결론">1. 결론</h2>

<p>Farad는 단순한 “LLM 채팅 GUI”가 아니다. 현재 구현은 다음 요소를 실제로 갖춘, 데스크톱 중심의 이벤트 구동 coding-agent 플랫폼이다.</p>

<ul>
  <li>SDK와 agent runtime을 분리한 3-패키지 구조</li>
  <li>Qt 메인 스레드와 단일 asyncio 런타임 스레드 사이의 명시적 proxy 경계</li>
  <li>callback inversion을 사용한 UI 비의존 query loop</li>
  <li>append-only transcript, rewind, compaction, protocol normalization</li>
  <li>정책 계층이 분명한 permission/approval 시스템</li>
  <li>stdio·SSE·Streamable HTTP를 포함한 MCP lifecycle과 circuit breaker</li>
  <li>skill·plugin·connector·marketplace를 합성하는 capability catalog</li>
  <li>session·scheduled run·subagent·background shell을 하나의 제품 안에서 다루는 runtime</li>
  <li>HPC/NFS와 누적 10,000개 엔티티를 의식한 파생 인덱스·bounded cache·hot-path guard test</li>
  <li>매우 넓고 빠른 회귀 테스트군</li>
</ul>

<p>핵심 설계 방향은 좋다. 특히 hvppyai에 agent loop를 넣지 않은 점, query_loop를 async generator와 callback으로 평탄화한 점, UI projection을 authority로 삼지 않은 점, 외부 MCP의 readOnly annotation을 곧바로 신뢰하지 않는 점, session sidebar를 파생 인덱스로 만든 점은 경쟁 제품을 참고해 만든 흉내 수준이 아니라 Farad의 배포 환경과 제품 요구에 맞춘 독자 설계다.</p>

<p>다만 “구조적 완성도”와 “프로덕션 위험이 제거됨”은 같은 말이 아니다. 현재 가장 큰 위험은 알고리즘 자체보다 경계에 몰려 있다.</p>

<ol>
  <li>단일 asyncio loop에서 transcript 파일 I/O가 동기 실행된다.</li>
  <li>SchedulerEngine의 1분 tick이 Qt 메인 스레드에서 전체 schedule 파일을 stat하고 정렬한다.</li>
  <li>문서는 explicit refresh만 허용한다고 선언하지만 settings/endpoints는 5분 QTimer, 매-turn mtime stat, 20분 주기의 endpoint 강제 재해석을 수행한다. 현재 기본 composition에는 discovery provider가 주입되지 않으므로 이 20분 경로를 곧바로 Ollama network probe라고 부르면 안 된다.</li>
  <li>첫 turn 또는 MCP dirty turn은 MCP reconcile 완료를 기다리므로 문서의 “non-blocking readiness”와 다르다.</li>
  <li>ModelSelection이 전역 mutable endpoint/model/client 하나를 소유하며, 교체된 client를 닫지 않는다.</li>
  <li>permission은 강한 정책 계층이지만 OS sandbox가 아니며, Bash·hook·stdio MCP는 사용자 권한으로 직접 실행된다.</li>
  <li>transcript는 flush와 atomic replace를 쓰지만 fsync, index corruption rebuild, corruption surfacing이 없다.</li>
  <li>AsyncSession·UiSession·SubAgentManager와 주요 UI class가 지나치게 커서 authority가 다시 집중되고 있다.</li>
</ol>

<p>따라서 최종 평가는 다음과 같다.</p>

<blockquote>
  <p>Farad의 기능 폭과 runtime 분리, 상태 모델, 테스트 깊이는 상당하다. 그러나 전체 suite, field
reliability, OS security와 실환경 NFS/10k benchmark 없이 상용급 운영 품질을 확정하지 않는다.
“NFS·10k·explicit refresh·thread ownership·durability”라는 스스로 세운 계약도 모든 경로가 아직
일관되게 지키지는 않는다. 다음 단계는 새 기능 추가보다 I/O execution boundary, scheduler index,
model-client authority, refresh truth를 바로잡는 것이다.</p>
</blockquote>

<h2 id="2-증거-등급과-분석-한계">2. 증거 등급과 분석 한계</h2>

<p>이 보고서는 작업 폴더의 00-methodology.md에 정의된 등급을 따른다.</p>

<ul>
  <li>[A] 현재 소스 코드와 테스트에서 직접 확인</li>
  <li>[B] 저장소의 SPEC.md, docs 문서, AGENTS.md에 명시</li>
  <li>[C] 실행·테스트·명령 출력으로 관찰</li>
  <li>[D] A~C를 토대로 한 설계적 추론</li>
</ul>

<p>중요한 판정은 가능한 한 [A]를 우선했다. 문서와 구현이 다를 때는 구현을 현재 truth로 잡고, 문서 차이는 별도 표에 기록했다.</p>

<p>이번 검증은 전체 test suite, ruff, mypy를 실행한 것이 아니다. 분석 영역에 직접 대응하는 1,527개 focused test를 실행했고 모두 통과했다. 따라서 “해당 회귀 테스트가 현재 통과한다”는 결론은 가능하지만, “전체 저장소가 모든 gate를 통과한다”거나 “아래 구조적 위험이 없다”는 뜻은 아니다.</p>

<h2 id="3-분석-스냅샷">3. 분석 스냅샷</h2>

<h3 id="31-버전과-제품-형태">3.1 버전과 제품 형태</h3>

<table>
  <thead>
    <tr>
      <th>항목</th>
      <th style="text-align: right">현재 값</th>
      <th>근거</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Farad</td>
      <td style="text-align: right">26.7.3</td>
      <td>packages/farad/pyproject.toml [A]</td>
    </tr>
    <tr>
      <td>hvppyai</td>
      <td style="text-align: right">26.6.0</td>
      <td>packages/hvppyai/pyproject.toml [A]</td>
    </tr>
    <tr>
      <td>hvppyterm</td>
      <td style="text-align: right">0.1.0</td>
      <td>packages/hvppyterm/pyproject.toml [A]</td>
    </tr>
    <tr>
      <td>주 표면</td>
      <td style="text-align: right">PySide6 데스크톱 앱</td>
      <td>packages/farad/farad/app/bootstrap.py [A]</td>
    </tr>
    <tr>
      <td>runtime process</td>
      <td style="text-align: right">GUI process 내부 단일 asyncio QThread</td>
      <td>app/runtime/runtime_thread.py [A]</td>
    </tr>
    <tr>
      <td>CLI run</td>
      <td style="text-align: right">이미 실행 중인 GUI에 shared-FS IPC 요청</td>
      <td>farad/<strong>main</strong>.py:58-161 [A]</td>
    </tr>
    <tr>
      <td>headless daemon</td>
      <td style="text-align: right">없음</td>
      <td>CLI와 bootstrap 구성 [A]</td>
    </tr>
    <tr>
      <td>주 배포</td>
      <td style="text-align: right">RHEL 8.10/HPC/NFS + Windows</td>
      <td>AGENTS.md, SPEC.md [B]</td>
    </tr>
  </tbody>
</table>

<p>Farad CLI의 “run”은 독립 agent runtime이 아니다. 실행 중인 GUI의 singleton lease와 IPC mailbox에 요청을 넣고, –wait이면 ExternalRunResultStore를 약 0.5초 간격으로 확인한다. 즉 제품 topology는 “CLI와 GUI가 동일 core에 각각 접속”하는 client/server 구조가 아니라 “GUI가 runtime host이고 CLI는 GUI에 전달하는 보조 ingress”다. 데스크톱 중심 제품에는 일관된 선택이지만 CI, remote worker, 여러 front-end를 독립 지원하려면 composition root 분리가 추가로 필요하다.</p>

<h3 id="32-코드-크기와-집중도">3.2 코드 크기와 집중도</h3>

<p>farad-clean-review-context의 repository metrics script로 현재 트리를 분석한 결과는 다음과 같다. 수치는 non-comment LOC 또는 AST span 기반의 근사치이며 설계 냄새를 찾는 탐색 지표이지 품질 점수는 아니다.</p>

<ul>
  <li>분석 Python 파일: 989개</li>
  <li>근사 non-comment LOC: 303,932</li>
  <li>Farad package 파일: 587개</li>
  <li>Farad test 파일: 351개</li>
  <li>hvppyai package/test 파일: 42/9개</li>
  <li>레이어 분류: Test 351, UI 227, Other 159, Factory/Service 86, Runtime/Core 65, SDK 51, Tools 29, Registry/Store/Provider 14, Loader/Scanner/Adapter 7</li>
</ul>

<p>큰 모듈·클래스는 다음 영역에 집중된다.</p>

<table>
  <thead>
    <tr>
      <th>영역</th>
      <th style="text-align: right">대략적 규모</th>
      <th>판정</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>SidebarPanel</td>
      <td style="text-align: right">raw 6,722 lines</td>
      <td>UI responsibility 과집중</td>
    </tr>
    <tr>
      <td>CustomizePanel</td>
      <td style="text-align: right">raw 3,809 lines</td>
      <td>extension UI orchestration 과집중</td>
    </tr>
    <tr>
      <td>MainWindow</td>
      <td style="text-align: right">raw 3,794 lines</td>
      <td>composition 후속 wiring이 창으로 역류</td>
    </tr>
    <tr>
      <td>ChatPanel</td>
      <td style="text-align: right">raw 2,982 lines</td>
      <td>session/input/view integration 과집중</td>
    </tr>
    <tr>
      <td>AsyncSession</td>
      <td style="text-align: right">raw 2,853 lines</td>
      <td>runtime authority 과집중</td>
    </tr>
    <tr>
      <td>UiSession</td>
      <td style="text-align: right">raw 2,664 lines</td>
      <td>projection·dispatch·interaction 과집중</td>
    </tr>
    <tr>
      <td>SubAgentManager</td>
      <td style="text-align: right">raw 2,516 lines</td>
      <td>identity·queue·restore·routing 과집중</td>
    </tr>
    <tr>
      <td>TimelineView</td>
      <td style="text-align: right">raw 2,863 lines</td>
      <td>custom rendering 특성상 일부 정당</td>
    </tr>
    <tr>
      <td>query_loop</td>
      <td style="text-align: right">raw 1,272 lines</td>
      <td>단일 알고리즘은 크지만 dependency는 얕음</td>
    </tr>
    <tr>
      <td>tool_executor</td>
      <td style="text-align: right">raw 1,120 lines</td>
      <td>policy pipeline 복잡도 집중</td>
    </tr>
  </tbody>
</table>

<p>AST import graph에서도 runtime/services, extension registries, main window, permission, MCP adapter/manager 등의 strongly connected candidate가 나타났다. 이 결과만으로 cycle bug라고 단정할 수는 없지만, “클래스 수가 많아졌으므로 이미 modular하다”는 해석은 맞지 않는다. 특히 큰 orchestration object 안에 mutable authority와 lifecycle이 계속 추가되는 방향을 막아야 한다. [A][D]</p>

<p>반면 패키지 경계는 깨끗하다. hvppyai와 hvppyterm에서 Farad import는 발견되지 않았다. hvppyai는 HTTP/types/provider/streaming, hvppyterm은 embeddable terminal이고 agent logic과 제품 UI는 farad에 남는다. 이는 최상위 dependency direction을 잘 지킨 사례다. [A]</p>

<h2 id="4-제품배포-topology">4. 제품·배포 topology</h2>

<h3 id="41-실제-실행-구조">4.1 실제 실행 구조</h3>

<p>아래 <code class="language-plaintext highlighter-rouge">B1</code>~<code class="language-plaintext highlighter-rouge">B4</code>는 Mermaid 가독성을 위한 visual band이며, 코드 레이어 계약은
<code class="language-plaintext highlighter-rouge">docs/layers.md</code>의 UI / Registry / Factory-Service / Loader 4-layer다. 색은 실행 소속을 나타내고
붉은 외곽선은 최신 hotspot 실측과 책임 감사를 함께 통과한 attention signal이다.</p>

<pre class="mermaid">
flowchart TB
    subgraph B4[&quot;B4 · Entry와 UI shell — visual band&quot;]
        direction LR
        USER[&quot;User / GUI&quot;]:::neutral --&gt; QT[&quot;MainWindow 🔴&lt;br/&gt;&lt;i&gt;Qt application shell&lt;/i&gt;&quot;]:::qt
        CLI[&quot;farad run CLI&quot;]:::neutral --&gt; IPC[&quot;Shared-FS singleton IPC&lt;br/&gt;&lt;i&gt;lease · mailbox · result&lt;/i&gt;&quot;]:::boundary
        IPC --&gt; QT
    end

    subgraph B3[&quot;B3 · Composition, projection, thread boundary — visual band&quot;]
        direction LR
        SF[&quot;build_app_services&lt;br/&gt;&lt;i&gt;composition root&lt;/i&gt;&quot;]:::neutral --&gt; UI[&quot;UiSession · panels&lt;br/&gt;&lt;i&gt;Qt projection&lt;/i&gt;&quot;]:::qt
        UI --&gt;|queued command| HANDLE[&quot;AsyncSessionHandleProxy&lt;br/&gt;&lt;i&gt;thread-safe ingress&lt;/i&gt;&quot;]:::boundary
        HANDLE --&gt;|call_soon_threadsafe| LOOP[&quot;AsyncLoopThread&lt;br/&gt;&lt;i&gt;single asyncio loop&lt;/i&gt;&quot;]:::aio
        LOOP --&gt; AS[&quot;AsyncSession 🔴&lt;br/&gt;&lt;i&gt;turn runtime authority&lt;/i&gt;&quot;]:::aio
    end

    subgraph B2[&quot;B2 · Turn, capability, process services — visual band&quot;]
        direction LR
        QL[&quot;query_loop()&lt;br/&gt;&lt;i&gt;streaming and tool loop&lt;/i&gt;&quot;]:::aio
        SDK[&quot;hvppyai client&lt;br/&gt;&lt;i&gt;provider-neutral streaming&lt;/i&gt;&quot;]:::aio
        TOOLS[&quot;ToolExecutor · PermissionManager&lt;br/&gt;&lt;i&gt;policy and dispatch&lt;/i&gt;&quot;]:::aio
        MCP[&quot;MCP manager&lt;br/&gt;&lt;i&gt;stdio · SSE · HTTP&lt;/i&gt;&quot;]:::aio
        SHELL[&quot;Bash · background · hooks&lt;br/&gt;&lt;i&gt;host child processes&lt;/i&gt;&quot;]:::neutral
        SCH[&quot;SchedulerEngine 🔴&lt;br/&gt;&lt;i&gt;60s Qt timer&lt;/i&gt;&quot;]:::qt
        EXEC[&quot;ScheduleExecutor&lt;br/&gt;&lt;i&gt;scheduled session ingress&lt;/i&gt;&quot;]:::neutral
        DEPLOY[&quot;DeploymentStatePoller&lt;br/&gt;&lt;i&gt;20 min Qt timer&lt;/i&gt;&quot;]:::qt
        SETPOLL[&quot;SettingsService 🔴&lt;br/&gt;&lt;i&gt;5 min Qt timer&lt;/i&gt;&quot;]:::qt
    end

    subgraph B1[&quot;B1 · Registry와 persistence — visual band&quot;]
        direction LR
        TS[&quot;TranscriptStore&lt;br/&gt;&lt;i&gt;JSONL chunks and index&lt;/i&gt;&quot;]:::neutral
        STORES[&quot;Session · config · schedule stores&lt;br/&gt;&lt;i&gt;persistent truth&lt;/i&gt;&quot;]:::neutral
        CAT[&quot;CapabilityCatalog · registries&lt;br/&gt;&lt;i&gt;exposure projection&lt;/i&gt;&quot;]:::qt
        FILES[&quot;Filesystem · user config · NFS&lt;br/&gt;&lt;i&gt;external persistence&lt;/i&gt;&quot;]:::neutral
    end

    QT --&gt; SF
    SF --&gt; LOOP
    SF --&gt; STORES
    SF --&gt; CAT
    QT --&gt; SCH
    QT --&gt; DEPLOY
    QT --&gt; SETPOLL
    AS --&gt; QL
    QL --&gt; SDK
    QL --&gt; TOOLS
    TOOLS --&gt; MCP
    TOOLS --&gt; SHELL
    AS --&gt; TS
    AS -.-&gt;|queued AgentEvent| UI
    SCH --&gt; EXEC
    EXEC --&gt; AS
    TS --&gt; FILES
    STORES --&gt; FILES
    CAT --&gt; FILES
    SETPOLL --&gt; FILES
    DEPLOY --&gt;|periodic endpoint reconcile| STORES

    classDef qt fill:#E8F5E9,stroke:#388E3C,stroke-width:2px,color:#1B5E20
    classDef aio fill:#FFF3E0,stroke:#F57C00,stroke-width:2px,color:#E65100
    classDef boundary fill:#F3E5F5,stroke:#7B1FA2,stroke-width:2px,color:#4A148C
    classDef neutral fill:#FAFAFA,stroke:#9E9E9E,stroke-width:2px,color:#424242
    classDef warn stroke:#C62828,stroke-width:3px
    class QT,AS,SCH,SETPOLL warn

</pre>

<p>읽는 법: 위에서 아래가 UI/entry → runtime → persistence 의존 방향이고, 같은 band 안에서는 왼쪽이
Qt/UI, 가운데가 boundary, 오른쪽이 async/backend에 가깝다. 녹색은 Qt-thread, 주황은 asyncio,
보라는 thread/process boundary, 회색은 호출자 중립 객체다. 실선은 호출·소유 방향이고 점선 역방향은
queued event다. <code class="language-plaintext highlighter-rouge">MainWindow</code>, <code class="language-plaintext highlighter-rouge">AsyncSession</code>은 LOC·정의 수와 mutable responsibility가 함께 집중됐고,
<code class="language-plaintext highlighter-rouge">SchedulerEngine</code>, <code class="language-plaintext highlighter-rouge">SettingsService</code>는 누적/주기 hot path가 확인돼 경고로 표시했다.</p>

<table>
  <thead>
    <tr>
      <th>그룹</th>
      <th style="text-align: center">상태</th>
      <th>실측 근거</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">hvppyai</code> / <code class="language-plaintext highlighter-rouge">hvppyterm</code> / <code class="language-plaintext highlighter-rouge">farad</code> 패키지 경계</td>
      <td style="text-align: center">🟢</td>
      <td>하위 두 패키지에서 Farad 역의존이 없고 agent loop는 app에 남음</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">query_loop</code>와 tool protocol</td>
      <td style="text-align: center">🟢</td>
      <td>callback-inverted async generator, cancellation terminal result, focused tests 통과</td>
    </tr>
    <tr>
      <td>Qt↔async session bridge</td>
      <td style="text-align: center">🟡</td>
      <td>정석 proxy/queued path는 명확하나 permission snapshot 직접 read와 shared-loop sync I/O 예외 존재</td>
    </tr>
    <tr>
      <td>UI/runtime orchestration 응집도</td>
      <td style="text-align: center">🟡</td>
      <td>MainWindow 3,794행, AsyncSession 2,853행, UiSession 2,664행과 다중 mutable responsibility</td>
    </tr>
    <tr>
      <td>refresh·scheduler 반복 경로</td>
      <td style="text-align: center">🔴</td>
      <td>5분 settings poll과 per-turn stat, 60초 Qt tick의 N개 task/history stat 및 sort 확인</td>
    </tr>
    <tr>
      <td>agent child-process containment</td>
      <td style="text-align: center">🔴</td>
      <td>policy/approval은 있으나 Bash·hook·background·stdio MCP의 OS sandbox 없음</td>
    </tr>
  </tbody>
</table>

<h3 id="42-singleton과-hpc-배포">4.2 singleton과 HPC 배포</h3>

<p>bootstrap은 QApplication, data dir, logging/performance monitor, font/theme, app services, main window, tray/shutdown을 하나의 lifecycle로 묶는다. shared filesystem lease를 획득하지 못하면 두 번째 writer를 띄우지 않고 기존 GUI로 요청을 넘긴다. 여러 HPC node가 같은 home을 공유할 수 있다는 현실을 반영한 제품 고유 장점이다. packages/farad/farad/app/bootstrap.py:66-296 [A]</p>

<p>이 구조의 장점은 session write authority가 한 GUI process로 모인다는 점이다. 단점은 GUI가 죽거나 로그인 display가 없으면 CLI agent run도 사용할 수 없다는 점이다. 현재 제품 명세가 desktop assistant 중심이므로 당장 daemon을 추가할 이유는 없다. 다만 향후 remote/headless surface를 추가한다면 기존 GUI composition root를 그대로 server로 포장하기보다 다음 세 경계를 먼저 분리해야 한다. [D]</p>

<ol>
  <li>RuntimeHost: session pair, MCP, scheduler, subagent, background process lifecycle</li>
  <li>PresentationHost: QApplication, windows, tray, Qt-only signals</li>
  <li>Ingress: GUI interaction, shared-FS IPC, 선택적 future RPC</li>
</ol>

<h2 id="5-layer-design-6축-판정">5. layer-design 6축 판정</h2>

<table>
  <thead>
    <tr>
      <th>축</th>
      <th>현재 authority / contract</th>
      <th>강점</th>
      <th>실제 위험</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Authority</td>
      <td>transcript는 conversation truth, stores는 persistent truth, AsyncSession은 turn runtime truth, UiSession은 projection</td>
      <td>UI list를 registry truth로 캐시하지 않는 규약이 분명</td>
      <td>ModelSelection 전역 mutable client, 큰 Session/UI manager에 authority 재집중</td>
    </tr>
    <tr>
      <td>Thread / process</td>
      <td>Qt main thread + 단일 asyncio QThread + child process/MCP transport</td>
      <td>proxy와 queued signal이 주 경로에 명시됨</td>
      <td>일부 Qt→AsyncSession 직접 snapshot read, sync disk I/O가 Qt 또는 shared loop에서 실행</td>
    </tr>
    <tr>
      <td>Flow</td>
      <td>UI ingress → proxy → AsyncSession → query_loop → callback/tool → transcript → UI event</td>
      <td>query loop가 UI import 없이 callback inversion</td>
      <td>readiness가 turn start 전에 endpoint/MCP를 직렬 대기, 중복 runtime resolve</td>
    </tr>
    <tr>
      <td>Hot path</td>
      <td>반복 경로 O(1) 또는 O(visible), explicit refresh만 O(N) 허용</td>
      <td>session index, bounded LRU, background task counters, paging guard test</td>
      <td>scheduler 1분 O(N log N)+NFS stats, transcript initial/full rewind scan, settings per-turn stat</td>
    </tr>
    <tr>
      <td>Persistence</td>
      <td>append-only transcript, atomic metadata/index replace, session-derived index</td>
      <td>disk-before-UI ordering, chunk paging, schedule history append/paging</td>
      <td>fsync 없음, transcript index corruption rebuild 없음, sync I/O, marketplace source unpinned</td>
    </tr>
    <tr>
      <td>Failure</td>
      <td>retry/fallback scaffolding, abort synthetic tool results, MCP breaker, scheduled run phase recovery</td>
      <td>tool protocol 보존과 cancellation semantics가 강함</td>
      <td>production fallback unwired, hook child leak 가능, MCP readiness 문서와 timeout 체감 불일치</td>
    </tr>
  </tbody>
</table>

<p>이 표에서 가장 중요한 해석은 “authority 모델이 잘 문서화됐다”와 “모든 호출 경로가 authority를 지킨다”를 구분하는 것이다. Farad의 정석 경로는 좋지만 edge path와 오래된 서비스가 새 계약 밖에 남아 있다.</p>

<h2 id="6-패키지레이어-구조">6. 패키지·레이어 구조</h2>

<h3 id="61-hvppyai">6.1 hvppyai</h3>

<p>hvppyai는 agent loop를 소유하지 않고 AsyncHvppyAI, provider registry, request/response/streaming type, retry를 제공한다. provider registry는 OpenAI Chat Completions, OpenAI Responses, OpenAI Codex Responses, Anthropic 계열을 lazy resolution한다. Farad가 모든 LLM 호출에서 stream=True를 사용한다는 앱 계약과 결합된다. [A][B]</p>

<p>이 분리는 경쟁 제품 대비 중요한 자산이다. provider wire format 변경은 SDK provider에, transcript·tool policy·session은 app runtime에 남길 수 있다. 단, Farad의 ModelSelection lifecycle 문제는 SDK 문제가 아니라 앱의 client ownership 문제다.</p>

<table>
  <thead>
    <tr>
      <th>SDK 책임</th>
      <th>구현 증거</th>
      <th>아키텍처 판정</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>provider translation</td>
      <td><code class="language-plaintext highlighter-rouge">Provider</code> Protocol이 request build, non-stream parse, SSE event parse, done 판정, auth header, per-stream state를 정의</td>
      <td>inheritance보다 structural protocol을 택해 vendor adapter가 얇다</td>
    </tr>
    <tr>
      <td>provider registry</td>
      <td>openai, openai-responses, openai-codex-responses, anthropic을 문자열→lazy import로 resolve</td>
      <td>앱이 vendor module을 직접 분기할 필요가 없다</td>
    </tr>
    <tr>
      <td>HTTP lifecycle</td>
      <td>caller가 client를 주입하지 않으면 자체 <code class="language-plaintext highlighter-rouge">httpx.AsyncClient</code>를 소유하고 <code class="language-plaintext highlighter-rouge">close()</code>에서만 닫음</td>
      <td>ownership contract는 명확하지만 앱이 close를 누락하면 pool이 남는다</td>
    </tr>
    <tr>
      <td>retry</td>
      <td>408/409/429/5xx, Retry-After, capped full-jitter backoff</td>
      <td>transport retry와 agent rerun을 분리할 수 있다</td>
    </tr>
    <tr>
      <td>streaming</td>
      <td>Farad가 <code class="language-plaintext highlighter-rouge">stream=True</code>, usage 포함으로 열고 content/reasoning/tool delta를 정규화</td>
      <td>UI와 transcript가 provider-neutral event를 소비한다</td>
    </tr>
  </tbody>
</table>

<p>근거: packages/hvppyai/hvppyai/providers/_base.py:13-64, providers/_registry.py:10-40, _client.py:104-134, 285-314; packages/farad/farad/core/query_llm_stream.py:381-417, 735-766 [A]</p>

<p>SDK가 제공하지 않는 것은 의도적으로 분명하다. session, permission, tool registry, compaction policy, scheduler, subagent는 모두 app 계층 책임이다. 이 경계 덕분에 SDK를 라이브러리로 재사용할 수 있지만, 반대로 Farad를 headless agent로 쓰려면 SDK만으로는 부족하고 app runtime composition이 필요하다. [A][D]</p>

<h3 id="62-hvppyterm">6.2 hvppyterm</h3>

<p>hvppyterm은 pyte/PTY/preedit 기반 embeddable terminal이며 Farad에 의존하지 않는다. terminal widget을 제품에서 떼어낼 수 있고, GPL reference source를 복사하지 않는 자체 경계도 문서에 명확하다. [A][B]</p>

<h3 id="63-farad-4-layer">6.3 farad 4-layer</h3>

<p>문서는 Loader/Scanner/File Adapter → Registry/Store/Provider → Factory/Service → UI의 의존 방향을 정본으로 둔다. 실제 extension 경로도 대체로 이 형태다.</p>

<ul>
  <li>scanner/loader가 disk 형식을 domain def로 변환</li>
  <li>registry가 folder-keyed bounded cache와 changed signal을 소유</li>
  <li>capability catalog가 여러 registry와 exposure를 합성</li>
  <li>UI는 catalog 결과를 그때 읽고 registry list를 자체 truth로 보관하지 않음</li>
</ul>

<p>이 구조는 plugin/skill/MCP surface에서 특히 잘 드러난다. 다만 app/services_factory.py가 약 1,165 lines의 거대한 composition root이고, MainWindow에도 runtime wiring·refresh orchestration이 일부 역류한다. composition root가 많은 concrete type을 아는 것은 정상이나, 서비스 lifecycle을 창 method가 직접 조율하기 시작하면 UI/application 경계가 흐려진다. [A][D]</p>

<h2 id="7-startup과-composition-root">7. startup과 composition root</h2>

<p>build_app_services는 한 번에 다음을 조립한다.</p>

<ul>
  <li>SettingsService, SharedSettingsCache, runtime resolver</li>
  <li>ModelSelection과 provider client</li>
  <li>plugin/skill/agent/connector registry</li>
  <li>CapabilityCatalog와 explicit refresh service</li>
  <li>AsyncLoopThread, AsyncRuntime, SessionPairFactory</li>
  <li>Goal, Usage, permission/settings cascade</li>
  <li>PostBox, background task, subagent manager</li>
  <li>MCP manager와 internal MCP servers</li>
  <li>scheduler, global search, memory, operational diagnostics</li>
</ul>

<p>AsyncLoopThread는 최대 5초 ready barrier 뒤 사용되고, 종료 시 outstanding task를 cancel/gather한 후 loop를 닫는다. MCP SDK import는 LazyMCPManager로 첫 사용까지 약 245ms 지연된다. internal MCP에는 scheduled-tasks, working-folder, capabilities, models, session, 선택적 terminal이 등록되어 있다. AGENTS.md의 현재 미완 항목은 workspace server다. app/services_factory.py:527-565 [A]</p>

<p>startup에서 session/schedule/subagent 관련 정리와 복구 작업도 수행한다. 일부는 worker/daemon thread로 보내지만 모든 파일 path가 같은 비용 모델을 따르는 것은 아니다. composition root가 startup budget과 thread ownership을 한눈에 보장하는 manifest를 갖고 있지 않아, 새 서비스가 Qt startup에 sync I/O를 추가하기 쉽다. 서비스별 “startup read budget / periodic budget / shutdown owner”를 선언하는 작은 진단 표가 효과적이다. [D]</p>

<h2 id="8-explicit-refresh-계약-감사">8. Explicit Refresh 계약 감사</h2>

<h3 id="81-문서-계약">8.1 문서 계약</h3>

<p>AGENTS.md gate #9와 docs/layers.md는 capability discovery, settings, exposure, permission live state를 turn마다 rescan·re-poll·mtime stat하지 않는다고 선언한다. 외부 파일 편집은 /reload, Customize Refresh, capabilities MCP 같은 명시적 trigger에서만 반영하고, 앱 내부 write는 해당 cache를 직접 좁게 invalidate해야 한다. docs/config.md는 local settings mtime watcher를 DeploymentStatePoller에 섞지 말라고 명시한다. [B]</p>

<h3 id="82-실제-구현-capability-discovery">8.2 실제 구현: capability discovery</h3>

<p>CapabilityHotReloadService는 현재 standing scan timer가 없다. refresh_for_run은 generation snapshot만 돌려주며, fingerprint subprocess scan은 explicit refresh에서만 수행한다. registry는 working-folder별 LRU를 쓰고 in-app exposure change는 derived cache 또는 한 item만 갱신한다. MCP도 dirty flag가 없으면 connector-map rebuild와 connect_all을 건너뛴다. 이 부분은 계약과 대체로 일치한다. [A]</p>

<p>단, cold registry.items() cache miss의 loader는 동기 호출이다. 어떤 UI/async caller가 첫 access를 만들었는지에 따라 NFS scan이 Qt 또는 shared loop에서 일어날 수 있다. “explicit인가”와 “어느 thread에서 비싼 scan을 하는가”는 별도 계약이어야 한다. [A][D]</p>

<h3 id="83-실제-구현-settings와-endpoint">8.3 실제 구현: settings와 endpoint</h3>

<p>settings/endpoints는 계약과 직접 충돌한다.</p>

<ol>
  <li>SettingsService는 기본 300,000ms QTimer를 생성한다.</li>
  <li>services_factory.py:219가 이 timer를 start한다.</li>
  <li>매 tick은 settings와 providers의 global tier mtime을 확인하고, 바뀌면 Qt 메인 스레드에서 JSON을 다시 읽고 changed signal을 낸다.</li>
  <li>ConfigProvider.get 자체도 호출할 때마다 global/folder tier mtime tuple을 다시 계산한다.</li>
  <li>SessionRuntimeResolver의 주석은 “per-turn resolves … stat fresh and pick up live settings”를 의도된 live-reload 축이라고 명시한다.</li>
  <li>RunReadinessBuilder는 turn마다 runtime_resolver.resolve(session_id)를 호출한 뒤 refresh_endpoints_for_run을 호출한다.</li>
  <li>refresh_endpoints_for_run은 다시 settings_service.refresh_endpoints()를 호출한다.</li>
  <li>TurnContextProvider.build는 같은 turn에서 runtime_resolver.resolve(session_id)를 다시 호출한다.</li>
</ol>

<p>근거: app/settings_service.py:1-82, 135-181; config/provider.py:148-190, 217-308; app/runtime_resolver.py:47-108; app/runtime/run_readiness.py:67-84; app/services.py:546-581; app/runtime/turn_context_provider.py:171-218 [A]</p>

<p>즉 capability root는 no-poll이지만 settings/providers는 다음 세 가지 reload 축을 동시에 가진다.</p>

<table>
  <thead>
    <tr>
      <th>축</th>
      <th>주기/시점</th>
      <th>비용과 의미</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>SettingsService timer</td>
      <td>5분</td>
      <td>global settings/providers mtime stat, 변경 시 Qt에서 content read</td>
    </tr>
    <tr>
      <td>turn readiness/context</td>
      <td>매 turn</td>
      <td>session folder를 포함한 settings/providers mtime stat이 여러 번 발생</td>
    </tr>
    <tr>
      <td>DeploymentStatePoller callback</td>
      <td>20분</td>
      <td>endpoint 강제 재해석·signal fanout; 기본 composition에서는 network discovery 없음</td>
    </tr>
  </tbody>
</table>

<p>MainWindow는 20분 shared deployment poller에 <code class="language-plaintext highlighter-rouge">model_discovery</code>라는 callback을 추가하고 <code class="language-plaintext highlighter-rouge">force_discovery=True</code>로 endpoint를 다시 해석한다. 그러나 현재 <code class="language-plaintext highlighter-rouge">services_factory.py:217-235</code>는 <code class="language-plaintext highlighter-rouge">SessionRuntimeResolver</code>에 <code class="language-plaintext highlighter-rouge">ModelEndpointDiscoveryService</code>를 주입하지 않는다. resolver는 <code class="language-plaintext highlighter-rouge">include_discovery</code>가 참이어도 provider가 <code class="language-plaintext highlighter-rouge">None</code>이면 discovery branch를 실행하지 않는다(<code class="language-plaintext highlighter-rouge">app/runtime_resolver.py:24-35, 107-117</code>). 따라서 <strong>현재 기본 앱 경로가 20분마다 Ollama에 접속한다거나 1.5초 network stall을 만든다는 주장은 사실이 아니다</strong>. 현재 확정 가능한 비용은 Qt timer callback에서 config tier path 구성·mtime stat, endpoint selection 갱신, signal fanout을 동기 수행한다는 것이다. 이름과 flag는 discovery를 암시하지만 runtime effect는 endpoint config reconcile이다. app/main_window/window.py:1177-1183, 3292-3302; app/services.py:546-580 [A]</p>

<p>별도 <code class="language-plaintext highlighter-rouge">ModelEndpointDiscoveryService</code>와 동기 <code class="language-plaintext highlighter-rouge">urllib</code> 기반 Ollama probe 구현은 존재한다. 향후 이 provider를 resolver에 주입하면 같은 20분 Qt callback에서 실제 network I/O가 발생할 수 있으므로, 그 wiring을 추가하는 시점에는 worker/async boundary가 필수다. 이것은 현재 관찰 사실이 아니라 <strong>조건부 설계 위험</strong>이다. app/model_endpoint_discovery.py, app/ollama_discovery.py:18-31, 68-161 [A][D]</p>

<h3 id="84-판정">8.4 판정</h3>

<p>이것은 문서의 작은 표현 차이가 아니라 현재 architecture contract violation이다. [A][B]</p>

<ul>
  <li>docs/layers.md는 “old run-start settings mtime poll is gone”이라고 쓰지만 소스는 반대다.</li>
  <li>docs/config.md는 local settings mtime watching을 shared deployment poller에 섞지 말라고 하지만 별도 5분 SettingsService timer가 살아 있다.</li>
  <li>AGENTS.md는 turn start mtime stat을 금지하지만 runtime_resolver는 이를 의도된 동작이라고 주석으로 정당화한다.</li>
  <li>20분 callback은 deployment operator state 외의 endpoint config까지 강제 재해석한다. 현재 network discovery는 없지만 Qt에서 stat/selection/signal fanout을 수행하며, 이름·flag·실제 wiring이 서로 다른 상태다.</li>
</ul>

<p>구현과 문서 중 무엇을 살릴지 먼저 결정해야 한다. 현재 프로젝트 원칙을 유지하려면 권고안은 명확하다.</p>

<ol>
  <li>SettingsService의 기본 timer를 제거하고 in-app write 후 targeted cache reset만 유지한다.</li>
  <li>ConfigProvider.get을 “cached read”로 만들고 stat/reconcile은 explicit refresh method로 분리한다.</li>
  <li>RunReadinessBuilder에서 endpoint_refresher를 current immutable snapshot으로 바꾸거나, explicit dirty generation만 확인한다.</li>
  <li>runtime resolve를 readiness와 TurnContext가 공유해 한 turn에 중복 resolve하지 않는다.</li>
  <li>model picker open/Refresh는 명시적 endpoint reconcile을 수행한다. 실제 network discovery provider를 연결할 때만 worker/async I/O로 분리한다.</li>
  <li>자동 Ollama discovery가 제품 요구라면 explicit-refresh 원칙의 좁은 예외로 문서화하고 worker/async I/O로 옮긴다. 그렇지 않으면 오해를 부르는 20분 <code class="language-plaintext highlighter-rouge">model_discovery</code> callback과 <code class="language-plaintext highlighter-rouge">force_discovery</code> 명칭을 제거하거나 config reconcile로 정확히 이름 붙인다.</li>
</ol>

<h2 id="9-mcp-readiness의-non-blocking-불일치">9. MCP readiness의 “non-blocking” 불일치</h2>

<p>docs/layers.md와 docs/mcp.md는 external MCP refresh가 turn을 막지 않으며 마지막 completed snapshot으로 진행한다고 설명한다. 실제 composition은 다르다.</p>

<ul>
  <li>services_factory.py:701-712는 mcp_refresher를 refresh_mcp_for_run(…, wait=True)로 주입한다.</li>
  <li>RunReadinessBuilder.build는 이를 await한다.</li>
  <li>AsyncSession._run_turn은 readiness가 끝난 뒤 AgentRunStarted를 emit한다.</li>
  <li>MCP가 first-run/dirty이면 refresh_mcp_for_run은 connector def merge, connect_all, trust/connection/liveness 처리가 idle이 될 때까지 기다린다.</li>
</ul>

<p>clean turn은 dirty gate로 빨리 snapshot을 반환한다. 그러나 첫 turn, folder/server config change 뒤 turn, 이미 진행 중인 refresh와 경합한 turn은 MCP timeout 범위만큼 시작이 늦어질 수 있다. 그동안 UI에는 pending 상태가 보일 수 있지만 transcript persistence와 PostBox pickup도 readiness 뒤로 밀린다. app/services.py:583-733; app/runtime/run_readiness.py:67-84; app/runtime/async_session.py:1204-1219 [A]</p>

<p>이 판정은 호출 그래프 추정만이 아니다. <code class="language-plaintext highlighter-rouge">test_services_refresh.py:618-654</code>는 <code class="language-plaintext highlighter-rouge">wait=True</code> 호출이 background reconcile의 release event가 열리기 전에는 완료되지 않고, release 뒤에야 반환함을 명시적으로 검증한다. 반대로 capability hot reload의 run-start 테스트는 scan을 하지 않는다는 것을 고정한다. 즉 “capability discovery는 snapshot-only”와 “dirty MCP readiness는 blocking”이 현재 동시에 참이다. [A]</p>

<p>이 선택 자체가 반드시 나쁜 것은 아니다. “첫 요청부터 최신 MCP tool schema가 필요하다”면 blocking barrier가 일관적이다. 문제는 현재 문서가 반대 semantics를 약속한다는 점이다. 제품 결정을 다음 둘 중 하나로 명시해야 한다.</p>

<ul>
  <li>Snapshot-first: turn은 마지막 snapshot으로 즉시 시작, background reconcile 결과는 다음 tool round/turn부터 적용</li>
  <li>Readiness-first: first/dirty turn은 bounded reconcile을 기다리고 UI가 “Connecting tools” 단계를 명시</li>
</ul>

<p>현재 구현은 Readiness-first다. 문서는 Snapshot-first다.</p>

<p>최종 종합 <a href="https://hvppycoding.github.io/research/farad-architecture-roadmap/">06 로드맵</a>은 Snapshot-first를 선택한다. 단 core
settings/endpoint/model snapshot generation 1 전에는 prompt admission을 막고, optional MCP만 typed
generation-0 empty/stale snapshot으로 nonblocking 시작한다.</p>

<h2 id="10-qt--asyncio-bridge">10. Qt ↔ asyncio bridge</h2>

<h3 id="101-정석-경로">10.1 정석 경로</h3>

<p>SessionPairFactory.open은 Qt thread에서 UiSession과 AsyncSession을 한 쌍으로 만들고 두 registry에 등록한 뒤, async handle로 initial transcript warm-up을 예약한다. 핵심 bridge는 다음 세 proxy다.</p>

<ul>
  <li>UiSessionSinkProxy: async side event를 Qt queued Signal로 전달</li>
  <li>AsyncSessionHandleProxy: Qt call을 call_soon_threadsafe로 단일 asyncio loop에 예약</li>
  <li>UiPostBoxProxy: UI가 외부 agent message를 async PostBox에 deposit</li>
</ul>

<p>AsyncRuntime은 Qt를 import하지 않고 session lookup, suspend/resume, notifier와 wake task를 loop 안에서 관리한다. AsyncLoopThread는 하나의 OS thread와 event loop를 소유한다. 이는 “GUI framework callback과 async orchestration을 한 loop에 섞지 않는다”는 좋은 구조다. app/runtime/session_pair_factory.py:95-216; app/runtime/proxies.py:46-215; app/runtime/async_runtime.py:37-205; app/runtime/runtime_thread.py:22-68 [A]</p>

<p>disk-before-UI도 정석 경로에 적용된다. TranscriptAppended를 처리할 때 AsyncSession이 먼저 TranscriptStore에 append하고 cache를 갱신한 뒤 sink event를 보낸다. PostBox pickup 역시 transcript에 남긴 후 UI에 표시한다. crash 직전 UI만 보이고 disk에는 없는 대화를 줄이는 올바른 방향이다. app/runtime/async_session.py:1402-1458, 2108-2143 [A]</p>

<h3 id="102-예외-경로">10.2 예외 경로</h3>

<p>AsyncSessionHandleProxy의 list_permission_rules_by_scope, permission_cascade_scopes, permission_scope_paths는 async loop에 메시지를 보내지 않고 Qt thread에서 AsyncSession을 직접 dereference한다. 주석은 immutable snapshot 및 cold disk read를 허용하지만 실제로는 다음 위험이 남는다. app/runtime/proxies.py:455-483 [A][D]</p>

<ul>
  <li>cold permission read가 Qt를 막을 수 있음</li>
  <li>async loop가 cascade list를 교체하는 시점과 읽기가 겹칠 수 있음</li>
  <li>“AsyncSession은 loop-owned”라는 단순한 검증 규칙에 예외가 생김</li>
</ul>

<p>구현을 더 정확히 보면 <code class="language-plaintext highlighter-rouge">PermissionManager.set_cascade()</code>는 mutable <code class="language-plaintext highlighter-rouge">_cascade</code> list reference를 교체하고, Qt read는 그 list를 순회하며 각 <code class="language-plaintext highlighter-rouge">SettingsStore.read(populate_cache=False)</code>를 호출한다. cache hit은 lock 없는 <code class="language-plaintext highlighter-rouge">SharedSettingsCache</code> dict를 읽고, cold miss는 <code class="language-plaintext highlighter-rouge">Path.is_file()</code>·<code class="language-plaintext highlighter-rouge">read_text()</code>·JSON parse를 Qt thread에서 수행한다. app/runtime/ui_session.py:1298-1310; core/permission.py:475-498, 649-670; core/settings_store.py:57-76, 119-130; core/settings_cache.py:27-45, 85-119 [A]</p>

<p>여기서 “반드시 torn snapshot이 생긴다”고 단정할 증거는 없다. CPython에서 list reference 교체와 dict get이 현재 사실상 원자적으로 보이는 데 기대고 있기 때문이다. 정확한 판정은 <strong>명시적 synchronization/version contract가 없고, old-or-new revision 보장도 타입으로 표현되지 않으며, cold NFS read가 UI latency에 들어온다</strong>는 것이다. 이는 correctness bug의 관측 증거가 아니라 portability·maintainability·latency 위험이다. [A][D]</p>

<p>반면 mutation 완료 signal은 잘 설계됐다. UiSession은 async write의 <code class="language-plaintext highlighter-rouge">on_done</code>이 돌아온 뒤 snapshot을 다시 읽고 <code class="language-plaintext highlighter-rouge">permission_rules_changed</code>를 emit하며, 실패 시 success signal을 내지 않는다. <code class="language-plaintext highlighter-rouge">test_ui_session.py:2613-2715</code>가 이 post-commit ordering을 고정한다. 다만 cross-thread stress, revision 일관성, “cold UI read 중 filesystem 접근 시 fail” guard test는 찾지 못했다. [A]</p>

<p>UiPostBoxProxy도 target loop가 아직 돌지 않을 때 PostBox에 직접 deposit한다. startup/shutdown edge에 한정된 fallback이지만 PostBox의 “all access async thread, no mutex” 계약과 다르다. app/runtime/proxies.py:506-564 [A]</p>

<p>권고는 async owner가 commit/cascade 변경 뒤 <code class="language-plaintext highlighter-rouge">PermissionSnapshot(revision, scopes, paths, rules)</code>를 immutable value로 publish하고, proxy가 승인된 O(1) projection만 보관해 Qt가 disk와 AsyncSession object graph를 전혀 만지지 않게 하는 것이다. 테스트는 (1) cold UI read에서 <code class="language-plaintext highlighter-rouge">Path.read_text</code> 호출 시 fail, (2) 동시 revision 교체 중 old 또는 new 전체만 관찰, (3) failed write 뒤 revision/signal 불변을 고정해야 한다. PostBox는 loop ready 이전 ingress를 proxy 자체 queue에 보관한 뒤 loop가 ready해지면 drain하면 thread exception을 없앨 수 있다.</p>

<h2 id="11-turn-실행과-query-loop">11. turn 실행과 query loop</h2>

<h3 id="111-실제-turn-순서">11.1 실제 turn 순서</h3>

<pre class="mermaid">
sequenceDiagram
    participant UI as Qt / UiSession
    participant P as AsyncSessionHandleProxy
    participant AS as AsyncSession
    participant R as RunReadinessBuilder
    participant TC as TurnContextProvider
    participant Q as query_loop
    participant T as ToolExecutor
    participant D as TranscriptStore
    participant L as LLM Provider

    UI-&gt;&gt;P: submit user input
    P-&gt;&gt;AS: call_soon_threadsafe
    AS-&gt;&gt;R: build(session_id)
    R-&gt;&gt;R: capability generation / endpoint refresh / MCP wait
    AS--&gt;&gt;UI: AgentRunStarted
    AS-&gt;&gt;TC: build current TurnContext
    AS-&gt;&gt;AS: precompact / PostBox pickup
    loop model-tool rounds
        AS-&gt;&gt;Q: async iterate events
        Q-&gt;&gt;L: streaming completion
        L--&gt;&gt;Q: content / reasoning / tool calls
        Q-&gt;&gt;T: execute tool calls
        T--&gt;&gt;Q: ordered terminal results
        Q--&gt;&gt;AS: normalized events
        AS-&gt;&gt;D: append / flush
        AS--&gt;&gt;UI: queued projection event
    end

</pre>

<p>AsyncSession은 transcript, ToolSessionState, PermissionManager, pending interaction, abort/run state, goal/usage/compaction coordination을 소유한다. _run_turn은 readiness, run event, context build, precompaction, PostBox pickup, query_loop rerun을 조율한다. query loop는 최대 10회 외부 rerun될 수 있고 내부 max_turns 기본은 명시하지 않으면 unbounded다. app/runtime/async_session.py:1204 이후 [A]</p>

<h3 id="112-query_loop의-강점">11.2 query_loop의 강점</h3>

<p>core/query_loop.py는 UI와 app service를 import하지 않고 QueryCallbacks와 immutable context를 통해 다음을 수행한다.</p>

<ul>
  <li>항상 streaming completion</li>
  <li>reasoning_content와 content 처리</li>
  <li>tool call normalization과 protocol repair</li>
  <li>retry/fallback scaffolding</li>
  <li>context overflow 처리</li>
  <li>request body limit 단계적 회복</li>
  <li>max output recovery</li>
  <li>abort 시 모든 tool call에 synthetic terminal result 생성</li>
  <li>PostBox message injection</li>
  <li>safe tool의 제한적 병렬 실행</li>
  <li>완료 순서가 달라도 원래 tool-call 순서로 transcript terminal result 기록</li>
</ul>

<p>query loop가 “agent object graph”보다 flat async event generator에 가깝다는 점은 테스트 가능성과 surface 재사용에 유리하다. UI approval, compaction, usage, persistence를 callback으로 역전시켜 core loop를 제품 프레임워크와 분리했다. core/query_loop.py:1-16, 124-134, 237-249, 872-892, 1111-1164 [A]</p>

<h3 id="113-남은-문제">11.3 남은 문제</h3>

<p>첫째, production fallback이 사실상 연결되지 않았다. query_loop에는 fallback chain이 있지만 DefaultTurnContextProvider는 fallback_model=""을 넣는다. _resolve_turn_fallback_model helper는 존재하나 production 호출이 검색되지 않았다. app/runtime/turn_context_provider.py:433-467, 986 이후 [A]</p>

<p>둘째, RunReadiness는 llm_client를 만든다. 그러나 TurnContextProvider는 readiness client를 소비하지 않고 model/endpoints를 다시 해석한 뒤 client_provider(model, endpoints)를 호출한다. readiness snapshot과 실제 turn client authority가 이중화되어 있다. app/runtime/run_readiness.py:20-84; app/runtime/turn_context_provider.py:399-420 [A]</p>

<p>셋째, AsyncSession은 2,800 lines가 넘으며 Goal steering, transcript, pending interaction, compaction, query rerun, event persistence를 모두 조율한다. 단순히 파일을 나눌 것이 아니라 authority 단위로 다음을 추출하는 편이 맞다. [D]</p>

<ul>
  <li>RunCoordinator: readiness부터 query rerun·cancel까지</li>
  <li>TranscriptCommitter: append, projection, message id, PostBox marker</li>
  <li>InteractionBroker: ask/approval/pending response</li>
  <li>GoalTurnPolicy: reminder, rerun, terminal condition</li>
</ul>

<p>AsyncSession은 이들을 묶는 session-scoped aggregate root로 남기되 각 object의 owner와 shutdown 순서를 명시할 수 있다.</p>

<h2 id="12-transcript와-compaction">12. Transcript와 compaction</h2>

<h3 id="121-저장-구조와-commit-ordering">12.1 저장 구조와 commit ordering</h3>

<p>TranscriptStore는 session transcript를 100개 chat item 단위 JSONL chunk와 index.json으로 저장한다. chat entry와 rewind/compact marker를 append하고, index는 임시 파일에 쓴 뒤 replace한다. append는 파일 object를 flush한 후 반환한다. transcript cache와 UI event는 이 disk append 뒤에 갱신된다. transcript_store.py:40, 264-312, 913-917 [A]</p>

<p>이 구조의 장점은 분명하다.</p>

<ul>
  <li>transcript가 UI widget tree와 독립된 재현 가능한 truth다.</li>
  <li>normal pagination은 index의 ID range를 사용해 필요한 chunk만 읽는다.</li>
  <li>tool-call/result protocol을 transcript 단계에서 보존할 수 있다.</li>
  <li>resume, branch, compaction, session search가 동일 데이터를 공유한다.</li>
  <li>테스트가 “bounded chunk read”와 “비싼 fallback이 호출되면 fail”을 직접 검증한다.</li>
</ul>

<p>그러나 문서에서 쓰는 “durable”은 의미를 좁혀야 한다. 현재 append는 flush만 하고 fsync하지 않는다. index replace도 file 또는 parent directory fsync가 없다. 따라서 process-level ordering과 atomic rename은 제공하지만, 전원 장애·kernel crash·NFS server failure 뒤의 stable-storage durability까지 보장하지는 않는다. [A]</p>

<p>NFS에서 모든 message마다 fsync하는 것은 latency가 너무 크므로 단순히 fsync를 추가할 문제는 아니다. 제품이 보장할 durability level을 다음처럼 명시해야 한다. [D]</p>

<ul>
  <li>기본: disk-before-UI, process crash tolerant, power-loss best effort</li>
  <li>선택: 중요한 terminal state 또는 일정 batch마다 fsync</li>
  <li>shutdown: dirty transcript/index의 bounded sync</li>
</ul>

<h3 id="122-append-hot-path의-동기-io">12.2 append hot path의 동기 I/O</h3>

<p><code class="language-plaintext highlighter-rouge">AsyncSession</code> event dispatcher는 <code class="language-plaintext highlighter-rouge">TranscriptAppended</code>를 받으면 <code class="language-plaintext highlighter-rouge">_append_persisted()</code>를 await하지만, 그 coroutine 안의 <code class="language-plaintext highlighter-rouge">store.append_entry(entry)</code> 자체는 일반 동기 함수다. <code class="language-plaintext highlighter-rouge">TranscriptStore.append_entry()</code>는 JSONL line write/flush 뒤 index 전체를 temp file에 쓰고 <code class="language-plaintext highlighter-rouge">os.replace()</code>한다. abort marker append도 같은 sync 경로다. app/runtime/async_session.py:1402-1457, 2108-2194; session/transcript_store.py:246-312 [A]</p>

<p>여기서 <strong>fact</strong>는 “Qt thread가 아니라 Farad의 단 하나뿐인 asyncio runtime loop에서 filesystem call이 완료될 때까지 Python thread가 막힌다”는 것이다. <strong>inference</strong>는 NFS stall 하나가 같은 loop를 공유하는 다른 session run, MCP task, PostBox wake, background orchestration의 tail latency를 함께 키운다는 것이다. async 함수 안에 있다는 사실은 sync I/O를 non-blocking으로 만들지 않는다. [A][D]</p>

<p>이 경로의 correctness는 높다. <code class="language-plaintext highlighter-rouge">test_async_session.py</code>의 write-ordering fixture는 store append가 끝나기 전에 UI sink가 호출되지 않음을 검증한다. 따라서 수정 목표는 ordering 제거가 아니라 execution lane 이동이다. session별 serialized writer queue/thread에서 append를 수행하고, awaitable commit receipt가 돌아온 뒤에만 cache/UI event를 갱신해야 한다. fire-and-forget batch는 crash ordering을 깨므로 부적합하다. tests/test_async_session.py:149-176, 686-785 [A][D]</p>

<h3 id="123-initial-load와-shared-loop-blocking">12.3 initial load와 shared loop blocking</h3>

<p>AsyncSession._ensure_initial_state는 TranscriptStore.load_all()을 호출한 뒤 max_numeric_message_id()를 호출한다. 두 함수 모두 sync filesystem traversal이며 두 번째도 raw chunks를 다시 훑는다. resume도 load_all()을 async 함수 안에서 직접 실행한다. app/runtime/async_session.py:394-422, 1013-1020; transcript_store.py:820-829 [A]</p>

<p>문서의 “Qt thread를 막지 않는다”는 표현은 절반만 맞다. Qt thread는 막지 않지만 Farad가 모든 session에 공유하는 하나의 asyncio loop를 막는다. 큰 transcript 하나를 resume하는 동안 다른 session run, background orchestration, MCP work가 지연될 수 있다. 파일이 NFS에 있으면 small-file round trip이 더 커진다. [A][D]</p>

<p>개선 방향은 transcript 전체를 무조건 thread pool로 던지는 정도가 아니다.</p>

<ol>
  <li>session별 serialized TranscriptIO executor를 둔다.</li>
  <li>append ordering은 그 executor queue에서 보장한다.</li>
  <li>index에 max numeric message id와 active projection checkpoint를 유지해 initial double scan을 없앤다.</li>
  <li>AsyncSession은 awaitable commit receipt를 받은 뒤 UI event를 보낸다.</li>
  <li>여러 session I/O가 shared asyncio event loop를 막지 않되, 같은 session write 순서는 유지한다.</li>
</ol>

<h3 id="124-rewind-뒤-성능-퇴화">12.4 rewind 뒤 성능 퇴화</h3>

<p>rewind marker가 하나라도 존재하면 correctness를 위해 load_tail, load_older_before, load_since, reverse iteration, last compact marker, folder marker 등 다수의 최적화 경로가 full logical transcript projection으로 fallback한다. transcript_store.py의 rewind_count 분기(예: 518-522, 609-615, 696-735) [A]</p>

<p>이 선택은 잘못된 것이 아니다. edit/branch 뒤 숨겨진 entry를 pagination에 섞는 것보다 전량 projection이 안전하다. 문제는 한 번 rewind한 긴 session은 이후 계속 느린 경로를 탄다는 점이다. 10,000 message session에서 edit 한 번이 영구적인 hot-path degradation switch가 된다. [D]</p>

<p>권고는 rewind-aware projection checkpoint다.</p>

<ul>
  <li>marker append 시 active interval 또는 generation을 index에 기록</li>
  <li>chunk별 active-count/visible-ID range를 파생 metadata로 유지</li>
  <li>index가 없거나 깨졌을 때만 bounded rebuild</li>
  <li>full reconstruction은 explicit repair/migration에 한정</li>
</ul>

<h3 id="125-index와-corruption">12.5 index와 corruption</h3>

<p>index.json parse가 실패하면 현재 코드는 empty ChunkIndex를 반환한다. 기존 chunk를 scan해 rebuild하거나 corruption을 사용자·diagnostics에 surface하지 않는다. 그 상태에서 새 append가 0000/index를 다시 만들면 오래된 chunks가 orphan되고 논리적 history가 보이지 않을 수 있다. malformed JSONL line도 read 시 조용히 건너뛴다. transcript_store.py:232-258, 920-932 [A]</p>

<p>부분 tail write를 건너뛰는 tolerance는 유용하지만, “마지막 불완전 line”과 “중간 line corruption”을 같은 방식으로 무시하면 데이터 손실을 감춘다. 다음 integrity state가 필요하다. [D]</p>

<ul>
  <li>valid</li>
  <li>tail-truncated-recoverable</li>
  <li>index-missing/rebuildable</li>
  <li>chunk-corrupt/quarantined</li>
  <li>repair-failed/read-only</li>
</ul>

<p>index는 cache이므로 version stamp 후 drop/rebuild가 가능해야 한다. rebuild가 비싸도 session open 한 번의 explicit repair lane에서 bounded progress와 취소를 제공하면 된다.</p>

<h3 id="126-compaction">12.6 compaction</h3>

<p>Farad compaction은 단순 summary append보다 정교하다.</p>

<ul>
  <li>last compact marker 이후 logical slice 계산</li>
  <li>synthetic summary user entry 구성</li>
  <li>current turn, file context, activated skill context 보호</li>
  <li>provider가 reasoning replay를 지원하는지에 따른 reasoning strip</li>
  <li>tool call/result protocol sanitize</li>
  <li>missing result synthesis와 empty assistant 제거</li>
  <li>항상 streaming compaction</li>
  <li>request/SSE observability</li>
  <li>cancel/error 시 stream close</li>
  <li>일부 API error에서 stricter sanitized replay</li>
  <li>sidecar와 transcript marker persistence</li>
</ul>

<p>관련 구현은 compaction.py, transcript_normalize.py, app/runtime/compaction_runner.py에 분산되어 있다. [A]</p>

<p>강점은 “압축된 메시지가 API에 유효한가”와 “resume 후 같은 conversation context가 재현되는가”를 함께 본다는 점이다. 위험은 provider protocol, token budget, transcript marker semantics가 여러 큰 모듈에 걸쳐 있어 변경 시 implicit invariant를 놓치기 쉽다는 점이다. CompactionPlan과 CompactionCommit 같은 명시적 value object로 precondition, protected ranges, output, persisted marker를 묶으면 검증이 쉬워진다. [D]</p>

<h2 id="13-tool-execution">13. Tool execution</h2>

<h3 id="131-tool-contract">13.1 tool contract</h3>

<p>BaseTool Protocol과 ToolContext, ToolResult가 runtime tool API의 중심이다. ToolResult는 모델에 보여줄 output, UI/diagnostic metadata, 큰 artifact reference를 구분한다. ToolExecutor의 흐름은 다음과 같다. [A]</p>

<ol>
  <li>tool existence와 input schema 검증</li>
  <li>hard safety/permission 판정</li>
  <li>사용자 approval</li>
  <li>pre-hook</li>
  <li>tool call</li>
  <li>post-hook</li>
  <li>output guard·artifact persist</li>
  <li>terminal result 생성</li>
</ol>

<p>unknown tool과 exception은 terminal tool result로 귀결되고, trace는 file log에 보존된다. safe concurrency가 허용된 tool만 병렬 실행하며, abort되면 이미 시작한 호출을 취소하고 모든 call id에 synthetic result를 보장한다. 이 때문에 다음 LLM request의 tool protocol이 깨지지 않는다. tool_executor.py 전반, core/query_loop.py:1111-1164 [A]</p>

<h3 id="132-concurrency-semantics">13.2 concurrency semantics</h3>

<p>병렬 tool이 완료되는 순서는 달라도 query_loop는 original call order로 terminal transcript를 기록한다. 이는 replay determinism과 provider compatibility에 유리하다. 반면 side effect 자체의 실제 완료 순서는 여전히 다를 수 있으므로 “parallel safe” 분류가 correctness authority다. annotation 하나에 의존하지 말고 동일 path write, cwd mutation, session state mutation 같은 conflict key를 tool metadata로 표현할 여지가 있다. [D]</p>

<h3 id="133-hooks">13.3 hooks</h3>

<p>HookRunner는 user-configured shell command를 asyncio.create_subprocess_shell로 실행한다. TOOL_INPUT과 TOOL_OUTPUT을 environment로 전달하며 timeout 시 직접 process를 kill한다. 별도 process group을 만들지 않으므로 shell이 child/grandchild를 낳은 경우 timeout 뒤 남을 가능성이 있다. hooks/runner.py:154-240 [A][D]</p>

<p>hook은 extension point로 강력하지만 permission dialog를 거치지 않는다. 즉 settings file을 수정할 수 있는 주체는 사실상 arbitrary host code execution authority를 가진다. 이는 제품이 config를 trusted input으로 본다면 허용 가능한 모델이지만, UI에서 marketplace plugin/hook을 “기능 확장” 정도로만 표현하면 trust 수준을 과소 전달할 수 있다.</p>

<h2 id="14-permission-approval-attendance-sandbox">14. Permission, approval, attendance, sandbox</h2>

<h3 id="141-permission-decision-pipeline">14.1 permission decision pipeline</h3>

<p>PermissionManager의 우선순위는 대략 다음과 같다.</p>

<ol>
  <li>hard guard</li>
  <li>explicit deny</li>
  <li>contextual rule</li>
  <li>explicit ask</li>
  <li>tool-specific rule</li>
  <li>explicit allow</li>
  <li>permission mode</li>
  <li>fallback</li>
</ol>

<p>Bash command는 compound command를 segment로 나눠 rule을 적용하고, permission scopes는 user/folder/session cascade를 갖는다. core/permission.py:690-828 [A]</p>

<p>상호작용 가능성은 permission mode와 별도 attendance axis로 다룬다. scheduled/headless run에서 Ask tool은 사용자가 자리에 없으므로 즉시 deny되며, 이는 DONT_ASK와 BYPASS에서도 독립적으로 적용된다. unattended execution이 dialog에서 영원히 멈추지 않게 하는 좋은 설계다. tool_authorization.py:48-123 [A]</p>

<p>외부 MCP tool은 server가 readOnlyHint를 붙여도 기본 Ask다. Farad가 소유한 internal MCP에서만 readOnly annotation을 mode auto-allow에 사용할 수 있다. MCP metadata를 trust proof로 착각하지 않는 보수적 경계다. mcp/adapter.py:179-208 [A]</p>

<h3 id="142-gitenv-hard-guard의-실제-범위">14.2 .git/.env hard guard의 실제 범위</h3>

<p>Edit/Write tool은 .git 및 .env 계열 path를 lexical/resolved variant로 보호하고, BYPASS보다 먼저 hard guard를 적용한다. core/permission.py:897-956 [A]</p>

<p>그러나 이 hard guard는 tool name Edit/Write에만 적용된다. Bash에서 rm, sed, python, git command를 실행하거나 hook이 파일을 바꾸는 것은 막지 않는다. docs/permission.md의 “.git/.env hard safety guard가 BYPASS에서도 유지”라는 문구는 이 제한을 함께 쓰지 않아 전체 실행 경계를 보호하는 것처럼 읽힐 수 있다. [A][B]</p>

<p>Bash command를 완전하게 parse해 같은 보장을 제공하는 것은 shell semantics상 어렵다. 따라서 선택지는 둘이다.</p>

<ul>
  <li>문구를 “built-in Edit/Write guard”로 좁히고 Full Access/Bash는 host access라고 명확히 표시</li>
  <li>실제 immutable guarantee가 제품 요구라면 shell을 sandboxed filesystem view에서 실행</li>
</ul>

<p>첫 번째가 현재 architecture truth다.</p>

<h3 id="143-os-sandbox-부재">14.3 OS sandbox 부재</h3>

<p>소스에서 seccomp, bubblewrap, Landlock, namespace, macOS sandbox, Windows restricted token 같은 containment 구현은 발견되지 않았다. settings type은 sandbox를 future P2/P3 항목으로 명시한다. Bash/background shell은 사용자 계정 권한과 environment, cwd를 가지고 직접 subprocess를 띄운다. process group은 cancellation을 위한 것이지 filesystem/network containment가 아니다. background_shell.py:685-783; core/settings_types.py:180-185 [A]</p>

<p>따라서 Farad permission의 정확한 정의는 다음과 같다.</p>

<blockquote>
  <p>Farad permission은 모델이 어떤 동작을 요청할 때 UI/정책 수준에서 승인 여부를 결정하는 policy engine이다. 승인된 process를 OS에서 격리하는 sandbox는 아니다.</p>
</blockquote>

<p>Full Access UI가 삭제·시스템 변경·network 전송 위험을 명시하는 것은 정직하다. 하지만 cross-product 비교에서 Codex나 Claude Code의 sandbox/permission과 같은 축으로 점수를 매길 때 “permission feature가 있다”와 “host containment가 있다”를 분리해야 한다.</p>

<p>권고는 당장 모든 플랫폼에 강제 sandbox를 구현하는 것이 아니다. 먼저 ExecutionBoundary 또는 CommandRunner Protocol을 만들어 다음 execution source를 한 authority 아래 둔다.</p>

<ul>
  <li>foreground Bash</li>
  <li>background Bash</li>
  <li>hook</li>
  <li>stdio MCP</li>
  <li>future local connector</li>
</ul>

<p>그 위에 Linux optional bwrap/Landlock profile, Windows restricted process/job policy, sandbox unavailable 시 explicit policy-only mode를 붙일 수 있다. HPC에서 bwrap 설치나 user namespace가 보장되지 않으므로 graceful capability detection이 필요하다. [D]</p>

<h2 id="15-mcp-architecture">15. MCP architecture</h2>

<h3 id="151-transport와-lifecycle">15.1 transport와 lifecycle</h3>

<p>MCPManager는 external/internal server를 같은 manager 아래 두고 stdio, SSE, Streamable HTTP를 지원한다. connect_all은 server별 작업을 병렬화하고 다음 기능을 갖는다.</p>

<ul>
  <li>explicit trust gate</li>
  <li>connection timeout과 per-call timeout</li>
  <li>unchanged config liveness check</li>
  <li>server-set fingerprint와 delta reconcile</li>
  <li>removed/changed server stop</li>
  <li>active call drain 뒤 config 교체</li>
  <li>failure threshold circuit breaker</li>
  <li>half-open single probe</li>
  <li>expired transport/session reconnect 후 재시도</li>
  <li>bounded server instruction text</li>
</ul>

<p>mcp/manager.py:147-309, 430 이후; mcp/connection.py:344-500 [A]</p>

<p>MCPServerConnection이 anyio context lifetime을 같은 task에서 소유하는 것도 중요하다. async context manager를 다른 thread/task에서 무리하게 닫는 오류를 피한다.</p>

<h3 id="152-schema와-result-adapter">15.2 schema와 result adapter</h3>

<p>MCP adapter는 tool name을 normalize/truncate/hash하고, external tool은 기본 Ask로 둔다. 큰 output은 약 24k 기준을 넘으면 artifact writer가 있을 때 파일로 보존하고 모델에는 bounded result를 전달한다. mcp/adapter.py:35-93, 153-283 [A]</p>

<p>내부 서버는 app-control capability를 모델에게 on-demand tool로 제공한다. runtime status를 transcript에 매 turn 주입하지 않고 models/session/capabilities tool로 조회하게 한 것은 context cache와 재현성 관점에서 좋다.</p>

<h3 id="153-trust와-containment">15.3 trust와 containment</h3>

<p>bundled internal server는 auto-trusted이고 external server는 connect 전에 explicit trust를 요구한다. 그러나 trust는 “이 process/server와 통신하도록 허용”이지 sandbox가 아니다.</p>

<ul>
  <li>stdio server는 host user 권한의 local process다.</li>
  <li>remote server는 전달받은 prompt/tool argument를 외부 network로 보낼 수 있다.</li>
  <li>OAuth/header/env secret의 lifecycle은 connector별 설정과 process environment에 의존한다.</li>
</ul>

<p>여기서 Farad는 이미 중요한 방어를 한다. <code class="language-plaintext highlighter-rouge">build_mcp_trust_request()</code>는 server name만 저장하지 않고 transport, command, args, env, URL, headers 전체를 canonical JSON으로 만든 SHA-256 <code class="language-plaintext highlighter-rouge">trust_key</code>에 approval을 결합한다. config가 바뀌면 기존 승인이 자동으로 일치하지 않는다. mcp/approval.py:31-65, 108-129 [A]</p>

<p>남는 한계는 config fingerprint와 executable content identity가 같지 않다는 점이다. 같은 command path가 가리키는 binary/script 내용이나 marketplace checkout이 in-place로 바뀌면 command 문자열은 그대로일 수 있다. 따라서 현재 trust를 “정의 변경 감지”로 높게 평가하되, 공급망 provenance가 필요한 install source에는 resolved commit/content digest를 추가하는 것이 맞다. [A][D]</p>

<h2 id="16-session과-persistence">16. Session과 persistence</h2>

<h3 id="161-session-taxonomy">16.1 session taxonomy</h3>

<p>모든 대화는 Session이고 session_type으로 AGENT, CHAT, SCHEDULED, SUBAGENT를 구분한다. root, schedule run, subagent의 디렉터리 layout은 다르지만 SessionManager가 identifier containment와 lookup을 통합한다. SessionPairFactory가 UiSession/AsyncSession pair lifecycle을 일원화한다. [A][B]</p>

<p>SessionMetaStore는 session.json과 session.meta.json을 다루며 path별 lock, mtime validation, 최대 10,000 entry cache를 사용한다. write는 temp+replace다. SessionManager도 session-dir/transcript-size cache를 최대 10,000개 유지한다. session_store.py:28-50, 71-157, 187-305; session.py:366-592 [A]</p>

<h3 id="162-hinted-lookup과-10k">16.2 hinted lookup과 10k</h3>

<p>scheduled/subagent session은 type·parent 등 lookup hint가 있으면 정확한 path를 O(1)에 가깝게 찾는다. hint가 없는 cold miss는 schedule/subagent tree glob으로 갈 수 있다. 문서와 테스트는 hot path에서 hint를 잃지 않도록 fail-expensive-fallback guard를 둔다. [A]</p>

<p>list_sessions는 모든 session metadata를 scan/parse/sort하는 explicit O(N) operation이다. sidebar 반복 경로는 대신 state/session_index.jsonl 파생 인덱스를 사용한다. index는 list/group에 필요한 digest를 한 sequential read로 가져오고, append delta와 mtime/size signature로 reconcile하며 self-compact한다. session_index.py:1-23, 149-270 [A]</p>

<p>이것은 NFS 비용 모델을 실제 코드로 옮긴 좋은 사례다.</p>

<ul>
  <li>list view용 field를 index에 포함</li>
  <li>content는 lazy materialize</li>
  <li>stat-first validation</li>
  <li>cache corruption 시 rebuild 가능한 derived state</li>
  <li>UI click마다 metadata file N개를 읽지 않음</li>
</ul>

<p>ProjectStore도 project catalog를 한 index로 읽고 instruction body는 project별 bounded LRU에서 lazy read한다. [A]</p>

<h3 id="163-남은-persistence-위험">16.3 남은 persistence 위험</h3>

<ul>
  <li>SessionMetaStore도 atomic replace 뒤 fsync하지 않는다.</li>
  <li>unhinted cold lookup과 explicit list_sessions가 반복 경로로 새어 나오지 않도록 call-site guard가 계속 필요하다.</li>
  <li>SessionPairFactory.open은 config/permission scope list 일부를 Qt에서 sync read한다.</li>
  <li>session delete의 recursive removal은 unbounded지만 explicit one-shot이므로 허용 가능하다.</li>
  <li>branch는 staging 후 atomic publish를 사용해 중간 상태 노출을 줄인다.</li>
</ul>

<p>SessionPairFactory close는 UI registry removal 뒤 async unregister를 예약하므로 문서가 말하는 완전한 원자적 pair removal과는 작은 시간차가 있다. 현재 single Qt owner에서는 실질 문제가 작지만 lifecycle invariant를 테스트로 표현하는 편이 낫다. [A][D]</p>

<h2 id="17-skills-plugins-connectors-marketplace">17. Skills, plugins, connectors, marketplace</h2>

<h3 id="171-capabilitycatalog">17.1 CapabilityCatalog</h3>

<p>CapabilityCatalog는 QObject 기반 SSOT로 builtin tools, active/searchable MCP tools, SkillTool, agent types를 합성한다. generation, derived cache, batch invalidation, scope별 invalidation, item_exposure_changed를 제공한다. UI가 registry list를 자체 cache하지 않고 catalog generation을 읽는 구조는 explicit refresh와 잘 맞는다. [A]</p>

<p>노출 상태 Off / Searchable / Active는 단순 enable flag보다 좋다.</p>

<ul>
  <li>Active: prompt/tool schema에 바로 노출</li>
  <li>Searchable: 필요 시 capability search로 찾음</li>
  <li>Off: 제외</li>
</ul>

<p>큰 capability 집합이 매 turn tool schema와 prompt token을 폭증시키지 않게 한다.</p>

<h3 id="172-skill">17.2 Skill</h3>

<p>Skill은 발견된 SKILL.md instruction을 shared SkillTool로 모델에 제공한다. 호출 결과는 instruction text이며 skill 자체가 Python code를 직접 실행하는 ABI는 아니다. max weighted character budget은 약 36k이고 초과 시 partial preview를 반환한다. skills/skill_tool.py:35-176 [A]</p>

<p>이 모델은 instruction package와 executable tool을 구분한다는 장점이 있다. 단, skill instruction이 모델에게 Bash/MCP를 쓰게 할 수 있으므로 trust risk가 0은 아니다. Skill source provenance와 exposure를 UI에서 계속 보여줘야 한다.</p>

<h3 id="173-plugin">17.3 Plugin</h3>

<p>Plugin은 arbitrary in-process Python plugin이 아니라 skills, agent definitions, connector/MCP config, hook metadata를 묶는 data/config bundle이다. commands는 skill surface로 통합된다. plugins/models.py:38-67 [A]</p>

<p>이 선택은 ABI 안정성과 app crash isolation에 유리하다. 그러나 plugin hook이 실제 settings cascade hook execution과 어디서 합쳐지는지는 현재 wiring에서 명백하지 않았다. loader가 PluginHookDef metadata를 발견·표시하는 것과 HookRunner가 settings hook을 실행하는 것은 확인했지만, 모든 plugin hook이 production turn에 자동 실행된다고 단정할 직접 연결은 찾지 못했다. 이 부분은 문서에 “discovered metadata / executable wiring” 상태를 구분해 써야 한다. [A]</p>

<h3 id="174-marketplace">17.4 Marketplace</h3>

<p>Marketplace install은 많은 경우 source item을 Farad-owned immutable copy로 pin하지 않고 source path를 등록해 skills/plugins/agents를 직접 읽는다. connector도 source와 user config를 등록한다. marketplace/installer.py:153-174, 271-309, 365-388 [A]</p>

<p>개발 중 즉시 갱신과 storage 절약에는 좋지만 공급망 관점에서는 다음 문제가 있다.</p>

<ul>
  <li>enable/trust 뒤 source path 내용이 in-place로 변경될 수 있음</li>
  <li>source가 NFS/Git checkout이면 reproducibility가 약함</li>
  <li>executable connector/hook command가 바뀌어도 동일 marketplace identity로 보일 수 있음</li>
  <li>rollback할 설치 artifact가 없음</li>
</ul>

<p>최소한 install record에 source URI/path, resolved commit 또는 content digest, manifest digest, install time을 기록해야 한다. executable fingerprint가 바뀌면 trust를 다시 요구하고, instruction-only change는 provenance warning으로 좁힐 수 있다. [D]</p>

<p>scanner는 unsafe relative component와 symlink containment를 방어하므로 path traversal 기본기는 갖춰져 있다. [A]</p>

<h3 id="175-home-path-규약-위반">17.5 home path 규약 위반</h3>

<p>프로젝트 규약은 HPC에서 Path.home()/expanduser 대신 passwd/NSS 기반 home을 사용하라고 한다. 그러나 runtime_resolver.working_folder_path_for, turn context path coercion, schedule check 등 여러 곳에서 Path.expanduser()가 남아 있다. 보통 session path가 이미 absolute이므로 빈번한 오류는 아닐 수 있지만, $HOME이 passwd home과 다른 shared HPC에서는 실제 boundary violation이다. app/runtime_resolver.py:136-142 및 관련 search 결과 [A][B]</p>

<h2 id="18-subagent와-background-work">18. Subagent와 background work</h2>

<h3 id="181-subagentmanager">18.1 SubAgentManager</h3>

<p>SubAgentManager는 agent identity/tree/roster, pending/running records, delivery, restore를 관리한다. 기본 soft concurrency 4, hard cap 32, spawn depth 1, pending run 10,000, foreground waiter 10,000 등 명시적 bound가 있다. event mutation은 async loop로 marshal되고 session/team별 background activity count는 O(1) index를 사용한다. terminal actor는 hot registry에서 evict하되 ghost metadata를 남긴다. [A]</p>

<p>restore는 direct index를 사용해 hot path에서 SessionManager.list_sessions(SUBAGENT) 전량 scan을 피한다. startup cold restore만 의도적으로 scan한다. 테스트는 expensive fallback이 호출되면 fail하도록 구성되어 있다. subagents/manager.py:179-256, 488-523, 2366-2514 [A]</p>

<p>남은 scale issue는 pending queue다.</p>

<ul>
  <li>_pending_run_ids가 list이므로 remove/index가 O(N)</li>
  <li>admission의 running/pending/foreground count가 <code class="language-plaintext highlighter-rouge">current_records()</code>를 매번 materialize하고 반복 scan함</li>
  <li><code class="language-plaintext highlighter-rouge">_drain_pending_queue()</code>는 list copy를 순회하면서 원본 list에서 반복 <code class="language-plaintext highlighter-rouge">remove()</code>하므로 다수 admission/cleanup 시 O(N²)까지 갈 수 있음</li>
  <li>max pending 10,000을 실제 지원한다면 cancellation/queue position update가 체감될 수 있음</li>
</ul>

<p>근거: subagents/manager.py:353-443, 1171-1187, 1923; subagents/run_registry.py:153-159 [A]</p>

<p>또 하나는 skill injection 경로다. <code class="language-plaintext highlighter-rouge">_load_skill_blocks()</code>는 spawn 때마다 <code class="language-plaintext highlighter-rouge">discover_all_skills(folder)</code>를 직접 호출해 전 후보를 순회하고 shadow rank를 다시 고른다. 이 경로는 CapabilityCatalog/SkillRegistry의 folder cache와 exposure snapshot을 사용하지 않으므로, NFS discovery가 agent tool hot path에 새어 나오고 UI/catalog가 보는 selection과 subagent prompt가 보는 selection의 authority가 갈릴 수 있다. subagents/manager.py:63-90, 1028-1035 [A]</p>

<p>active hard cap 32 덕분에 일부 hot actor scan은 사실상 bounded다. 반면 pending 10,000은 문서상 허용치이므로 <code class="language-plaintext highlighter-rouge">OrderedDict</code>/intrusive queue와 scope·status별 O(1) counters로 바꿀 근거가 있다. skill body는 spawn request가 받은 registry generation의 immutable snapshot에서 resolve해야 한다. tests는 10k queue drain 중 <code class="language-plaintext highlighter-rouge">list.remove</code>/전량 record materialization/skill filesystem discovery가 호출되면 fail하도록 만드는 편이 맞다. [A][D]</p>

<p>round finalization도 thread 경계상 주의가 필요하다. <code class="language-plaintext highlighter-rouge">mark_round_complete()</code>는 MainWindow/Qt thread에서 호출되며, 그 안에서 round transcript <code class="language-plaintext highlighter-rouge">load_since()</code>, 최종 output/stats 계산, output file <code class="language-plaintext highlighter-rouge">write_text()</code>, session metadata 갱신이 동기 실행된다. timestamp가 있어 transcript read 범위는 보통 해당 round로 제한되고, output path containment도 검증되지만, 긴 round나 NFS workflow root에서는 UI를 막을 수 있다. subagents/manager.py:1436-1530, 718-779, 1741-1756 [A]</p>

<p>개선 시에도 “output file이 존재한 뒤 foreground waiter를 깨운다”는 commit ordering은 보존해야 한다. Qt 밖의 serialized finalization worker가 transcript/output commit receipt를 만든 뒤 Qt에는 bounded status snapshot만 전달하는 구조가 적합하다. [D]</p>

<h3 id="182-backgroundtaskmanager">18.2 BackgroundTaskManager</h3>

<p>background shell manager는 running dict, terminal result LRU 128, 두 lane deque, pending dict 최대 10,000, session activity counter를 쓴다. spawn/admission과 head-only drain은 대체로 O(1) 또는 freed-slot 수에 비례한다. output은 hard cap 64MiB, completion tail 8KiB이고 hard cap 도달 시 process를 종료한다. process group cancellation도 제공한다. background_shell.py:183-366, 881-1068 [A]</p>

<p>session-wide cancel은 pending queue를 scan할 수 있지만 사용자가 명시적으로 session을 정리하는 one-shot 경로라면 허용 가능하다. temp-root TTL cleanup은 constructor에서 sync scan하므로 파일이 비정상적으로 많이 쌓이면 startup 영향을 줄 수 있으나 기본 위치가 local temp라는 점에서 NFS 위험은 낮다. [A][D]</p>

<h2 id="19-scheduler">19. Scheduler</h2>

<h3 id="191-semantics는-강하다">19.1 semantics는 강하다</h3>

<p>SchedulerEngine은 하나의 60초 QTimer로 due run을 감지한다. overlap SKIP/QUEUE/REPLACE, missed-run policy, pre-check, one-shot, max run, auto-disable를 처리한다. ScheduleExecutor는 각 run을 독립 SCHEDULED Session으로 만들고 다음 순서를 지킨다. [A]</p>

<ol>
  <li>RUNNING state를 먼저 persist</li>
  <li>unattended permission/attendance policy 적용</li>
  <li>SessionWorkflowService를 통해 일반 AsyncSession/query_loop 경로 진입</li>
  <li>hard timeout과 cancel</li>
  <li>terminal phase idempotent transition</li>
  <li>child/background teardown</li>
  <li>terminal persist retry와 orphan recovery</li>
</ol>

<p>scheduled run이 별도 “간이 agent loop”를 만들지 않고 일반 runtime을 재사용하는 점이 좋다. 실행 결과가 session transcript로 남아 resume·diagnostics와 같은 도구를 쓸 수 있다.</p>

<p>ScheduleHistoryStore는 append-only/paged history, bounded repair lane, schedule metadata aggregate를 사용한다. 100k history에서도 reverse page가 bounded되도록 테스트한다. [A]</p>

<h3 id="192-10k-치명적-hot-path">19.2 10k 치명적 hot path</h3>

<p>반면 due detection은 현재 10k 계약을 명백히 위반한다.</p>

<ul>
  <li>QTimer tick은 Qt 메인 스레드에서 실행된다.</li>
  <li>매 tick ScheduleStore.list_tasks()를 호출한다.</li>
  <li>list_tasks는 모든 schedule path를 순회하고 task를 정렬한다.</li>
  <li>각 task load는 schedule.json stat과 history.signature() 확인을 수행한다.</li>
  <li>path listing 자체가 directory mtime cache를 쓰더라도 per-file stats는 남는다.</li>
</ul>

<p>관련 근거: scheduler/engine.py:58-95, 239-286; scheduler/store.py:141-158, 842-886 [A]</p>

<p>warm listing cache를 전제로 정적 비용을 풀면 다음과 같다. directory listing 자체는 schedule directory signature 하나로 재사용되지만, <code class="language-plaintext highlighter-rouge">list_tasks()</code>는 각 task의 metadata path에 <code class="language-plaintext highlighter-rouge">stat()</code>을 하고 <code class="language-plaintext highlighter-rouge">_history_signature()</code>를 통해 history path에도 <code class="language-plaintext highlighter-rouge">stat()</code>을 시도한다. 그 뒤 10,000개 <code class="language-plaintext highlighter-rouge">next_run_at</code>을 parse/비교하고 전체 task list를 sort한다. 따라서 N=10,000 steady tick의 근사치는 <strong>directory stat 1회 + metadata stat 10,000회 + history stat 시도 10,000회 + 10,000개 due loop + O(N log N) sort</strong>다. 파일이 없거나 OS cache가 있어 실제 syscall 비용은 달라지지만, 알고리즘과 호출 수 자체는 source에서 도출된다. scheduler/store.py:930-942; scheduler/history_store.py:67-75 [A][D]</p>

<p>NFS attribute cache가 있어도 UI freeze와 shared filesystem 부하를 피하기 어렵다. scheduler test는 due semantics와 cron policy를 폭넓게 검증하지만, <code class="language-plaintext highlighter-rouge">test_scheduler.py:2060-2113</code> 부근의 hot-path guard는 cron validator 같은 일부 비싼 fallback만 막는다. 10k steady tick에서 <code class="language-plaintext highlighter-rouge">list_tasks</code>, per-task stat, sort가 호출되면 fail하는 regression test는 찾지 못했다. [A]</p>

<p>이 문제의 올바른 해결은 timer interval을 늘리는 것이 아니다.</p>

<ul>
  <li>ScheduleStore가 next_due_at, enabled, overlap에 필요한 최소 field를 단일 derived index로 유지</li>
  <li>in-memory min-heap 또는 ordered due index에서 다음 시각만 조회</li>
  <li>in-app schedule write 직후 해당 task만 index/heap update</li>
  <li>external reconcile은 startup/explicit refresh worker에서 stat sweep</li>
  <li>timer tick은 O(number of due tasks × log N)</li>
  <li>UI thread에는 due id list만 전달하고 I/O/parse는 worker 또는 async service에서 수행</li>
  <li>테스트는 tick 중 list_tasks/full scan 호출 시 fail</li>
</ul>

<p>이것이 현재 Farad에서 가장 우선순위가 높은 성능 수정이다.</p>

<h3 id="193-check-command">19.3 check command</h3>

<p>schedule pre-check command는 max worker 2의 thread pool, timeout, bounded output을 사용해 Qt를 직접 막지 않는다. 다만 host user 권한으로 실행되는 unsandboxed command이고 Path.expanduser 사용이 남아 있다. scheduled BYPASS를 선택하면 UI도 위험을 명시하지만, unattended execution과 OS containment 부재의 결합은 운영자가 명확히 이해해야 한다. [A]</p>

<h2 id="20-provider-abstraction과-client-lifecycle">20. Provider abstraction과 client lifecycle</h2>

<h3 id="201-장점">20.1 장점</h3>

<p>hvppyai Provider Protocol은 provider wire translation을 SDK에 캡슐화한다. Farad의 TurnContext는 model별 context window, max output, reasoning, image input, sampling, admission profile을 endpoint metadata에서 해석하고 query loop는 provider-neutral request/event를 사용한다. 주요 LLM 호출은 모두 streaming이다. [A]</p>

<p>이 구조 덕분에 Farad는 특정 vendor CLI의 내부 model protocol에 제품 전체가 결박되지 않는다. GLM/Kimi/OpenAI-compatible/Ollama/Anthropic 계열을 같은 runtime semantics에 넣을 수 있다.</p>

<h3 id="202-modelselection의-전역-mutable-authority">20.2 ModelSelection의 전역 mutable authority</h3>

<p>ModelSelection은 current endpoint, current model, client 하나를 전역으로 소유한다. session turn이 다른 model을 요청하면 ensure_client_for_model이 global endpoint/model을 바꾸고 _client=None으로 만든 뒤 새 AsyncHvppyAI를 생성한다. 이전 client를 close하지 않는다. app/services.py:172-258, 374-449 [A]</p>

<p>동시에 실행 중인 turn은 이미 local TurnContext에 이전 client reference를 갖고 있으므로 즉시 깨질 가능성은 낮다. 그러나 session A/B가 endpoint를 번갈아 쓰면 client와 httpx connection pool이 반복 생성되고 명시적으로 회수되지 않는다. shutdown은 현재 ModelSelection.client 하나만 닫는다. shutdown.py:308-316 [A][D]</p>

<p>또한 RunReadinessBuilder가 ensure_client()로 client를 만든 뒤 TurnContextProvider가 다시 session model을 해석해 ensure_client_for_model을 호출한다. readiness의 llm_client field는 실제 query에 사용되지 않는다. global selection churn과 불필요한 client 생성 가능성을 키운다.</p>

<p>repository-wide <code class="language-plaintext highlighter-rouge">llm_client</code> 사용 검색에서도 <code class="language-plaintext highlighter-rouge">RunReadiness.llm_client</code>는 생성·저장 외 consumer가 없다. 더구나 readiness는 asyncio runtime loop에서, MainWindow endpoint refresh/model picker는 Qt thread에서 같은 ModelSelection의 <code class="language-plaintext highlighter-rouge">_endpoints</code>, <code class="language-plaintext highlighter-rouge">_current_endpoint</code>, <code class="language-plaintext highlighter-rouge">_model</code>, <code class="language-plaintext highlighter-rouge">_client</code>를 만질 수 있는데 명시적 owner-thread assertion이나 lock이 없다. 현재 CPython reference assignment에 기대어 큰 사고가 안 보이는 것과 lifecycle authority가 올바른 것은 다르다. app/runtime/run_readiness.py:20-84; app/runtime/turn_context_provider.py:399-420; app/main_window/window.py:3292-3302 [A][D]</p>

<p>권고는 endpoint credential/config fingerprint를 key로 한 ClientPool이다.</p>

<ul>
  <li>key: provider, wire API, base URL, auth identity, relevant headers, timeout/retry</li>
  <li>value: ref-counted 또는 idle-TTL AsyncHvppyAI</li>
  <li>TurnContext가 immutable client lease와 model을 소유</li>
  <li>UI model picker selection은 default selection일 뿐 active turn authority를 변경하지 않음</li>
  <li>shutdown은 pool 전체를 await close</li>
  <li>endpoint config refresh는 stale key를 retire하고 active lease 종료 뒤 close</li>
</ul>

<p>필수 회귀 테스트는 (1) endpoint/model 교체 시 retired client가 정확히 한 번 close, (2) in-flight turn lease는 picker 변경에도 유지, (3) shutdown이 pool 전부 close, (4) readiness snapshot과 실제 TurnContext client key 일치다. 현재 provider/query tests는 넓지만 이 lifecycle 계약을 고정하는 테스트는 찾지 못했다. [A][D]</p>

<h3 id="203-미완-경로">20.3 미완 경로</h3>

<ul>
  <li>command_bearer auth는 RuntimeError로 명시적 미구현이다. app/services.py:427-428 [A]</li>
  <li>query_loop fallback machinery는 있으나 production TurnContext의 fallback_model은 빈 문자열이다. [A]</li>
  <li>Model speed/health는 자동 selection에 쓰지 않고 UI와 internal MCP advisory로만 제공한다. 이는 runtime metadata를 transcript에 넣지 않는 계약과 일치한다. [A][B]</li>
</ul>

<h2 id="21-uiterminaldesktop-rendering">21. UI·terminal·desktop rendering</h2>

<h3 id="211-ui-authority와-rendering-선택">21.1 UI authority와 rendering 선택</h3>

<p>Farad UI는 browser shell이나 Electron surface가 아니라 native PySide6 widget tree다. 표준 form·panel·dialog는 QWidget/QSS를 쓰고, 대량 반복 surface에만 custom painting을 제한한다. 제품 규약과 실제 구현이 일치하는 대표 경로는 다음 둘이다. [A][B]</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">TimelineView</code>: <code class="language-plaintext highlighter-rouge">QAbstractScrollArea</code> 위에 message element layout, visible-range paint, selection/hit-test/copy를 같은 text model로 처리</li>
  <li><code class="language-plaintext highlighter-rouge">SessionListWidget</code>: fixed-row virtual scrolling과 flyweight state로 visible row만 materialize/paint</li>
</ul>

<p><code class="language-plaintext highlighter-rouge">TimelineView</code>는 scroll offset, search generation, selection anchor, active-effect dirty rect를 view 하나가 소유한다. 이전 transcript page는 UiSession/ChatPanel이 명시적으로 요청하고, canvas는 visible region만 다시 그린다. ui/widgets/timeline_view/timeline_view.py:203-300 및 canvas 계열 [A]</p>

<p><code class="language-plaintext highlighter-rouge">SessionListWidget</code>은 row마다 QWidget을 만들지 않는다. viewport height와 scroll offset에서 first/last visible row를 계산하고, busy animation도 해당 row rect만 invalidate한다. project tree도 visual-unit index를 통해 visible range를 구한다. ui/panels/sidebar_panel.py:2290-2320, 2900-2975 [A]</p>

<p>이 선택은 공유 HPC·원격 X11·저사양 PC라는 배포 목표에 맞다. WebView process, DOM layout, row별 QObject explosion을 피하며, session 10,000개를 전부 widget으로 보유하지 않는다. 반면 custom canvas는 paint·hit-test·selection·accessibility가 갈라지기 쉬우므로 selection surface invariant와 회귀 테스트가 일반 QWidget보다 훨씬 중요하다. [A][D]</p>

<h3 id="212-hvppyterm">21.2 hvppyterm</h3>

<p>hvppyterm은 Farad와 독립된 embeddable package다. PTY bytes를 즉시 전부 parse하지 않고 33ms cadence로 coalesce하며, pending burst는 callback당 64KiB만 처리하고 나머지는 zero-delay timer로 양보한다. paint는 event rect가 가리키는 visible terminal rows만 순회하고 dirty cursor rows만 invalidate한다. packages/hvppyterm/hvppyterm/widget.py:37-48, 445-503 [A]</p>

<p>이 구조는 터미널 폭주가 Qt event loop를 독점하지 않게 하는 실용적 backpressure다. 1MiB pending threshold, parser slice, scrollback, selection, preedit IME를 같은 widget이 조율하므로 복잡도는 높지만 패키지 경계는 깨끗하다. Farad import가 없고 agent/tool semantics도 없다. [A]</p>

<h3 id="213-ui-hot-path-판정">21.3 UI hot path 판정</h3>

<table>
  <thead>
    <tr>
      <th>반복 경로</th>
      <th>현재 비용 모델</th>
      <th>판정</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>timeline paint</td>
      <td>visible/dirty rect와 loaded page에 비례</td>
      <td>양호; custom surface invariant가 핵심</td>
    </tr>
    <tr>
      <td>session sidebar paint</td>
      <td>visible rows에 비례</td>
      <td>양호; 10k widget explosion 없음</td>
    </tr>
    <tr>
      <td>older transcript load</td>
      <td>chunk/index paging, rewind가 있으면 full projection fallback 가능</td>
      <td>정상 session은 양호, rewind session은 위험</td>
    </tr>
    <tr>
      <td>terminal feed</td>
      <td>33ms coalesce + 64KiB parser slice</td>
      <td>양호; event-loop yielding 명시</td>
    </tr>
    <tr>
      <td>scheduler tick</td>
      <td>Qt에서 10k task stat/sort</td>
      <td>치명적 위반</td>
    </tr>
    <tr>
      <td>permission popup read</td>
      <td>Qt에서 cold settings JSON read 가능</td>
      <td>NFS latency 위반</td>
    </tr>
    <tr>
      <td>subagent round completion</td>
      <td>Qt에서 transcript slice read/output write/meta update</td>
      <td>긴 round·NFS에서 위험</td>
    </tr>
    <tr>
      <td>settings timer</td>
      <td>Qt에서 mtime stat, 변경 시 config read/build</td>
      <td>explicit-refresh 계약 위반</td>
    </tr>
  </tbody>
</table>

<p>즉 UI renderer 자체는 규모를 잘 다루지만, application/service의 동기 I/O가 Qt callback으로 역류한다. “UI가 느리다”는 증상이 나와도 painter를 먼저 의심하기보다 scheduler, permission, settings, subagent finalization의 filesystem span을 correlation해야 한다. [A][D]</p>

<h3 id="214-ui-구조-부채">21.4 UI 구조 부채</h3>

<p>SidebarPanel 6,700여 lines, MainWindow 3,700여 lines, CustomizePanel 3,800여 lines, ChatPanel 2,900여 lines는 변경 충돌과 lifecycle 추론 비용이 크다. 다만 파일 길이만 보고 widget을 쪼개면 signal relay와 wrapper가 늘 수 있다. 분해 기준은 다음 authority여야 한다. [D]</p>

<ul>
  <li>MainWindow: composition/wiring과 user interaction orchestration 분리</li>
  <li>Sidebar: virtual data model, row renderer, project/session action policy 분리</li>
  <li>Customize: catalog query/projection과 panel widgets 분리</li>
  <li>ChatPanel: session binding, composer command, timeline paging 분리</li>
  <li>Timeline: layout/text model과 interaction/paint policy는 현재처럼 별도 module 유지</li>
</ul>

<p>theme/i18n/font-aware geometry, modern tooltip, shortcut binding, controlled radius 같은 제품 규약은 촘촘하다. 이들은 시각 일관성을 높이지만 architecture 관점에서는 “UI가 source truth를 소유하지 않는다”와 “반복 경로에 timer/file read를 넣지 않는다”보다 우선하지 않는다. [B]</p>

<h2 id="22-failure-cancellation-observability">22. Failure, cancellation, observability</h2>

<h3 id="221-failurecancellation에서-잘-된-부분">22.1 failure/cancellation에서 잘 된 부분</h3>

<ul>
  <li>LLM stream은 cancel/error에서 close</li>
  <li>tool abort 시 모든 call id에 terminal synthetic result</li>
  <li>MCP call timeout, liveness, breaker, reconnect</li>
  <li>scheduled run은 RUNNING pre-persist와 orphan recovery</li>
  <li>background shell은 process group kill과 output hard cap</li>
  <li>transcript append 뒤 UI projection</li>
  <li>provider request/SSE와 slow span logging</li>
  <li>app diagnostics가 settings poll interval, endpoint, scheduler, capability generation, tool run을 수집</li>
  <li>performance monitor와 log_span이 주요 cold/hot boundary에 배치</li>
</ul>

<p>[A]</p>

<p>특히 “사용자가 취소했다”를 단순 task.cancel로 끝내지 않고 provider stream, tool protocol, child process, transcript terminal state까지 연결한 점은 강하다.</p>

<h3 id="222-관측성-구조">22.2 관측성 구조</h3>

<p>Farad는 desktop app 치고 관측 범위가 넓다.</p>

<ul>
  <li>loguru console/file sink, 10MB rotation, gzip, newest 5 retention과 total footprint maintenance</li>
  <li><code class="language-plaintext highlighter-rouge">log_span</code> slow threshold와 선택적 structured perf JSONL; count/min/p50/p95/p99/max 및 frame threshold counters</li>
  <li>Qt <code class="language-plaintext highlighter-rouge">QTimer</code> drift 기반 event-loop stall monitor와 선택적 watchdog stack sampler</li>
  <li><code class="language-plaintext highlighter-rouge">RunMeasurementAccumulator</code>의 session/run별 duration, API time, LLM/tool count, provider usage; transcript history를 다시 scan하지 않고 current-run event만 누적</li>
  <li>최근 API request/response snapshot의 secret redaction, gzip, session별 10 pair, 7일 TTL</li>
  <li>config path, HOME mismatch, filesystem type/NFS, session index, API log corpus, capability generation, model, scheduler, crash trace, tool run을 묶는 bounded operational diagnostics snapshot</li>
</ul>

<p>근거: core/logging_setup.py:1-28, 51-82; perf/event_loop_monitor.py:29-115; perf/recorder.py:1-95; app/runtime/run_measurement.py:49-150; session/api_log_store.py:18-40, 175 이후; app/diagnostics.py:201-330 [A]</p>

<p>API log write가 <code class="language-plaintext highlighter-rouge">asyncio.to_thread()</code>로 event loop 밖에서 수행된다는 점은 특히 중요하다. 같은 AsyncSession 안에서 transcript append는 sync로 남아 있으므로, 이미 프로젝트 내부에 “ordering은 await하되 blocking filesystem work는 thread에서 실행”하는 선례가 존재한다. app/runtime/async_session.py:1518-1595 [A]</p>

<p>관측성의 빈칸도 명확하다.</p>

<ol>
  <li>Qt stall monitor는 env/perf opt-in이고, 단일 asyncio runtime loop의 heartbeat/stall은 동등하게 직접 측정하지 않는다.</li>
  <li>session_id/run_id/llm_call_id는 존재하지만 log·tool·MCP·filesystem span을 하나의 trace tree로 묶는 강제 correlation contract는 없다.</li>
  <li>scheduler tick의 stat 수, ConfigProvider tier stat 수, transcript queue wait 같은 NFS budget counter가 없다.</li>
  <li>local desktop 제품이므로 분산 telemetry가 반드시 필요한 것은 아니지만, support snapshot만으로 “어느 owner thread가 몇 ms 막혔는가”를 재구성하기 어렵다.</li>
</ol>

<p>권고는 무거운 원격 APM을 먼저 넣는 것이 아니다. Qt와 asyncio 각각에 heartbeat를 두고, hot-path span에 owner thread·session/run id·filesystem operation count를 넣으며, diagnostics가 최근 stall과 budget violation을 bounded ring buffer로 보여주게 하면 된다. [D]</p>

<h3 id="223-실패가-감춰지는-곳">22.3 실패가 감춰지는 곳</h3>

<ul>
  <li>TranscriptStore index corruption은 empty index로 축소</li>
  <li>malformed JSONL은 조용히 skip</li>
  <li>initial transcript load failure 시 AsyncSession은 log 후 empty transcript로 진행</li>
  <li>MCP readiness failure는 background task log와 snapshot 상태로 남지만 turn UI 단계 표현이 약함</li>
  <li>hook timeout이 descendant process 전체 종료를 보장하지 않음</li>
  <li>endpoint/settings auto refresh의 Qt blocking을 diagnostics는 interval로만 보여주고 contract violation은 표시하지 않음</li>
</ul>

<p>[A][D]</p>

<p>“복구 가능하게 계속 진행”은 desktop app에 중요하지만, empty state로 계속하는 것이 데이터 손실을 확정할 수 있는 경우에는 read-only/quarantine가 더 안전하다.</p>

<h2 id="23-문서와-구현의-차이">23. 문서와 구현의 차이</h2>

<table>
  <thead>
    <tr>
      <th>주제</th>
      <th>문서의 주장</th>
      <th>현재 구현</th>
      <th>판정</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>settings refresh</td>
      <td>mtime poll 제거, external edit는 explicit refresh</td>
      <td>SettingsService 5분 QTimer가 settings/endpoints globals를 poll</td>
      <td>중대 불일치</td>
    </tr>
    <tr>
      <td>turn hot path</td>
      <td>turn start에서 mtime stat 없음</td>
      <td>RuntimeResolver/ConfigProvider가 per-turn settings/endpoints tier stat</td>
      <td>중대 불일치</td>
    </tr>
    <tr>
      <td>endpoint refresh</td>
      <td>deployment poll은 operator state 전용</td>
      <td>MainWindow가 20분 poll에 endpoint 강제 재해석 callback 등록; 기본 composition에는 discovery provider 없음</td>
      <td>계약·명명 불일치, Qt stat/fanout</td>
    </tr>
    <tr>
      <td>MCP readiness</td>
      <td>last snapshot으로 non-blocking turn</td>
      <td>first/dirty turn은 wait=True reconcile 완료 대기</td>
      <td>semantics 불일치</td>
    </tr>
    <tr>
      <td>transcript durability</td>
      <td>durable disk-first</td>
      <td>flush/atomic replace는 있으나 fsync 없음</td>
      <td>용어 범위 과장</td>
    </tr>
    <tr>
      <td>architecture Mermaid</td>
      <td>Projects·현재 서비스 topology</td>
      <td>SPACEHOME/SPACESVC/SPACESTORE/MPOLL 같은 정의 없는 stale node reference</td>
      <td>문서 diagram broken</td>
    </tr>
    <tr>
      <td>fallback</td>
      <td>retry/fallback을 지원</td>
      <td>core machinery는 있으나 production fallback_model=""</td>
      <td>기능 wiring 미완</td>
    </tr>
    <tr>
      <td>hard guard</td>
      <td>.git/.env가 BYPASS에서도 보호</td>
      <td>built-in Edit/Write에만 적용, Bash/hook은 가능</td>
      <td>trust boundary 설명 부족</td>
    </tr>
    <tr>
      <td>home path</td>
      <td>passwd/NSS home만 사용</td>
      <td>runtime/scheduler 일부 Path.expanduser</td>
      <td>환경 규약 위반</td>
    </tr>
    <tr>
      <td>plugin hook</td>
      <td>plugin extension 구성요소</td>
      <td>discovery metadata와 production hook wiring 경계 불명확</td>
      <td>상태 명시 필요</td>
    </tr>
  </tbody>
</table>

<p>docs/architecture.md의 Mermaid는 SPACEHOME, SPACESVC, SPACESTORE, MPOLL edge를 참조하지만 해당 node 정의가 현재 문서에 없다. Space→Project rename과 MtimePoller 제거 뒤 stale edge가 남은 것으로 보인다. 구조 문서가 architecture review의 source라면 diagram parse/test를 CI에 넣는 것이 좋다. [A][B]</p>

<h2 id="24-정성-판정표">24. 정성 판정표</h2>

<p>소수점 점수는 입증 범위보다 정밀해 보이므로 쓰지 않는다. <code class="language-plaintext highlighter-rouge">강</code>은 현재 구현 증거가 있고,
<code class="language-plaintext highlighter-rouge">조건부</code>는 좋은 구조와 미검증·예외 경로가 함께 있으며, <code class="language-plaintext highlighter-rouge">취약/치명</code>은 다음 release tranche에서
종료 기준이 필요한 상태다.</p>

<table>
  <thead>
    <tr>
      <th>영역</th>
      <th>판정</th>
      <th>요약</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Desktop/HPC 제품 적합성</td>
      <td>강한 방향</td>
      <td>shared-FS singleton, Qt UX, NFS 의식이 독자적</td>
    </tr>
    <tr>
      <td>패키지 경계</td>
      <td>강</td>
      <td>SDK/terminal/app 의존 방향이 깨끗함</td>
    </tr>
    <tr>
      <td>Qt↔async ownership</td>
      <td>조건부</td>
      <td>주 경로 우수, snapshot/direct access 예외 존재</td>
    </tr>
    <tr>
      <td>query loop</td>
      <td>강</td>
      <td>callback inversion, streaming, recovery, protocol handling 강함</td>
    </tr>
    <tr>
      <td>transcript correctness</td>
      <td>강한 기반</td>
      <td>append/rewind/normalization 강함</td>
    </tr>
    <tr>
      <td>transcript integrity·scale</td>
      <td>취약</td>
      <td>sync shared-loop I/O, rewind fallback, fsync/rebuild 부재</td>
    </tr>
    <tr>
      <td>permission policy</td>
      <td>강</td>
      <td>precedence, scope, attendance, external MCP 보수성 우수</td>
    </tr>
    <tr>
      <td>OS containment</td>
      <td>부재</td>
      <td>sandbox 없음</td>
    </tr>
    <tr>
      <td>MCP lifecycle</td>
      <td>조건부</td>
      <td>transport, delta, breaker, drain 강함; readiness contract 충돌</td>
    </tr>
    <tr>
      <td>session/NFS</td>
      <td>조건부</td>
      <td>derived index와 guard test 우수, 일부 cold/full paths</td>
    </tr>
    <tr>
      <td>subagent/background</td>
      <td>강한 기반</td>
      <td>bounds/index/cancellation 우수, pending list hotspot</td>
    </tr>
    <tr>
      <td>scheduler semantics</td>
      <td>강</td>
      <td>overlap, unattended, recovery, history가 정교</td>
    </tr>
    <tr>
      <td>scheduler 10k 성능</td>
      <td>치명</td>
      <td>매분 Qt 전체 scan/stat/sort</td>
    </tr>
    <tr>
      <td>provider abstraction</td>
      <td>강</td>
      <td>SDK provider boundary 강함</td>
    </tr>
    <tr>
      <td>provider client lifecycle</td>
      <td>취약</td>
      <td>전역 mutable client와 close 누락</td>
    </tr>
    <tr>
      <td>extension model</td>
      <td>조건부</td>
      <td>exposure/catalog/data plugin은 좋음, provenance/pinning 약함</td>
    </tr>
    <tr>
      <td>explicit refresh 일관성</td>
      <td>취약</td>
      <td>capability는 준수, settings/endpoints는 정반대</td>
    </tr>
    <tr>
      <td>테스트</td>
      <td>강</td>
      <td>폭과 속도, bounded fallback guard가 강함</td>
    </tr>
    <tr>
      <td>module cohesion</td>
      <td>취약</td>
      <td>거대 UI/runtime orchestration class 다수</td>
    </tr>
  </tbody>
</table>

<h2 id="25-우선순위-권고">25. 우선순위 권고</h2>

<p>이 단독 감사의 우선순위는 최종 교차 종합을 반영한 <a href="https://hvppycoding.github.io/research/farad-architecture-roadmap/">06 로드맵</a>이
권위다. 아래 항목도 그 최종 판정에 맞춰 P0/P1을 정규화했다.</p>

<h3 id="p0--다음-architecture-tranche에서-반드시-처리">P0 — 다음 architecture tranche에서 반드시 처리</h3>

<h4 id="final-p0-2-scheduler-next-due-index">Final P0-2. Scheduler next-due index</h4>

<p>목표는 1분 tick에서 전체 schedule read/stat/sort를 없애는 것이다.</p>

<ul>
  <li>schedule derived index에 due 판단 최소 field 저장</li>
  <li>in-memory min-heap</li>
  <li>write-through targeted update</li>
  <li>startup/explicit worker reconcile</li>
  <li>Qt tick에서 full list 호출 금지 guard test</li>
</ul>

<p>효과: NFS 10k 계약 회복, UI freeze 제거, 공유 장비 부하 감소.</p>

<h4 id="final-p0-3-transcript-io-execution-boundary">Final P0-3. Transcript I/O execution boundary</h4>

<ul>
  <li>session별 serialized I/O executor</li>
  <li>disk-before-UI commit receipt 유지</li>
  <li>initial max ID/index checkpoint로 double scan 제거</li>
  <li>rewind-aware projection index</li>
  <li>corruption rebuild/quarantine</li>
</ul>

<p>효과: 한 session의 NFS I/O가 모든 async session을 막는 문제 제거.</p>

<h4 id="final-p0-4-refresh-truth-단일화">Final P0-4. Refresh truth 단일화</h4>

<ul>
  <li>5분 SettingsService timer 제거</li>
  <li>ConfigProvider cached get과 explicit reconcile 분리</li>
  <li>per-turn mtime stat 제거</li>
  <li>20분 endpoint 강제 재해석 callback 제거 또는 공식 예외화; 향후 network discovery를 실제 연결하면 worker화</li>
  <li>readiness/runtime/TurnContext의 중복 resolve 제거</li>
  <li>docs/layers.md, docs/config.md, docs/architecture.md를 구현과 동시에 수정</li>
</ul>

<p>효과: 팀이 같은 architecture contract를 보고 개발할 수 있음.</p>

<h4 id="final-p0-1-security-truth와-execution-seam">Final P0-1. Security truth와 execution seam</h4>

<ul>
  <li>permission을 “policy, not sandbox”로 문서/UI에서 일관되게 정의</li>
  <li>.git/.env guard가 Edit/Write 한정임을 명시</li>
  <li>Bash/background/hook과 stdio MCP <strong>server spawn</strong>을 위한 ExecutionBoundary Protocol</li>
  <li>HTTP/SSE MCP의 원격 side effect는 local sandbox가 아니라 trust/credential/permission으로 구분</li>
  <li>sandbox capability detection과 future platform adapter point</li>
  <li>최소 한 지원 platform adapter에서 prohibited read/write/network integration test 통과</li>
  <li>hook descendant process cancellation 보강</li>
</ul>

<p>효과: 기능이 없는 sandbox를 암묵적으로 약속하는 위험 제거, 향후 containment 구현 기반.</p>

<h3 id="p0--같은-correctnesssecurity-tranche에서-계속">P0 — 같은 correctness/security tranche에서 계속</h3>

<h4 id="final-p0-6-endpoint-keyed-clientpool">Final P0-6. endpoint-keyed ClientPool</h4>

<p>전역 client 하나를 turn-scoped lease로 교체하고 모든 client를 lifecycle에서 close한다. immutable
RuntimeSnapshot에는 live lease가 아니라 selection key/generation만 두며, RunReadiness는 client를
생성·보관하지 않는다. TurnContext admission에서 한 번 acquire/release한다. production fallback은
SPEC상 실제 지원 요구가 있으면 같은 lease 계약으로 wiring하고, 아니면 scaffold·UI·문서에서 완성
기능처럼 표현하지 않는다.</p>

<h4 id="final-p0-5-완전한-qtasync-snapshot-boundary">Final P0-5. 완전한 Qt/async snapshot boundary</h4>

<p>permission cascade와 PostBox startup ingress의 direct access를 제거한다. immutable snapshot publication과 async request/result를 사용한다.</p>

<h4 id="final-p0-3-continuation-transcript-integrity-contract">Final P0-3 continuation. Transcript integrity contract</h4>

<p>fsync policy를 NFS 비용과 함께 명문화하고 index validation/rebuild, corruption diagnostics, read-only recovery를 추가한다.</p>

<h4 id="final-p0-4-continuation-snapshot-first-mcp-readiness">Final P0-4 continuation. Snapshot-first MCP readiness</h4>

<p>최종 제품 결정은 Snapshot-first다. core settings/endpoint/model snapshot generation 1 전에는 prompt
admission을 막되, optional MCP는 typed generation-0 empty/stale snapshot으로 시작하고 slow reconcile을
기다리지 않는다. 현재 <code class="language-plaintext highlighter-rouge">wait=True</code> 동작은 제거한다.</p>

<h4 id="final-p0-7-nss-account-home-correctness">Final P0-7. NSS account-home correctness</h4>

<p>runtime/turn/scheduler의 account-relative path를 passwd/NSS helper로 통일하고 mismatched <code class="language-plaintext highlighter-rouge">$HOME</code> fixture로
permission, cwd, storage 경계를 고정한다.</p>

<h4 id="final-p0-4-continuation-cold-registry-scan-worker화">Final P0-4 continuation. cold registry scan worker화</h4>

<p>explicit refresh라는 시점 계약에 “disk discovery는 Qt/shared event loop에서 실행하지 않는다”는 thread 계약을 추가한다.</p>

<h3 id="p1--응집도provenance자료구조">P1 — 응집도·provenance·자료구조</h3>

<h4 id="final-p1-1p1-2-authority-기준으로-큰-class-분해">Final P1-1/P1-2. authority 기준으로 큰 class 분해</h4>

<p>AsyncSession, UiSession, SubAgentManager, MainWindow, SidebarPanel, CustomizePanel을 파일 길이가 아니라 owner/lifecycle/change reason 기준으로 나눈다.</p>

<h4 id="final-p1-5-marketplace-provenance">Final P1-5. marketplace provenance</h4>

<p>source identity, commit/content digest, executable fingerprint를 install record에 남기고 executable change 시 trust를 재확인한다.</p>

<h4 id="final-p1-continuation-pending-queue-자료구조">Final P1 continuation. pending queue 자료구조</h4>

<p>SubAgentManager pending list의 remove/index/count를 O(1) 구조로 바꾸고 10k queue guard test를 추가한다.</p>

<h3 id="p2-조건부--headless-core는-요구가-생길-때">P2-조건부 — headless core는 요구가 생길 때</h3>

<p>현재 제품이 desktop-first인 동안 별도 daemon/RPC를 미리 만들 필요는 없다. CI/remote/second UI가 roadmap에 들어올 때 RuntimeHost/PresentationHost를 분리한다.</p>

<h2 id="26-권장-목표-아키텍처">26. 권장 목표 아키텍처</h2>

<pre class="mermaid">
flowchart TB
    IN[&quot;GUI · shared-FS CLI ingress&quot;]:::boundary --&gt; HOST[&quot;RuntimeHost&quot;]:::aio
    UI[&quot;Qt PresentationHost&quot;]:::qt --&gt;|command| SNAP[&quot;Immutable snapshots and commands&quot;]:::boundary
    SNAP --&gt; HOST
    HOST -.-&gt;|published snapshot| SNAP
    SNAP -.-&gt;|queued projection| UI

    HOST --&gt; RUN[&quot;Session RunCoordinator&quot;]:::aio
    RSNAP[&quot;Published RuntimeSnapshot&quot;]:::neutral
    RUN --&gt;|read one generation| RSNAP
    RUN --&gt; CTX[&quot;TurnSnapshot&lt;br/&gt;selection key · config/capability generation&quot;]:::neutral
    RSNAP --&gt;|copy selected generation| CTX
    RUN --&gt; LOOP[&quot;query_loop&quot;]:::aio

    RUN --&gt;|acquire/release turn lease| POOL[&quot;Endpoint-keyed ClientPool&quot;]:::neutral
    POOL --&gt;|lease in TurnContext| LOOP
    LOOP --&gt; AUTH[&quot;Authorization Policy&quot;]:::neutral
    AUTH --&gt; INPROC[&quot;In-process built-in&quot;]:::neutral
    AUTH --&gt; LREQ[&quot;Local process request&quot;]:::neutral
    LREQ --&gt; EXEC[&quot;ExecutionBoundary&quot;]:::boundary
    AUTH --&gt; MTRUST[&quot;MCP trust / credential adapter&quot;]:::neutral
    MTRUST --&gt; RMCP[&quot;Remote HTTP/SSE MCP&quot;]:::aio
    MTRUST --&gt; STDIO[&quot;stdio MCP spawn request&quot;]:::neutral
    STDIO --&gt; EXEC
    EXEC --&gt; SB[&quot;Optional platform sandbox&quot;]:::neutral
    EXEC --&gt; PROC[&quot;Shell · hook · background&quot;]:::neutral

    RUN --&gt; TIO[&quot;Per-session TranscriptIO queue&quot;]:::aio
    TIO --&gt; TIDX[&quot;Validated projection and index&quot;]:::neutral

    HOST --&gt; SCH[&quot;Scheduler service&quot;]:::aio
    SCH --&gt; DUE[&quot;Derived due index or min-heap&quot;]:::neutral
    DUE --&gt; RUN

    REF[&quot;Explicit reconcile service&quot;]:::aio --&gt;|publish next generation| RSNAP
    REF --&gt; CAP[&quot;Capability registries&quot;]:::neutral
    REF --&gt; DUE
    REF --&gt; PROV[&quot;Config providers&quot;]:::neutral

    classDef qt fill:#E8F5E9,stroke:#388E3C,stroke-width:2px,color:#1B5E20
    classDef aio fill:#FFF3E0,stroke:#F57C00,stroke-width:2px,color:#E65100
    classDef boundary fill:#F3E5F5,stroke:#7B1FA2,stroke-width:2px,color:#4A148C
    classDef neutral fill:#FAFAFA,stroke:#9E9E9E,stroke-width:2px,color:#424242

</pre>

<p>이 목표 구조에서 중요한 것은 layer를 더 많이 만드는 것이 아니다.</p>

<ul>
  <li>turn은 시작 시 live resource가 없는 immutable snapshot을 받고 selection key로 client lease를 한 번
acquire/release한다.</li>
  <li>Qt와 async는 command/snapshot만 교환한다.</li>
  <li>disk I/O는 owner queue에서 serialize한다.</li>
  <li>local process execution과 stdio MCP spawn은 boundary를 지난다. remote MCP side effect는 local sandbox가
아니라 trust/credential/permission 경계로 다룬다.</li>
  <li>반복 timer는 index를 읽고 source tree를 scan하지 않는다.</li>
  <li>explicit reconcile만 source file과 NFS를 다시 확인한다.</li>
</ul>

<h2 id="27-검증-기록">27. 검증 기록</h2>

<h3 id="271-repository-metrics">27.1 repository metrics</h3>

<p>실행:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>./.venv/bin/python .agents/skills/farad-clean-review-context/scripts/collect_repo_metrics.py --root "$PWD"
</code></pre></div></div>

<p>주요 결과는 3.2절에 반영했다. [C]</p>

<h3 id="272-focused-tests--runtime-transcript-tool-mcp-session-scheduler-concurrency">27.2 focused tests — runtime, transcript, tool, MCP, session, scheduler, concurrency</h3>

<p>실행:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>./.venv/bin/pytest -q \
  packages/farad/tests/test_query_loop.py \
  packages/farad/tests/test_query_loop_helpers.py \
  packages/farad/tests/test_transcript_store.py \
  packages/farad/tests/test_transcript_normalize.py \
  packages/farad/tests/test_permission.py \
  packages/farad/tests/test_tool_executor.py \
  packages/farad/tests/test_mcp_integration.py \
  packages/farad/tests/test_session.py \
  packages/farad/tests/test_session_index.py \
  packages/farad/tests/test_session_meta_cache.py \
  packages/farad/tests/test_scheduler.py \
  packages/farad/tests/test_schedule_history_store.py \
  packages/farad/tests/test_subagents.py \
  packages/farad/tests/test_background_shell.py \
  packages/farad/tests/test_capability_hot_reload.py \
  packages/hvppyai/tests/test_providers.py \
  packages/hvppyai/tests/test_streaming_regressions.py
</code></pre></div></div>

<p>결과: 903 passed in 7.85s. [C]</p>

<h3 id="273-focused-tests--asyncsession-compaction-turncontext-ui-bridge-catalog-sdk">27.3 focused tests — AsyncSession, compaction, TurnContext, UI bridge, catalog, SDK</h3>

<p>실행:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>./.venv/bin/pytest -q \
  packages/farad/tests/test_async_session.py \
  packages/farad/tests/test_compaction.py \
  packages/farad/tests/test_turn_context_provider.py \
  packages/farad/tests/test_turn_context_freshness_contract.py \
  packages/farad/tests/test_ui_session.py \
  packages/farad/tests/test_async_session_handle_proxy.py \
  packages/farad/tests/test_capability_catalog.py \
  packages/farad/tests/test_capability_discovery.py \
  packages/hvppyai/tests/test_client_retry_regressions.py \
  packages/hvppyai/tests/test_integration.py
</code></pre></div></div>

<p>결과: 411 passed in 0.98s. [C]</p>

<h3 id="274-focused-tests--readiness-pair-factory-postbox-schedulersubagent-boundary">27.4 focused tests — readiness, pair factory, PostBox, scheduler/subagent boundary</h3>

<p>실행:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>./.venv/bin/pytest -q \
  packages/farad/tests/test_run_readiness_builder.py \
  packages/farad/tests/test_session_pair_factory.py \
  packages/farad/tests/test_postbox.py \
  packages/farad/tests/test_postbox_boundary_proxy.py \
  packages/farad/tests/test_postbox_pump.py \
  packages/farad/tests/test_scheduler_local_timezone.py \
  packages/farad/tests/test_scheduled_tasks_server.py \
  packages/farad/tests/test_subagent_router.py \
  packages/farad/tests/test_subagent_bootstrap.py \
  packages/farad/tests/test_subagent_output_file.py
</code></pre></div></div>

<p>결과: 165 passed in 1.26s. [C]</p>

<h3 id="275-추가-focused-tests--settingsendpoint-refresh-contract">27.5 추가 focused tests — settings/endpoint refresh contract</h3>

<p>실행:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>./.venv/bin/pytest -q \
  packages/farad/tests/test_settings_service.py \
  packages/farad/tests/test_services_refresh.py \
  packages/farad/tests/test_runtime_resolver.py \
  packages/farad/tests/test_settings_discovery_decouple.py \
  packages/farad/tests/test_operational_diagnostics.py
</code></pre></div></div>

<p>결과: 48 passed in 0.67s. [C]</p>

<p>총 focused test: 1,527 passed. 최종 QA에서는 위 42개 파일을 한 invocation으로 다시 실행했고 <code class="language-plaintext highlighter-rouge">1527 passed in 10.20s</code>를 확인했다. [C]</p>

<p>이 테스트들은 현재 behavior를 잘 고정하고 있다. 특히 settings test가 통과한다는 사실은 5분 poll/per-turn freshness가 우연히 남은 dead code가 아니라 현재 테스트된 behavior라는 뜻이다. 그러므로 explicit-refresh 계약으로 바꾸려면 테스트도 함께 의도적으로 갱신해야 한다.</p>

<h3 id="276-실행하지-않은-gate">27.6 실행하지 않은 gate</h3>

<ul>
  <li>전체 packages/farad/tests suite</li>
  <li>전체 hvppyai/hvppyterm suite</li>
  <li>ruff</li>
  <li>mypy</li>
  <li>scripts/check_farad_types.py</li>
  <li>GUI end-to-end/manual latency test</li>
  <li>실제 NFS에서 10,000 schedule/session benchmark</li>
</ul>

<h2 id="28-최종-판정">28. 최종 판정</h2>

<p>Farad는 네 제품 비교에서 “후발 GUI wrapper”로 분류하면 안 된다. 오히려 다음 영역에서는 자체 architecture가 명확하다.</p>

<ul>
  <li>desktop/HPC shared-filesystem deployment</li>
  <li>provider-neutral SDK와 app-owned agent loop</li>
  <li>transcript 중심 session truth</li>
  <li>Qt/async proxy</li>
  <li>capability exposure와 explicit discovery 구상</li>
  <li>scheduler/subagent/background work의 제품 통합</li>
  <li>NFS 파생 인덱스와 10k guard test</li>
</ul>

<p>반대로 경쟁 제품과 비교할 때 감점해야 할 영역도 분명하다.</p>

<ul>
  <li>GUI process에 runtime이 종속되어 headless surface가 약함</li>
  <li>OS sandbox가 없음</li>
  <li>전역 model client authority와 lifecycle이 약함</li>
  <li>refresh contract가 구현 내부에서 갈라짐</li>
  <li>scheduler와 transcript에 큰 반복경로 병목이 있음</li>
  <li>대형 orchestration class로 clean boundary가 다시 뭉침</li>
</ul>

<p>가장 중요한 로드맵 원칙은 이것이다.</p>

<blockquote>
  <p>Farad는 기능을 더 얹기 전에 스스로 선언한 authority·thread·hot path·persistence 계약을 실제 모든 경로에서 같은 의미로 만들어야 한다. Scheduler index, transcript I/O queue, refresh truth, ClientPool, ExecutionBoundary가 그 핵심이다.</p>
</blockquote>

<p>이 다섯 가지를 바로잡으면 Farad의 현재 장점은 유지하면서도 OpenCode·Claude Code·Codex와의 비교에서 “기능은 많지만 runtime foundation이 불안한 제품”이 아니라 “desktop-first라는 명확한 선택을 가진 production architecture”로 평가할 수 있다.</p>

<style>pre.mermaid{overflow-x:auto;background:transparent;border:0;padding:0}.mermaid svg{max-width:100%;height:auto}</style>

<script type="module">
import mermaid from "https://cdn.jsdelivr.net/npm/mermaid@11.16.0/dist/mermaid.esm.min.mjs";
mermaid.initialize({startOnLoad:true,securityLevel:"strict"});
</script>]]></content><author><name>hvppycoding</name></author><category term="research" /><summary type="html"><![CDATA[Farad 현재 구현 아키텍처 심층 감사]]></summary></entry><entry><title type="html">Codex 제품·런타임·아키텍처 심층 분석</title><link href="https://hvppycoding.github.io/research/codex-architecture-analysis/" rel="alternate" type="text/html" title="Codex 제품·런타임·아키텍처 심층 분석" /><published>2026-07-13T23:53:00+09:00</published><updated>2026-07-13T23:53:00+09:00</updated><id>https://hvppycoding.github.io/research/codex-architecture-analysis</id><content type="html" xml:base="https://hvppycoding.github.io/research/codex-architecture-analysis/"><![CDATA[<h1 id="codex-제품런타임아키텍처-심층-분석">Codex 제품·런타임·아키텍처 심층 분석</h1>

<blockquote>
  <p><a href="https://hvppycoding.github.io/research/coding-agent-architecture-comparison/">전체 보고서 인덱스</a></p>
</blockquote>

<blockquote>
  <p>조사 기준일: 2026-07-13 (Asia/Seoul)<br />
공개 소스 스냅샷: <code class="language-plaintext highlighter-rouge">openai/codex@2b0b37abb7b29ea272fa856d6dff9221a6bd9ba8</code><br />
로컬 실행 관찰: 설치된 <code class="language-plaintext highlighter-rouge">codex-cli</code> 0.144.1<br />
분석 범위: Codex CLI/core, app-server, non-interactive interface, 로컬 sandbox,
subagent/thread, tools/MCP/skills/plugins, 저장·복구 구조. 호스티드 Codex 내부 구현은
공개 계약 이상으로 추정하지 않는다.</p>
</blockquote>

<h2 id="1-결론부터">1. 결론부터</h2>

<p>Codex의 핵심은 “좋은 터미널 UI”보다 훨씬 아래에 있다. 공개 Rust 구현에서 확인되는 본체는
다음 네 가지가 결합된 <strong>이벤트 기반 thread/turn 실행 커널</strong>이다.</p>

<ol>
  <li>클라이언트가 <code class="language-plaintext highlighter-rouge">Submission</code>을 넣고 <code class="language-plaintext highlighter-rouge">Event</code>를 받는 queue-pair 계약</li>
  <li>thread마다 한 번에 하나의 active task만 허용하면서 interrupt/steer/approval을 같은 순서열로
직렬화하는 session authority</li>
  <li>모델에 노출할 tool schema와 실제 dispatch registry를 분리한 per-step tool plan</li>
  <li>JSONL을 durable replay 원본으로, SQLite를 조회용 metadata projection으로 두는 저장 경계</li>
</ol>

<p>이 설계 덕분에 TUI, IDE, app-server, <code class="language-plaintext highlighter-rouge">codex exec</code>가 같은 엔진을 서로 다른 표면으로 사용할 수
있다. 동시에 운영 복잡도도 높다. core에 auth, model/provider, MCP, extensions, hooks, skills,
permissions, sandbox, telemetry, storage, subagent graph가 한 session 수명 안에서 만나므로, 강한 타입과
세밀한 concurrency protocol이 없으면 유지하기 어려운 구조다.</p>

<p>한 문장으로 평가하면 다음과 같다.</p>

<blockquote>
  <p><strong>Codex는 공개 로컬 구현에서 surface-agnostic agent runtime과 OS-enforced execution boundary,
replay 가능한 thread protocol을 특히 강하게 결합한다. 대신 OpenAI 제품 생태계와 거대한 Rust workspace에
최적화되어 있어, Farad가 통째로 모방할 대상이 아니라 경계와 불변식만 선별할 대상이다.</strong></p>
</blockquote>

<p>위 결론과 뒤의 판정표는 공개 구현 <code class="language-plaintext highlighter-rouge">A</code>와 공식 계약 <code class="language-plaintext highlighter-rouge">B</code>에서 도출한 <code class="language-plaintext highlighter-rouge">D</code> 등급의 비교 평가다.
메커니즘의 존재를 실제 운영 품질, 특정 규모의 성능, 비공개 제품 surface의 구현 동일성으로
승격하지 않는다.</p>

<h2 id="2-증거-등급과-분석-한계">2. 증거 등급과 분석 한계</h2>

<p>이 보고서는 <a href="https://hvppycoding.github.io/research/coding-agent-comparison-methodology/">공통 방법론</a>의 등급을 따른다.</p>

<table>
  <thead>
    <tr>
      <th>항목</th>
      <th>근거</th>
      <th style="text-align: right">등급</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>CLI/core/app-server 내부 구조</td>
      <td>고정 commit의 공개 Rust 소스</td>
      <td style="text-align: right">A</td>
    </tr>
    <tr>
      <td>sandbox, cloud task, subagent의 사용자 계약</td>
      <td>공식 Codex 문서</td>
      <td style="text-align: right">B</td>
    </tr>
    <tr>
      <td>로컬 설치 버전</td>
      <td><code class="language-plaintext highlighter-rouge">codex --version</code> 실행 관찰</td>
      <td style="text-align: right">C</td>
    </tr>
    <tr>
      <td>ChatGPT/Codex 호스티드 backend 내부</td>
      <td>공개되지 않음</td>
      <td style="text-align: right">판단 보류</td>
    </tr>
    <tr>
      <td>Farad에 미치는 함의</td>
      <td>A/B 근거에서 도출한 분석</td>
      <td style="text-align: right">D</td>
    </tr>
  </tbody>
</table>

<p>중요한 제한은 두 가지다.</p>

<ul>
  <li>공개 저장소는 매우 빠르게 변한다. 여기서 말하는 class/module 이름과 line anchor는 위 commit에
고정한다.</li>
  <li>로컬 <code class="language-plaintext highlighter-rouge">codex-cli</code> 0.144.1은 실행 환경의 버전 표지일 뿐이며, 위 source commit과 binary가 byte-level
동일 build라고 단정하지 않는다.</li>
  <li>Codex App/Web의 모든 backend가 이 로컬 core와 동일하게 동작한다고 단정하지 않는다. 공식 문서가
명시한 컨테이너·권한·task 계약만 제품 사실로 취급한다.</li>
</ul>

<h2 id="3-제품-표면과-공유-코어">3. 제품 표면과 공유 코어</h2>

<p>공개 README는 Codex CLI를 로컬 coding agent로 정의하고 IDE, desktop app, Codex Web을 별도
surface로 안내한다. 로컬 쪽에서 가장 중요한 통합 경계는 <code class="language-plaintext highlighter-rouge">codex-core</code>와 <code class="language-plaintext highlighter-rouge">app-server</code>다.</p>

<table>
  <thead>
    <tr>
      <th>표면</th>
      <th>주 사용 방식</th>
      <th>실행 위치</th>
      <th>자동화/통합 경계</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>CLI/TUI</td>
      <td>대화형 로컬 작업</td>
      <td>사용자 호스트</td>
      <td>core queue/event + TUI</td>
    </tr>
    <tr>
      <td>IDE</td>
      <td>편집기 안 대화·diff·승인</td>
      <td>사용자 호스트</td>
      <td>app-server protocol</td>
    </tr>
    <tr>
      <td>custom rich client / remote TUI</td>
      <td>자체 client 또는 원격 TUI</td>
      <td>local/remote host</td>
      <td>experimental JSON-RPC app-server</td>
    </tr>
    <tr>
      <td>Codex app</td>
      <td>데스크톱 planning·review·interactive work</td>
      <td>desktop product</td>
      <td>공개 문서만으로 내부 app-server topology 미확인</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">codex exec</code></td>
      <td>CI·script·pipeline</td>
      <td>사용자/CI host</td>
      <td>stdout/stderr 또는 JSONL</td>
    </tr>
    <tr>
      <td>Codex cloud</td>
      <td>branch checkout 기반 task</td>
      <td>관리형 container</td>
      <td>호스티드 task 계약</td>
    </tr>
  </tbody>
</table>

<p>공식 문서는 app-server를 인증, 대화 이력, 승인, streamed agent event까지 필요한 rich client용
interface로 설명한다. wire는 JSON-RPC 2.0 계열이며 <code class="language-plaintext highlighter-rouge">thread/start</code>, <code class="language-plaintext highlighter-rouge">turn/start</code> 같은 명시적
method와 server notification을 갖는다. 공개 protocol은 client request, server request,
notification을 타입으로 생성한다. 다만 <code class="language-plaintext highlighter-rouge">codex app-server</code> 명령 자체는 local development/debugging
용 experimental surface이고 변경될 수 있다. 기본 transport는 JSONL-over-stdio이며 WebSocket
transport는 구현되어 있어도 공식 README가 <strong>experimental/unsupported</strong>로 명시한다. 따라서 이를
production remote API 안정성 보장으로 읽으면 안 된다.
<a href="https://learn.chatgpt.com/docs/app-server.md">App server</a> <code class="language-plaintext highlighter-rouge">A</code>, <code class="language-plaintext highlighter-rouge">B</code></p>

<p>VS Code extension 같은 app-server client 경로와 custom client 계약은 공개되어 있지만, Codex
desktop app이 정확히 같은 app-server topology를 쓴다는 근거는 확인하지 못했다. 아래 그림도 공개
local core와 app-server만 나타내며 비공개 desktop/cloud 내부를 설명하지 않는다.</p>

<p>이것은 UI가 runtime 객체를 직접 잡는 구조가 아니다. <strong>프로토콜이 surface와 runtime 사이의
anti-corruption layer</strong> 역할을 한다.</p>

<h3 id="31-ui와-human-in-the-loop-계약">3.1 UI와 human-in-the-loop 계약</h3>

<p>공개 app-server protocol은 rich client가 임의의 core 객체를 해석하지 않게 UI projection을
구체화한다. turn/item lifecycle은 <code class="language-plaintext highlighter-rouge">started → delta* → completed</code>로 흐르고, <code class="language-plaintext highlighter-rouge">turn/diff/updated</code>는
aggregated unified diff, <code class="language-plaintext highlighter-rouge">turn/plan/updated</code>는 plan state를 전달한다. command/file change/permission
승인은 server-initiated request와 client decision으로 왕복하고, tool user-input과 MCP elicitation도
별도 typed request다. terminal <code class="language-plaintext highlighter-rouge">item/completed</code>가 최종 실행 상태의 권위다. <code class="language-plaintext highlighter-rouge">A</code>, <code class="language-plaintext highlighter-rouge">B</code></p>

<p>이 계약은 approval card, live output, diff, plan, reconnecting client를 만들기 좋은 기반이다.
그러나 proprietary Codex app의 widget/state-store/threading 구조, optimistic UI, offline cache가
이 protocol과 어떻게 연결되는지는 공개 근거가 없다. 따라서 여기서 평가하는 것은 protocol-level
HITL completeness이지 desktop UI 내부 아키텍처나 실제 UX latency가 아니다. <code class="language-plaintext highlighter-rouge">D</code></p>

<h2 id="4-전체-런타임-아키텍처">4. 전체 런타임 아키텍처</h2>

<pre class="mermaid">
flowchart TB
    subgraph Surface[Client surfaces]
        TUI[CLI / TUI]
        IDE[IDE extension]
        APP[Custom rich client / remote TUI]
        EXEC[codex exec]
    end

    TUI --&gt; CORE
    EXEC --&gt; CORE
    IDE --&gt; RPC
    APP --&gt; RPC

    RPC[app-server\nJSON-RPC typed protocol] --&gt; TM[ThreadManager]
    TM --&gt; CORE[Codex queue pair\nSubmission in / Event out]
    CORE --&gt; SL[submission_loop]
    SL --&gt; SESS[Session\none active task]
    SESS --&gt; TURN[run_turn sampling loop]

    TURN --&gt; MODEL[Model client]
    TURN --&gt; PLAN[Per-step Tool Plan]
    PLAN --&gt; SPECS[Model-visible specs]
    PLAN --&gt; REG[Dispatch registry]
    REG --&gt; EXECUTOR[Tool runtimes]
    EXECUTOR --&gt; SBX[Sandbox + approval + exec policy]
    EXECUTOR --&gt; MCP[MCP / extension / hosted tools]

    SESS --&gt; EVT[Typed Event stream]
    EVT --&gt; Surface

    SESS --&gt; STORE[ThreadStore]
    STORE --&gt; JSONL[Canonical rollout JSONL]
    STORE --&gt; SQL[Queryable SQLite projection]

</pre>

<h3 id="41-authority">4.1 Authority</h3>

<p><code class="language-plaintext highlighter-rouge">Codex</code> 자체는 <a href="https://github.com/openai/codex/blob/2b0b37abb7b29ea272fa856d6dff9221a6bd9ba8/codex-rs/core/src/session/mod.rs#L387-L398"><code class="language-plaintext highlighter-rouge">Submission</code> sender와 <code class="language-plaintext highlighter-rouge">Event</code> receiver를 가진 queue pair</a>다.
실제 mutable authority는 <code class="language-plaintext highlighter-rouge">Session</code>에 있다. 소스 주석은 session에 동시에 최대 한 개의 running task만
존재하며 user input으로 interrupt될 수 있다고 명시한다.</p>

<p>이 단일 active-task 규칙은 단순한 구현 편의가 아니다. 다음 순서를 보장하는 핵심 불변식이다.</p>

<ul>
  <li>turn 시작 전에 model/permissions/environment 같은 live settings 적용</li>
  <li>active turn이 있으면 새 입력을 steer 또는 pending input으로 처리</li>
  <li>approval response와 interrupt가 해당 turn보다 뒤집히지 않음</li>
  <li>종료 시 task cancellation, event flush, persistence teardown 순서 유지</li>
</ul>

<p><code class="language-plaintext highlighter-rouge">SessionState</code>는 대화 history/context manager, rate limit, 이전 turn settings, auto-compaction window,
active connector selection, permissions, first-turn 상태를 가진다. 반면 <code class="language-plaintext highlighter-rouge">SessionServices</code>는 auth,
models, MCP runtime snapshot, exec manager, hooks, approval store, skills/plugins, telemetry, state DB,
thread store 같은 장수 서비스를 모은다. 즉 <strong>대화 상태와 서비스 수명은 타입 수준에서 분리</strong>되어
있지만 한 <code class="language-plaintext highlighter-rouge">Session</code>이 이들을 조정한다.</p>

<h3 id="42-thread-ownership과-process-경계">4.2 Thread ownership과 process 경계</h3>

<p><code class="language-plaintext highlighter-rouge">ThreadManager</code>는 새 thread, resume, fork, 메모리 내 thread tracking을 소유한다. parent에서
subagent를 spawn할 때 parent rollout을 flush하고 fork snapshot을 읽어 child history를 만든다.
이 때문에 multi-agent가 단순히 같은 transcript list를 공유하지 않는다.</p>

<p><code class="language-plaintext highlighter-rouge">codex app-server</code>는 이 runtime을 stdio·WebSocket·Unix socket transport로 노출하는 <strong>별도 OS
process 경계</strong>다. TUI/exec가 core library를 직접 쓰는 것과 app-server client가 protocol을
통해 접근하는 것은 다른 topology다. protocol에는
initialize handshake, thread lifecycle, turn lifecycle, approval/elicitation, MCP, skills/plugins,
model/environment method가 분리되어 있다. 이 구조의 가치:</p>

<ul>
  <li>app-server를 client와 독립 daemon으로 운영할 때 UI crash와 runtime lifetime을 분리할 수 있음</li>
  <li>여러 surface가 동일한 의미론을 재사용</li>
  <li>schema generation으로 TypeScript/JSON client contract 생성 가능</li>
  <li>server request로 승인처럼 runtime→client→runtime 왕복이 가능</li>
</ul>

<p>protocol이 있다고 수명 분리가 자동 보장되지는 않는다. client가 app-server child process의 수명을
소유하면 함께 종료될 수 있고, 독립 daemon·reconnect·thread recovery를 실제로 배치해야 분리가
성립한다. 반대 비용도 분명하다. versioning, experimental capability negotiation, backpressure,
인증, 연결별 초기화 상태, reconnect semantics가 모두 제품 코드가 된다. 특히 WebSocket listener는
인증이 기본 강제되지 않으므로 remote 노출 시 bearer auth와 TLS termination을 별도로 구성해야 한다.
단일 데스크톱 앱만 있는 제품에는 과할 수 있다. <code class="language-plaintext highlighter-rouge">B</code>, <code class="language-plaintext highlighter-rouge">D</code></p>

<h3 id="43-process와-resource-topology">4.3 Process와 resource topology</h3>

<p>공개 local topology에서 app-server의 <code class="language-plaintext highlighter-rouge">ThreadManager</code>는 여러 loaded thread/session actor를 같은
host process에서 추적한다. thread마다 active task는 하나지만 thread마다 OS process를 하나씩
만드는 구조는 아니다. shell·sandbox·MCP/remote execution은 필요에 따라 child/external process
경계를 만든다. 이 선택은 shared auth/model catalog/MCP/store service를 재사용해 active thread당
process overhead를 줄이는 대신, host process crash와 unbounded event accumulation의 blast radius를
여러 loaded thread가 공유하게 한다.</p>

<p>고정 소스에는 agent thread limit과 nesting guard가 있지만 전체 app-server RSS ceiling, loaded
thread eviction 정책, 10,000 active thread 보장은 확인하지 못했다. 따라서 process-per-session인
다른 runtime보다 가볍다고 추론할 수는 있어도 실제 memory curve나 저사양 HPC 적합성은 benchmark
없이 확정할 수 없다. <code class="language-plaintext highlighter-rouge">A</code>, <code class="language-plaintext highlighter-rouge">D</code></p>

<h2 id="5-turn-실행-로직">5. Turn 실행 로직</h2>

<p><code class="language-plaintext highlighter-rouge">run_turn</code>의 공식 소스 주석은 loop의 의미를 직접 설명한다. 모델이 function call을 반환하면 실행
결과를 다음 sampling request에 넣고, assistant message만 반환하면 history에 기록하고 turn을
완료한다 (<a href="https://github.com/openai/codex/blob/2b0b37abb7b29ea272fa856d6dff9221a6bd9ba8/codex-rs/core/src/session/turn.rs#L129-L149">source</a>).</p>

<p>실제 흐름은 더 넓다.</p>

<pre class="mermaid">
sequenceDiagram
    participant C as Client
    participant Q as submission_loop
    participant S as Session
    participant T as run_turn
    participant M as Model
    participant R as ToolRouter
    participant P as Persistence/Event

    C-&gt;&gt;Q: Submission(UserInput + TurnSettings)
    Q-&gt;&gt;S: apply settings / start-or-steer
    S-&gt;&gt;T: spawn active task
    T-&gt;&gt;T: context + world state + skills/plugins + compaction check
    loop until final assistant message
        T-&gt;&gt;M: prompt/history/tool specs
        M--&gt;&gt;T: stream items
        alt tool call
            T-&gt;&gt;R: typed ToolCall
            R--&gt;&gt;T: tool output / model-visible error
            T-&gt;&gt;P: rollout items + events
        else assistant only
            T-&gt;&gt;P: assistant item + turn complete
        else context threshold
            T-&gt;&gt;T: compact and continue
        end
    end
    P--&gt;&gt;C: typed streamed Events

</pre>

<p>Turn 시작 시 context/world-state를 준비하고, skills/plugins를 선택적으로 주입하고 lifecycle hook을
실행하며, context limit 전후로 compaction을 수행한다. 여기에는 서로 다른 두 수명이 있다.
<code class="language-plaintext highlighter-rouge">TurnContext</code>는 user-visible turn 전체에서 공유되는 model/provider, approval policy, permission
profile, collaboration mode, dynamic tools/extensions, config 등의 turn-scoped 값이다. 반면
<code class="language-plaintext highlighter-rouge">StepContext</code>는 <strong>각 model sampling request마다</strong> 캡처되는 request-scoped view다. 후자는
<code class="language-plaintext highlighter-rouge">Arc&lt;TurnContext&gt;</code>와 함께 environment snapshot, 선택 capability roots, exact MCP runtime/tool list,
관찰된 <code class="language-plaintext highlighter-rouge">AGENTS.md</code>를 고정한다.</p>

<p>따라서 “한 sampling step이 <code class="language-plaintext highlighter-rouge">TurnContext</code> snapshot 하나를 쓴다”보다 “turn 설정은 <code class="language-plaintext highlighter-rouge">TurnContext</code>,
매 request의 동적 capability view는 <code class="language-plaintext highlighter-rouge">StepContext</code>”라고 구분하는 편이 정확하다. request 조립과
그 request가 광고한 tool의 실행이 같은 <code class="language-plaintext highlighter-rouge">StepContext</code>를 공유하므로 step consistency를 얻는다.
<code class="language-plaintext highlighter-rouge">DeferredExecutor</code>가 켜지면 다음 sampling request의 <code class="language-plaintext highlighter-rouge">StepContext</code>를 캡처하면서 filesystem-derived
environment/<code class="language-plaintext highlighter-rouge">AGENTS.md</code>/MCP state를 다시 관찰할 수 있다. 이 refresh 의미는 §11에서 따로 평가한다.
<a href="https://github.com/openai/codex/blob/2b0b37abb7b29ea272fa856d6dff9221a6bd9ba8/codex-rs/core/src/session/step_context.rs">StepContext source</a> <code class="language-plaintext highlighter-rouge">A</code></p>

<h3 id="interrupt와-steer">Interrupt와 steer</h3>

<p><code class="language-plaintext highlighter-rouge">submission_loop</code>는 user input만 받는 loop가 아니다. interrupt, approval answer, MCP refresh,
configuration reload, compact, rollback, review, shell, shutdown을 같은 control plane에서 분기한다.
특히 settings mutation도 submission queue를 통과하도록 한 이유가 source comment에 “후속 user input과의
ordering 보존”으로 명시돼 있다.</p>

<p>이것은 async agent runtime에서 매우 중요한 선택이다. UI가 session field를 직접 바꾸고 별도로 message를
보내면 어떤 설정으로 그 turn이 시작됐는지 재현하기 어렵다. Codex는 <code class="language-plaintext highlighter-rouge">Op</code> 자체를 ordered command로
만든다.</p>

<h2 id="6-tool-architecture">6. Tool architecture</h2>

<h3 id="61-schema-exposure와-dispatch-분리">6.1 Schema exposure와 dispatch 분리</h3>

<p><a href="https://github.com/openai/codex/blob/2b0b37abb7b29ea272fa856d6dff9221a6bd9ba8/codex-rs/core/src/tools/router.rs#L35-L45"><code class="language-plaintext highlighter-rouge">ToolRouter</code></a>는
<code class="language-plaintext highlighter-rouge">ToolRegistry</code>와 <code class="language-plaintext highlighter-rouge">model_visible_specs</code>를 별도 field로 가진다. 따라서 “실행할 수 있는 도구”와 “이번
step에서 모델에게 광고하는 도구”가 동일 집합일 필요가 없다.</p>

<p><code class="language-plaintext highlighter-rouge">spec_plan</code>은 tool runtime과 hosted spec을 먼저 계획한 뒤, exposure를 <code class="language-plaintext highlighter-rouge">Direct</code>, <code class="language-plaintext highlighter-rouge">Deferred</code>,
<code class="language-plaintext highlighter-rouge">DirectModelOnly</code>, <code class="language-plaintext highlighter-rouge">Hidden</code> 네 상태로 적용하고 per-step router를 조립한다.
<code class="language-plaintext highlighter-rouge">DirectModelOnly</code>는 일반 model-visible list에는 넣되 code-mode-only session의 nested code-mode
surface에서는 제외한다. 이 분리는 다음을 가능하게 한다.</p>

<ul>
  <li>deferred tool search: 수백 개 schema를 prompt에 항상 넣지 않음</li>
  <li>code mode와 일반 function-call mode의 다른 노출</li>
  <li>MCP, extension, dynamic tool을 같은 dispatch contract로 수렴</li>
  <li>모델/기능 flag/multi-agent version에 따른 schema 선택</li>
  <li>실제 handler는 등록돼 있으나 모델에는 숨기는 내부 control tool</li>
</ul>

<p>Farad 관점에서 이 패턴의 핵심은 enum 이름이 아니라 <strong>catalog/dispatch capability와 prompt exposure를
별도 projection으로 유지하는 것</strong>이다.</p>

<h3 id="62-typed-dispatch와-hook-envelope">6.2 Typed dispatch와 hook envelope</h3>

<p>registry는 duplicate tool name을 거부하고, payload를 tool 종류에 맞게 decode한 뒤 runtime executor를
호출한다. 공통 경계는 lifecycle accounting, pre/post hook, telemetry, terminal notification,
model-visible error 변환을 묶는다. 승인·sandbox·denial retry의 공통 순서는 generic registry가 아니라
<code class="language-plaintext highlighter-rouge">ToolRuntime</code>을 사용하는 shell/apply-patch 계열의 <code class="language-plaintext highlighter-rouge">ToolOrchestrator</code>가 담당한다. 그 순서는
approval → sandbox 선택 → 실행 attempt → denial 시 escalated sandbox retry이며 approval cache로
불필요한 재승인을 피한다. MCP·hosted tool 등 다른 runtime은 별도 policy adapter와 실행 경로를 쓴다.
따라서 <strong>registry lifecycle envelope가 모든 tool의 approval을 보장한다</strong>고 일반화하면 안 되고,
적용 가능한 execution runtime에서는 <code class="language-plaintext highlighter-rouge">ToolOrchestrator</code>, 나머지는 각 adapter의 fail-closed 계약을
따로 검증해야 한다.
<a href="https://github.com/openai/codex/blob/2b0b37abb7b29ea272fa856d6dff9221a6bd9ba8/codex-rs/core/src/tools/orchestrator.rs">ToolOrchestrator source</a> <code class="language-plaintext highlighter-rouge">A</code></p>

<h3 id="63-병렬-실행">6.3 병렬 실행</h3>

<p>tool-call runtime은 parallel-safe tool에는 read lock, 직렬화가 필요한 tool에는 write lock을 사용한다.
cancellation token과 dispatch를 race해 중단된 call도 protocol상 결과로 닫는다. “여러 tool call이 한
model response에 왔다”는 이유만으로 무조건 병렬화하지 않는다는 점이 중요하다.</p>

<p>평가:</p>

<ul>
  <li>강점: schema budget, 확장성, tool policy, observability가 한 경계에서 만난다.</li>
  <li>위험: <code class="language-plaintext highlighter-rouge">spec_plan</code>이 feature matrix의 집결지가 되어 조합 폭발을 일으킬 수 있다.</li>
  <li>필요한 검증: tool exposure snapshot test, duplicate/unknown tool fail-closed test, cancellation 뒤
terminal event 보장, unsafe tool serialization test.</li>
</ul>

<h2 id="7-권한과-sandbox">7. 권한과 sandbox</h2>

<p>Codex는 <strong>정책적 승인(approval)</strong>과 <strong>기술적 격리(sandbox)</strong>를 별개 층으로 정의한다. 공식 문서는
local CLI/IDE에서 기본적으로 network를 끄고, OS-enforced sandbox로 writable 범위를 workspace로
제한한다고 설명한다. macOS는 Seatbelt, Linux/WSL2는 bubblewrap 계열, Windows는 native sandbox를
사용한다 (<a href="https://learn.chatgpt.com/docs/agent-approvals-security.md">Agent approvals &amp; security</a>,
<a href="https://learn.chatgpt.com/docs/sandboxing.md">Sandbox</a>).</p>

<table>
  <thead>
    <tr>
      <th>층</th>
      <th>질문</th>
      <th>대표 상태</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>sandbox</td>
      <td>command가 기술적으로 무엇에 접근 가능한가?</td>
      <td>read-only, workspace-write, danger-full-access</td>
    </tr>
    <tr>
      <td>approval policy</td>
      <td>경계를 넘기 전 누구에게 물을 것인가?</td>
      <td>untrusted/on-request/never/granular</td>
    </tr>
    <tr>
      <td>exec policy</td>
      <td>특정 command pattern을 허용·질문·차단할 것인가?</td>
      <td>rule evaluation</td>
    </tr>
    <tr>
      <td>tool annotation</td>
      <td>shell 외 app/MCP side effect인가?</td>
      <td>read-only/destructive 등</td>
    </tr>
    <tr>
      <td>network policy</td>
      <td>허용된 network가 어디로 나갈 수 있는가?</td>
      <td>domain/socket allow/deny proxy</td>
    </tr>
  </tbody>
</table>

<p>중요한 세부 계약:</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">workspace-write</code> 안에서도 <code class="language-plaintext highlighter-rouge">.git</code>, <code class="language-plaintext highlighter-rouge">.agents</code>, <code class="language-plaintext highlighter-rouge">.codex</code> 같은 protected path는 재귀 read-only다.</li>
  <li>non-interactive flow에서 새 승인을 표시할 수 없으면 해당 행동은 실패하여 parent workflow로 돌아간다.</li>
  <li>subagent는 parent turn의 live permission/sandbox override를 상속한다.</li>
  <li>cloud는 setup과 agent phase를 나누고, secrets는 setup에만 제공한 뒤 agent phase 전에 제거한다.</li>
  <li>network enable과 destination policy는 별개이며, allowlist/deny 및 private destination 제약을 둘 수 있다.</li>
</ul>

<p>현재 확인 범위에서 이 영역은 Codex의 큰 구조적 강점이다. “모델이 허용된다고 판단했다”가 보안
경계가 아니라 실제 child process가 OS boundary 안에서 실행된다. Claude Code도 permission과 OS
sandbox를 분리하므로, 최종 비교에서는 기본 활성화 여부·플랫폼 범위·degraded fallback·shell 외
도구의 적용 범위를 같은 조건에서 따로 판정한다.</p>

<p>한계도 있다.</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">danger-full-access</code>는 이 방어선을 의도적으로 제거한다.</li>
  <li>sandbox는 외부 도구의 논리적 오용, 허용된 파일 안의 파괴적 수정, prompt injection을 모두 해결하지
않는다.</li>
  <li>플랫폼별 kernel/OS 기능 차이와 HPC 정책(user namespace, bubblewrap 설치 제한)이 실제 가용성을
좌우한다.</li>
</ul>

<h2 id="8-context-transcript-compaction">8. Context, transcript, compaction</h2>

<p>Codex의 rollout은 단순 <code class="language-plaintext highlighter-rouge">[{role, content}]</code> 배열이 아니다. protocol의 <code class="language-plaintext highlighter-rouge">RolloutItem</code>에는 session
metadata, model response item, inter-agent communication, turn context, world state, compacted item,
<code class="language-plaintext highlighter-rouge">EventMsg</code>가 <strong>같은 enum과 rollout stream 안의 typed variant</strong>로 구분되어 저장된다. 따라서 UI event와
model replay가 별도 stream이라고 말할 수는 없다. 장점은 한 durable sequence에서 provenance를
보존하면서 consumer가 variant별로 필터링할 수 있다는 것이고, 위험은 replay/context 조립 코드가
<code class="language-plaintext highlighter-rouge">EventMsg</code>를 model-visible item으로 잘못 승격하지 않도록 invariant를 계속 지켜야 한다는 점이다.
<a href="https://github.com/openai/codex/blob/2b0b37abb7b29ea272fa856d6dff9221a6bd9ba8/codex-rs/protocol/src/protocol.rs#L3141-L3154">RolloutItem source</a> <code class="language-plaintext highlighter-rouge">A</code></p>

<h3 id="compaction-계약">Compaction 계약</h3>

<p><code class="language-plaintext highlighter-rouge">CompactedItem</code>은 단순 summary 문자열뿐 아니라 replacement history와 chain/window 식별 정보를
가질 수 있다. turn loop는 sampling 전에 threshold를 검사하고, model token limit에 닿으면 mid-turn
compaction 후 이어갈 수 있다.</p>

<p>이 설계가 지키려는 것은 세 가지다.</p>

<ol>
  <li>현재 모델에 넣는 context가 window를 넘지 않음</li>
  <li>durable rollout과 현재 compacted prompt가 어떻게 연결되는지 추적 가능</li>
  <li>resume/fork가 단순 최종 텍스트보다 풍부한 history·window·fork provenance를 복원</li>
</ol>

<p>그러나 이것은 실행 세계 전체의 checkpoint가 아니다. resume/fork가 in-flight approval, 실행 중인
child process/tool call, 외부 MCP side effect, 현재 filesystem을 원자적으로 되살린다는 보장은 없다.
위험은 compaction implementation이 prompt replay semantics의 일부가 된다는 점이다. summary 품질뿐
아니라 tool call/result pairing, event filtering, subagent message, world-state 기준선과 window chain을
테스트해야 한다. <code class="language-plaintext highlighter-rouge">A</code>, <code class="language-plaintext highlighter-rouge">D</code></p>

<h2 id="9-저장과-조회">9. 저장과 조회</h2>

<p><code class="language-plaintext highlighter-rouge">ThreadStore</code>는 create/resume/append/flush/load/read/list/search/update metadata/archive/delete를 제공하는
storage-neutral trait이다. local 구현의 주석은 저장 전략을 명확히 선언한다.</p>

<blockquote>
  <p>rollout JSONL은 SQLite 없이도 읽을 수 있는 durable replay format이고, SQLite는 빠른 list/read를
위한 queryable metadata index다.</p>
</blockquote>

<p>(<a href="https://github.com/openai/codex/blob/2b0b37abb7b29ea272fa856d6dff9221a6bd9ba8/codex-rs/thread-store/src/local/mod.rs#L45-L63">source</a>)</p>

<pre class="mermaid">
flowchart LR
    TURN[Turn / rollout append] --&gt; REC[Live recorder]
    REC --&gt; JSONL[Canonical JSONL]
    TURN --&gt; META[Metadata patch]
    META --&gt; DB[(SQLite projection)]
    DB --&gt; LIST[list / search / fast read]
    JSONL --&gt; RESUME[resume / replay / repair]
    DB -. unavailable or stale .-&gt; REPAIR[rebuild/compat path]
    JSONL --&gt; REPAIR

</pre>

<p>이중화처럼 보이지만 authority는 비대칭이다.</p>

<ul>
  <li>대화 실행 사실: JSONL canonical</li>
  <li>목록/검색/정렬 metadata: SQLite projection</li>
  <li>SQLite 부재·손상·구버전: JSONL/name-index compatibility path</li>
</ul>

<p>이는 append-heavy transcript를 유지하면서 목록·검색 때 전량 JSONL scan을 피하기 위한 합리적
구조다. 하지만 구조만으로 N=10,000 latency, projection lag, crash 후 repair 비용이 입증되지는
않는다. 두 projection의 consistency와 bounded rebuild가 새 운영 책임이 된다. SQLite를 원격 NFS
canonical store로 그대로 쓰는 것이 안전하다는 뜻도 아니다. Farad에 적용한다면 로컬 derived index
여부와 NFS locking, rebuild bound를 별도로 설계하고 계측해야 한다.</p>

<p>구체적으로 SQLite-backed state는 기본적으로 <code class="language-plaintext highlighter-rouge">CODEX_HOME</code> 아래에 있고 <code class="language-plaintext highlighter-rouge">CODEX_SQLITE_HOME</code> 또는
<code class="language-plaintext highlighter-rouge">sqlite_home</code>으로 별도 위치에 둘 수 있다. source는 SQLite에 WAL mode와 busy timeout을 설정한다.
따라서 NFS home 사용자가 SQLite만 node-local path로 분리하는 것은 가능하지만 canonical rollout
JSONL까지 자동으로 옮겨지는 것은 아니다. state-DB-only list path도 존재하지만 일반 list/read에는
JSONL compatibility·metadata repair path가 남는다. 공식 문서와 source에는 NFS consistency 보장,
파일당 round-trip 상한, 10,000개 rebuild 시간, crash 중 projection lag의 최대치가 없다. <code class="language-plaintext highlighter-rouge">A</code>, <code class="language-plaintext highlighter-rouge">B</code>, <code class="language-plaintext highlighter-rouge">D</code></p>

<h2 id="10-multi-agent-구조">10. Multi-agent 구조</h2>

<p>공식 문서는 subagent를 별도 agent thread로 정의하며, main thread가 결과 요약을 수집한다. 현재 local
release는 직접 요청 또는 AGENTS/skill 지시로 delegate할 수 있고, agent마다 model/reasoning/instruction을
달리 설정할 수 있다. child는 기본적으로 parent permission mode를 상속한다
(<a href="https://learn.chatgpt.com/docs/agent-configuration/subagents.md">Subagents</a>).</p>

<p>공개 core에서 중요한 것은 UI의 “agent 카드”가 아니라 다음 구조다.</p>

<ul>
  <li>agent graph가 parent/child thread 관계를 추적</li>
  <li>spawn 전 parent rollout flush</li>
  <li>child는 fork된 history snapshot에서 시작</li>
  <li>inter-agent communication도 <code class="language-plaintext highlighter-rouge">Op</code>/rollout item으로 기록</li>
  <li>thread limit와 nesting depth로 자원 폭발 제한</li>
  <li>완료/interrupt/close가 명시적인 lifecycle operation</li>
</ul>

<p>장점:</p>

<ul>
  <li>noisy exploration을 main context에서 격리</li>
  <li>parallel read/test/review latency 단축</li>
  <li>결과와 child transcript를 별도 감사 가능</li>
</ul>

<p>비용:</p>

<ul>
  <li>token/compute 증가</li>
  <li>동시 write conflict와 shared workspace race</li>
  <li>child approval이 inactive thread에서 발생할 수 있음</li>
  <li>parent summary가 child evidence를 손실할 수 있음</li>
  <li>graph shutdown, orphan recovery, max concurrency가 runtime correctness가 됨</li>
</ul>

<p>따라서 “subagent 수가 많다”는 것 자체는 성숙도 지표가 아니다. isolation, ownership, completion
protocol, budget, artifact merge가 더 중요하다.</p>

<h2 id="11-확장-모델-agents-skills-plugins-mcp-hooks">11. 확장 모델: AGENTS, skills, plugins, MCP, hooks</h2>

<p>Codex는 여러 확장 메커니즘을 서로 다른 역할로 둔다.</p>

<table>
  <thead>
    <tr>
      <th>메커니즘</th>
      <th>주 역할</th>
      <th>runtime 영향</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">AGENTS.md</code></td>
      <td>directory-scoped project instruction</td>
      <td>prompt/context assembly</td>
    </tr>
    <tr>
      <td>skill</td>
      <td>반복 가능한 지식·workflow·script/assets</td>
      <td>선택적 instruction/resource 주입</td>
    </tr>
    <tr>
      <td>plugin</td>
      <td>skill/MCP/app/hook 등의 배포 묶음</td>
      <td>discovery/install/trust surface</td>
    </tr>
    <tr>
      <td>MCP</td>
      <td>외부 tool/resource/prompt 연결</td>
      <td>tool registry + connection lifecycle</td>
    </tr>
    <tr>
      <td>hook</td>
      <td>lifecycle 전후 정책·automation</td>
      <td>dispatch/session envelope</td>
    </tr>
    <tr>
      <td>extension/dynamic tool</td>
      <td>typed runtime 확장</td>
      <td>per-step tool plan</td>
    </tr>
  </tbody>
</table>

<p>turn loop는 user mention, skill snapshot, plugin snapshot, MCP tools, extension contributors를 바탕으로
이번 turn에 넣을 대상을 결정하고 실제 주입된 항목을 기록한다. tool spec planner는 direct/deferred
노출까지 조절한다. 즉 discovery, selection, prompt exposure, dispatch가 하나의 list로 뭉개져 있지
않다.</p>

<p>보안상 주의할 점:</p>

<ul>
  <li>skill script와 plugin은 사실상 code execution/supply-chain surface다.</li>
  <li>MCP OAuth credential과 app-server client auth는 다른 trust boundary다.</li>
  <li>tool schema의 annotation을 신뢰하더라도 실제 sandbox/approval enforcement가 최종 경계여야 한다.</li>
  <li>file watcher/hot reload가 실행 중 snapshot을 깨뜨리지 않도록 publish 시점이 필요하다.</li>
</ul>

<h3 id="111-configinstructionmemory-경계">11.1 Config·instruction·memory 경계</h3>

<p>공식 계약에서 <code class="language-plaintext highlighter-rouge">AGENTS.md</code>는 global → repository root → cwd 방향으로 합쳐지고 가까운 directory
guidance가 뒤에 와 override한다. 각 directory에서는 <code class="language-plaintext highlighter-rouge">AGENTS.override.md</code>가 우선하며 합산 기본 한도는
32 KiB다. 일반 제품 문서는 이 instruction chain을 CLI run/TUI session 시작에 한 번 만든다고
설명한다. mandatory rule은 memory가 아니라 checked-in <code class="language-plaintext highlighter-rouge">AGENTS.md</code>나 문서에 둬야 한다.
<a href="https://learn.chatgpt.com/docs/agent-configuration/agents-md.md">AGENTS.md</a> <code class="language-plaintext highlighter-rouge">B</code></p>

<p>local Codex memories는 별도 local store이며 현재 공식 계약상 opt-in/experimental이다. prior task에서
추출한 recall을 future session에 주입할 수 있지만 generation이 즉시 일어나지 않을 수 있고, 외부
context 사용 task를 제외하는 정책도 있다. ChatGPT web memory, ChatGPT Work memory, local Codex
memory가 같은 authority라고 보면 안 된다. 이 보고서는 local memory file의 내부 ranking·consolidation
정확도나 장기 schema 호환성은 평가하지 않는다.
<a href="https://learn.chatgpt.com/docs/customization/memories.md">Memories</a> <code class="language-plaintext highlighter-rouge">B</code></p>

<h3 id="112-refresh와-snapshot-경계">11.2 Refresh와 snapshot 경계</h3>

<p>고정 소스의 기본 경로와 feature-gated 경로가 다르다. explicit config reload와 MCP refresh는 ordered
submission으로 들어가며 publish된 service snapshot을 바꾼다. 한편 under-development이고 기본 off인
<code class="language-plaintext highlighter-rouge">DeferredExecutor</code>가 켜지면 매 sampling request의 <code class="language-plaintext highlighter-rouge">StepContext</code>를 캡처할 때 environment snapshot과
filesystem-derived <code class="language-plaintext highlighter-rouge">AGENTS.md</code>, capability roots, MCP runtime/tool list를 다시 관찰할 수 있다. request
안에서는 fixed snapshot을 공유하지만 turn 전체가 frozen이라는 뜻은 아니다. <code class="language-plaintext highlighter-rouge">A</code></p>

<p>이는 Farad의 explicit refresh 계약과 직접 다르다. NFS 홈에서 이 feature path를 모방하면 한 turn의
반복 sampling 횟수만큼 filesystem/network discovery 비용이 들어갈 수 있으므로, Farad에는 request
consistency만 가져오고 per-request refresh는 가져오지 않는 편이 맞다. 실제 Codex의 해당 경로에 대한
NFS round-trip bound는 공개 benchmark로 확인하지 못했다. <code class="language-plaintext highlighter-rouge">D</code></p>

<h2 id="12-provider와-모델-계층">12. Provider와 모델 계층</h2>

<p>공개 config와 core에는 <code class="language-plaintext highlighter-rouge">model_provider</code>, model catalog, OpenAI-compatible base URL, OSS provider 같은
abstraction이 있다. 하지만 제품 경험, 인증, 기능 최적화는 OpenAI/ChatGPT가 중심이다. 따라서
“provider interface가 존재한다”와 “모든 near-frontier provider가 동등한 first-class experience다”는
다른 주장이다.</p>

<p>Codex가 모델 특성에 따라 다르게 처리하는 예:</p>

<ul>
  <li>context window와 auto-compact threshold</li>
  <li>reasoning effort/summary/verbosity</li>
  <li>tool mode와 supported feature</li>
  <li>collaboration mode와 agent role/model override</li>
  <li>model upgrade/compatibility handling</li>
</ul>

<p>이 정도의 model capability matrix는 강력하지만, feature condition이 turn/tool planner 전역으로
퍼지면 testing burden도 크게 늘어난다.</p>

<h2 id="13-성능과-concurrency">13. 성능과 concurrency</h2>

<h3 id="확인되는-구조적-장치">확인되는 구조적 장치</h3>

<ul>
  <li>bounded submission queue로 producer 폭주에 backpressure</li>
  <li>thread당 하나의 active task로 대화 순서 안정화</li>
  <li>event channel로 UI와 model loop decoupling. 다만 현재 core event channel은
<a href="https://github.com/openai/codex/blob/2b0b37abb7b29ea272fa856d6dff9221a6bd9ba8/codex-rs/core/src/session/mod.rs#L538-L540"><code class="language-plaintext highlighter-rouge">async_channel::unbounded()</code></a>이므로
submission 입력의 backpressure가 end-to-end event backpressure를 뜻하지는 않음</li>
  <li>service snapshot을 <code class="language-plaintext highlighter-rouge">ArcSwap</code>으로 publish하여 MCP/hooks 등의 read path 경량화</li>
  <li>per-tool parallel-safety lock</li>
  <li>cancellation token을 turn/tool/process lifecycle에 전달</li>
  <li>state-DB-only metadata path로 전체 JSONL scan을 피할 수 있는 list/search 경로</li>
  <li>tool schema direct/deferred 분리로 context cost 제어</li>
  <li>app-server WebSocket ingress에 bounded queue</li>
</ul>

<h3 id="여전히-검증이-필요한-hot-path">여전히 검증이 필요한 hot path</h3>

<ul>
  <li>매우 긴 rollout의 resume/compaction 비용</li>
  <li>수천 thread와 깊은 agent graph에서 list/search/shutdown bound</li>
  <li>MCP server가 많을 때 refresh/reconcile 비용</li>
  <li>deferred tool search 자체의 latency와 정확도</li>
  <li>core의 unbounded event channel과 app-server slow client 사이의 memory bound, drop/reconnect semantics</li>
  <li>parallel subagent가 같은 worktree를 수정할 때 충돌 제어</li>
</ul>

<p>Rust와 Tokio 선택은 이 구조에 적합하지만, 언어만으로 bounded behavior가 보장되지는 않는다. queue
capacity, lock granularity, store index, lifecycle invariant가 실질적인 성능 설계다.</p>

<h3 id="131-테스트-가능성과-변경-용이성">13.1 테스트 가능성과 변경 용이성</h3>

<p>공개 source에는 tool exposure의 <code class="language-plaintext highlighter-rouge">spec_plan_tests.rs</code>, dispatch의 <code class="language-plaintext highlighter-rouge">router_tests.rs</code>, session/approval/
sandbox tests, thread-store list/search/metadata-sync tests, app-server protocol schema와 integration
suite가 분리되어 있다. typed protocol code generation, storage-neutral <code class="language-plaintext highlighter-rouge">ThreadStore</code>, tool runtime
trait, in-memory test stores는 변화 지점을 격리하는 좋은 seam이다. <code class="language-plaintext highlighter-rouge">A</code></p>

<p>다만 test file의 존재는 production quality의 충분조건이 아니다. 이 조사에서는 전체 coverage,
mutation/fuzz 결과, production provider backend, hosted surface와 local core의 conformance, 10,000
thread 부하, crash/rebuild chaos test 결과를 확인하지 않았다. 큰 feature matrix와 central
<code class="language-plaintext highlighter-rouge">SessionServices</code> composition root 때문에 cross-product regression 비용은 여전히 높다. <code class="language-plaintext highlighter-rouge">D</code></p>

<h3 id="132-observabilityprivacycost-경계">13.2 Observability·privacy·cost 경계</h3>

<p>local core와 app-server에는 typed lifecycle event, structured tracing, configurable OpenTelemetry,
token/rate-limit update가 있다. 이는 turn/tool/thread correlation과 UI 진행 상태, 운영 계측을 만들기
좋은 instrumentation이다. 그러나 exporter 설정, prompt/tool payload 기록 범위, redaction, retention,
hosted product의 데이터 처리 정책은 서로 다른 계약이다. 공개 Rust source만으로 hosted request가
어디에 얼마나 저장되는지 또는 privacy가 보장된다고 추론하지 않는다. <code class="language-plaintext highlighter-rouge">A</code>, <code class="language-plaintext highlighter-rouge">B</code></p>

<p>token usage는 비용 관찰에는 유용하지만 stable한 per-thread hard budget으로 확인되지 않았다.
source의 rollout-budget 기능은 under-development/default-off이므로 비용 폭주 방지의 보편 계약으로
평가하지 않는다. 실제 provider pricing과 organization quota도 runtime architecture 밖의 변동 요소다.
<code class="language-plaintext highlighter-rouge">A</code>, <code class="language-plaintext highlighter-rouge">D</code></p>

<h2 id="14-failure와-recovery-boundary">14. Failure와 recovery boundary</h2>

<table>
  <thead>
    <tr>
      <th>실패</th>
      <th>containment/recovery 단위</th>
      <th>평가</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>model stream 오류</td>
      <td>sampling/turn retry 또는 terminal error event</td>
      <td>turn 경계가 명시적</td>
    </tr>
    <tr>
      <td>tool 오류</td>
      <td>model-visible tool result/error</td>
      <td>대화 loop를 반드시 crash시키지 않음</td>
    </tr>
    <tr>
      <td>command cancellation</td>
      <td>cancellation token + aborted terminal result</td>
      <td>dangling call 방지 필요</td>
    </tr>
    <tr>
      <td>client disconnect</td>
      <td>app-server connection/thread 분리</td>
      <td>reconnect 계약이 중요</td>
    </tr>
    <tr>
      <td>SQLite 부재/손상</td>
      <td>canonical JSONL compatibility/repair</td>
      <td>좋은 authority 분리</td>
    </tr>
    <tr>
      <td>MCP startup 실패</td>
      <td>required 여부에 따라 fail/continue</td>
      <td>policy가 명시적</td>
    </tr>
    <tr>
      <td>subagent 실패</td>
      <td>child terminal state → parent summary</td>
      <td>parent가 부분 실패를 보존해야 함</td>
    </tr>
    <tr>
      <td>approval 불가</td>
      <td>noninteractive에서는 action fail</td>
      <td>암묵적 승인보다 안전</td>
    </tr>
    <tr>
      <td>process crash</td>
      <td>append된 rollout에서 resume</td>
      <td>마지막 partially written record 처리 필요</td>
    </tr>
  </tbody>
</table>

<p>가장 좋은 점은 실패가 대부분 typed event/item 또는 thread state로 표면화된다는 것이다. 가장 어려운
점은 경계 수가 많아 “정상 종료의 정확한 순서” 자체가 correctness가 된다는 것이다.</p>

<h2 id="15-공식-cloud-실행-계약">15. 공식 cloud 실행 계약</h2>

<p>공식 문서가 공개한 cloud task 흐름은 다음과 같다.</p>

<ol>
  <li>container 생성과 선택 branch/commit checkout</li>
  <li>setup script 및 cache resume 시 maintenance script</li>
  <li>agent phase internet policy 적용</li>
  <li>AGENTS.md를 포함한 repository instruction 아래 command/edit/test loop</li>
  <li>final answer와 diff, 이후 PR 또는 follow-up</li>
</ol>

<p>setup phase에는 dependency 설치를 위한 network를 허용할 수 있고, secret은 setup에만 제공한 뒤 agent
phase 전에 제거한다. container cache는 후속 task latency를 줄인다
(<a href="https://learn.chatgpt.com/docs/environments/cloud-environment.md">Cloud environments</a>).</p>

<p>이 계약은 로컬 thread runtime과 다른 deployment boundary다. cloud backend 내부 class 구조나 queue가
공개 core와 같다고 추정하지 않는다.</p>

<h2 id="16-아키텍처-판정표">16. 아키텍처 판정표</h2>

<p>숫자 점수는 메커니즘의 존재를 실제 품질이나 특정 규모의 성능 입증으로 오해하게 만들 수 있어 쓰지
않는다. 아래 <code class="language-plaintext highlighter-rouge">판정</code> 열 전체는 고정 source <code class="language-plaintext highlighter-rouge">A</code>와 공식 계약 <code class="language-plaintext highlighter-rouge">B</code>를 비교 기준에 적용한 <strong><code class="language-plaintext highlighter-rouge">D</code> 등급
평가</strong>다. 확인된 구조와 아직 별도 부하·장애 검증이 필요한 부분을 분리한다.</p>

<table>
  <thead>
    <tr>
      <th>축</th>
      <th>확인된 구조</th>
      <th>미검증·제약</th>
      <th>판정</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>runtime/surface 분리</td>
      <td>queue/event core + typed app-server</td>
      <td>app-server는 experimental, WebSocket unsupported; desktop/hosted 내부 미확인</td>
      <td>강한 local 기반, public API 안정성 조건부</td>
    </tr>
    <tr>
      <td>turn ordering/ownership</td>
      <td>submission 순서열 + single active task</td>
      <td>steer·cancel·approval race의 전체 상태공간</td>
      <td>강</td>
    </tr>
    <tr>
      <td>tool exposure/dispatch</td>
      <td>per-step plan, direct/deferred/direct-model-only/hidden</td>
      <td>feature 조합 폭발과 tool별 enforcement 편차</td>
      <td>강</td>
    </tr>
    <tr>
      <td>OS execution containment</td>
      <td>approval과 별도인 platform sandbox</td>
      <td>플랫폼·kernel별 degraded 동작, full-access 선택</td>
      <td>강하지만 환경 의존</td>
    </tr>
    <tr>
      <td>durable replay</td>
      <td>typed rollout JSONL canonical</td>
      <td>partial record·schema migration·repair bound</td>
      <td>강한 기반</td>
    </tr>
    <tr>
      <td>대규모 metadata 조회</td>
      <td>storage-neutral store + SQLite projection</td>
      <td>N=10,000 latency·projection lag·rebuild 계측 없음</td>
      <td>좋은 기반, 성능 미입증</td>
    </tr>
    <tr>
      <td>multi-agent lifecycle</td>
      <td>thread graph, fork/flush/limits/control</td>
      <td>큰 graph와 공유 worktree 충돌 비용</td>
      <td>강한 기반</td>
    </tr>
    <tr>
      <td>provider 중립성</td>
      <td>provider/model abstraction</td>
      <td>제품과 semantics는 OpenAI 중심</td>
      <td>중간</td>
    </tr>
    <tr>
      <td>확장 생태계</td>
      <td>skills/plugins/MCP/hooks/extensions</td>
      <td>공급망·조합 복잡도와 신뢰 경계 확대</td>
      <td>강하지만 복잡</td>
    </tr>
    <tr>
      <td>구현 단순성</td>
      <td>강한 타입과 명시적 protocol</td>
      <td>매우 큰 Rust workspace와 feature matrix</td>
      <td>낮음</td>
    </tr>
    <tr>
      <td>저사양/HPC 적합성</td>
      <td>native binary, bounded submission</td>
      <td>sandbox 가용성·서비스 수·event memory bound 불확실</td>
      <td>조건부</td>
    </tr>
    <tr>
      <td>local UX surface</td>
      <td>TUI/IDE와 typed app-server HITL contract</td>
      <td>proprietary app 내부와 실제 UX latency는 공개 소스로 평가 불가</td>
      <td>protocol은 강, desktop 내부 판정 제한</td>
    </tr>
    <tr>
      <td>관측성</td>
      <td>typed event, tracing/OTel/telemetry 경계</td>
      <td>운영 audit 완결성·privacy는 별도 검증</td>
      <td>강한 instrumentation</td>
    </tr>
    <tr>
      <td>architecture auditability</td>
      <td>핵심 local runtime 공개</td>
      <td>hosted backend와 일부 surface는 비공개</td>
      <td>local core는 강</td>
    </tr>
  </tbody>
</table>

<h2 id="17-가장-강한-설계-결정">17. 가장 강한 설계 결정</h2>

<ol>
  <li><strong>Ordered command plane</strong>: user input, settings, interrupt, approval을 같은 submission ordering에 둔다.</li>
  <li><strong>Single active turn per thread</strong>: mutable conversation authority를 명확히 한다.</li>
  <li><strong>Turn/step context 분리</strong>: turn-scoped 설정과 request-scoped capability snapshot을 구분한다.</li>
  <li><strong>Schema exposure ≠ dispatch registry</strong>: prompt cost와 capability를 독립적으로 관리한다.</li>
  <li><strong>Sandbox ≠ approval</strong>: UX 정책과 OS security boundary를 혼동하지 않는다.</li>
  <li><strong>JSONL canonical + query index projection</strong>: replay를 보존하면서 목록 조회의 전량 scan을 피할
구조다. N=10,000 성능은 별도 검증 대상이다.</li>
  <li><strong>Typed app protocol</strong>: surface가 runtime 내부 객체에 종속되지 않는다.</li>
  <li><strong>Subagent as thread, not callback</strong>: 별도 history/lifecycle/permission을 가진 실행 단위다.</li>
</ol>

<h2 id="18-주요-약점과-설계-부채-위험">18. 주요 약점과 설계 부채 위험</h2>

<h3 id="181-거대한-composition-root">18.1 거대한 composition root</h3>

<p><code class="language-plaintext highlighter-rouge">SessionServices</code>와 spawn arguments가 수많은 subsystem을 한 session에 조립한다. 명시적 dependency라는
장점과 동시에, session creation이 모든 feature 조합을 아는 central composition root가 되는 위험이
있다.</p>

<h3 id="182-feature-matrix-폭발">18.2 feature matrix 폭발</h3>

<p>model capability × client surface × sandbox platform × tool mode × MCP/plugin/skill × collaboration mode의
조합이 크다. type safety만으로 의미적 조합 버그를 막지는 못한다.</p>

<h3 id="183-projection-consistency">18.3 projection consistency</h3>

<p>JSONL과 SQLite가 분리되어 있는 만큼 stale index, crash 중 metadata patch, repair 시 bound, schema
migration이 운영 책임이다.</p>

<h3 id="184-protocol-evolution-cost">18.4 protocol evolution cost</h3>

<p>app-server가 좋아질수록 experimental negotiation, old client compatibility, notification ordering,
connection authentication이 복잡해진다.</p>

<h3 id="185-생태계-trust-surface">18.5 생태계 trust surface</h3>

<p>plugin/skill/MCP/hook/connector가 늘수록 discovery UX보다 provenance, permission, credential separation,
side-effect annotation의 신뢰가 더 중요해진다.</p>

<h3 id="186-제품-중심-provider-abstraction">18.6 제품 중심 provider abstraction</h3>

<p>OpenAI 외 provider hook이 존재해도 최신 model behavior와 hosted surface는 OpenAI 중심으로 진화한다.
다중 provider가 제품의 핵심인 Farad에는 그대로 적용할 수 없다.</p>

<h3 id="187-명시적-미확인-경계">18.7 명시적 미확인 경계</h3>

<p>다음은 이 보고서에서 확정하지 않는다.</p>

<ul>
  <li>Codex app/Web/cloud backend가 고정 commit의 local core 또는 app-server를 그대로 사용하는지</li>
  <li>proprietary desktop의 process/IPC, state store, Qt/Electron/native UI threading topology</li>
  <li>app-server experimental protocol과 WebSocket transport의 장기 compatibility/SLA</li>
  <li>모든 MCP/hosted/dynamic tool에 shell과 동일한 approval·OS sandbox가 적용되는지</li>
  <li>resume/fork가 in-flight process, approval, external side effect를 복원하는지</li>
  <li>10,000 thread의 list/open/search/rebuild latency, RSS curve, loaded-thread eviction 상한</li>
  <li>NFS 위 JSONL·SQLite의 consistency, lock, round-trip, crash recovery 보장</li>
  <li>local memory ranking/consolidation 품질과 장기 file schema</li>
  <li>hosted request의 retention/privacy 또는 local test suite와 production backend의 conformance</li>
</ul>

<p>이 항목들은 “공개 소스가 없으니 약하다”는 점수가 아니라 <strong>판정 범위 밖</strong>이다. 교차 비교에서는
공식 행동 계약 또는 재현 가능한 benchmark가 생길 때만 승격해야 한다.</p>

<h2 id="19-farad가-가져갈-것과-가져가지-말-것">19. Farad가 가져갈 것과 가져가지 말 것</h2>

<p>이 절은 비교 결론이 아니라 Codex 단독 분석에서 보이는 후보이며, 최종 우선순위는
<a href="https://hvppycoding.github.io/research/farad-architecture-roadmap/">Farad 판정 및 로드맵</a>에서 다른 제품과 함께 결정한다.</p>

<h3 id="가져갈-가치가-큰-불변식">가져갈 가치가 큰 불변식</h3>

<ul>
  <li>turn 설정 변경과 user input의 ordered command contract</li>
  <li><code class="language-plaintext highlighter-rouge">TurnContext</code>의 per-turn 설정과 <code class="language-plaintext highlighter-rouge">StepContext</code>의 per-request capability snapshot 분리</li>
  <li>prompt exposure와 dispatch registry의 분리</li>
  <li>transcript canonical + bounded metadata projection</li>
  <li>UI/runtime 사이 typed application event protocol</li>
  <li>subagent parent flush/fork/lifecycle state와 permission inheritance</li>
  <li>cancellation 뒤에도 terminal event/result를 반드시 남기는 규칙</li>
  <li>policy approval과 OS enforcement를 별도 계층으로 두는 보안 모델</li>
</ul>

<h3 id="조건부로-가져갈-것">조건부로 가져갈 것</h3>

<ul>
  <li>app-server: 두 번째 surface나 remote/headless 요구가 구체화될 때</li>
  <li>SQLite projection: local derived index + NFS authority/repair 계약을 먼저 설계한 뒤</li>
  <li>tool deferred search: schema 수가 실제 prompt budget을 압박할 때</li>
  <li>깊은 multi-agent graph: 실제 workflow가 artifact-aware orchestration으로 부족할 때</li>
</ul>

<h3 id="그대로-가져오면-안-되는-것">그대로 가져오면 안 되는 것</h3>

<ul>
  <li>Rust workspace의 crate 분할 수를 architecture maturity로 모방</li>
  <li>OpenAI model capability matrix를 Farad provider 정책으로 hardcode</li>
  <li>Linux bubblewrap 전제를 RHEL/HPC 운영에 검증 없이 강제</li>
  <li>desktop-only 제품에 protocol/server/daemon을 선제 도입</li>
  <li>SQLite를 NFS canonical state로 사용</li>
</ul>

<h2 id="20-재현-절차와-출처">20. 재현 절차와 출처</h2>

<h3 id="공개-소스-재현">공개 소스 재현</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git clone https://github.com/openai/codex.git
git <span class="nt">-C</span> codex checkout 2b0b37abb7b29ea272fa856d6dff9221a6bd9ba8
git <span class="nt">-C</span> codex show <span class="nt">-s</span> <span class="nt">--format</span><span class="o">=</span><span class="s1">'%H %cI %s'</span>
</code></pre></div></div>

<p>핵심 source entry:</p>

<ul>
  <li><a href="https://github.com/openai/codex/blob/2b0b37abb7b29ea272fa856d6dff9221a6bd9ba8/codex-rs/core/src/session/mod.rs#L387-L449">Codex queue-pair/session spawn</a></li>
  <li><a href="https://github.com/openai/codex/blob/2b0b37abb7b29ea272fa856d6dff9221a6bd9ba8/codex-rs/core/src/session/session.rs#L25-L48">Session authority</a></li>
  <li><a href="https://github.com/openai/codex/blob/2b0b37abb7b29ea272fa856d6dff9221a6bd9ba8/codex-rs/core/src/state/service.rs#L50-L104">Session services</a></li>
  <li><a href="https://github.com/openai/codex/blob/2b0b37abb7b29ea272fa856d6dff9221a6bd9ba8/codex-rs/core/src/session/turn.rs#L129-L460">Turn loop</a></li>
  <li><a href="https://github.com/openai/codex/blob/2b0b37abb7b29ea272fa856d6dff9221a6bd9ba8/codex-rs/core/src/session/step_context.rs">Request-scoped StepContext</a></li>
  <li><a href="https://github.com/openai/codex/blob/2b0b37abb7b29ea272fa856d6dff9221a6bd9ba8/codex-rs/core/src/session/mod.rs#L533-L540">Bounded submission and unbounded event channels</a></li>
  <li><a href="https://github.com/openai/codex/blob/2b0b37abb7b29ea272fa856d6dff9221a6bd9ba8/codex-rs/core/src/tools/router.rs#L35-L263">Tool router</a></li>
  <li><a href="https://github.com/openai/codex/blob/2b0b37abb7b29ea272fa856d6dff9221a6bd9ba8/codex-rs/core/src/tools/spec_plan.rs#L103-L271">Per-step tool plan</a></li>
  <li><a href="https://github.com/openai/codex/blob/2b0b37abb7b29ea272fa856d6dff9221a6bd9ba8/codex-rs/tools/src/tool_executor.rs#L13-L43">Tool exposure states</a></li>
  <li><a href="https://github.com/openai/codex/blob/2b0b37abb7b29ea272fa856d6dff9221a6bd9ba8/codex-rs/core/src/tools/orchestrator.rs">Approval/sandbox ToolOrchestrator</a></li>
  <li><a href="https://github.com/openai/codex/blob/2b0b37abb7b29ea272fa856d6dff9221a6bd9ba8/codex-rs/protocol/src/protocol.rs">Protocol operations/events</a></li>
  <li><a href="https://github.com/openai/codex/blob/2b0b37abb7b29ea272fa856d6dff9221a6bd9ba8/codex-rs/protocol/src/protocol.rs#L3141-L3154">RolloutItem variants</a></li>
  <li><a href="https://github.com/openai/codex/blob/2b0b37abb7b29ea272fa856d6dff9221a6bd9ba8/codex-rs/thread-store/src/store.rs">ThreadStore trait</a></li>
  <li><a href="https://github.com/openai/codex/blob/2b0b37abb7b29ea272fa856d6dff9221a6bd9ba8/codex-rs/thread-store/src/local/mod.rs#L45-L63">Local JSONL/SQLite store</a></li>
  <li><a href="https://github.com/openai/codex/blob/2b0b37abb7b29ea272fa856d6dff9221a6bd9ba8/codex-rs/thread-store/src/local/list_threads.rs#L112-L194">Thread list DB/compatibility paths</a></li>
  <li><a href="https://github.com/openai/codex/blob/2b0b37abb7b29ea272fa856d6dff9221a6bd9ba8/codex-rs/state/src/runtime.rs#L365-L382">SQLite WAL runtime configuration</a></li>
  <li><a href="https://github.com/openai/codex/blob/2b0b37abb7b29ea272fa856d6dff9221a6bd9ba8/codex-rs/app-server-protocol/src/protocol/common.rs">App-server typed protocol</a></li>
  <li><a href="https://github.com/openai/codex/blob/2b0b37abb7b29ea272fa856d6dff9221a6bd9ba8/codex-rs/app-server/README.md">App-server transport and protocol guide</a></li>
</ul>

<p>공식 제품 계약:</p>

<ul>
  <li><a href="https://learn.chatgpt.com/docs">Codex documentation</a></li>
  <li><a href="https://learn.chatgpt.com/docs/agent-configuration/subagents.md">Subagents</a></li>
  <li><a href="https://learn.chatgpt.com/docs/agent-configuration/agents-md.md">AGENTS.md</a></li>
  <li><a href="https://learn.chatgpt.com/docs/customization/memories.md">Memories</a></li>
  <li><a href="https://learn.chatgpt.com/docs/config-file/config-advanced">Configuration</a></li>
  <li><a href="https://learn.chatgpt.com/docs/agent-approvals-security.md">Agent approvals &amp; security</a></li>
  <li><a href="https://learn.chatgpt.com/docs/sandboxing.md">Sandbox</a></li>
  <li><a href="https://learn.chatgpt.com/docs/app-server.md">App server</a></li>
  <li><a href="https://learn.chatgpt.com/docs/non-interactive-mode.md">Non-interactive mode</a></li>
  <li><a href="https://learn.chatgpt.com/docs/environments/cloud-environment.md">Cloud environments</a></li>
</ul>

<h2 id="21-최종-판정">21. 최종 판정</h2>

<p>Codex의 설계 수준을 결정하는 것은 기능 개수가 아니다. <strong>실행 순서, state ownership, OS boundary,
tool exposure, durable replay, client protocol을 각각 독립 계약으로 만든 뒤 한 turn에 조립했다는 점</strong>이
핵심이다.</p>

<p>Farad가 Codex와 경쟁하려면 Codex의 모든 surface를 따라갈 필요는 없다. 오히려 Farad가 이미 강한
provider 중립성, native desktop UX, scheduler, HPC/NFS 운영 계약을 유지하면서 다음 세 축만 Codex
수준으로 끌어올리는 편이 논리적이다.</p>

<ol>
  <li>runtime command/event와 per-turn/per-request snapshot 수명의 명문화</li>
  <li>policy 승인 밖의 실제 command containment</li>
  <li>canonical transcript와 대규모 query projection의 검증 가능한 복구 계약</li>
</ol>

<p>이 세 축은 “나중에 기능을 더 넣기 위한 추상화”가 아니라, 현재 agent가 실제 파일과 프로세스를
다루고 session이 누적되는 순간부터 필요한 production foundation이다. 이 최종 권고는 <code class="language-plaintext highlighter-rouge">A/B</code> 근거를
Farad 조건에 적용한 <code class="language-plaintext highlighter-rouge">D</code> 등급의 판단이며, §18.7의 미확인 항목을 성능·제품 사실로 간주하지 않는다.</p>

<style>pre.mermaid{overflow-x:auto;background:transparent;border:0;padding:0}.mermaid svg{max-width:100%;height:auto}</style>

<script type="module">
import mermaid from "https://cdn.jsdelivr.net/npm/mermaid@11.16.0/dist/mermaid.esm.min.mjs";
mermaid.initialize({startOnLoad:true,securityLevel:"strict"});
</script>]]></content><author><name>hvppycoding</name></author><category term="research" /><summary type="html"><![CDATA[Codex 제품·런타임·아키텍처 심층 분석]]></summary></entry><entry><title type="html">Claude Code 제품·런타임·아키텍처 심층 분석</title><link href="https://hvppycoding.github.io/research/claude-code-architecture-analysis/" rel="alternate" type="text/html" title="Claude Code 제품·런타임·아키텍처 심층 분석" /><published>2026-07-13T23:52:00+09:00</published><updated>2026-07-13T23:52:00+09:00</updated><id>https://hvppycoding.github.io/research/claude-code-architecture-analysis</id><content type="html" xml:base="https://hvppycoding.github.io/research/claude-code-architecture-analysis/"><![CDATA[<h1 id="claude-code-제품런타임아키텍처-심층-분석">Claude Code 제품·런타임·아키텍처 심층 분석</h1>

<blockquote>
  <p><a href="https://hvppycoding.github.io/research/coding-agent-architecture-comparison/">전체 보고서 인덱스</a></p>
</blockquote>

<blockquote>
  <p>조사 기준일: 2026-07-13 (Asia/Seoul)<br />
제품 계약 기준: 2026-07-13 공식 문서와 npm <code class="language-plaintext highlighter-rouge">latest</code> 2.1.207(배포 일치 보장이 아닌 버전 표지)<br />
로컬 실행 관찰: 설치된 <code class="language-plaintext highlighter-rouge">claude</code> CLI 2.1.206<br />
내부 관찰 기준: 로컬에 보관된 <code class="language-plaintext highlighter-rouge">@anthropic-ai/claude-code</code> 2.1.88 소스맵 복원본<br />
증거 등급: <code class="language-plaintext highlighter-rouge">B</code> 공식 1차 문서, <code class="language-plaintext highlighter-rouge">C</code> 복원 소스·실행 관찰, <code class="language-plaintext highlighter-rouge">D</code> 앞선 근거에서 도출한 추론</p>
</blockquote>

<h2 id="1-결론부터">1. 결론부터</h2>

<p>Claude Code의 가장 강한 아키텍처적 선택은 <strong>완성도 높은 로컬 agent runtime과 공통 agentic
behavior를 제품의 중심에 둔 것</strong>이다. CLI와 Agent SDK 사이에는 CLI subprocess 재사용이 공식적으로
확인되고, 공식 문서는 IDE·Desktop·Web을 포함한 표면들이 같은 agentic loop의 행동 모델을 제공한다고
설명한다. 다만 이것은 모든 표면이 동일 코드·동일 process kernel을 공유한다는 증거는 아니다.
확인된 code reuse와 제품 수준의 behavioral parity를 구분해야 한다. <code class="language-plaintext highlighter-rouge">B</code>, <code class="language-plaintext highlighter-rouge">D</code></p>

<p>반대로 가장 큰 구조적 비용도 같은 선택에서 나온다. Agent SDK는 가벼운 라이브러리 함수처럼
보이지만 실제로는 <code class="language-plaintext highlighter-rouge">query()</code>마다 별도의 <code class="language-plaintext highlighter-rouge">claude</code> CLI subprocess를 stdio로 구동하며, 그 프로세스가
shell·working directory·로컬 JSONL session을 소유한다. 즉 Claude Code는 본질적으로 <strong>stateful
process-local runtime</strong>이다. 동시에 실행 중인 Agent SDK session N개는 CLI subprocess N개를
요구하므로, 수평 확장에는 session affinity, working tree 격리, 로컬 상태 hydration, 메모리 상한이
필요하다. 저장된 과거 session N개가 process N개라는 뜻은 아니다. 공식 hosting 문서가 active
execution의 이 제약을 직접 설명한다.
<a href="https://code.claude.com/docs/en/agent-sdk/hosting">Agent SDK hosting</a> <code class="language-plaintext highlighter-rouge">B</code></p>

<p>종합 판정은 다음과 같다.</p>

<table>
  <thead>
    <tr>
      <th>항목</th>
      <th>판정</th>
      <th>핵심 이유</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>개인 개발자의 대화형 coding loop</td>
      <td>매우 강함</td>
      <td>context 수집, 도구, 승인, diff, resume가 한 흐름으로 응집됨</td>
    </tr>
    <tr>
      <td>장시간 자율 작업</td>
      <td>강함</td>
      <td>subagent, background session, agent view, worktree, goal, schedule을 계층적으로 제공</td>
    </tr>
    <tr>
      <td>확장 생태계</td>
      <td>매우 강함</td>
      <td>MCP·skills·plugins·hooks·custom agents가 각기 다른 변경 지점을 담당</td>
    </tr>
    <tr>
      <td>세션 재현·복구</td>
      <td>강함</td>
      <td>append-oriented JSONL, resume/fork, compaction boundary, file checkpoint 제공</td>
    </tr>
    <tr>
      <td>보안 기본 구조</td>
      <td>강하지만 조건부</td>
      <td>permission과 Bash OS sandbox를 분리했으나 sandbox는 opt-in이고 native Windows에는 OS sandbox가 없음</td>
    </tr>
    <tr>
      <td>대규모 서버 멀티테넌시</td>
      <td>중간</td>
      <td>SDK가 가능하게는 하지만 프로세스·로컬 상태·설정 누출 방지를 운영자가 책임짐</td>
    </tr>
    <tr>
      <td>10,000 세션/NFS 홈</td>
      <td>약함~중간</td>
      <td>2.1.88 관찰에서 디렉터리 전량 stat과 파일별 JSONL 구조가 NFS 비용 모델과 맞지 않음; current 내부는 미확인</td>
    </tr>
    <tr>
      <td>provider 중립성</td>
      <td>낮음</td>
      <td>여러 호스팅 provider를 지원해도 Claude Messages semantics와 Claude 모델 기능에 깊게 결합</td>
    </tr>
    <tr>
      <td>내부 변경 용이성</td>
      <td>혼합·과거 관찰</td>
      <td>2.1.88 복원본은 전역 상태와 대형 TUI/메시지 모듈 결합을 보이나 current 내부는 미확인</td>
    </tr>
  </tbody>
</table>

<p>위 표와 이 절의 종합 판정은 공식 계약·복원 관찰에서 도출한 <code class="language-plaintext highlighter-rouge">D</code> 등급의 평가다.</p>

<p>이 보고서가 내리는 가장 중요한 판정은 “기능이 많다”가 아니다. Claude Code는 <strong>agent loop를
제품의 단일 중심 권위로 삼고, UI와 확장을 그 주위에 붙이는 데 성공</strong>했다. 다만 실행 상태와 저장
형식이 한 로컬 프로세스에 강하게 붙어 있어, 데스크톱 개인 도구로서는 장점이지만 서버·HPC·NFS
환경에서는 그대로 모방하면 안 된다. <code class="language-plaintext highlighter-rouge">D</code></p>

<h2 id="2-범위버전증거-경계">2. 범위·버전·증거 경계</h2>

<h3 id="21-현재-제품과-내부-관찰의-버전-차이">2.1 현재 제품과 내부 관찰의 버전 차이</h3>

<p>2026-07-13에 npm registry를 확인한 결과 <code class="language-plaintext highlighter-rouge">@anthropic-ai/claude-code</code>의 <code class="language-plaintext highlighter-rouge">latest</code>는 <strong>2.1.207</strong>
(2026-07-10 게시), <code class="language-plaintext highlighter-rouge">stable</code>은 <strong>2.1.197</strong>(2026-06-30 게시)이었다. npm 설치는 현재 공식
README에서 deprecated로 안내되지만, registry version은 로컬 복원본과 현재 제품 사이의 간격을
확인하는 기준으로 사용했다. 같은 시점 로컬에 설치된 binary는 2.1.206이었다. 따라서 2.1.207은
모든 surface의 실제 배포 build를 뜻하지 않고 문서·복원본의 시간 간격을 표시하는 registry
marker다. <a href="https://github.com/anthropics/claude-code">공식 저장소</a>,
<a href="https://registry.npmjs.org/@anthropic-ai/claude-code">npm registry metadata</a> <code class="language-plaintext highlighter-rouge">B</code></p>

<p>로컬 <code class="language-plaintext highlighter-rouge">packages/claude-code-sourcemap/</code>은 public npm package의 <code class="language-plaintext highlighter-rouge">cli.js.map</code>에 포함된
<code class="language-plaintext highlighter-rouge">sourcesContent</code>에서 복원한 <strong>2.1.88, TypeScript 1,884개 파일</strong>의 사본이다. 이 사본을 현재 위치로
옮긴 repository commit은 <code class="language-plaintext highlighter-rouge">3815436a54de47649b612002330c505e1d369926</code>
(2026-04-24)이다. 그러나 Claude Code 본체는 일반적인 의미의 공개 OSS 구현 계약이 아니며,
공식 repository의 license도 “All rights reserved”이다.
<a href="https://github.com/anthropics/claude-code/blob/main/LICENSE.md">공식 license</a> <code class="language-plaintext highlighter-rouge">C</code></p>

<p>따라서 이 보고서는 다음 규칙을 지킨다.</p>

<ul>
  <li>현재 사용자 동작·지원 기능·운영 보장은 공식 문서 <code class="language-plaintext highlighter-rouge">B</code>를 우선한다.</li>
  <li><code class="language-plaintext highlighter-rouge">QueryEngine</code>, tool executor, JSONL 최적화, 전역 상태 같은 내부 구조는 2.1.88 복원본의 관찰
<code class="language-plaintext highlighter-rouge">C</code>로만 쓴다.</li>
  <li>복원본에 현재보다 늦게 공개된 기능의 feature-gated 코드가 있어도 현재 구현과 동일하다고
승격하지 않는다.</li>
  <li>공식 문서와 복원본이 충돌하면 현재 공식 문서를 채택하고 차이를 명시한다.</li>
  <li>비공개 코드의 테스트 체계, 배포 backend 내부, Desktop IPC 같은 확인 불가능한 부분은 추측하지
않는다.</li>
</ul>

<h3 id="22-분석-대상">2.2 분석 대상</h3>

<p>여기서 Claude Code는 단순 CLI만 뜻하지 않는다.</p>

<ul>
  <li>로컬 interactive CLI와 headless <code class="language-plaintext highlighter-rouge">-p</code></li>
  <li>VS Code·JetBrains 통합</li>
  <li>Claude Desktop의 Code/Cowork 표면</li>
  <li>Claude Code on the web과 Remote Control</li>
  <li>Claude Agent SDK(TypeScript/Python)</li>
  <li>subagent, agent view, experimental agent teams</li>
  <li>MCP, skills, plugins, hooks, LSP</li>
</ul>

<p>GitHub Action이나 Slack 통합은 배포 표면으로는 포함하지만 각 integration wrapper의 구현 자체는
깊게 분석하지 않는다.</p>

<h2 id="3-제품-경계와-배포-형태">3. 제품 경계와 배포 형태</h2>

<h3 id="31-제품이-최적화하는-핵심-작업">3.1 제품이 최적화하는 핵심 작업</h3>

<p>Claude Code가 최적화하는 단위는 “한 번의 completion”이 아니라 <strong>repository를 읽고, 계획하고,
도구를 실행하고, 결과를 다시 모델에 공급하는 stateful working session</strong>이다. 공식 agent-loop
설명에서 입력은 prompt·system prompt·tools·history이고, 모델이 tool call을 내면 실행 결과를 다시
conversation에 추가해 tool call이 사라질 때까지 반복한다. 마지막에는 usage·cost·session ID를
포함한 result가 나온다. <a href="https://code.claude.com/docs/en/agent-sdk/agent-loop">Agent loop</a> <code class="language-plaintext highlighter-rouge">B</code></p>

<p>이 제품 경계는 다음 결과를 낳는다.</p>

<ol>
  <li>파일시스템과 shell은 부가 기능이 아니라 runtime state의 일부다.</li>
  <li>transcript는 단순 UI history가 아니라 resume·compaction·fork의 기초다.</li>
  <li>permission UI는 장식이 아니라 실행 state machine의 한 단계다.</li>
  <li>MCP와 skills는 prompt에 문자열을 보태는 것 이상으로 capability graph를 바꾼다.</li>
  <li>UI는 agent의 진행·대기·승인 상태를 투영해야 한다.</li>
</ol>

<h3 id="32-표면별-실행-위치">3.2 표면별 실행 위치</h3>

<table>
  <thead>
    <tr>
      <th>표면</th>
      <th>agent 실행 위치</th>
      <th>파일 실행 위치</th>
      <th>중요한 경계</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>CLI / IDE</td>
      <td>사용자 로컬 머신</td>
      <td>로컬 project</td>
      <td>터미널·IDE는 같은 local config/memory/MCP를 공유</td>
    </tr>
    <tr>
      <td>Desktop Code</td>
      <td>로컬</td>
      <td>로컬 또는 제품이 관리하는 작업 공간</td>
      <td>parallel session, diff, editor, terminal을 시각화</td>
    </tr>
    <tr>
      <td>Remote Control</td>
      <td>로컬 CLI process</td>
      <td>로컬</td>
      <td>browser/mobile은 control plane; 코드와 command는 로컬에 남음</td>
    </tr>
    <tr>
      <td>Claude Code on the web</td>
      <td>Anthropic 관리 VM</td>
      <td>격리된 cloud VM</td>
      <td>repository clone, proxy credential, egress proxy</td>
    </tr>
    <tr>
      <td>Agent SDK self-hosting</td>
      <td>사용자의 app/container가 띄운 CLI child</td>
      <td>해당 container/working dir</td>
      <td>session당 subprocess, 로컬 JSONL, app이 HTTP/WS 제공</td>
    </tr>
    <tr>
      <td>Agent view</td>
      <td>per-user local supervisor의 child process</td>
      <td>각 session cwd/worktree</td>
      <td>terminal에서 detach된 background session 관리</td>
    </tr>
  </tbody>
</table>

<p>공식 문서는 local·cloud VM·Remote Control을 구분하고, 표면들이 같은 agentic loop를 사용한다고
설명한다. <a href="https://code.claude.com/docs/en/how-claude-code-works">How Claude Code works</a>,
<a href="https://code.claude.com/docs/en/platforms">Platforms</a>,
<a href="https://code.claude.com/docs/en/remote-control">Remote Control</a> <code class="language-plaintext highlighter-rouge">B</code></p>

<h3 id="33-세-가지-핵심-토폴로지">3.3 세 가지 핵심 토폴로지</h3>

<h4 id="로컬-cli">로컬 CLI</h4>

<pre class="mermaid">
flowchart LR
    U[User / Terminal] --&gt; TUI[Ink TUI / CLI]
    TUI --&gt; QE[Agent loop runtime]
    QE --&gt; API[Claude Messages API]
    QE --&gt; PERM[Permission + hooks]
    PERM --&gt; TOOLS[Built-in tools]
    TOOLS --&gt; CHILD[Shell / editor / LSP child processes]
    QE --&gt; MCP[MCP clients]
    MCP --&gt; MCPS[MCP local or remote servers]
    QE --&gt; JSONL[Local JSONL transcript]
    QE --&gt; FS[Working tree]

</pre>

<p>이 그림에서 authoritative execution owner는 TUI가 아니라 agent runtime process다. TUI는 입력,
stream, permission prompt, status를 표시한다. <code class="language-plaintext highlighter-rouge">D</code></p>

<h4 id="agent-sdk-self-hosting">Agent SDK self-hosting</h4>

<pre class="mermaid">
flowchart LR
    C[Client] --&gt; APP[Host application HTTP / WebSocket]
    APP --&gt; SDK[Claude Agent SDK]
    SDK &lt;--&gt;|stdio control + message stream| CLI[Dedicated claude subprocess]
    CLI --&gt; API[Claude API / Bedrock / Vertex / Foundry]
    CLI --&gt; DISK[Local JSONL + cwd]
    CLI --&gt; PROC[Tool and MCP process tree]
    SDK --&gt; STORE[Optional SessionStore mirror]
    APP --&gt; IMCP[In-process SDK MCP tools]
    CLI -.MCP call.-&gt; IMCP

</pre>

<p>공식 hosting 문서에 따르면 한 agent session은 한 subprocess이며, subprocess 자체는 network
listener가 아니다. 외부 client endpoint는 host application이 제공한다. <code class="language-plaintext highlighter-rouge">SessionStore</code>는 local-first
mirror이지 로컬 write를 대체하지 않는다. <a href="https://code.claude.com/docs/en/agent-sdk/hosting">Agent SDK hosting</a>,
<a href="https://code.claude.com/docs/en/agent-sdk/session-storage">External session storage</a> <code class="language-plaintext highlighter-rouge">B</code></p>

<p>TypeScript SDK의 <code class="language-plaintext highlighter-rouge">createSdkMcpServer()</code>는 host application과 같은 process에서 custom MCP tool을
실행할 수 있다. 따라서 모든 tool이 CLI child에만 있는 것은 아니며, 이 경우 stdio control channel을
거쳐 application process로 실행이 되돌아온다.
<a href="https://code.claude.com/docs/en/agent-sdk/typescript">TypeScript SDK reference</a> <code class="language-plaintext highlighter-rouge">B</code></p>

<h4 id="background-agent-view">background agent view</h4>

<pre class="mermaid">
flowchart TB
    VIEW[claude agents UI] --&gt; SUP[Per-user supervisor]
    SHELL[claude background / attach / respawn] --&gt; SUP
    SUP --&gt; S1[Session process A]
    SUP --&gt; S2[Session process B]
    SUP --&gt; SN[Session process N]
    S1 --&gt; J1[Transcript + state]
    S2 --&gt; J2[Transcript + state]
    SUP --&gt; ROSTER[daemon roster / job state]

</pre>

<p>supervisor는 terminal과 분리되어 session을 살려 두며, finished session이 약 한 시간 unattached이면
process를 내려 resource를 회수하고 transcript를 유지한다. machine sleep/shutdown까지 이겨 내는
remote scheduler는 아니며, 종료된 session은 <code class="language-plaintext highlighter-rouge">respawn</code>으로 다시 띄운다.
<a href="https://code.claude.com/docs/en/agent-view">Agent view</a> <code class="language-plaintext highlighter-rouge">B</code></p>

<h2 id="4-핵심-agent-loop와-상태-머신">4. 핵심 agent loop와 상태 머신</h2>

<h3 id="41-공식-동작-계약">4.1 공식 동작 계약</h3>

<p>공식 SDK 문서의 loop는 단순하지만 중요한 계약을 가진다.</p>

<pre class="mermaid">
stateDiagram-v2
    [*] --&gt; AssembleContext
    AssembleContext --&gt; ModelStream
    ModelStream --&gt; ToolDecision
    ToolDecision --&gt; ExecuteTools: tool_use exists
    ExecuteTools --&gt; AppendResults
    AppendResults --&gt; CompactCheck
    CompactCheck --&gt; AssembleContext: continue
    ToolDecision --&gt; StopHooks: no tool_use
    StopHooks --&gt; Completed: accepted
    StopHooks --&gt; AssembleContext: hook requests continuation
    ModelStream --&gt; Failed: terminal error / cancellation
    AssembleContext --&gt; Limited: maxTurns or maxBudget
    Completed --&gt; [*]
    Failed --&gt; [*]
    Limited --&gt; [*]

</pre>

<p>message stream은 init/compact/info/worker shutdown 같은 system message, assistant message, tool result를
담은 user message, raw stream event, final result로 구성된다. <code class="language-plaintext highlighter-rouge">maxTurns</code>와 <code class="language-plaintext highlighter-rouge">maxBudgetUsd</code>는 무한
반복을 제한하고, final result subtype은 success, max turns, max budget, execution error, structured
output retry exhaustion을 구분한다. <code class="language-plaintext highlighter-rouge">B</code></p>

<h3 id="42-복원-소스에서-보이는-실제-loop-구조">4.2 복원 소스에서 보이는 실제 loop 구조</h3>

<p>2.1.88 복원본에서는 <code class="language-plaintext highlighter-rouge">QueryEngine</code>이 “conversation 하나당 한 instance”로 message, file cache,
usage, abort controller를 소유한다. <code class="language-plaintext highlighter-rouge">submitMessage()</code>는 같은 instance 안에서 새 turn을 시작하고,
<code class="language-plaintext highlighter-rouge">query()</code> async generator에 위임한다.</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">packages/claude-code-sourcemap/src/QueryEngine.ts:175</code> — conversation lifecycle owner 설명</li>
  <li><code class="language-plaintext highlighter-rouge">packages/claude-code-sourcemap/src/QueryEngine.ts:209</code> — <code class="language-plaintext highlighter-rouge">submitMessage()</code> 진입점</li>
  <li><code class="language-plaintext highlighter-rouge">packages/claude-code-sourcemap/src/QueryEngine.ts:436</code> — API 응답 전 user message 선영속화</li>
  <li><code class="language-plaintext highlighter-rouge">packages/claude-code-sourcemap/src/QueryEngine.ts:675</code> — <code class="language-plaintext highlighter-rouge">query()</code> stream 소비</li>
  <li><code class="language-plaintext highlighter-rouge">packages/claude-code-sourcemap/src/query.ts:241</code> — <code class="language-plaintext highlighter-rouge">queryLoop()</code></li>
  <li><code class="language-plaintext highlighter-rouge">packages/claude-code-sourcemap/src/query.ts:307</code> — explicit <code class="language-plaintext highlighter-rouge">while (true)</code> state machine</li>
</ul>

<p><code class="language-plaintext highlighter-rouge">queryLoop()</code>는 immutable params와 iteration 사이에 넘기는 mutable <code class="language-plaintext highlighter-rouge">State</code>를 분리한다. state에는
messages, tool context, compact tracking, max-output recovery count, turn count, 직전 transition reason이
있다. 재귀 호출 대신 <code class="language-plaintext highlighter-rouge">state = next; continue</code>를 사용하고 terminal reason을 return한다. <code class="language-plaintext highlighter-rouge">C</code></p>

<p>이 설계의 좋은 점은 다음과 같다.</p>

<ul>
  <li>async generator가 model stream, progress, tool result, compact boundary를 호출자에게 순차 노출한다.</li>
  <li>continue reason이 명시되어 recovery path를 테스트할 수 있다.</li>
  <li>사용자 메시지를 model request 전에 transcript에 써 kill 직후에도 resume 가능성을 높인다.</li>
  <li>recoverable API error를 곧바로 SDK client에 노출하지 않고 내부 recovery가 끝난 뒤 terminal error만
보낸다.</li>
  <li>tool result와 attachment를 다음 iteration의 message에 합쳐 loop invariants를 한곳에서 유지한다.</li>
</ul>

<p>그러나 완전한 순수 state machine은 아니다. <code class="language-plaintext highlighter-rouge">query.ts</code>는 process-wide bootstrap state, feature gate,
settings, telemetry, queue 등 많은 ambient dependency를 읽는다. 최근 구조에는 <code class="language-plaintext highlighter-rouge">QueryDeps</code>로 model
call·microcompact·autocompact·UUID 네 가지를 주입하는 seam이 생겼지만, 주석 자체가 “scope is
intentionally narrow”라고 밝힌다.
<code class="language-plaintext highlighter-rouge">packages/claude-code-sourcemap/src/query/deps.ts:1</code> <code class="language-plaintext highlighter-rouge">C</code></p>

<h3 id="43-streaming-tool-execution">4.3 streaming tool execution</h3>

<p>복원본의 <code class="language-plaintext highlighter-rouge">StreamingToolExecutor</code>는 assistant response가 모두 끝난 뒤에만 tool을 시작하는 구조가
아니다. 완성된 <code class="language-plaintext highlighter-rouge">tool_use</code> block을 stream 중 파악해 schema를 검증하고 concurrency-safe call을
queue/run한다. <code class="language-plaintext highlighter-rouge">getCompletedResults()</code>는 앞선 concurrency-safe call이 아직 실행 중이어도 뒤에서
먼저 끝난 safe call의 결과를 내보낼 수 있다. 따라서 모든 결과의 model 선언 순서를 보존한다고
일반화하면 안 된다. 대신 unsafe call은 exclusive barrier가 되고, side-effect-sensitive context
modifier는 필요한 순서로 적용된다. 이는 긴 model stream과 tool latency를 일부 겹치는 지연
최적화다.</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">packages/claude-code-sourcemap/src/services/tools/StreamingToolExecutor.ts:35</code></li>
  <li><code class="language-plaintext highlighter-rouge">packages/claude-code-sourcemap/src/services/tools/StreamingToolExecutor.ts:104</code></li>
  <li><code class="language-plaintext highlighter-rouge">packages/claude-code-sourcemap/src/services/tools/StreamingToolExecutor.ts:412</code> <code class="language-plaintext highlighter-rouge">C</code></li>
</ul>

<p>이 최적화의 대가는 cancellation, permission wait, tool-result ordering, context modifier 병합이 훨씬
어려워진다는 점이다. 이 복잡성을 executor 한곳에 모아 둔 것은 합리적이지만, 임의의 tool이
concurrency-safe를 잘못 선언하면 안전성이 무너질 수 있어 기본값을 conservative하게 해야 한다.
실제 <code class="language-plaintext highlighter-rouge">buildTool()</code> 기본값은 concurrency-safe와 read-only를 모두 false로 둔다.
<code class="language-plaintext highlighter-rouge">packages/claude-code-sourcemap/src/Tool.ts:743</code> <code class="language-plaintext highlighter-rouge">C</code>, <code class="language-plaintext highlighter-rouge">D</code></p>

<h2 id="5-상태권위데이터-흐름">5. 상태·권위·데이터 흐름</h2>

<p>Claude Code를 이해할 때 가장 유용한 구분은 “무엇이 어디의 권위인가”다.</p>

<table>
  <thead>
    <tr>
      <th>상태</th>
      <th>실질적 권위</th>
      <th>생명주기</th>
      <th>비고</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>현재 agent turn</td>
      <td>CLI agent-loop process memory; 2.1.88 SDK/headless에서는 <code class="language-plaintext highlighter-rouge">QueryEngine</code></td>
      <td>process·session</td>
      <td>model stream과 tool execution을 직렬화</td>
    </tr>
    <tr>
      <td>대화 이력</td>
      <td>로컬 project별 JSONL</td>
      <td>session 이후 유지</td>
      <td>resume/fork/compact chain의 기반</td>
    </tr>
    <tr>
      <td>실제 코드 상태</td>
      <td>working directory filesystem/git</td>
      <td>session과 독립</td>
      <td>transcript를 resume해도 filesystem은 자동 복원되지 않음</td>
    </tr>
    <tr>
      <td>file checkpoint</td>
      <td>editing tool이 만든 snapshot</td>
      <td>session/checkpoint</td>
      <td>Bash나 외부 editor 변경은 포착하지 않음</td>
    </tr>
    <tr>
      <td>effective permission</td>
      <td>merged settings + mode + runtime prompt + hook</td>
      <td>tool call</td>
      <td>deny &gt; ask &gt; allow, hook은 추가 차단 가능</td>
    </tr>
    <tr>
      <td>context window</td>
      <td>현재 request에 조립된 prompt/message/tool schema</td>
      <td>API request</td>
      <td>transcript 전체와 동일하지 않음</td>
    </tr>
    <tr>
      <td>model memory</td>
      <td>CLAUDE.md + auto memory + invoked skill context</td>
      <td>session/turn</td>
      <td>enforcement가 아니라 model context</td>
    </tr>
    <tr>
      <td>MCP capability</td>
      <td>각 MCP server의 advertised state + client cache</td>
      <td>connection/session</td>
      <td><code class="language-plaintext highlighter-rouge">list_changed</code>로 동적 갱신 가능</td>
    </tr>
    <tr>
      <td>external SessionStore</td>
      <td>로컬 transcript의 durable mirror</td>
      <td>cross-host</td>
      <td>local-first; file state와 memory는 미러하지 않음</td>
    </tr>
    <tr>
      <td>background roster</td>
      <td>per-user supervisor state files</td>
      <td>machine user</td>
      <td>각 child session transcript와 분리</td>
    </tr>
  </tbody>
</table>

<p>이 구조에서 JSONL은 “모든 것의 event store”가 아니다. conversation을 복구하지만 git tree, shell의
외부 side effect, MCP external state, memory files까지 transaction으로 묶지 않는다. resume은 agent의
지식 상태를 복구할 뿐 세계 상태를 원자적으로 되돌리지 않는다. 공식 SDK session 문서도 session은
conversation을 보존하고 filesystem은 보존하지 않는다고 명시한다.
<a href="https://code.claude.com/docs/en/agent-sdk/sessions">SDK sessions</a> <code class="language-plaintext highlighter-rouge">B</code></p>

<h2 id="6-context-조립-transcript-compaction-prompt-cache">6. context 조립, transcript, compaction, prompt cache</h2>

<h3 id="61-서로-다른-네-층을-구분해야-한다">6.1 서로 다른 네 층을 구분해야 한다</h3>

<p>Claude Code에서는 다음 네 대상을 같은 것으로 보면 안 된다.</p>

<ol>
  <li><strong>Raw transcript</strong> — user/assistant/tool/system record를 담은 로컬 JSONL.</li>
  <li><strong>Active conversation chain</strong> — branch·rewind·compaction을 반영해 다음 request에 이어질 logical path.</li>
  <li><strong>Request context</strong> — active history에 system prompt, tool definitions, CLAUDE.md, skills 등을 합친 것.</li>
  <li><strong>Provider prompt cache</strong> — 매 request의 exact stable prefix가 이전 request와 같을 때 재사용되는 계산.</li>
</ol>

<p>공식 문서에 따르면 매 turn 전체 context를 다시 보내며 cache는 file별이 아니라 exact prefix
기반이다. Claude Code는 system/tool definitions → project context → conversation 순으로 안정적인
부분을 앞에 놓고, plan이나 skill body처럼 중간에 바뀔 수 있는 내용은 conversation 쪽에 붙여 cache
prefix를 지킨다. model이나 effort 변경도 cache key에 영향을 준다.
<a href="https://code.claude.com/docs/en/prompt-caching">Prompt caching</a> <code class="language-plaintext highlighter-rouge">B</code></p>

<h3 id="62-startupturn-context">6.2 startup·turn context</h3>

<p>일반적으로 context에는 다음이 들어간다.</p>

<ul>
  <li>system prompt와 tool schema</li>
  <li>대화 history와 tool results</li>
  <li>root/ancestor <code class="language-plaintext highlighter-rouge">CLAUDE.md</code>, <code class="language-plaintext highlighter-rouge">CLAUDE.local.md</code>, <code class="language-plaintext highlighter-rouge">.claude/rules</code></li>
  <li>auto memory의 <code class="language-plaintext highlighter-rouge">MEMORY.md</code> 앞 200줄 또는 25KB</li>
  <li>skill descriptions와 호출된 skill body</li>
  <li>path를 읽을 때 발견한 nested instructions</li>
  <li>IDE selection, working-directory metadata, hooks가 추가한 context</li>
</ul>

<p><code class="language-plaintext highlighter-rouge">CLAUDE.md</code>와 auto memory는 policy enforcement가 아니라 model에게 주는 context다. Claude Code는
<code class="language-plaintext highlighter-rouge">AGENTS.md</code>를 직접 읽지 않으며 <code class="language-plaintext highlighter-rouge">CLAUDE.md</code>에서 <code class="language-plaintext highlighter-rouge">@AGENTS.md</code>를 import해야 한다.
<a href="https://code.claude.com/docs/en/memory">Memory</a> <code class="language-plaintext highlighter-rouge">B</code></p>

<p>이 차이는 아키텍처적으로 중요하다. “반드시 실행되어야 하는 규칙”은 natural-language memory가
아니라 permission, managed setting, hook, sandbox에 있어야 한다. <code class="language-plaintext highlighter-rouge">D</code></p>

<h3 id="63-compaction의-계약과-손실">6.3 compaction의 계약과 손실</h3>

<p>context가 커지면 Claude Code는 오래된 tool output을 먼저 줄이고 conversation을 summary로
compact한다. <code class="language-plaintext highlighter-rouge">/compact</code>로 수동 실행할 수 있고, 자동 threshold에서도 실행된다. compaction 뒤에도
system prompt와 tool definitions는 유지되며 root <code class="language-plaintext highlighter-rouge">CLAUDE.md</code>와 auto memory는 다시 주입된다. 그러나
nested <code class="language-plaintext highlighter-rouge">CLAUDE.md</code>와 path-scoped rule은 해당 경로의 파일을 다시 읽기 전까지 돌아오지 않는다.
<a href="https://code.claude.com/docs/en/context-window">Context window</a> <code class="language-plaintext highlighter-rouge">B</code></p>

<p>호출된 skill body도 무제한 보존되지 않는다. 현재 공식 계약은 compaction 후 skill당 최대 5,000
tokens, 전체 25,000 tokens이며 최근 호출부터 채워 오래된 skill은 완전히 빠질 수 있다.
<a href="https://code.claude.com/docs/en/skills">Skills</a> <code class="language-plaintext highlighter-rouge">B</code></p>

<p>복원본 <code class="language-plaintext highlighter-rouge">compact.ts</code>에도 다음 상한이 보인다.</p>

<ul>
  <li>복구 file 5개, 전체 50,000 tokens, file당 5,000 tokens</li>
  <li>skill당 5,000 tokens, skill 전체 25,000 tokens</li>
  <li>plan mode, async agent attachment, recent file restoration</li>
</ul>

<p>관련 경로는 <code class="language-plaintext highlighter-rouge">packages/claude-code-sourcemap/src/services/compact/compact.ts:122</code>, <code class="language-plaintext highlighter-rouge">:1415</code>,
<code class="language-plaintext highlighter-rouge">:1494</code>, <code class="language-plaintext highlighter-rouge">:1542</code>다. <code class="language-plaintext highlighter-rouge">C</code></p>

<p>이런 compaction은 transcript 보존과 model-visible 재현성을 다르게 만든다. raw JSONL이 과거 record를
가지고 있어도 active model context에는 summary만 남을 수 있다. 따라서 “resume 가능”은 “모델이
과거 모든 세부를 byte-identical하게 다시 본다”와 동의어가 아니다. <code class="language-plaintext highlighter-rouge">D</code></p>

<h3 id="64-overflow-recovery">6.4 overflow recovery</h3>

<p>2.1.88 복원본은 prompt-too-long/413을 바로 끝내지 않는다. staged context collapse를 drain해
retry하고, 그래도 실패하면 reactive compact를 한 번 시도한다. recovery가 실패하면 Stop hook으로
흘려 재시도 death spiral을 만들지 않고 terminal error로 종료한다.
<code class="language-plaintext highlighter-rouge">packages/claude-code-sourcemap/src/query.ts:1062</code> <code class="language-plaintext highlighter-rouge">C</code></p>

<p>max output token도 제한 횟수 안에서 더 큰 cap 또는 synthetic continuation으로 복구한다.
<code class="language-plaintext highlighter-rouge">packages/claude-code-sourcemap/src/query.ts:1185</code> <code class="language-plaintext highlighter-rouge">C</code></p>

<p>이는 실패를 “API error 한 종류”로 뭉개지 않고 context recovery state로 모델링한 좋은 예다. 다만
summary 품질은 model-dependent이고, context가 줄어드는 과정의 의미 손실을 transactionally 검증할
방법은 없다. <code class="language-plaintext highlighter-rouge">D</code></p>

<h2 id="7-세션-저장-resume-branchfork-checkpoint">7. 세션 저장, resume, branch/fork, checkpoint</h2>

<h3 id="71-기본-session-model">7.1 기본 session model</h3>

<p>local session은 <code class="language-plaintext highlighter-rouge">~/.claude/projects/&lt;project&gt;/...jsonl</code>에 연속 저장된다. 공식 CLI는
<code class="language-plaintext highlighter-rouge">--continue</code>, <code class="language-plaintext highlighter-rouge">--resume</code>, <code class="language-plaintext highlighter-rouge">/resume</code>, session name을 제공하고, current worktree·repository·all
projects 범위에서 session을 찾는다. 기본 cleanup은 30일이며 <code class="language-plaintext highlighter-rouge">cleanupPeriodDays</code>로 조정할 수 있다.
history write를 끄려면 <code class="language-plaintext highlighter-rouge">CLAUDE_CODE_SKIP_PROMPT_HISTORY</code>, headless의
<code class="language-plaintext highlighter-rouge">--no-session-persistence</code>, SDK의 <code class="language-plaintext highlighter-rouge">persistSession: false</code>를 사용한다.
<a href="https://code.claude.com/docs/en/sessions">CLI sessions</a>,
<a href="https://code.claude.com/docs/en/configuration">Settings</a> <code class="language-plaintext highlighter-rouge">B</code></p>

<p>resume와 fork의 차이는 명확하다.</p>

<ul>
  <li>resume: 같은 session ID와 history 뒤에 append한다.</li>
  <li>fork/branch: 기존 history를 복사해 새 session ID에서 갈라진다.</li>
  <li>session-scoped permission approval은 branch에 따라오지 않는다.</li>
  <li>같은 session ID를 두 terminal에서 동시에 resume하면 message가 하나의 JSONL에 interleave될 수
있다. 공식 문서가 경고하는 실제 concurrency hazard다. <code class="language-plaintext highlighter-rouge">B</code></li>
</ul>

<h3 id="72-복원본의-jsonl-chain">7.2 복원본의 JSONL chain</h3>

<p>복원본은 단순한 배열 dump가 아니라 message별 UUID와 <code class="language-plaintext highlighter-rouge">parentUuid</code>를 가진 append-oriented chain을
쓴다. compact boundary는 physical parent를 끊고 <code class="language-plaintext highlighter-rouge">logicalParentUuid</code>로 이전 tail을 가리킬 수 있다.
tool result는 해당 assistant tool-use UUID를 parent로 연결한다.</p>

<ul>
  <li>transcript 구성: <code class="language-plaintext highlighter-rouge">packages/claude-code-sourcemap/src/utils/sessionStorage.ts:993</code></li>
  <li>incremental dedup과 parent hint: <code class="language-plaintext highlighter-rouge">packages/claude-code-sourcemap/src/utils/sessionStorage.ts:1391</code></li>
  <li>resume/fork 복원: <code class="language-plaintext highlighter-rouge">packages/claude-code-sourcemap/src/utils/sessionRestore.ts:409</code></li>
</ul>

<p>좋은 점은 branch·rewind 후 dead record를 물리적으로 덮어쓰지 않아 crash tolerance와 audit 단서가
남는다는 것이다. 대신 file이 계속 커지고 leaf chain을 재구성해야 한다. 복원본 주석은 session file이
여러 GB까지 커질 수 있음을 인정하고, 50MB를 넘는 full raw read/rewrite slow path를 막는다.
<code class="language-plaintext highlighter-rouge">packages/claude-code-sourcemap/src/utils/sessionStorage.ts:121</code>, <code class="language-plaintext highlighter-rouge">:227</code> <code class="language-plaintext highlighter-rouge">C</code></p>

<h3 id="73-file-checkpoint는-vcs가-아니다">7.3 file checkpoint는 VCS가 아니다</h3>

<p>Claude Code는 user prompt마다 file checkpoint를 만들고 code와 conversation을 따로 rewind할 수
있다. 그러나 checkpoint는 built-in file editing tool로 한 변경만 추적한다. Bash command, 외부
editor, concurrent process가 바꾼 파일은 추적하지 않는다. 공식 문서도 checkpoint는 version control의
대체가 아니라고 명시한다.
<a href="https://code.claude.com/docs/en/checkpointing">Checkpointing</a> <code class="language-plaintext highlighter-rouge">B</code></p>

<p>따라서 정확한 복구 모델은 다음과 같다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>conversation resume != filesystem rollback
file checkpoint       != git history
session fork          != worktree isolation
</code></pre></div></div>

<p>서로 다른 branch를 실제로 동시에 수정한다면 session fork와 별개로 git worktree를 사용해야 한다.</p>

<h3 id="74-external-sessionstore">7.4 external SessionStore</h3>

<p>Agent SDK의 <code class="language-plaintext highlighter-rouge">SessionStore</code>는 <code class="language-plaintext highlighter-rouge">append</code>와 <code class="language-plaintext highlighter-rouge">load</code>, 선택적으로 list/delete/listSubkeys를 구현한다. 중요한
운영 계약은 local-first dual write다.</p>

<ol>
  <li>CLI subprocess가 먼저 local JSONL에 쓴다.</li>
  <li>SDK가 batch를 external store로 전달한다.</li>
  <li>store가 reject하면 짧은 backoff를 포함해 총 세 번까지 보내지만 timeout은 retry하지 않는다.</li>
  <li>끝내 실패하면 <code class="language-plaintext highlighter-rouge">mirror_error</code>를 stream에 내보내고 agent query는 계속된다.</li>
  <li>store는 transcript만 미러하며 memory와 working-directory artifacts는 별도다.</li>
</ol>

<p><a href="https://code.claude.com/docs/en/agent-sdk/hosting">Agent SDK hosting</a>,
<a href="https://code.claude.com/docs/en/agent-sdk/session-storage">Session storage</a> <code class="language-plaintext highlighter-rouge">B</code></p>

<p>가용성을 우선한 합리적인 선택이지만 external store를 audit-grade canonical log로 생각하면 안 된다.
<code class="language-plaintext highlighter-rouge">mirror_error</code> 감시가 없으면 local은 성공하고 remote durability만 조용히 유실될 수 있다. <code class="language-plaintext highlighter-rouge">D</code></p>

<h2 id="8-tool-architecture와-실행-논리">8. Tool architecture와 실행 논리</h2>

<h3 id="81-현재-tool-surface">8.1 현재 tool surface</h3>

<p>현재 공식 tool reference에는 파일·검색·shell뿐 아니라 Agent, plan/worktree, task, monitor,
schedule/cron, web, MCP resource, notification, workflow, LSP 계열까지 포함된다.
<a href="https://code.claude.com/docs/en/tools-reference">Tools reference</a> <code class="language-plaintext highlighter-rouge">B</code></p>

<p>아키텍처적으로 tool은 다섯 부류로 보는 편이 정확하다.</p>

<table>
  <thead>
    <tr>
      <th>부류</th>
      <th>예</th>
      <th>state 영향</th>
      <th>격리 주체</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>순수/준순수 read</td>
      <td>Read, Glob, Grep, LSP</td>
      <td>context 증가</td>
      <td>permission + path rules</td>
    </tr>
    <tr>
      <td>local mutation</td>
      <td>Edit, Write, NotebookEdit</td>
      <td>working tree 변경</td>
      <td>permission, checkpoint</td>
    </tr>
    <tr>
      <td>open-world execution</td>
      <td>Bash, PowerShell, WebFetch</td>
      <td>임의 side effect 가능</td>
      <td>permission + Bash sandbox/network policy</td>
    </tr>
    <tr>
      <td>orchestration</td>
      <td>Agent, Task<em>, Worktree</em>, Monitor</td>
      <td>child runtime/task state</td>
      <td>parent session + worktree/supervisor</td>
    </tr>
    <tr>
      <td>external capability</td>
      <td>MCP tools/resources</td>
      <td>외부 system state</td>
      <td>MCP trust + per-tool permission + server auth</td>
    </tr>
  </tbody>
</table>

<h3 id="82-tool-contract">8.2 Tool contract</h3>

<p>2.1.88 복원본의 <code class="language-plaintext highlighter-rouge">Tool</code> type은 다음 책임을 한 interface에 모은다.</p>

<ul>
  <li>Zod/JSON schema와 output schema</li>
  <li><code class="language-plaintext highlighter-rouge">call()</code>과 progress callback</li>
  <li>input validation과 tool-specific permission check</li>
  <li><code class="language-plaintext highlighter-rouge">isConcurrencySafe</code>, <code class="language-plaintext highlighter-rouge">isReadOnly</code>, <code class="language-plaintext highlighter-rouge">isDestructive</code>, <code class="language-plaintext highlighter-rouge">isOpenWorld</code></li>
  <li>interrupt behavior</li>
  <li>MCP/LSP/deferred loading metadata</li>
  <li>result size cap과 mapping</li>
  <li>TUI render methods</li>
</ul>

<p><code class="language-plaintext highlighter-rouge">packages/claude-code-sourcemap/src/Tool.ts:362</code> <code class="language-plaintext highlighter-rouge">C</code></p>

<p>execution pipeline은 대략 다음과 같다.</p>

<pre class="mermaid">
flowchart LR
    TC[tool_use block] --&gt; FIND[Resolve tool]
    FIND --&gt; SCHEMA[Schema parse]
    SCHEMA --&gt; VALID[Tool validation]
    VALID --&gt; PRE[PreToolUse hooks]
    PRE --&gt; RULES[Permission rules / classifier / prompt]
    RULES --&gt; CALL[tool.call]
    CALL --&gt; MAP[Bound or spill result]
    MAP --&gt; POST[PostToolUse hooks]
    POST --&gt; MSG[tool_result user message]
    MSG --&gt; LOOP[Next model turn]

</pre>

<p>복원본 <code class="language-plaintext highlighter-rouge">toolExecution.ts</code>에서 schema validation, PreToolUse, permission, actual call, PostToolUse가 이
순서로 보인다. <code class="language-plaintext highlighter-rouge">packages/claude-code-sourcemap/src/services/tools/toolExecution.ts:599</code>, <code class="language-plaintext highlighter-rouge">:795</code>,
<code class="language-plaintext highlighter-rouge">:1207</code>, <code class="language-plaintext highlighter-rouge">:1483</code> <code class="language-plaintext highlighter-rouge">C</code></p>

<h3 id="83-concurrency">8.3 concurrency</h3>

<p>공식 SDK 계약은 read-only tool을 concurrent하게 실행하고 mutation tool을 serial하게 실행한다.
custom tool은 기본 serial이며 MCP annotation <code class="language-plaintext highlighter-rouge">readOnlyHint</code>가 concurrency 판단에 사용된다.
<a href="https://code.claude.com/docs/en/agent-sdk/agent-loop">Agent loop</a> <code class="language-plaintext highlighter-rouge">B</code></p>

<p>복원본은 더 일반화된 <code class="language-plaintext highlighter-rouge">isConcurrencySafe(input)</code>을 사용해 연속된 safe call을 batch로 묶고, unsafe
call은 하나씩 실행한다. 최대 concurrency 기본값은 10이다. parse나 classifier가 실패하면 safe로
간주하지 않는다.
<code class="language-plaintext highlighter-rouge">packages/claude-code-sourcemap/src/services/tools/toolOrchestration.ts:8</code>, <code class="language-plaintext highlighter-rouge">:19</code>, <code class="language-plaintext highlighter-rouge">:86</code> <code class="language-plaintext highlighter-rouge">C</code></p>

<p>이 방식은 <code class="language-plaintext highlighter-rouge">Read A, Read B, Edit C, Read D</code>를 <code class="language-plaintext highlighter-rouge">[A,B] parallel → C serial → D</code>로 실행해 model이
낸 ordering을 과도하게 깨지 않는다. 동시에 tool result가 context를 바꾸는 경우 modifier를 call
순서대로 적용한다. 다만 같은 safe batch 안에서는 §4.3처럼 뒤의 call이 먼저 끝나 결과가 먼저
yield될 수 있으므로 전체 tool-result 순서를 보장한다는 뜻은 아니다. 이는 단순
<code class="language-plaintext highlighter-rouge">Promise.all(all calls)</code>보다 훨씬 안전하다. <code class="language-plaintext highlighter-rouge">D</code></p>

<h3 id="84-large-result와-context-pressure">8.4 large result와 context pressure</h3>

<p>현재 Bash output은 기본 30,000 characters를 session file에 보존하고 preview를 context에 보내며,
최대 150,000 characters 제한을 둔다. MCP output은 10,000 tokens부터 경고하고 기본 최대 25,000
tokens이며, oversized output은 disk에 저장할 수 있고 tool별 character limit metadata를 지원한다.
<a href="https://code.claude.com/docs/en/tools-reference">Tools reference</a>,
<a href="https://code.claude.com/docs/en/mcp">MCP</a> <code class="language-plaintext highlighter-rouge">B</code></p>

<p>이 패턴의 핵심은 결과를 단순 truncate하지 않고 <strong>durable spill + model-visible preview/path</strong>로
전환하는 것이다. context 비용과 정보 접근성을 함께 보존한다. 단, spill file의 lifecycle·민감정보
정책·remote filesystem 비용은 운영 환경이 책임져야 한다. <code class="language-plaintext highlighter-rouge">D</code></p>

<h3 id="85-readedit-semantic-guard의-한계">8.5 Read/Edit semantic guard의 한계</h3>

<p>Edit는 read-before-edit, stale content, exact/unique replacement를 검사한다. 그러나 공식 tool
reference는 임의 Bash script가 Read/Edit의 semantic rule recognition을 우회할 수 있으므로 OS
sandbox가 필요하다고 설명한다. 이것은 중요한 신뢰 경계다. <code class="language-plaintext highlighter-rouge">Edit</code> tool의 안전성이 곧 “agent가
파일을 안전하게만 바꾼다”는 뜻은 아니다. <code class="language-plaintext highlighter-rouge">B</code>, <code class="language-plaintext highlighter-rouge">D</code></p>

<h2 id="9-permission-hooks-sandbox-보안-경계">9. Permission, hooks, sandbox, 보안 경계</h2>

<h3 id="91-permission과-sandbox는-다른-층이다">9.1 permission과 sandbox는 다른 층이다</h3>

<p>Claude Code의 보안 구조는 다음 세 층을 구분한다.</p>

<ol>
  <li><strong>Permission policy</strong> — 어떤 tool/input을 allow, ask, deny할지 결정.</li>
  <li><strong>Lifecycle hook</strong> — 실행 전후에 조직 정책·검증·logging을 삽입.</li>
  <li><strong>OS sandbox</strong> — Bash와 그 child process가 실제 filesystem/network boundary를 넘지 못하게 강제.</li>
</ol>

<p>permission은 모든 tool에 적용될 수 있지만 policy decision이고, sandbox는 Bash descendants에 대한
OS-level enforcement다. Read/Edit 같은 in-process file tool은 sandbox가 아니라 permission path로
통제된다. <a href="https://code.claude.com/docs/en/permissions">Permissions</a>,
<a href="https://code.claude.com/docs/en/sandboxing">Sandboxing</a> <code class="language-plaintext highlighter-rouge">B</code></p>

<h3 id="92-rule과-mode">9.2 rule과 mode</h3>

<p>permission rule 우선순위는 <strong>deny &gt; ask &gt; allow</strong>다. managed settings가 가장 높은 configuration
scope를 가지며, explicit deny는 다른 scope의 allow를 이긴다. PreToolUse hook은 permission prompt
전에 실행되고 exit code 2로 tool을 막을 수 있으며, allow rule이 있어도 hook 차단을 우회하지 못한다.
<code class="language-plaintext highlighter-rouge">B</code></p>

<p>현재 permission mode는 다음과 같다.</p>

<table>
  <thead>
    <tr>
      <th>mode</th>
      <th>의미</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>default / Manual</td>
      <td>read 중심, 추가 action은 prompt/rule 평가</td>
    </tr>
    <tr>
      <td>acceptEdits</td>
      <td>project 내부 file edit와 일부 filesystem command 자동 승인</td>
    </tr>
    <tr>
      <td>plan</td>
      <td>read-only 조사·계획</td>
    </tr>
    <tr>
      <td>auto</td>
      <td>별도 classifier가 action 안전성을 검토해 승인 여부 결정</td>
    </tr>
    <tr>
      <td>dontAsk</td>
      <td>pre-approved action만 실행하고 나머지는 묻지 않고 거부</td>
    </tr>
    <tr>
      <td>bypassPermissions</td>
      <td>permission prompt를 우회하는 고위험 모드; explicit deny/ask 일부는 별도 계약 유지</td>
    </tr>
  </tbody>
</table>

<p><a href="https://code.claude.com/docs/en/permission-modes">Permission modes</a> <code class="language-plaintext highlighter-rouge">B</code></p>

<p>auto mode classifier는 hostile content, scope 이탈, destructive infrastructure action 등을 검토하지만
공식 문서도 안전 보장이 아니라고 명시한다. classifier가 unavailable이면 fail-open하지 않고 action을
막는다. <a href="https://code.claude.com/docs/en/errors">Errors</a> <code class="language-plaintext highlighter-rouge">B</code></p>

<h3 id="93-sandbox">9.3 sandbox</h3>

<p>native sandbox는 macOS Seatbelt, Linux/WSL2 bubblewrap와 외부 network proxy를 사용한다. 기본 write
범위는 cwd, 기본 read는 일부 deny path를 제외한 넓은 범위이며, outbound network는 allowed domain과
prompt로 통제한다. built-in proxy는 hostname을 기준으로 허용하고 TLS 내용을 검사하지 않는다.
따라서 broad domain allowlist, domain fronting, privileged Unix socket, 과도한 write path는 여전히
escape/exfiltration 위험이 있다. <a href="https://code.claude.com/docs/en/sandboxing">Sandboxing</a> <code class="language-plaintext highlighter-rouge">B</code></p>

<p>native Windows에서는 이 Bash sandbox가 동작하지 않는다. PowerShell tool도 Windows sandbox
미지원을 preview limitation으로 명시한다. 따라서 Windows에서는 permission rule이나 auto classifier를
macOS/Linux의 OS-enforced containment와 같은 보안 경계로 평가하면 안 되며, 강한 격리가 필요하면
WSL2·container·VM 같은 별도 경계가 필요하다. <code class="language-plaintext highlighter-rouge">B</code>, <code class="language-plaintext highlighter-rouge">D</code></p>

<p>특히 다음을 과장하면 안 된다.</p>

<ul>
  <li>sandbox는 기본 활성화가 아니며 <code class="language-plaintext highlighter-rouge">/sandbox</code> 또는 설정으로 켜야 한다.</li>
  <li>native Windows에는 동일한 OS sandbox가 없고 WSL2는 Linux 전제 조건을 따른다.</li>
  <li>platform support가 없을 때 <code class="language-plaintext highlighter-rouge">failIfUnavailable</code>을 쓰지 않으면 경고 후 unsandboxed fallback이 가능하다.</li>
  <li><code class="language-plaintext highlighter-rouge">dangerouslyDisableSandbox</code> retry escape hatch는 별도 승인을 받아야 하지만 존재한다.</li>
  <li>network proxy는 TLS-inspecting DLP가 아니다.</li>
  <li>Docker socket 같은 Unix socket allow는 사실상 host 권한을 열 수 있다.</li>
</ul>

<h3 id="94-trust-boundary">9.4 trust boundary</h3>

<p>project는 code만 제공하지 않는다. <code class="language-plaintext highlighter-rouge">.claude/settings.json</code>, hooks, skills, agents, <code class="language-plaintext highlighter-rouge">.mcp.json</code>이 실행
행위를 바꿀 수 있으므로 cloned repository 자체가 policy input이다. Claude Code는 first-run project와
project-scoped MCP를 trust prompt로 보호하지만, headless <code class="language-plaintext highlighter-rouge">-p</code>에는 일부 trust verification 예외가
있다. official security 문서는 untrusted content에 대해 VM 사용까지 권고한다.
<a href="https://code.claude.com/docs/en/security">Security</a> <code class="language-plaintext highlighter-rouge">B</code></p>

<p>운영 배포에서 반드시 분리해야 할 secret 경계는 다음과 같다.</p>

<ul>
  <li>model provider credential은 secret manager 또는 outbound proxy가 주입.</li>
  <li>host application의 inbound auth는 agent process 바깥 gateway가 담당.</li>
  <li>external tool credential은 가능한 agent environment에 직접 넣지 않고 egress proxy가 주입.</li>
  <li>multi-tenant host에서는 <code class="language-plaintext highlighter-rouge">settingSources: []</code>, tenant별 <code class="language-plaintext highlighter-rouge">CLAUDE_CONFIG_DIR</code>, cwd, auto-memory disable,
egress policy가 필요.</li>
</ul>

<p>이는 공식 SDK hosting 문서의 권고다. <code class="language-plaintext highlighter-rouge">B</code></p>

<h2 id="10-mcp-skills-plugins-hooks-memory의-확장-모델">10. MCP, skills, plugins, hooks, memory의 확장 모델</h2>

<p>Claude Code의 확장 체계가 강한 이유는 하나의 만능 plugin API가 아니라 <strong>서로 다른 변경 축을 서로
다른 primitive로 분리</strong>했기 때문이다.</p>

<table>
  <thead>
    <tr>
      <th>primitive</th>
      <th>바꾸는 것</th>
      <th>loading/context 비용</th>
      <th>신뢰 위험</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>CLAUDE.md/rules</td>
      <td>항상/경로별 instruction</td>
      <td>context token</td>
      <td>model adherence에 의존</td>
    </tr>
    <tr>
      <td>auto memory</td>
      <td>모델이 축적한 장기 note</td>
      <td>startup 일부 + on-demand</td>
      <td>stale/incorrect memory</td>
    </tr>
    <tr>
      <td>skill</td>
      <td>필요할 때 procedural instruction</td>
      <td>description upfront, body lazy</td>
      <td>instruction/tool permission 확대</td>
    </tr>
    <tr>
      <td>custom agent</td>
      <td>별도 context·tool/model policy</td>
      <td>child agent token/process</td>
      <td>shared FS conflict</td>
    </tr>
    <tr>
      <td>hook</td>
      <td>deterministic lifecycle action/policy</td>
      <td>process/HTTP/MCP/model call latency</td>
      <td>arbitrary code execution</td>
    </tr>
    <tr>
      <td>MCP</td>
      <td>external tool/resource/prompt</td>
      <td>schema/tool-result context + connection</td>
      <td>external auth/side effect</td>
    </tr>
    <tr>
      <td>plugin</td>
      <td>위 요소의 배포 package</td>
      <td>install/cache/reload</td>
      <td>supply chain</td>
    </tr>
    <tr>
      <td>LSP plugin</td>
      <td>semantic code intelligence</td>
      <td>server process/index</td>
      <td>language server trust/resource</td>
    </tr>
  </tbody>
</table>

<h3 id="101-mcp">10.1 MCP</h3>

<p>현재 지원 transport는 HTTP 권장, stdio local, deprecated SSE, WebSocket이다. remote HTTP/SSE는 연결
실패 시 exponential backoff로 reconnect하고 stdio server는 자동 reconnect하지 않는다. MCP
<code class="language-plaintext highlighter-rouge">list_changed</code> notification으로 tools/prompts/resources를 session 중 동적 갱신할 수 있다.
<a href="https://code.claude.com/docs/en/mcp">MCP</a> <code class="language-plaintext highlighter-rouge">B</code></p>

<p>scope는 local, project <code class="language-plaintext highlighter-rouge">.mcp.json</code>, user, managed enterprise로 나뉜다. tool name은
<code class="language-plaintext highlighter-rouge">mcp__server__tool</code>로 permission·hook·skill에서 같은 identifier를 사용한다. tool schema가 많으면
ToolSearch가 schema를 deferred loading해 startup context를 줄인다. <code class="language-plaintext highlighter-rouge">B</code></p>

<p>복원본 <code class="language-plaintext highlighter-rouge">query.ts:1659</code>에서도 turn 사이 <code class="language-plaintext highlighter-rouge">refreshTools()</code>를 호출해 새로 연결된 MCP capability를 다음
iteration에 반영한다. <code class="language-plaintext highlighter-rouge">C</code></p>

<p>이 동적 갱신은 인터랙티브 제품에 유리하지만, capability set이 turn 중 바뀔 수 있다는 뜻이다.
재현성이 중요한 실행에서는 init message의 MCP status와 실제 loaded tool version을 함께 기록해야
한다. <code class="language-plaintext highlighter-rouge">D</code></p>

<h3 id="102-skills와-memory">10.2 skills와 memory</h3>

<p>skill은 <code class="language-plaintext highlighter-rouge">SKILL.md</code> frontmatter description을 model-visible catalog에 올리고, body는 호출될 때
context에 넣는 progressive disclosure다. enterprise &gt; personal &gt; project precedence가 있으며 plugin
skill은 namespace를 가진다. project/user skill directory는 file watch로 current session에 live
반영되지만, 시작 때 없던 최상위 directory를 새로 만들면 restart가 필요하다.
<a href="https://code.claude.com/docs/en/skills">Skills</a> <code class="language-plaintext highlighter-rouge">B</code></p>

<p><code class="language-plaintext highlighter-rouge">allowed-tools</code>는 해당 skill 동안 tool permission을 미리 허용하지만 “나머지 tool을 제거하는
allowlist”는 아니다. 실제 capability restriction에는 <code class="language-plaintext highlighter-rouge">disallowed-tools</code>나 agent/tool policy가
필요하다. project skill은 workspace trust 뒤에 로딩된다. <code class="language-plaintext highlighter-rouge">B</code></p>

<p><code class="language-plaintext highlighter-rouge">context: fork</code> skill은 현재 대화 전체를 그대로 오염시키지 않고 isolated subagent에서 procedure를
실행해 summary만 되돌리는 유용한 context hygiene pattern이다. <code class="language-plaintext highlighter-rouge">B</code></p>

<h3 id="103-plugins">10.3 plugins</h3>

<p>plugin은 <code class="language-plaintext highlighter-rouge">.claude-plugin/plugin.json</code> identity와 skills, agents, hooks, MCP, LSP, monitors, binaries,
default settings를 묶어 배포한다. marketplace plugin은 local cache에 복사되며 current session에서
설치·enable·disable 후 <code class="language-plaintext highlighter-rouge">/reload-plugins</code>가 필요하다. skills의 live watch와 plugin 전체의 explicit
reload가 다르다는 점이 중요하다.
<a href="https://code.claude.com/docs/en/plugins">Plugins</a>,
<a href="https://code.claude.com/docs/en/plugins-reference">Plugins reference</a> <code class="language-plaintext highlighter-rouge">B</code></p>

<h3 id="104-hooks">10.4 hooks</h3>

<p>hook lifecycle은 Setup, SessionStart/End, InstructionsLoaded, UserPromptSubmit/Expansion,
PreToolUse, PermissionRequest, PostToolUse/Failure/Batch, PermissionDenied, Notification,
SubagentStart/Stop, TaskCreated/Completed, Stop/StopFailure, TeammateIdle, Config/Cwd/FileChanged,
WorktreeCreate/Remove, Pre/PostCompact, Elicitation/Result 등 매우 넓다.
<a href="https://code.claude.com/docs/en/hooks">Hooks</a> <code class="language-plaintext highlighter-rouge">B</code></p>

<p>handler는 command, HTTP, MCP tool, single-turn prompt, experimental agent가 될 수 있다. 같은 event에
matching되는 hook은 parallel하게 실행될 수 있으므로 서로가 순차 side effect를 보장한다고 가정하면
안 된다. Hook은 observability, policy, formatter, notification을 core loop를 fork하지 않고 붙이는
강력한 seam이다. 동시에 arbitrary command hook은 plugin/project trust surface를 키운다. <code class="language-plaintext highlighter-rouge">B</code>, <code class="language-plaintext highlighter-rouge">D</code></p>

<h2 id="11-multi-agent와-background-work">11. Multi-agent와 background work</h2>

<p>Claude Code는 “멀티에이전트”를 하나의 기능으로 뭉개지 않고 네 종류로 나눈다.</p>

<table>
  <thead>
    <tr>
      <th>방식</th>
      <th>coordinator</th>
      <th>child context</th>
      <th>통신</th>
      <th>process/FS isolation</th>
      <th>적합한 일</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>subagent</td>
      <td>현재 main session</td>
      <td>기본 fresh context; <code class="language-plaintext highlighter-rouge">fork</code>는 parent conversation 상속</td>
      <td>top-level summary를 parent에 반환</td>
      <td>같은 session/process, 기본 shared FS; worktree 선택</td>
      <td>조사/로그처럼 main context를 오염시키는 side task</td>
    </tr>
    <tr>
      <td>agent view session</td>
      <td>사용자</td>
      <td>완전 독립 session</td>
      <td>사용자에게만 상태/대화</td>
      <td>각 process, cwd/worktree 선택</td>
      <td>독립 작업 여러 개 dispatch</td>
    </tr>
    <tr>
      <td>agent team</td>
      <td>lead agent</td>
      <td>독립 full session</td>
      <td>shared task list + mailbox + peer message</td>
      <td>별도 session, FS 충돌은 설계 필요</td>
      <td>worker끼리 협력해야 하는 실험적 workflow</td>
    </tr>
    <tr>
      <td>worktree session</td>
      <td>사용자/agent</td>
      <td>독립 conversation 가능</td>
      <td>방식에 따라 다름</td>
      <td>git worktree로 file isolation</td>
      <td>같은 repo의 병렬 mutation</td>
    </tr>
  </tbody>
</table>

<p><a href="https://code.claude.com/docs/en/agents">Agents overview</a> <code class="language-plaintext highlighter-rouge">B</code></p>

<h3 id="111-subagent">11.1 subagent</h3>

<p>일반 subagent는 fresh context, own system prompt, tool/model/effort/maxTurns/permission/skills/memory/
hooks/MCP 설정을 가질 수 있다. parent history 전체를 받는 대신 task description과 project context를
받고 마지막 result를 parent에 되돌린다. 다만 <code class="language-plaintext highlighter-rouge">fork</code> subagent는 parent conversation을 상속하는
명시적 예외다.</p>

<p>v2.1.172부터 subagent도 <code class="language-plaintext highlighter-rouge">Agent</code> tool이 허용되어 있으면 nested subagent를 spawn할 수 있다. depth는
main 아래 다섯 단계로 고정되고 depth 5에는 <code class="language-plaintext highlighter-rouge">Agent</code> tool이 주어지지 않는다. 특정 subagent의
재위임을 막으려면 <code class="language-plaintext highlighter-rouge">Agent</code>를 tools에서 빼거나 deny해야 한다. fork는 다른 fork를 만들 수 없지만
일반 subagent type은 만들 수 있다. top-level subagent의 summary만 main conversation으로 돌아오므로
nested intermediate output은 main context에 직접 누적되지 않는다.
<a href="https://code.claude.com/docs/en/sub-agents">Subagents</a>,
<a href="https://code.claude.com/docs/en/sub-agents#spawn-nested-subagents">Nested subagents</a> <code class="language-plaintext highlighter-rouge">B</code></p>

<p>현재 tool reference 기준으로 2.1.198부터 subagent는 background가 기본이고 필요할 때 foreground를
쓴다. 2.1.186부터 background subagent permission prompt가 main session으로 bubble된다. 로컬 2.1.88
복원본이나 과거 문서의 “background permission은 자동 거부”를 현재 계약으로 쓰면 안 된다. <code class="language-plaintext highlighter-rouge">B</code></p>

<h3 id="112-agent-view">11.2 agent view</h3>

<p>agent view는 2.1.139부터 제공되는 research preview이며, parent conversation에 결과를 합치는
subagent와 다르다. per-user supervisor가 각각의
session process를 terminal에서 detach해 살려 두고, global machine list에서 working/needs input/ready
상태를 보여 준다. session summary를 위해 configured Haiku-class model call을 추가로 사용한다.
<a href="https://code.claude.com/docs/en/agent-view">Agent view</a> <code class="language-plaintext highlighter-rouge">B</code></p>

<p>장점은 명시적 process ownership과 attach/stop/respawn이다. 단점은 process와 token cost가 session
수에 선형으로 늘고 machine shutdown을 넘는 durable scheduler가 아니라는 것이다. <code class="language-plaintext highlighter-rouge">D</code></p>

<h3 id="113-agent-teams">11.3 agent teams</h3>

<p>agent teams는 experimental이며 기본 disabled다. lead와 independent teammate session, shared task
list, mailbox, direct message를 제공한다. teammate는 project CLAUDE/MCP/skills와 spawn prompt를 받지만
lead conversation history는 받지 않는다. task dependency가 끝나면 blocked task가 unblock된다.
<a href="https://code.claude.com/docs/en/agent-teams">Agent teams</a> <code class="language-plaintext highlighter-rouge">B</code></p>

<p>현재 제한도 크다.</p>

<ul>
  <li>in-process teammate resume/rewind 미지원</li>
  <li>task status lag와 slow shutdown 가능</li>
  <li>session당 team 하나, nested team 없음, lead 고정</li>
  <li>teammate permission은 spawn 때 정하며 peer가 consent를 relay할 수 없음</li>
  <li>in-process teammate 일부 background/subagent 제약</li>
  <li>같은 files를 건드리는 conflict는 자동 해결되지 않음</li>
</ul>

<p>따라서 “autonomous organization”이라기보다 <strong>shared task DAG와 mailbox를 가진 실험적 multi-session
orchestrator</strong>로 보는 것이 정확하다. <code class="language-plaintext highlighter-rouge">D</code></p>

<h3 id="114-scheduledgoal-autonomy">11.4 scheduled/goal autonomy</h3>

<p><code class="language-plaintext highlighter-rouge">/loop</code> scheduled task는 CLI session이 열려 있어야 하며 resume 시 유효한 task를 복원한다. cloud와
Desktop scheduler는 persistence semantics가 다르다. <code class="language-plaintext highlighter-rouge">/goal</code>은 매 turn 뒤 fresh evaluator가 goal
충족 여부를 판단하고 미충족이면 다음 turn을 시작한다.
<a href="https://code.claude.com/docs/en/scheduled-tasks">Scheduled tasks</a>,
<a href="https://code.claude.com/docs/en/goal">Goal</a> <code class="language-plaintext highlighter-rouge">B</code></p>

<p>이는 loop ownership을 “모델이 tool을 더 부르는 동안”에서 “외부 evaluator/scheduler가 새 turn을
계속 만드는 동안”까지 확장한다. 따라서 budget, cancellation, persistence가 더 중요해진다. <code class="language-plaintext highlighter-rouge">D</code></p>

<h2 id="12-uiux와-human-in-the-loop">12. UI/UX와 human-in-the-loop</h2>

<h3 id="121-공통-행동-모델-서로-다른-control-surface">12.1 공통 행동 모델, 서로 다른 control surface</h3>

<p>CLI는 full feature reference surface이고, IDE는 open file/selection/diff와 editor affordance를,
Desktop은 Chat/Cowork/Code tab, parallel sessions, integrated terminal/editor, visual diff, app preview,
PR monitor/connectors를 제공한다. Web은 cloud VM에서 long-running task를 수행하며 mobile은 thin
client 역할을 한다. local surface들은 config, project memory, MCP를 공유한다.
<a href="https://code.claude.com/docs/en/platforms">Platforms</a>,
<a href="https://code.claude.com/docs/en/desktop">Desktop</a> <code class="language-plaintext highlighter-rouge">B</code></p>

<p>아키텍처적으로 UI가 알아야 하는 runtime state는 최소 다음과 같다.</p>

<ul>
  <li>model streaming/thinking/text</li>
  <li>tool queued/running/progress/completed/failed</li>
  <li>permission requested/allowed/denied</li>
  <li>user question/elicitation</li>
  <li>subagent/background task state</li>
  <li>compact/retry/rate limit</li>
  <li>context/cost/usage</li>
  <li>file diff/checkpoint</li>
  <li>session detached/needs input/ready</li>
</ul>

<p>Claude Code의 공식 event/message 계약이 이 상태를 비교적 명시적으로 노출하므로 여러 UI surface가
일관된 agentic behavior를 제공할 수 있다. 각 surface가 실제로 같은 implementation을 감싼다는 뜻은
아니다. <code class="language-plaintext highlighter-rouge">B</code>, <code class="language-plaintext highlighter-rouge">D</code></p>

<h3 id="122-hitl의-강점">12.2 HITL의 강점</h3>

<p>Plan mode, AskUserQuestion, permission prompt, diff review, checkpoint rewind, background needs-input가
각각 다른 종류의 human decision을 나타낸다. 사용자는 “모든 tool마다 approve”와 “완전 bypass” 사이에
rule, acceptEdits, auto, sandbox라는 중간 지점을 선택할 수 있다. 이것이 approval fatigue를 줄이면서
위험을 단계화하는 핵심 UX다. <code class="language-plaintext highlighter-rouge">B</code>, <code class="language-plaintext highlighter-rouge">D</code></p>

<h3 id="123-내부-tui-결합도">12.3 내부 TUI 결합도</h3>

<p>복원 소스 규모는 다음과 같다.</p>

<table>
  <thead>
    <tr>
      <th>파일</th>
      <th style="text-align: right">line 수</th>
      <th>관찰</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">src/screens/REPL.tsx</code></td>
      <td style="text-align: right">5,005</td>
      <td>interactive orchestration과 표시 책임이 매우 큼</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">src/main.tsx</code></td>
      <td style="text-align: right">4,683</td>
      <td>CLI bootstrap/option/entry routing이 큼</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">src/utils/messages.ts</code></td>
      <td style="text-align: right">5,512</td>
      <td>message normalization과 다양한 format path 집중</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">src/services/compact/compact.ts</code></td>
      <td style="text-align: right">1,705</td>
      <td>compaction policy와 복구 context가 복잡</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">src/query.ts</code></td>
      <td style="text-align: right">1,729</td>
      <td>core loop와 recovery state 집중</td>
    </tr>
  </tbody>
</table>

<p>또한 <code class="language-plaintext highlighter-rouge">Tool</code> interface는 execution contract와 React render methods를 같이 가진다.
<code class="language-plaintext highlighter-rouge">packages/claude-code-sourcemap/src/Tool.ts:566</code> 이후 <code class="language-plaintext highlighter-rouge">C</code></p>

<p>이는 tool-specific UI를 쉽게 붙이고 exhaustiveness를 한 type에서 얻는 장점이 있지만, headless core와
TUI presentation이 완전히 분리된 hexagonal architecture는 아니다. <code class="language-plaintext highlighter-rouge">QueryEngine</code>을 SDK/headless용으로
뽑아내는 방향은 보이지만 REPL과 message pipeline에는 역사적 결합이 남아 있다. <code class="language-plaintext highlighter-rouge">D</code></p>

<h2 id="13-modelprovider-결합도">13. Model/provider 결합도</h2>

<h3 id="131-transport-선택-폭">13.1 transport 선택 폭</h3>

<p>Claude Code는 direct Anthropic API 외에 Amazon Bedrock, Google Vertex AI, Microsoft Foundry,
Claude Platform on AWS, <code class="language-plaintext highlighter-rouge">ANTHROPIC_BASE_URL</code> gateway를 지원한다. model family별 ID pinning,
provider-specific override, available-model allowlist, gateway model discovery를 제공한다.
<a href="https://code.claude.com/docs/en/model-config">Model configuration</a>,
<a href="https://code.claude.com/docs/en/amazon-bedrock">Bedrock</a>,
<a href="https://code.claude.com/docs/en/google-vertex-ai">Vertex AI</a> <code class="language-plaintext highlighter-rouge">B</code></p>

<p>provider별 version rollout이 다르기 때문에 enterprise 배포에는 Opus/Sonnet/Haiku model ID pinning을
권장한다. custom ID가 capability detection pattern과 맞지 않으면 <code class="language-plaintext highlighter-rouge">_SUPPORTED_CAPABILITIES</code>로 effort,
thinking, adaptive/interleaved thinking 등을 선언할 수 있다. <code class="language-plaintext highlighter-rouge">B</code></p>

<h3 id="132-본질적으로-provider-neutral하지-않다">13.2 본질적으로 provider-neutral하지 않다</h3>

<p>endpoint가 바뀌어도 runtime은 다음 Claude-specific semantics에 깊이 의존한다.</p>

<ul>
  <li>Anthropic Messages API의 assistant/tool_use/tool_result 구조</li>
  <li>prompt caching과 cache breakpoint/prefix 설계</li>
  <li>thinking/redacted thinking signature와 effort/model family</li>
  <li>Opus/Sonnet/Haiku 역할 및 fallback</li>
  <li>Claude-specific beta header와 output/token recovery</li>
  <li>tool schema, deferred loading, stop reason</li>
</ul>

<p>따라서 이는 “임의 OpenAI-compatible model을 갈아 끼우는 agent shell”이 아니라 <strong>여러 enterprise
호스팅 경로를 지원하는 Claude runtime</strong>이다. provider coupling은 약점이라기보다 제품 선택이다.
Claude의 model/runtime 공동 최적화를 얻는 대신 model portability를 포기한다. <code class="language-plaintext highlighter-rouge">C</code>, <code class="language-plaintext highlighter-rouge">D</code></p>

<h3 id="133-surface별-provider-차이">13.3 surface별 provider 차이</h3>

<p>공식 platform 문서 기준 enterprise Desktop은 Vertex와 gateway provider를 지원하지만 Bedrock/Foundry는
CLI 또는 VS Code를 사용해야 한다. 즉 core runtime의 provider 지원과 모든 UI surface의 provider
지원은 동일하지 않다. <a href="https://code.claude.com/docs/en/platforms">Platforms</a> <code class="language-plaintext highlighter-rouge">B</code></p>

<h2 id="14-실패-경계-취소-retry-observability">14. 실패 경계, 취소, retry, observability</h2>

<h3 id="141-api-retry와-timeout">14.1 API retry와 timeout</h3>

<p>현재 Claude Code는 server error, overload, request timeout, temporary 429, dropped connection을 기본
최대 10회 exponential backoff로 retry한다. <code class="language-plaintext highlighter-rouge">CLAUDE_CODE_MAX_RETRIES</code> 기본값은 10,
<code class="language-plaintext highlighter-rouge">API_TIMEOUT_MS</code> 기본값은 600,000ms다. 최악 시간은 각 retry마다 timeout window를 새로 가지므로
매우 길 수 있다. <a href="https://code.claude.com/docs/en/errors">Errors</a>,
<a href="https://code.claude.com/docs/en/agent-sdk/typescript">TypeScript SDK</a> <code class="language-plaintext highlighter-rouge">B</code></p>

<p>background subagent에는 stream event가 없을 때 abort하는 stall watchdog이 있지만 total-runtime
deadline은 아니다. top-level session도 자동 timeout이 없으므로 <code class="language-plaintext highlighter-rouge">maxTurns</code>, budget, application
deadline을 별도로 설계해야 한다. <code class="language-plaintext highlighter-rouge">B</code></p>

<h3 id="142-cancellation-surface">14.2 cancellation surface</h3>

<p>TypeScript SDK는 <code class="language-plaintext highlighter-rouge">AbortController</code>, streaming query의 <code class="language-plaintext highlighter-rouge">interrupt()</code>, <code class="language-plaintext highlighter-rouge">stopTask()</code>, <code class="language-plaintext highlighter-rouge">close()</code>를
제공한다. <code class="language-plaintext highlighter-rouge">interrupt()</code>는 현재 turn을 끊어도 session stream 자체를 끝내는 것과 같지 않으며 이후
새 prompt를 보낼 수 있다. Python 문서는 interrupt 직후 새 query를 보낸 경우 이전 interrupted
response를 먼저 drain해야 하는 ordering 주의사항을 명시한다.
<a href="https://code.claude.com/docs/en/agent-sdk/typescript">TypeScript SDK</a>,
<a href="https://code.claude.com/docs/en/agent-sdk/python">Python SDK</a> <code class="language-plaintext highlighter-rouge">B</code></p>

<p>복원본 tool contract는 새 user message가 들어올 때 tool별로 <code class="language-plaintext highlighter-rouge">cancel</code> 또는 <code class="language-plaintext highlighter-rouge">block</code>을 선택하게 한다.
기본은 block이다. 이는 “abort signal 하나가 모든 side effect를 원자적으로 취소한다”는 환상을 피한다.
이미 외부 system에 적용된 MCP/Bash side effect는 abort로 rollback되지 않는다.
<code class="language-plaintext highlighter-rouge">packages/claude-code-sourcemap/src/Tool.ts:407</code> <code class="language-plaintext highlighter-rouge">C</code>, <code class="language-plaintext highlighter-rouge">D</code></p>

<h3 id="143-failure-taxonomy">14.3 failure taxonomy</h3>

<table>
  <thead>
    <tr>
      <th>경계</th>
      <th>동작</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>model transient failure</td>
      <td>내부 retry/backoff 후 terminal API error</td>
    </tr>
    <tr>
      <td>max turns/budget</td>
      <td>명시적 result subtype, session ID/usage로 resume 가능</td>
    </tr>
    <tr>
      <td>prompt too long</td>
      <td>자동 compact/recovery 후 실패 표면화</td>
    </tr>
    <tr>
      <td>tool validation/permission failure</td>
      <td>error tool result로 model에 되돌려 self-correction 가능</td>
    </tr>
    <tr>
      <td>tool runtime failure</td>
      <td>PostToolUseFailure/stream event, 다음 loop 판단</td>
    </tr>
    <tr>
      <td>MCP connection failure</td>
      <td>init status, remote reconnect/backoff, stdio는 수동</td>
    </tr>
    <tr>
      <td>external SessionStore failure</td>
      <td>local query 계속, <code class="language-plaintext highlighter-rouge">mirror_error</code>; durability degradation</td>
    </tr>
    <tr>
      <td>process kill</td>
      <td>선기록된 user message와 JSONL로 resume 가능, 외부 side effect는 별도</td>
    </tr>
    <tr>
      <td>background process stall</td>
      <td>background subagent watchdog; top-level total deadline 아님</td>
    </tr>
  </tbody>
</table>

<p>좋은 점은 failure를 UI string 하나로 뭉개지 않고 stream protocol/result subtype/hook event로 노출한다는
것이다. 약점은 외부 side effect와 transcript commit 사이에 distributed transaction이 없다는 모든
agent system 공통의 한계다. idempotency key나 compensating action은 tool provider가 설계해야 한다.
<code class="language-plaintext highlighter-rouge">D</code></p>

<h3 id="144-observability">14.4 observability</h3>

<p>Claude Code는 opt-in OpenTelemetry로 metrics, log/events, beta trace를 export한다. session/model/version,
API latency·token·cost, tool decision/execution, permission escalation, hook, auth, MCP connection, plugin
install 등을 관찰할 수 있다. trace는 host app의 <code class="language-plaintext highlighter-rouge">TRACEPARENT</code>/<code class="language-plaintext highlighter-rouge">TRACESTATE</code>를 CLI child에 전달해
parent-child span을 구성할 수 있다. prompt와 tool input은 기본 redacted이고 세부 logging은 별도
flag가 필요하다.
<a href="https://code.claude.com/docs/en/monitoring-usage">Monitoring</a>,
<a href="https://code.claude.com/docs/en/agent-sdk/observability">SDK observability</a> <code class="language-plaintext highlighter-rouge">B</code></p>

<p>API retry attempt도 span event에 남고 terminal <code class="language-plaintext highlighter-rouge">api_error</code>의 attempt count로 retry exhaustion을
판별할 수 있다. 그러나 anomaly detection, cross-session correlation, alert는 backend/SIEM 책임이다.
<code class="language-plaintext highlighter-rouge">B</code></p>

<h2 id="15-데이터telemetryprivacy">15. 데이터·telemetry·privacy</h2>

<p>local Claude Code라도 prompt와 model output은 TLS로 provider에 전송된다. commercial Team/Enterprise,
API, third-party platform 데이터는 opt-in model improvement를 제외하고 generative model training에
사용하지 않는다는 현재 정책이며, consumer plan은 사용자 설정에 따라 다르다. <code class="language-plaintext highlighter-rouge">/feedback</code>은 선택한
conversation history와 code를 별도 전송할 수 있다.
<a href="https://code.claude.com/docs/en/data-usage">Data usage</a> <code class="language-plaintext highlighter-rouge">B</code></p>

<p>operational telemetry는 code나 file path를 포함하지 않는다고 문서화되어 있고
<code class="language-plaintext highlighter-rouge">DISABLE_TELEMETRY</code>, error logging은 <code class="language-plaintext highlighter-rouge">DISABLE_ERROR_REPORTING</code>으로 끌 수 있다. third-party provider에서는
기본값이 다르며 host-managed provider flag가 metrics 기본값을 바꿀 수 있다. WebFetch는 provider와
무관하게 hostname을 Anthropic safety check에 보낼 수 있고 별도 설정으로 끈다. <code class="language-plaintext highlighter-rouge">B</code></p>

<p>cloud execution은 isolated VM에 repository를 clone하고 GitHub credential을 VM에 직접 넣지 않는
proxy를 사용하며 outbound traffic도 proxy를 통과한다. 이는 local sandbox와 다른 managed trust
boundary다. <a href="https://code.claude.com/docs/en/claude-code-on-the-web">Claude Code on the web</a>,
<a href="https://code.claude.com/docs/en/security">Security</a> <code class="language-plaintext highlighter-rouge">B</code></p>

<h2 id="16-성능확장성nfs10000개-관점">16. 성능·확장성·NFS·10,000개 관점</h2>

<h3 id="161-latency를-줄이는-잘된-선택">16.1 latency를 줄이는 잘된 선택</h3>

<p>Claude Code는 token과 wall-clock latency를 동시에 다룬다.</p>

<ul>
  <li>streaming response와 streaming tool start <code class="language-plaintext highlighter-rouge">C</code></li>
  <li>concurrency-safe read batch, mutation serial <code class="language-plaintext highlighter-rouge">B/C</code></li>
  <li>stable prompt prefix와 provider prompt cache <code class="language-plaintext highlighter-rouge">B</code></li>
  <li>MCP/tool schema deferred loading via ToolSearch <code class="language-plaintext highlighter-rouge">B</code></li>
  <li>large output disk spill + preview <code class="language-plaintext highlighter-rouge">B</code></li>
  <li>proactive/automatic compaction <code class="language-plaintext highlighter-rouge">B</code></li>
  <li>SDK <code class="language-plaintext highlighter-rouge">startup()</code>으로 subprocess prewarm <code class="language-plaintext highlighter-rouge">B</code></li>
  <li>resume picker는 stat-only lite list 후 최근 50개만 head/tail metadata enrich <code class="language-plaintext highlighter-rouge">C</code></li>
  <li>large fork-heavy JSONL에서 dead branch를 byte level로 prefilter <code class="language-plaintext highlighter-rouge">C</code></li>
</ul>

<p>복원본은 fork-heavy session에서 41MB file parse 56.0ms→3.9ms, 151MB file 47.3ms→9.4ms라는 내부
측정치를 주석에 기록한다. 이는 공식 benchmark가 아니라 해당 구현자의 관찰이며 환경 재현 없이
일반화하면 안 된다. <code class="language-plaintext highlighter-rouge">packages/claude-code-sourcemap/src/utils/sessionStorage.ts:3226</code> <code class="language-plaintext highlighter-rouge">C</code></p>

<h3 id="162-한-session이-길어질-때">16.2 한 session이 길어질 때</h3>

<p>append-only JSONL과 in-memory message state는 장기 session에서 증가한다. compaction은 model context를
줄이지만 raw file과 process memory의 모든 비용을 자동으로 0으로 만들지 않는다. 공식 SDK hosting
문서도 long session의 memory growth를 limitation으로 두고 process recycle 또는 session cap을
권고한다. <code class="language-plaintext highlighter-rouge">B</code></p>

<p>복원본은 다음을 통해 비용을 줄인다.</p>

<ul>
  <li>current session UUID set memoization과 resume load 재사용</li>
  <li>incremental transcript dedup, parent hint로 반복 O(n) scan 회피</li>
  <li>full file read 한 번으로 metadata/message map 구성</li>
  <li>parent→children index를 한 번 만들어 leaf lookup O(1)</li>
  <li>lite metadata는 file의 head/tail 약 64KB만 읽기</li>
</ul>

<p>그러나 특정 session full resume는 결국 그 JSONL의 active chain을 구성하는 O(file size/messages)
작업이고, raw file이 multi-GB가 될 가능성 자체는 남는다. <code class="language-plaintext highlighter-rouge">C</code>, <code class="language-plaintext highlighter-rouge">D</code></p>

<h3 id="163-세션-n10000">16.3 세션 N=10,000</h3>

<p>복원본의 resume picker는 모든 session JSONL을 내용까지 읽지는 않는다. 먼저 directory를 <code class="language-plaintext highlighter-rouge">readdir</code>,
모든 candidate를 <code class="language-plaintext highlighter-rouge">stat</code>, mtime으로 정렬한 뒤 최근 항목만 enrich한다.</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">getSessionFilesWithMtime</code>: <code class="language-plaintext highlighter-rouge">packages/claude-code-sourcemap/src/utils/sessionStorage.ts:4526</code></li>
  <li>initial enrich 50개: <code class="language-plaintext highlighter-rouge">packages/claude-code-sourcemap/src/utils/sessionStorage.ts:4571</code></li>
  <li>stat-only lite log: <code class="language-plaintext highlighter-rouge">packages/claude-code-sourcemap/src/utils/sessionStorage.ts:4970</code></li>
</ul>

<p>이것은 local SSD에서 매우 실용적이다. 하지만 N=10,000에서는 초기 list마다 적어도 O(N) dirent,
O(N) stat, O(N log N) sort를 수행하고 <code class="language-plaintext highlighter-rouge">Promise.all</code>로 모든 stat을 동시에 만든다. 반복 UI path가
10,000개에 대해 O(1) 또는 visible-only여야 한다는 Farad 기준에는 맞지 않는다. <code class="language-plaintext highlighter-rouge">D</code></p>

<h3 id="164-nfshpc-판정">16.4 NFS/HPC 판정</h3>

<p>Claude Code의 기본 persistence는 사용자 home 아래 작은/큰 JSONL 여러 개와 memory/config/plugin
파일을 사용한다. local filesystem에서는 append, stat, head/tail read가 싸지만 NFS에서는 다음 비용이
증폭된다.</p>

<ul>
  <li>resume picker의 session별 stat round trip</li>
  <li>recent 50개 각각의 head/tail content read</li>
  <li>transcript append/flush와 metadata append</li>
  <li>project/skill/plugin/MCP directory discovery</li>
  <li>subagent별 JSONL과 metadata file</li>
  <li>background supervisor roster/job state file</li>
</ul>

<p>공식 문서는 SDK container local disk를 전제로 설명하고 NFS 최적화 계약을 제공하지 않는다. 복원본에도
session 목록 전체를 대표하는 단일 persistent derived index와 <code class="language-plaintext highlighter-rouge">(mtime,size)</code> delta reconcile 계약은
확인되지 않는다. 따라서 shared HPC home/NFS에서는 <code class="language-plaintext highlighter-rouge">CLAUDE_CONFIG_DIR</code>를 node-local SSD로 두고 필요한
transcript만 external store에 미러하거나, session index를 별도 service로 관리하는 편이 낫다. 다만
external SessionStore는 memory와 working tree를 보존하지 않으므로 그것만으로 cross-node resume가
완성되지는 않는다. <code class="language-plaintext highlighter-rouge">B</code>, <code class="language-plaintext highlighter-rouge">C</code>, <code class="language-plaintext highlighter-rouge">D</code></p>

<h3 id="165-process-scale">16.5 process scale</h3>

<p>공식 SDK 권장 시작점은 agent instance당 1GiB RAM, 5GiB disk, 1 CPU이며 1GiB는 ceiling이 아니라
floor라고 명시한다. 동시에 실행 중인 SDK session 하나가 subprocess 하나이므로 host capacity는
대략 다음 식으로 제한된다.</p>

<div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>agents_per_host = (host_RAM - host_overhead) / measured_per_session_peak_RSS
</code></pre></div></div>

<p>장기 session pool은 session ID로 consistent hashing해 같은 container/process로 route하거나, idle 때
내렸다가 SessionStore와 working state를 hydrate해야 한다. wide subagent fanout은 process뿐 아니라 API
rate limit과 token 비용도 곱한다. <a href="https://code.claude.com/docs/en/agent-sdk/hosting">Agent SDK hosting</a> <code class="language-plaintext highlighter-rouge">B</code></p>

<h2 id="17-내부-모듈-경계테스트-가능성변경-용이성">17. 내부 모듈 경계·테스트 가능성·변경 용이성</h2>

<h3 id="171-좋은-경계">17.1 좋은 경계</h3>

<p>복원본에서 확인되는 좋은 구조는 다음과 같다.</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">QueryEngine</code>이 headless/SDK conversation lifecycle을 별도 class로 소유한다.</li>
  <li><code class="language-plaintext highlighter-rouge">queryLoop</code>가 immutable config와 iteration state를 구분한다.</li>
  <li>async generator protocol로 core와 caller의 backpressure/streaming을 연결한다.</li>
  <li>tool execution이 validate → hook → permission → call → result 단계를 가진다.</li>
  <li>concurrency와 context modifier ordering을 executor가 중앙 관리한다.</li>
  <li>session storage가 append path와 resume/read optimization을 명시적으로 다룬다.</li>
  <li><code class="language-plaintext highlighter-rouge">QueryDeps</code> injection으로 model/compaction path를 fake할 seam을 만들었다.</li>
  <li>hook/MCP/plugin으로 core fork 없이 동작을 확장한다.</li>
</ul>

<h3 id="172-약한-경계">17.2 약한 경계</h3>

<p>반면 <code class="language-plaintext highlighter-rouge">bootstrap/state.ts</code>에는 process-global mutable <code class="language-plaintext highlighter-rouge">STATE</code>가 있고 파일 자체가 “DO NOT ADD MORE
STATE HERE”라고 경고한다. session ID, cwd, cost, model usage, telemetry, plugin, cached CLAUDE.md,
feature latch 등 폭넓은 상태를 가진다. session ID와 project directory를 atomic하게 바꾸는
<code class="language-plaintext highlighter-rouge">switchSession()</code>은 drift bug를 줄이는 좋은 보완이지만, 전역 singleton 자체는 multi-session
in-process execution과 unit isolation을 어렵게 한다.
<code class="language-plaintext highlighter-rouge">packages/claude-code-sourcemap/src/bootstrap/state.ts:31</code>, <code class="language-plaintext highlighter-rouge">:428</code>, <code class="language-plaintext highlighter-rouge">:456</code> <code class="language-plaintext highlighter-rouge">C</code></p>

<p>또한 다음 결합이 보인다.</p>

<ul>
  <li>core Tool type에 React renderer가 포함됨</li>
  <li>5,000-line REPL과 5,512-line messages pipeline</li>
  <li>query loop가 feature gates, settings, queue, telemetry, global state를 직접 참조</li>
  <li>JSONL schema와 resume optimization이 serialization key order 같은 low-level invariant에 의존</li>
</ul>

<p>이것은 빠르게 진화하는 단일 제품에서는 이해할 수 있는 trade-off다. 동일 팀이 UI, tool, session을
함께 바꾸면 end-to-end 최적화가 쉽다. 하지만 독립 embeddable engine, 여러 UI toolkit, 동일 process의
다중 session으로 확장할수록 비용이 커진다. Agent SDK가 session마다 child process를 쓰는 구조는 이
전역 상태 문제를 process isolation으로 우회하는 효과도 있다. <code class="language-plaintext highlighter-rouge">D</code></p>

<h3 id="173-테스트에-관해-말할-수-있는-것과-없는-것">17.3 테스트에 관해 말할 수 있는 것과 없는 것</h3>

<p>복원 npm sourcemap에는 test files가 포함되어 있지 않아 공식 internal test coverage를 평가할 수 없다.
다만 source comment에는 테스트 fake를 위한 <code class="language-plaintext highlighter-rouge">QueryDeps</code>, transcript render fidelity test, large-session
측정, chain invariant 검증 흔적이 있다. 공식 SDK는 SessionStore conformance suite와 structured result
type을 제공한다. <code class="language-plaintext highlighter-rouge">B/C</code></p>

<p>따라서 “테스트가 없다”는 결론은 근거가 없고, 더 정확한 결론은 다음이다.</p>

<ul>
  <li>public contract-level SDK testing seam은 강하다.</li>
  <li>복원본의 일부 core seam은 개선 중이다.</li>
  <li>process-global state와 UI/core 혼합은 isolated test 비용을 키울 가능성이 높다.</li>
  <li>proprietary current implementation의 실제 regression suite 품질은 확인 불가다. <code class="language-plaintext highlighter-rouge">D</code></li>
</ul>

<h2 id="18-구조적-강점">18. 구조적 강점</h2>

<p>이 절은 앞선 <code class="language-plaintext highlighter-rouge">B/C</code> 근거를 종합한 <code class="language-plaintext highlighter-rouge">D</code> 등급의 구조 평가다.</p>

<ol>
  <li>
    <p><strong>단일 agent loop의 제품화</strong><br />
model/tool/permission/session이 각각 흩어진 library가 아니라 하나의 실행 계약을 이룬다.</p>
  </li>
  <li>
    <p><strong>로컬 권위가 명확한 interactive architecture</strong><br />
CLI process가 shell/cwd/transcript를 소유해 개인 개발자 workflow가 예측 가능하다.</p>
  </li>
  <li>
    <p><strong>progressive disclosure의 일관성</strong><br />
ToolSearch, skills, nested memory, large-output spill이 context를 무제한 선적재하지 않는다.</p>
  </li>
  <li>
    <p><strong>다층 확장 모델</strong><br />
instruction은 memory/skill, deterministic policy는 hook/permission, external action은 MCP, 배포는
plugin으로 구분한다.</p>
  </li>
  <li>
    <p><strong>실패를 state로 모델링</strong><br />
retry, compact, max-output recovery, max turns/budget, mirror degradation, MCP status를 구분한다.</p>
  </li>
  <li>
    <p><strong>병렬성의 보수적 규칙</strong><br />
read/safe batch만 parallel, mutation은 serial, worktree가 실제 file isolation을 담당한다.</p>
  </li>
  <li>
    <p><strong>HITL이 runtime에 내장</strong><br />
permission, plan, question, diff, rewind, background needs-input가 같은 event 흐름에 있다.</p>
  </li>
  <li>
    <p><strong>여러 표면의 일관된 agentic behavior와 SDK의 확인된 CLI 재사용</strong><br />
Agent SDK는 CLI subprocess를 재사용해 semantics 재구현을 줄인다. 다른 surface의 내부 code reuse는
공개 근거가 없으므로 behavioral parity까지만 평가한다.</p>
  </li>
</ol>

<h2 id="19-구조적-약점과-리스크">19. 구조적 약점과 리스크</h2>

<p>이 절도 현재 공개 계약과 2.1.88 관찰을 종합한 <code class="language-plaintext highlighter-rouge">D</code> 등급이며, current proprietary 내부 구조를
확정하지 않는다.</p>

<ol>
  <li>
    <p><strong>동시 active SDK session당 process 구조의 비용</strong><br />
isolation은 좋지만 live session 수에 따라 memory·startup·process tree·routing 비용이 선형
증가한다. persisted session 수와는 별개다.</p>
  </li>
  <li>
    <p><strong>local JSONL과 filesystem affinity</strong><br />
interactive resume에는 좋지만 cross-host, NFS, 10,000 session 목록에는 별도 index/state plane이
필요하다.</p>
  </li>
  <li>
    <p><strong>conversation과 world state의 비원자성</strong><br />
transcript, file checkpoint, git, external MCP side effect가 하나의 transaction이 아니다.</p>
  </li>
  <li>
    <p><strong>sandbox opt-in·native Windows 부재·범위 한계</strong><br />
permission prompt만으로 arbitrary Bash/script의 실제 행동을 강제할 수 없고 native Windows에서는
macOS/Linux와 같은 OS sandbox 경계가 없다.</p>
  </li>
  <li>
    <p><strong>Claude-specific provider coupling</strong><br />
호스팅 provider는 여러 개지만 model/runtime protocol은 portable하지 않다.</p>
  </li>
  <li>
    <p><strong>확장 surface의 공급망 위험</strong><br />
project settings, hook, skill, plugin, MCP가 모두 실행/context surface를 넓힌다.</p>
  </li>
  <li>
    <p><strong>전역 상태와 역사적 TUI 결합</strong><br />
process isolation으로 문제를 감추지만 core-as-library 관점에서는 composition이 어렵다.</p>
  </li>
  <li>
    <p><strong>compaction의 의미 손실</strong><br />
raw transcript가 남아도 active model context는 summary이며 nested instruction/old skill이 탈락할 수
있다.</p>
  </li>
  <li>
    <p><strong>parallel agent의 conflict 책임</strong><br />
subagent/team/session 수를 늘리는 것은 쉽지만 shared file과 external resource conflict는 자동으로
해결되지 않는다.</p>
  </li>
  <li>
    <p><strong>observability는 제공하되 운영 완성은 사용자 책임</strong><br />
OTEL event는 풍부하지만 alert, SIEM correlation, idempotency, recovery runbook은 별도다.</p>
  </li>
</ol>

<h2 id="20-farad-비교에-넘길-핵심-판정">20. Farad 비교에 넘길 핵심 판정</h2>

<p>이 절은 Farad 구현 지시가 아니라 교차 비교 보고서가 사용할 논점이다.</p>

<h3 id="201-배워야-할-것">20.1 배워야 할 것</h3>

<ul>
  <li><strong>agent loop event protocol을 UI보다 먼저 정의</strong>해야 한다. Claude Code가 여러 표면을 유지하는 힘은
streaming message/result/event 계약에서 나온다.</li>
  <li><strong>tool pipeline을 validate → policy hook → permission → execute → post hook → bounded result로
고정</strong>하는 것이 좋다.</li>
  <li><strong>context와 transcript를 분리</strong>하고 compaction boundary가 무엇을 잃고 무엇을 재주입하는지 계약으로
써야 한다.</li>
  <li><strong>large output은 truncate보다 spill + preview + path</strong>가 낫다.</li>
  <li><strong>read parallel / mutation serial을 기본값</strong>으로 하고 tool별 safe declaration은 fail-closed여야 한다.</li>
  <li><strong>session resume와 file rollback을 다른 기능</strong>으로 표현해야 한다.</li>
  <li><strong>MCP/skills/plugins/hooks를 하나의 registry abstraction으로 평탄화하지 말고</strong> discovery, prompt
exposure, execution, policy, distribution 책임을 분리해야 한다.</li>
  <li><strong>failure subtype과 terminal reason을 UI/event에 보존</strong>해야 한다.</li>
  <li><strong>background work는 process/task owner, needs-input, attach/stop/resume 상태를 명시</strong>해야 한다.</li>
</ul>

<h3 id="202-그대로-모방하면-안-되는-것">20.2 그대로 모방하면 안 되는 것</h3>

<ul>
  <li>Farad desktop runtime에서 session마다 1GiB급 child CLI process를 만드는 방식.</li>
  <li>NFS home에서 session file 전량 stat 후 최근 것만 읽는 목록 구조.</li>
  <li>UI render method를 core Tool protocol에 포함하는 결합.</li>
  <li>process-global mutable state로 여러 session을 구분하는 방식.</li>
  <li>turn 사이 동적 refresh를 “항상 최신”이라는 이유로 무제한 수행하는 방식. Farad의 explicit refresh와
cache invalidation 계약을 우선해야 한다.</li>
  <li>proprietary Claude-specific message semantics를 hvppyai의 provider-neutral public SDK에 누출하는 것.</li>
  <li>transcript mirror를 canonical database처럼 오해하는 것.</li>
</ul>

<h3 id="203-farad가-의도적으로-더-잘할-수-있는-지점">20.3 Farad가 의도적으로 더 잘할 수 있는 지점</h3>

<table>
  <thead>
    <tr>
      <th>지점</th>
      <th>Claude Code</th>
      <th>Farad가 가져갈 방향</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>runtime/API 경계</td>
      <td>SDK가 CLI subprocess를 감쌈</td>
      <td>hvppyai는 HTTP/types, Farad app이 loop 소유라는 현재 경계 유지</td>
    </tr>
    <tr>
      <td>multi-session</td>
      <td>process isolation 중심</td>
      <td>application-owned session actor/task와 Qt thread authority 명시</td>
    </tr>
    <tr>
      <td>session list</td>
      <td>file-per-session stat scan</td>
      <td>derived index + bounded lazy enrichment + lookup hint</td>
    </tr>
    <tr>
      <td>refresh</td>
      <td>일부 live watch/dynamic turn refresh</td>
      <td>explicit refresh와 국소 cache invalidation 유지</td>
    </tr>
    <tr>
      <td>UI/core</td>
      <td>Tool type에 TUI render 책임 일부 혼합</td>
      <td>UI projection/factory와 core event를 분리</td>
    </tr>
    <tr>
      <td>provider</td>
      <td>Claude semantics에 최적화</td>
      <td>provider capability adapter를 app 계층에 두고 SDK는 중립 유지</td>
    </tr>
    <tr>
      <td>NFS</td>
      <td>local disk 전제</td>
      <td>content read 최소화, mtime/size delta, node-local/cache 전략</td>
    </tr>
    <tr>
      <td>transcript</td>
      <td>강한 append chain이나 local file affinity</td>
      <td>append-only 재현성은 배우되 10k/NFS index 계약 추가</td>
    </tr>
  </tbody>
</table>

<h2 id="21-반증-가능성미확인-영역">21. 반증 가능성·미확인 영역</h2>

<p>다음 내용은 확정하지 않았다.</p>

<ul>
  <li>2.1.207 내부 <code class="language-plaintext highlighter-rouge">QueryEngine</code>, file schema, class name이 2.1.88과 같은지 여부</li>
  <li>current Desktop/IDE의 실제 process/IPC topology와 UI state store</li>
  <li>Anthropic cloud agent scheduler, VM orchestration, backend transcript database 구현</li>
  <li>current proprietary source의 module size와 test coverage</li>
  <li>feature-gated local source code가 어떤 account/build에서 활성화되는지</li>
  <li>prompt compaction summary model/prompt의 비공개 세부와 품질 보장</li>
  <li>auto permission classifier의 내부 model/policy implementation</li>
  <li>local JSONL format이 장기 호환 public schema인지 여부</li>
</ul>

<p>이 항목을 교차 비교에서 동일한 사실처럼 사용하면 안 된다. 제품 계약은 공식 문서에서 확인 가능한
행동까지만 비교해야 한다.</p>

<h2 id="22-증거-인덱스">22. 증거 인덱스</h2>

<h3 id="221-공식-1차-문서-b">22.1 공식 1차 문서 <code class="language-plaintext highlighter-rouge">B</code></h3>

<ul>
  <li><a href="https://code.claude.com/docs/llms.txt">Documentation index</a></li>
  <li><a href="https://code.claude.com/docs/en/how-claude-code-works">How Claude Code works</a></li>
  <li><a href="https://code.claude.com/docs/en/platforms">Platforms</a></li>
  <li><a href="https://code.claude.com/docs/en/remote-control">Remote Control</a></li>
  <li><a href="https://code.claude.com/docs/en/desktop">Desktop</a></li>
  <li><a href="https://code.claude.com/docs/en/agent-sdk/agent-loop">Agent loop</a></li>
  <li><a href="https://code.claude.com/docs/en/agent-sdk/hosting">Agent SDK hosting</a></li>
  <li><a href="https://code.claude.com/docs/en/agent-sdk/typescript">Agent SDK TypeScript reference</a></li>
  <li><a href="https://code.claude.com/docs/en/agent-sdk/python">Agent SDK Python reference</a></li>
  <li><a href="https://code.claude.com/docs/en/agent-sdk/sessions">SDK sessions</a></li>
  <li><a href="https://code.claude.com/docs/en/agent-sdk/session-storage">External session storage</a></li>
  <li><a href="https://code.claude.com/docs/en/sessions">CLI sessions</a></li>
  <li><a href="https://code.claude.com/docs/en/configuration">Settings</a></li>
  <li><a href="https://code.claude.com/docs/en/prompt-caching">Prompt caching</a></li>
  <li><a href="https://code.claude.com/docs/en/context-window">Context window</a></li>
  <li><a href="https://code.claude.com/docs/en/checkpointing">Checkpointing</a></li>
  <li><a href="https://code.claude.com/docs/en/tools-reference">Tools reference</a></li>
  <li><a href="https://code.claude.com/docs/en/permissions">Permissions</a></li>
  <li><a href="https://code.claude.com/docs/en/permission-modes">Permission modes</a></li>
  <li><a href="https://code.claude.com/docs/en/sandboxing">Sandboxing</a></li>
  <li><a href="https://code.claude.com/docs/en/security">Security</a></li>
  <li><a href="https://code.claude.com/docs/en/mcp">MCP</a></li>
  <li><a href="https://code.claude.com/docs/en/skills">Skills</a></li>
  <li><a href="https://code.claude.com/docs/en/memory">Memory</a></li>
  <li><a href="https://code.claude.com/docs/en/plugins">Plugins</a></li>
  <li><a href="https://code.claude.com/docs/en/plugins-reference">Plugins reference</a></li>
  <li><a href="https://code.claude.com/docs/en/hooks">Hooks</a></li>
  <li><a href="https://code.claude.com/docs/en/agents">Agents overview</a></li>
  <li><a href="https://code.claude.com/docs/en/sub-agents">Subagents</a></li>
  <li><a href="https://code.claude.com/docs/en/agent-view">Agent view</a></li>
  <li><a href="https://code.claude.com/docs/en/agent-teams">Agent teams</a></li>
  <li><a href="https://code.claude.com/docs/en/scheduled-tasks">Scheduled tasks</a></li>
  <li><a href="https://code.claude.com/docs/en/goal">Goal</a></li>
  <li><a href="https://code.claude.com/docs/en/model-config">Model configuration</a></li>
  <li><a href="https://code.claude.com/docs/en/amazon-bedrock">Amazon Bedrock</a></li>
  <li><a href="https://code.claude.com/docs/en/google-vertex-ai">Google Vertex AI</a></li>
  <li><a href="https://code.claude.com/docs/en/errors">Errors</a></li>
  <li><a href="https://code.claude.com/docs/en/monitoring-usage">Monitoring</a></li>
  <li><a href="https://code.claude.com/docs/en/agent-sdk/observability">SDK observability</a></li>
  <li><a href="https://code.claude.com/docs/en/data-usage">Data usage</a></li>
  <li><a href="https://code.claude.com/docs/en/claude-code-on-the-web">Claude Code on the web</a></li>
  <li><a href="https://github.com/anthropics/claude-code">Official repository</a></li>
</ul>

<h3 id="222-로컬-복원-관찰-c">22.2 로컬 복원 관찰 <code class="language-plaintext highlighter-rouge">C</code></h3>

<table>
  <thead>
    <tr>
      <th>관찰</th>
      <th>경로</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>source/version provenance</td>
      <td><code class="language-plaintext highlighter-rouge">packages/claude-code-sourcemap/CLAUDE_CODE_SOURCEMAP.md</code></td>
    </tr>
    <tr>
      <td>conversation owner</td>
      <td><code class="language-plaintext highlighter-rouge">packages/claude-code-sourcemap/src/QueryEngine.ts:175</code></td>
    </tr>
    <tr>
      <td>agent loop state machine</td>
      <td><code class="language-plaintext highlighter-rouge">packages/claude-code-sourcemap/src/query.ts:241</code></td>
    </tr>
    <tr>
      <td>test dependency seam</td>
      <td><code class="language-plaintext highlighter-rouge">packages/claude-code-sourcemap/src/query/deps.ts:1</code></td>
    </tr>
    <tr>
      <td>tool contract</td>
      <td><code class="language-plaintext highlighter-rouge">packages/claude-code-sourcemap/src/Tool.ts:362</code></td>
    </tr>
    <tr>
      <td>tool orchestration</td>
      <td><code class="language-plaintext highlighter-rouge">packages/claude-code-sourcemap/src/services/tools/toolOrchestration.ts:19</code></td>
    </tr>
    <tr>
      <td>streamed tool execution</td>
      <td><code class="language-plaintext highlighter-rouge">packages/claude-code-sourcemap/src/services/tools/StreamingToolExecutor.ts:35</code></td>
    </tr>
    <tr>
      <td>tool pipeline</td>
      <td><code class="language-plaintext highlighter-rouge">packages/claude-code-sourcemap/src/services/tools/toolExecution.ts:599</code></td>
    </tr>
    <tr>
      <td>compaction</td>
      <td><code class="language-plaintext highlighter-rouge">packages/claude-code-sourcemap/src/services/compact/compact.ts:122</code></td>
    </tr>
    <tr>
      <td>JSONL/session chain</td>
      <td><code class="language-plaintext highlighter-rouge">packages/claude-code-sourcemap/src/utils/sessionStorage.ts:993</code></td>
    </tr>
    <tr>
      <td>resume/fork</td>
      <td><code class="language-plaintext highlighter-rouge">packages/claude-code-sourcemap/src/utils/sessionRestore.ts:409</code></td>
    </tr>
    <tr>
      <td>large-file optimizations</td>
      <td><code class="language-plaintext highlighter-rouge">packages/claude-code-sourcemap/src/utils/sessionStorage.ts:3226</code></td>
    </tr>
    <tr>
      <td>session list/stat path</td>
      <td><code class="language-plaintext highlighter-rouge">packages/claude-code-sourcemap/src/utils/sessionStorage.ts:4526</code></td>
    </tr>
    <tr>
      <td>global state</td>
      <td><code class="language-plaintext highlighter-rouge">packages/claude-code-sourcemap/src/bootstrap/state.ts:31</code></td>
    </tr>
  </tbody>
</table>

<h2 id="23-최종-판정">23. 최종 판정</h2>

<p>Claude Code는 “CLI에 LLM을 붙인 것”이 아니라 <strong>로컬 stateful agent operating environment</strong>에 가깝다.
확인 가능한 로컬 중심에는 process-owned query loop, append-oriented session,
capability/permission pipeline, context compaction이 있다. CLI와 Agent SDK는 이 runtime 관계가
확인되며, Desktop·IDE·Web은 공식적으로 같은 agentic behavior를 서로 다른 deployment boundary에서
제공한다. 그 표면들의 동일 내부 engine 재사용 여부는 공개되지 않았다.</p>

<p>이 구조는 개인 개발자의 실제 repository 작업, 긴 tool-use loop, 확장 생태계, human-in-the-loop에는
매우 강하다. 특히 model/tool/session/permission을 한 runtime에 응집시키고 failure를 명시적인 상태로
노출한 점은 비교 대상 중 높은 기준점이 될 가능성이 크다.</p>

<p>그러나 서버형 SDK, shared HPC, NFS, 10,000-session desktop catalog의 기준으로 보면 local process와
file affinity가 명확한 한계다. 또한 Claude-specific runtime 최적화와 내부 TUI/global-state 결합은
범용 agent kernel로서의 중립성과 변경 용이성을 낮춘다.</p>

<p>따라서 Farad가 취할 전략은 Claude Code를 기능 단위로 복제하는 것이 아니다. <strong>agent loop의 명시적
state, tool/permission pipeline, context/transcript 구분, progressive disclosure, failure taxonomy는
배우고, process-per-session·file-per-session scan·UI/core 결합·provider lock-in은 Farad의
authority/thread/NFS/10k 계약에 맞게 다시 설계</strong>해야 한다. <code class="language-plaintext highlighter-rouge">D</code></p>

<style>pre.mermaid{overflow-x:auto;background:transparent;border:0;padding:0}.mermaid svg{max-width:100%;height:auto}</style>

<script type="module">
import mermaid from "https://cdn.jsdelivr.net/npm/mermaid@11.16.0/dist/mermaid.esm.min.mjs";
mermaid.initialize({startOnLoad:true,securityLevel:"strict"});
</script>]]></content><author><name>hvppycoding</name></author><category term="research" /><summary type="html"><![CDATA[Claude Code 제품·런타임·아키텍처 심층 분석]]></summary></entry><entry><title type="html">OpenCode 제품·런타임·아키텍처 심층 분석</title><link href="https://hvppycoding.github.io/research/opencode-architecture-analysis/" rel="alternate" type="text/html" title="OpenCode 제품·런타임·아키텍처 심층 분석" /><published>2026-07-13T23:51:00+09:00</published><updated>2026-07-13T23:51:00+09:00</updated><id>https://hvppycoding.github.io/research/opencode-architecture-analysis</id><content type="html" xml:base="https://hvppycoding.github.io/research/opencode-architecture-analysis/"><![CDATA[<h1 id="opencode-제품런타임아키텍처-심층-분석">OpenCode 제품·런타임·아키텍처 심층 분석</h1>

<blockquote>
  <p><a href="https://hvppycoding.github.io/research/coding-agent-architecture-comparison/">전체 보고서 인덱스</a></p>
</blockquote>

<blockquote>
  <p>조사 기준일: 2026-07-13 (Asia/Seoul)<br />
공식 저장소: <a href="https://github.com/anomalyco/opencode"><code class="language-plaintext highlighter-rouge">anomalyco/opencode</code></a><br />
고정 소스 스냅샷: <code class="language-plaintext highlighter-rouge">b3a012cbdde854b0fe14f95b1c48543bf6517c0a</code><br />
commit 시각·제목: <code class="language-plaintext highlighter-rouge">2026-07-13T10:59:16Z</code>, <code class="language-plaintext highlighter-rouge">fix(cli): switch org after console logout (#36276)</code><br />
패키지 버전: <code class="language-plaintext highlighter-rouge">1.17.18</code> (<code class="language-plaintext highlighter-rouge">dev</code> 브랜치 HEAD; 확인한 stable tag <code class="language-plaintext highlighter-rouge">v1.17.18</code>보다 1 commit 앞)<br />
분석 범위: CLI/TUI, HTTP server, SDK, desktop/web/IDE/ACP surface, V1/V2 session runtime,
SQLite/event model, providers, tools, permissions, agents, MCP/LSP, skills/plugins, 실패·성능·운영 경계</p>
</blockquote>

<h2 id="1-결론부터">1. 결론부터</h2>

<p>OpenCode의 본질은 단순한 터미널 챗봇이 아니다. <strong>한 coding-agent runtime을 HTTP/OpenAPI 경계에
두고 TUI, 비대화형 CLI, 웹, 데스크톱, IDE, ACP, SDK가 공유하도록 만든 provider-neutral
client/server 제품</strong>이다. 이 선택은 비교 대상 중에서도 특히 표면 확장성과 임베딩 가능성이 좋다.
75개 이상의 provider, MCP, LSP, plugin, custom tool, skill, command를 같은 server authority 아래
수용한다.</p>

<p>그러나 2026-07-13의 고정 commit은 완성된 단일 architecture가 아니다. 현재 표준 요청은 여전히
<code class="language-plaintext highlighter-rouge">SessionPrompt</code> 중심의 V1 loop와 JSON <code class="language-plaintext highlighter-rouge">message</code>/<code class="language-plaintext highlighter-rouge">part</code> projection을 사용하고, 동시에
<code class="language-plaintext highlighter-rouge">packages/core</code>의 공용 EventV2 substrate는 V1 lifecycle 변경도 durable event로 append하고 같은
transaction에서 JSON <code class="language-plaintext highlighter-rouge">message</code>/<code class="language-plaintext highlighter-rouge">part</code> projection을 갱신한다. 그 위에 typed session projection,
input admission, context epoch, 새 runner를 갖춘 V2가 이식되고 있다. V2에는 compaction continuation과
durable replay API까지 구현돼 있지만, durable multi-node ownership, bounded retry, 완전한 tool
resolution과 cancellation settlement는 아직 남아 있다. 따라서 OpenCode를 평가할 때 <strong>현재 primary인
V1 runtime</strong>과 <strong>공용 durable 기반 위에서 더 강한 실행 의미론으로 이동 중인 V2</strong>를 합쳐서 이미
완성된 단일 runtime으로 서술하면 안 된다.</p>

<p>한 문장으로 요약하면 다음과 같다.</p>

<blockquote>
  <p><strong>OpenCode는 개방형 provider·extension ecosystem과 다중 client/server spine이 매우 강하고,
typed Effect lifecycle, V1/V2 공용 durable event/projector, typed V2 runtime으로 재구성 중인
야심찬 제품이다. 반면 현재 hot path는 매 agent step마다 전체 transcript를 다시 읽으며,
권한은 host sandbox가 아닌
사용자 승인 정책이고, 무제한 cache/queue와 문서-구현 불일치 때문에 장시간 server·공유
HPC/NFS 환경에는 그대로 적용하기 어렵다.</strong></p>
</blockquote>

<p>핵심 판정은 다음과 같다.</p>

<ul>
  <li>가장 강한 자산: 하나의 headless core와 OpenAPI/SDK로 여러 surface를 지탱하는 경계, 폭넓은
provider·extension 호환성, SQLite와 typed Effect resource lifecycle, V1/V2 공용 durable
event+projection transaction과 V2 typed runtime 방향.</li>
  <li>가장 큰 구조 위험: V1/V2 이중 architecture 전환기라 session runtime, typed/JSON projection과 API
의미론이 중복되고 primary runtime이 여전히 큰 <code class="language-plaintext highlighter-rouge">SessionPrompt</code> orchestration에 모여 있다.</li>
  <li>가장 큰 보안 위험: permission parser와 승인 UX를 containment로 오해하기 쉽다. shell과 plugin은
사용자 계정 권한으로 직접 실행되고, 현재 소스의 <code class="language-plaintext highlighter-rouge">plan</code>·<code class="language-plaintext highlighter-rouge">explore</code> agent는 문서가 암시하는
read-only보다 훨씬 강한 권한을 갖는다.</li>
  <li>가장 큰 규모 위험: V1이 provider turn마다 전체 message/part history를 hydrate한 뒤 compaction
view를 계산하고, instance cache·event queue·background-job registry에 명시적 bound가 없다.</li>
  <li>Farad가 가져올 것: server/API anti-corruption layer, typed generated client, keyset pagination,
durable event projection, resource scope/finalizer, tool-output truncation, provider adapter.</li>
  <li>Farad가 거부할 것: per-turn instruction file/network read, in-process arbitrary plugin trust,
whole-instance reload, unbounded SSE/cache, 문서상의 read-only를 shell allow로 구현하는 방식.</li>
</ul>

<h2 id="2-증거-등급과-분석-한계">2. 증거 등급과 분석 한계</h2>

<p>이 보고서는 <a href="https://hvppycoding.github.io/research/coding-agent-comparison-methodology/">공통 방법론</a>의 등급을 따른다.</p>

<table>
  <thead>
    <tr>
      <th>항목</th>
      <th>근거</th>
      <th style="text-align: right">등급</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>process, session loop, DB, permission, extension 내부 구조</td>
      <td>고정 commit의 TypeScript 소스</td>
      <td style="text-align: right">A</td>
    </tr>
    <tr>
      <td>사용자에게 약속된 server/agent/MCP/LSP/skill/plugin 계약</td>
      <td>공식 OpenCode 문서</td>
      <td style="text-align: right">B</td>
    </tr>
    <tr>
      <td>패키지 버전·commit·source-tree test inventory</td>
      <td>고정 checkout 직접 확인</td>
      <td style="text-align: right">C</td>
    </tr>
    <tr>
      <td>성능·보안·Farad 함의와 점수</td>
      <td>A/B/C에서 도출한 해석</td>
      <td style="text-align: right">D</td>
    </tr>
  </tbody>
</table>

<p>분석의 제한도 명시한다.</p>

<ol>
  <li>공식 저장소의 기본 개발 브랜치는 빠르게 변한다. 함수와 line anchor는 모두 위 SHA에 고정했다.</li>
  <li>공식 문서는 배포 시점이 commit과 완전히 일치하지 않는다. 특히 보안·운영 판정에서는 문서보다
고정 소스를 우선한다.</li>
  <li>source inspection으로 algorithm과 bound는 판정했지만, 이 보고서를 위해 별도 10,000-session
benchmark나 NFS fault injection을 실행하지는 않았다. 해당 성능·운영 결론은 구조 기반 추론(D)이다.</li>
  <li>OpenCode Zen 같은 hosted service의 비공개 backend는 공개 client 계약 이상으로 추정하지 않는다.</li>
</ol>

<h2 id="3-제품-경계와-surface">3. 제품 경계와 surface</h2>

<p>OpenCode는 MIT 라이선스의 공개 coding agent이며, 사용자가 보는 shell/TUI가 전부가 아니다. 현재
저장소와 공식 문서에서 확인되는 surface는 다음과 같다.</p>

<table>
  <thead>
    <tr>
      <th>표면</th>
      <th>역할</th>
      <th>runtime과의 연결</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">opencode</code> TUI</td>
      <td>기본 대화형 terminal UI</td>
      <td>기본값은 Worker 안 server에 RPC <code class="language-plaintext highlighter-rouge">fetch</code>; 명시한 network flag가 있으면 HTTP</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">opencode run</code></td>
      <td>script/CI용 비대화형 실행</td>
      <td>같은 session/server service를 CLI adapter가 사용</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">opencode serve</code></td>
      <td>headless server</td>
      <td>HTTP + SSE + OpenAPI 3.1</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">opencode web</code></td>
      <td>browser UI 포함 server</td>
      <td>HTTP server + web app</td>
    </tr>
    <tr>
      <td>Desktop beta</td>
      <td>Electron native shell</td>
      <td>loopback sidecar 또는 외부 server에 연결</td>
    </tr>
    <tr>
      <td>IDE extension</td>
      <td>editor context와 terminal launch</td>
      <td>독립 engine이 아니라 TUI/server 조작·context 전달 중심</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">opencode acp</code></td>
      <td>editor/agent client protocol</td>
      <td>stdio JSON-RPC ACP adapter + 내부 server</td>
    </tr>
    <tr>
      <td>JS/TS SDK</td>
      <td>자동화·임베딩</td>
      <td>server OpenAPI에서 생성된 typed client</td>
    </tr>
  </tbody>
</table>

<p>공식 <a href="https://opencode.ai/docs/server/">Server 문서</a>는 headless HTTP server와 <code class="language-plaintext highlighter-rouge">/doc</code>의 OpenAPI 3.1,
SDK 생성을 명시한다. <a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/index.ts"><code class="language-plaintext highlighter-rouge">index.ts</code></a>는
TUI, attach, run, serve, web, ACP 등 command를 한 binary에 등록한다. ACP adapter는
<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/cli/cmd/acp.ts"><code class="language-plaintext highlighter-rouge">acp.ts</code></a>,
desktop sidecar는 <a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/desktop/src/main/server.ts"><code class="language-plaintext highlighter-rouge">desktop/src/main/server.ts</code></a>와
<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/desktop/src/main/sidecar.ts"><code class="language-plaintext highlighter-rouge">sidecar.ts</code></a>에
구현되어 있다 [A/B].</p>

<p>현재 desktop은 과거의 인상과 달리 Tauri가 아니라 Electron이다. 고정 commit의
<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/desktop/package.json"><code class="language-plaintext highlighter-rouge">packages/desktop/package.json</code></a>은
Electron <code class="language-plaintext highlighter-rouge">42.3.3</code>을 사용한다. utility-process sidecar가 loopback에서 random password로 server를
띄우고 renderer는 shared SolidJS app을 사용한다. 외부 URL과 WSL 연결도 별도 path로 다룬다 [A].</p>

<p>이 surface 전략의 architecture적 의미는 크다. UI가 provider SDK와 tool object를 직접 소유하는
것이 아니라 server protocol에 의존하므로 새 client를 추가할 때 runtime을 복제하지 않는다. 반대로
API authentication, directory routing, reconnect, SSE consistency, schema version transition이 모두
제품의 핵심 correctness 문제가 된다.</p>

<h2 id="4-전체-architecture">4. 전체 architecture</h2>

<pre class="mermaid">
flowchart TB
    subgraph Clients[Client surfaces]
        TUI[TUI / attach]
        RUN[run CLI]
        WEB[Web / Electron]
        IDE[IDE]
        ACP[ACP client]
        SDK[Generated SDK]
    end

    TUI --&gt;|default: Worker RPC fetch\nnetwork flags: HTTP| API
    RUN --&gt; API
    WEB --&gt;|HTTP + SSE| API
    IDE --&gt; API
    ACP --&gt;|stdio adapter| API
    SDK --&gt;|HTTP| API

    API[Effect HTTP API\nV1 handlers + V2 typed APIs]
    API --&gt; ROUTE[workspace/directory routing]
    ROUTE --&gt; INST[InstanceStore\nper normalized directory]

    subgraph Runtime[Per-instance runtime services]
        V1[V1 SessionPrompt loop]
        V2[V2 durable runner\nin migration]
        CFG[Config / Skills / Plugins]
        MCP[MCP / LSP]
        TOOL[Tool registry + permissions]
        PROV[Provider + model adapters]
    end

    INST --&gt; V1
    INST --&gt; V2
    INST --&gt; CFG
    INST --&gt; MCP
    V1 --&gt; TOOL
    V2 --&gt; TOOL
    V1 --&gt; PROV
    V2 --&gt; PROV

    V1 --&gt;|lifecycle events| EVT[Shared EventV2 durable log&lt;br/&gt;projectors in same transaction]
    V2 --&gt;|typed events| EVT
    EVT --&gt; DB[(SQLite channel database)]
    V1 --&gt;|high-frequency non-durable deltas| BUS[Global/Event bus]
    EVT --&gt; BUS
    BUS --&gt;|SSE, no resume cursor in legacy route| Clients

</pre>

<h3 id="41-기본-tui의-실제-process-topology">4.1 기본 TUI의 실제 process topology</h3>

<p>공식 문서는 “TUI를 시작하면 임의 hostname/port의 server를 띄운다”고 설명한다. 그러나 고정
commit의 기본 path는 더 흥미롭다. <a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/cli/cmd/tui.ts#L210-L252"><code class="language-plaintext highlighter-rouge">tui.ts#L210-L252</code></a>는
TUI parent가 Bun Worker를 만들고 <code class="language-plaintext highlighter-rouge">http://opencode.internal</code>이라는 synthetic URL을 쓴다.
<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/cli/tui/worker.ts#L24-L43"><code class="language-plaintext highlighter-rouge">worker.ts#L24-L43</code></a>는
요청을 <code class="language-plaintext highlighter-rouge">Server.Default().app.fetch(request)</code>에 직접 전달하고 <code class="language-plaintext highlighter-rouge">GlobalBus</code> event를 RPC로 parent에
forward한다 [A].</p>

<p>즉 기본 TUI에는 <strong>UI와 runtime을 같은 OS process의 Bun Worker로 분리하되 TCP listener를 열지
않는 worker-isolated direct-fetch topology</strong>가 있다. 사용자가 hostname/port 같은 network flag를 주거나
<code class="language-plaintext highlighter-rouge">serve</code>를 실행할 때 real listener가 열린다. 이 선택은 기본 local 실행의 port 충돌·노출을 줄이면서
server/client 경계를 유지한다. 문서의 “random port”는 현재 source와 drift한다.</p>

<p>Worker는 shutdown 때 scoped resource를 dispose하고, reload signal에서는 config를 invalidate한 뒤
모든 instance를 dispose한다. lifecycle은 분명하지만 reload granularity는 process 전체에 가깝다.
Farad의 항목별 explicit refresh/delta reconcile과는 다른 비용 모델이다.</p>

<h3 id="42-headlessdesktop-topology">4.2 headless·desktop topology</h3>

<p><code class="language-plaintext highlighter-rouge">opencode serve</code>는 Effect HTTP listener를 열고 선택적으로 mDNS를 광고한다. server layer는 graceful
close 후 강제 close fallback을 갖는다. 기본 bind는 <code class="language-plaintext highlighter-rouge">127.0.0.1:4096</code>이고 Basic auth는 password
환경변수가 있을 때만 활성화된다. <a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/cli/cmd/serve.ts#L15-L17"><code class="language-plaintext highlighter-rouge">serve.ts#L15-L17</code></a>와
<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/cli/cmd/web.ts#L40-L42"><code class="language-plaintext highlighter-rouge">web.ts#L40-L42</code></a>는
password가 없으면 실행을 거부하지 않고 warning만 출력한다 [A].</p>

<p>Electron desktop은 별도 utility process sidecar를 loopback에 띄우고 random password를 주므로
일반 <code class="language-plaintext highlighter-rouge">serve</code>보다 안전한 기본값이다. renderer crash와 agent server lifetime도 분리된다. 그러나 외부
server 연결을 허용하므로 remote transport의 TLS, auth, directory authority는 사용자 운영 책임으로
남는다.</p>

<h2 id="5-authority와-ownership">5. Authority와 ownership</h2>

<p>OpenCode의 상태 권위를 계층별로 나누면 다음과 같다.</p>

<table>
  <thead>
    <tr>
      <th>상태/행위</th>
      <th>실질 authority</th>
      <th>client가 하는 일</th>
      <th>영속 여부</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>workspace/directory 선택</td>
      <td>HTTP routing middleware + session location</td>
      <td>query/header/session ID 제공</td>
      <td>session에 directory/workspace 저장</td>
    </tr>
    <tr>
      <td>instance service 수명</td>
      <td>process-local <code class="language-plaintext highlighter-rouge">InstanceStore</code>/Effect scope</td>
      <td>dispose/reload 요청 가능</td>
      <td>cache 자체는 비영속</td>
    </tr>
    <tr>
      <td>한 session의 active run</td>
      <td><code class="language-plaintext highlighter-rouge">SessionRunState</code>와 V1 <code class="language-plaintext highlighter-rouge">SessionPrompt</code></td>
      <td>prompt/steer/abort/approval</td>
      <td>transcript lifecycle은 영속, active status/owner는 메모리</td>
    </tr>
    <tr>
      <td>V2 실행 ownership</td>
      <td>local <code class="language-plaintext highlighter-rouge">RunCoordinator</code></td>
      <td>durable input admit/queue</td>
      <td>multi-node owner는 미완</td>
    </tr>
    <tr>
      <td>transcript/message/part</td>
      <td>shared EventV2 + SQLite projection</td>
      <td>REST 조회, SSE projection 소비</td>
      <td>V1 lifecycle부터 durable; live delta는 비영속</td>
    </tr>
    <tr>
      <td>durable aggregate sequence</td>
      <td>shared EventV2 service + projector</td>
      <td>history/replay API 조회 가능</td>
      <td>SQLite 영속</td>
    </tr>
    <tr>
      <td>V1 permission ask/always</td>
      <td>per-directory instance Permission service</td>
      <td>allow once/always/reject</td>
      <td>pending/<code class="language-plaintext highlighter-rouge">always</code> 모두 메모리; reload/dispose/restart 시 소멸</td>
    </tr>
    <tr>
      <td>V2 permission ask/always</td>
      <td>PermissionV2 + project-scoped saved rules</td>
      <td>allow once/always/reject·saved rule 관리</td>
      <td>pending은 메모리, save resource의 <code class="language-plaintext highlighter-rouge">always</code>는 SQLite 영속</td>
    </tr>
    <tr>
      <td>provider/MCP credentials</td>
      <td>auth JSON/MCP auth JSON</td>
      <td>login/logout</td>
      <td>disk, mode <code class="language-plaintext highlighter-rouge">0600</code></td>
    </tr>
    <tr>
      <td>tool side effect</td>
      <td>host process/tool subprocess</td>
      <td>승인 응답</td>
      <td>외부 filesystem/process에 즉시 반영</td>
    </tr>
    <tr>
      <td>UI projection</td>
      <td>각 client</td>
      <td>refetch/render</td>
      <td>UI cache 비권위</td>
    </tr>
  </tbody>
</table>

<h3 id="51-directory-routing은-server-authority의-일부다">5.1 directory routing은 server authority의 일부다</h3>

<p><a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/server/routes/instance/httpapi/middleware/workspace-routing.ts#L69-L88"><code class="language-plaintext highlighter-rouge">workspace-routing.ts#L69-L88</code></a>는
session이 있으면 그 session location을 우선하고, 아니면 <code class="language-plaintext highlighter-rouge">directory</code> query,
<code class="language-plaintext highlighter-rouge">x-opencode-directory</code> header, 마지막으로 server cwd를 사용한다. 정규화한 directory별로
<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/project/instance-store.ts"><code class="language-plaintext highlighter-rouge">InstanceStore</code></a>가
service graph를 boot하고 동시 boot를 coalesce한다 [A].</p>

<p>이는 multi-workspace server를 자연스럽게 지원하지만 보안 함의도 있다. Basic-auth credential을 가진
remote client는 directory를 선택하고 file, PTY, shell, session endpoint를 호출할 수 있다. 이
middleware 층에는 per-project ACL이나 tenant isolation이 없다. 따라서 server password는 단순 UI
login이 아니라 <strong>해당 OS 사용자로 접근 가능한 workspace와 command 실행 전반의 capability</strong>로
취급해야 한다 [D].</p>

<h3 id="52-instance-cache와-lifecycle">5.2 instance cache와 lifecycle</h3>

<p><code class="language-plaintext highlighter-rouge">InstanceStore</code>는 normalized directory를 key로 <code class="language-plaintext highlighter-rouge">Map</code>을 유지하고 concurrent initialization을
<code class="language-plaintext highlighter-rouge">Deferred</code>로 합친다. dispose, reload, dispose-all은 있지만 capacity나 idle TTL eviction은 없다.
Effect service 쪽 <a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/effect/instance-state.ts#L24-L35"><code class="language-plaintext highlighter-rouge">instance-state.ts#L24-L35</code></a>도
<code class="language-plaintext highlighter-rouge">ScopedCache</code> capacity를 <code class="language-plaintext highlighter-rouge">Number.POSITIVE_INFINITY</code>로 둔다 [A].</p>

<p>짧게 실행하는 local TUI에서는 큰 문제가 아닐 수 있다. 하지만 하나의 headless server로 수천 개
directory를 오래 방문하면 config, plugin, MCP, LSP 등 per-instance object가 명시적 dispose 전까지
누적될 수 있다. long-lived server에는 LRU/idle disposal 또는 운영상 주기적인 recycle이 필요하다 [D].</p>

<h2 id="6-표준-v1-agent-run-흐름">6. 표준 V1 agent run 흐름</h2>

<p>고정 commit에서 안정된 <code class="language-plaintext highlighter-rouge">/session/:id/message</code>, <code class="language-plaintext highlighter-rouge">prompt_async</code>, command 등 primary handler는
<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/session/prompt.ts"><code class="language-plaintext highlighter-rouge">SessionPrompt</code></a>를
호출한다. 따라서 다음 흐름이 현재 실제 주 경로다.</p>

<pre class="mermaid">
sequenceDiagram
    participant C as Client
    participant H as HTTP/CLI handler
    participant R as SessionRunState
    participant P as SessionPrompt.runLoop
    participant E as Shared EventV2/projectors
    participant DB as SQLite
    participant L as Provider LLM
    participant T as Tool/Permission
    participant S as GlobalBus/legacy SSE

    C-&gt;&gt;H: prompt / command
    H-&gt;&gt;E: append user message/part lifecycle events
    E-&gt;&gt;DB: durable append + JSON projection in one tx
    H-&gt;&gt;R: ensureRunning(sessionID)
    R-&gt;&gt;P: join existing run or create one
    loop provider/tool steps
        P-&gt;&gt;DB: load all messages+parts
        P-&gt;&gt;P: filter compacted view / tasks / limits
        P-&gt;&gt;P: resolve agent, tools, skills, env, instructions, MCP
        P-&gt;&gt;E: append assistant + step lifecycle state
        E-&gt;&gt;DB: durable append + projection
        P-&gt;&gt;L: streamText(messages, tools), maxRetries=0
        L--&gt;&gt;P: text/reasoning/tool deltas
        P--&gt;&gt;S: non-durable high-frequency/status deltas
        P-&gt;&gt;E: append durable message/part/usage checkpoints
        E-&gt;&gt;DB: update durable log + projection
        E--&gt;&gt;S: committed lifecycle events
        alt tool call
            P-&gt;&gt;T: permission check + execute
            T--&gt;&gt;P: completed/error output
        else overflow
            P-&gt;&gt;E: append compaction request/summary
            E-&gt;&gt;DB: durable append + projection
        else final assistant and no pending task
            P--&gt;&gt;R: settle
        end
    end
    S--&gt;&gt;C: legacy SSE, client refetches authority as needed

</pre>

<h3 id="61-loop-orchestration">6.1 loop orchestration</h3>

<p><a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/session/prompt.ts#L1081-L1347"><code class="language-plaintext highlighter-rouge">prompt.ts#L1081-L1347</code></a>의
<code class="language-plaintext highlighter-rouge">runLoop</code>는 다음 책임을 한 loop에 조정한다 [A].</p>

<ol>
  <li>session을 busy로 표시하고 compacted transcript view를 계산한다.</li>
  <li>마지막 user/assistant, unfinished tool/task, step count와 overflow를 판정한다.</li>
  <li>첫 step에서 title 생성, 종료 시 summary 같은 auxiliary work를 fork한다.</li>
  <li>subtask/compaction marker를 처리한다.</li>
  <li>agent와 model, step limit을 선택하고 새 assistant record를 만든다.</li>
  <li>built-in/custom/MCP tool registry를 resolve한다.</li>
  <li>skill 목록, environment, instruction system text, MCP instructions, model messages를 병렬 조립한다.</li>
  <li>plugin hook으로 message/parameter/system을 변환한다.</li>
  <li><code class="language-plaintext highlighter-rouge">SessionProcessor</code>에 streaming step을 맡기고 continue/break/compact를 결정한다.</li>
  <li>run 뒤 오래된 tool output pruning을 비동기로 수행한다.</li>
</ol>

<p><a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/session/run-state.ts#L88-L107"><code class="language-plaintext highlighter-rouge">SessionRunState.ensureRunning</code></a>은
같은 process의 같은 session에서 active loop를 하나로 직렬화한다. 이미 실행 중이면 caller가 그
run에 join하고, abort는 그 실행을 interrupt한다. distinct session은 동시에 실행될 수 있다 [A].</p>

<p>이 구조는 central coordinator가 있어 의미를 찾기 쉽지만 <code class="language-plaintext highlighter-rouge">SessionPrompt</code>가 context, tools, plugin,
compaction, background task, termination을 동시에 아는 큰 orchestration module이 되었다. V2가 이를
typed core service로 분해하려는 이유가 소스 주석에 직접 드러난다.</p>

<h3 id="62-streaming과-tool-settlement">6.2 streaming과 tool settlement</h3>

<p>기본 provider 실행은 Vercel AI SDK의 <code class="language-plaintext highlighter-rouge">streamText</code>다.
<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/session/llm.ts#L280-L324"><code class="language-plaintext highlighter-rouge">session/llm.ts#L280-L324</code></a>는
AI SDK 내부 retry를 <code class="language-plaintext highlighter-rouge">maxRetries: 0</code>으로 두고, OpenCode의 외부 retry schedule이 whole stream을
관리하게 한다. experimental native <code class="language-plaintext highlighter-rouge">@opencode-ai/llm</code> path도 있지만 지원되지 않는 경우 AI SDK로
fallback한다 [A].</p>

<p><a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/session/processor.ts"><code class="language-plaintext highlighter-rouge">processor.ts</code></a>는
stream 중 text/reasoning delta, tool pending/running/completed/error, snapshot, patch, usage와 finish reason을
message part로 증분 저장한다. tool call은 AI SDK가 병렬 dispatch할 수 있으며, 동일 tool과 동일
input이 3회 반복되면 <code class="language-plaintext highlighter-rouge">doom_loop</code> permission을 물어 무한 반복을 human-in-the-loop로 끊는다 [A].</p>

<p>좋은 점은 model의 최종 답이 올 때까지 메모리에서만 기다리지 않는다는 것이다. client는 SSE로
progress를 받고 DB에서 projection을 다시 읽을 수 있다. V1의 message/part lifecycle update는 공용
EventV2 durable log에 먼저 append되고 같은 transaction에서 mutable JSON projection으로 반영된다.
반면 <code class="language-plaintext highlighter-rouge">message.part.delta</code> 같은 고빈도 live delta와 legacy SSE transport는 비영속이므로 둘을 durable
lifecycle event와 구분해야 한다.</p>

<h2 id="7-v2-architecture-checklist보다-앞섰지만-recovery-경계는-남음">7. V2 architecture: checklist보다 앞섰지만 recovery 경계는 남음</h2>

<p>V2는 V1도 사용하는 공용 durable event/projector substrate 위에서 <code class="language-plaintext highlighter-rouge">packages/core</code>와 typed
<code class="language-plaintext highlighter-rouge">@opencode-ai/server</code> API를 중심으로 다음 요소를 도입한다.</p>

<ul>
  <li>aggregate별 monotonic sequence를 가진 durable event와 projector</li>
  <li>typed <code class="language-plaintext highlighter-rouge">session_message</code> projection과 context epoch</li>
  <li><code class="language-plaintext highlighter-rouge">session_input</code>의 admitted/promoted sequence로 queue/steer를 영속 표현</li>
  <li>one-local-owner run coordinator</li>
  <li>runner history를 latest compaction/baseline 이후로 제한</li>
  <li>tool call 기록과 local settlement를 분리한 execution service</li>
  <li>wait/queue/steer 등 새로운 API 의미론</li>
</ul>

<p>동시에 server entry는 legacy V1 API와 V2 API layer를 한 Effect graph에 mount한다
(<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/server/routes/instance/httpapi/server.ts"><code class="language-plaintext highlighter-rouge">httpapi/server.ts</code></a>) [A].</p>

<p>새 runner의 header checklist는 설계 의도를 보여 주지만 일부 항목이 실제 code path보다 늦게
갱신된 <strong>stale self-documentation</strong>이다.
<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/core/src/session/runner/llm.ts#L44-L86"><code class="language-plaintext highlighter-rouge">packages/core/src/session/runner/llm.ts#L44-L86</code></a>는
완료와 미완을 체크리스트로 기록하지만, 판정은 아래 implementation과 함께 해야 한다 [A].</p>

<p>완료된 핵심:</p>

<ul>
  <li>local active-run drain과 step limit</li>
  <li>message translation과 한 provider turn streaming</li>
  <li>assistant/reasoning/tool event의 durable 기록</li>
  <li>기록된 local tool call을 eager start하고 모두 settlement할 때까지 대기</li>
  <li>active provider turn 중 들어온 user steering의 continuation</li>
  <li>step 시작·종료 snapshot capture와 changed-files 기록</li>
  <li>자동/overflow compaction 후 다음 provider turn continuation</li>
  <li>finite durable history와 <code class="language-plaintext highlighter-rouge">after</code> sequence 기반 replay-then-live event consumer</li>
</ul>

<p>남은 핵심:</p>

<ul>
  <li>clustered 환경의 durable multi-node ownership</li>
  <li>durable busy/retry/idle/interrupted/failure status</li>
  <li>interruption attachment replacement</li>
  <li>bounded provider retry와 repeated-tool-call policy</li>
  <li>built-in/MCP/plugin/structured-output tool의 완전한 resolution</li>
  <li>incremental snapshot/patch/retry notice parity, scoped progress/plugin integration과 cancel settlement</li>
  <li>durable final execution status와 restart-safe continuation ownership</li>
  <li>streamed delta coalescing과 covering projection index 최적화</li>
  <li>title/summary/cleanup의 bounded execution</li>
</ul>

<p><a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/core/src/session/compaction.ts"><code class="language-plaintext highlighter-rouge">compaction.ts</code></a>와
runner의 <code class="language-plaintext highlighter-rouge">ContinueAfterCompaction</code>/<code class="language-plaintext highlighter-rouge">ContinueAfterOverflowCompaction</code> transition은 checklist와 달리
compaction continuation을 구현한다. 또한 typed protocol의
<code class="language-plaintext highlighter-rouge">GET /api/session/:sessionID/history</code>와 <code class="language-plaintext highlighter-rouge">GET /api/session/:sessionID/event?after=&lt;seq&gt;</code>는 각각 finite
durable page와 replay-then-live durable SSE를 제공한다
(<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/protocol/src/groups/session.ts"><code class="language-plaintext highlighter-rouge">groups/session.ts</code></a>,
<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/server/src/handlers/session.ts"><code class="language-plaintext highlighter-rouge">handlers/session.ts</code></a>) [A].</p>

<p>따라서 V2는 단순 계획이 아니라 compaction continuation과 durable replay까지 동작하는 migration
architecture다. 다만 <strong>durable event replay와 durable execution recovery는 같지 않다</strong>. owner lease,
terminal/busy/retry status, bounded retry, restart-safe continuation이 남았으므로 foundation과 current
recovery/ownership 점수를 분리해야 한다 [D].</p>

<h2 id="8-저장-모델과-event-흐름">8. 저장 모델과 event 흐름</h2>

<h3 id="81-sqlite가-local-authority다">8.1 SQLite가 local authority다</h3>

<p>기본 DB filename은 installation channel에 따라 달라진다. <code class="language-plaintext highlighter-rouge">latest</code>·<code class="language-plaintext highlighter-rouge">beta</code>·<code class="language-plaintext highlighter-rouge">prod</code> channel은 XDG data
directory 아래 <code class="language-plaintext highlighter-rouge">opencode.db</code>를 쓰고, 그 밖의 channel은 <code class="language-plaintext highlighter-rouge">opencode-&lt;channel&gt;.db</code>를 쓴다.
<code class="language-plaintext highlighter-rouge">OPENCODE_DB</code>는 <code class="language-plaintext highlighter-rouge">:memory:</code>, absolute path, 또는 XDG data directory 기준 relative filename을 받을 수
있다. 구현상 node-local DB 배치 수단은 있지만 공식 deployment contract로는 충분히 문서화되지
않았다. DB initialization은
<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/core/src/database/database.ts"><code class="language-plaintext highlighter-rouge">database.ts</code></a>에서
<code class="language-plaintext highlighter-rouge">journal_mode=WAL</code>, <code class="language-plaintext highlighter-rouge">synchronous=NORMAL</code>, <code class="language-plaintext highlighter-rouge">busy_timeout=5000</code>, 약 64 MiB negative <code class="language-plaintext highlighter-rouge">cache_size</code>, foreign
key를 설정한다 [A].</p>

<p><a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/core/src/session/sql.ts"><code class="language-plaintext highlighter-rouge">session/sql.ts</code></a>는
전환기의 이중 저장 모델을 그대로 보여준다 [A].</p>

<table>
  <thead>
    <tr>
      <th>table</th>
      <th>역할</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">session</code></td>
      <td>project/workspace/parent/directory/title/version/share/summary/cost/tokens/revert/permission/agent/model/timestamps</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">message</code></td>
      <td>V1 message info를 JSON data로 저장, session/time/id index</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">part</code></td>
      <td>V1 text/reasoning/tool/file/snapshot/patch/step/retry 등 JSON part, message/session index</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">todo</code></td>
      <td>session todo projection</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">session_message</code></td>
      <td>V2 typed/event-projected row, per-session sequence unique 및 type/sequence index</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">session_input</code></td>
      <td>queue/steer input, admitted/promoted sequence</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">session_context_epoch</code></td>
      <td>baseline/snapshot과 baseline sequence</td>
    </tr>
  </tbody>
</table>

<p>session row에 workspace와 directory를 저장하므로 resume request가 다른 current directory에서 와도
원래 instance로 route할 수 있다. parent ID를 통해 subagent/fork tree도 표현한다.</p>

<h3 id="82-durable-event와-projection">8.2 durable event와 projection</h3>

<p><a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/core/src/event.ts"><code class="language-plaintext highlighter-rouge">packages/core/src/event.ts</code></a>는
aggregate owner/sequence와 exact event ID/type/data를 저장하고, 같은 SQLite immediate transaction에서
projector를 적용한 뒤 publish한다. replay 시 event payload와 projected result의 일치도 검증한다.
<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/core/src/session/projector.ts"><code class="language-plaintext highlighter-rouge">session/projector.ts</code></a>는
V1/V2 projection을 갱신한다 [A].</p>

<p>구체적으로 V1 schema의 <code class="language-plaintext highlighter-rouge">session.created/updated/deleted</code>, <code class="language-plaintext highlighter-rouge">message.updated/removed</code>,
<code class="language-plaintext highlighter-rouge">message.part.updated/removed</code>도 <code class="language-plaintext highlighter-rouge">{ durable: { aggregate: "sessionID", version: 1 } }</code>로 정의되고,
V1 session service가 <code class="language-plaintext highlighter-rouge">EventV2Bridge</code>를 통해 publish한다. 따라서 JSON row는 mutable serving model이지만
그 lifecycle 변경의 commit source는 durable event다. 별도 durable option이 없는 <code class="language-plaintext highlighter-rouge">message.part.delta</code>
같은 live delta는 이 보장에서 제외된다
(<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/schema/src/v1/session.ts"><code class="language-plaintext highlighter-rouge">schema/src/v1/session.ts</code></a>,
<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/session/session.ts"><code class="language-plaintext highlighter-rouge">opencode/src/session/session.ts</code></a>,
<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/event-v2-bridge.ts"><code class="language-plaintext highlighter-rouge">event-v2-bridge.ts</code></a>) [A].</p>

<p>이는 미래의 recovery와 audit에 좋은 기반이다. command가 commit되기 전에 client event가 먼저
나가는 문제를 줄이고, aggregate sequence가 order의 권위가 된다. <code class="language-plaintext highlighter-rouge">EventV2Bridge</code>는 여기에
directory/workspace/project context를 붙여 legacy <code class="language-plaintext highlighter-rouge">GlobalBus</code> 소비자와 durable-event 소비자를
연결한다.</p>

<p>하지만 현재 client event transport와 durable log를 동일시하면 안 된다.
<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/server/routes/instance/httpapi/handlers/event.ts#L31-L72"><code class="language-plaintext highlighter-rouge">handlers/event.ts#L31-L72</code></a>는
연결마다 <code class="language-plaintext highlighter-rouge">Queue.unbounded</code>를 만들고 10초 heartbeat와 location-filtered event를 SSE로 보낸다. SSE
encoding에 replay 가능한 durable sequence를 <code class="language-plaintext highlighter-rouge">id</code>로 제공하거나 <code class="language-plaintext highlighter-rouge">Last-Event-ID</code>로 resume하는 계약은
이 legacy route에 없다 [A].</p>

<p>따라서 disconnect 후 client correctness는 “놓친 event를 replay”가 아니라 <strong>REST/DB의 authoritative
projection을 refetch하고 SSE를 다시 구독</strong>하는 방식이어야 한다 [D]. V2가 durable event를 갖고
있어도 legacy client transport가 자동으로 exactly-once가 되는 것은 아니다. 반면 새 typed V2 route는
<code class="language-plaintext highlighter-rouge">after</code> aggregate sequence 이후의 durable history를 먼저 replay한 뒤 live event로 전환한다. 즉 replay
primitive의 부재가 아니라 <strong>legacy client transport가 그것을 사용하지 않는 이중 API 상태</strong>가 정확한
판정이다 [A/D].</p>

<h3 id="83-unbounded-event-path">8.3 unbounded event path</h3>

<p>legacy SSE는 client별 unbounded queue를, core event service는 aggregate/global unbounded PubSub를
사용한다 (<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/core/src/event.ts#L175-L189"><code class="language-plaintext highlighter-rouge">event.ts#L175-L189</code></a>) [A].
Core에는 capacity를 넘으면 <code class="language-plaintext highlighter-rouge">SubscriberOverflowError</code>로 종료하는 opt-in <code class="language-plaintext highlighter-rouge">allBounded(events, capacity)</code>
dropping-queue helper도 있다. 그러나 고정 commit의 legacy SSE route는 이를 사용하지 않는다 [A].
로컬 TUI처럼 consumer가 빠르고 연결 수가 작다는 전제에서는 간단하다. 느린 remote consumer나 event
폭주 시 메모리 backpressure가 없고 connection별로 누적될 수 있다. long-running multi-client
server의 <strong>shipping default/integration</strong>에는 bounded queue, coalescing, overflow 후 snapshot/refetch
signal이 더 필요하다 [D].</p>

<h2 id="9-transcript-context와-compaction">9. transcript, context와 compaction</h2>

<h3 id="91-append되는-내용과-model-view는-다르다">9.1 append되는 내용과 model view는 다르다</h3>

<p>V1의 message/part는 text, reasoning, tool lifecycle, file, snapshot, patch, step, retry, subtask,
compaction까지 세밀하게 보존한다. compaction은 과거 row를 삭제하지 않고 synthetic compaction user
part와 <code class="language-plaintext highlighter-rouge">summary: true</code>인 assistant summary를 추가한다. 이후 model에 보이는 view만 summary와 최근
turn으로 재구성한다 [A].</p>

<p><a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/session/compaction.ts"><code class="language-plaintext highlighter-rouge">session/compaction.ts</code></a>의
주요 정책은 다음과 같다.</p>

<ul>
  <li>model limit overflow 시 자동 compaction</li>
  <li>기본적으로 최근 2개 turn을 원문으로 유지</li>
  <li>사용 가능한 context의 약 25% 또는 2,000~8,000 token 범위의 preserve budget</li>
  <li>이전 summary가 있으면 새 summary 입력에 포함</li>
  <li>summary 입력의 media 제거와 tool output 축약</li>
  <li>summary 후 작업이 남아 있으면 synthetic continue message 추가</li>
  <li>오래된 tool output은 최근 약 40,000 token을 보호하고, 최소 약 20,000 token 절감이 있을 때 prune</li>
  <li>skill tool output은 prune 대상에서 보호</li>
</ul>

<p>이 설계는 transcript 재현성과 model-context 비용을 분리한다는 점에서 좋다. 원문과 tool lifecycle을
DB에 남기므로 UI, resume, share가 summary text만 보는 구조는 아니다.</p>

<h3 id="92-v1의-치명적인-read-amplification">9.2 V1의 치명적인 read amplification</h3>

<p>compact model view와 DB hot path도 구분해야 한다. <a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/session/message-v2.ts#L425-L466"><code class="language-plaintext highlighter-rouge">MessageV2.page</code></a>는
base64 cursor의 <code class="language-plaintext highlighter-rouge">{id,time}</code>을 이용한 keyset pagination을 하고, 한 page의 parts를 batch hydrate한다.
이 자체는 좋은 구현이다 [A].</p>

<p>그러나 같은 파일의 stream helper는 50개씩 <strong>모든 page를 순회해 full transcript를 메모리에
materialize</strong>하고, <a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/session/message-v2.ts#L574"><code class="language-plaintext highlighter-rouge">filterCompactedEffect#L574</code></a>는
그 전체를 읽은 뒤 compacted view를 filter/reorder한다. V1 <code class="language-plaintext highlighter-rouge">runLoop</code>는 provider/tool step마다 이 함수를
호출한다 [A].</p>

<p>세션에 message/part가 N개 누적되고 한 user request가 K개의 model/tool step을 만든다면 history
조립만 대략 <code class="language-plaintext highlighter-rouge">O(KN)</code>이다. compaction 이전 row도 DB에서 읽은 후 버린다. 이는 Farad의 “누적
10,000개에서 turn hot path는 O(1) 또는 visible/active window” 계약과 정면으로 충돌한다 [D].</p>

<p>session 전체 조회, fork의 prefix clone, share 최초 sync도 full transcript를 hydrate할 수 있다. 이들은
명시적 1회성이라 turn마다 반복되는 것보다는 낫지만, 10k 규모에서는 pagination·background copy·bound가
필요하다.</p>

<h3 id="93-v2-context-epoch가-해결하려는-것">9.3 V2 context epoch가 해결하려는 것</h3>

<p>V2의 <a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/core/src/session/history.ts"><code class="language-plaintext highlighter-rouge">history.ts</code></a>는
latest compaction/context baseline의 sequence 이후만 index query한다. <a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/core/src/session/context-epoch.ts"><code class="language-plaintext highlighter-rouge">context-epoch.ts</code></a>는
instruction/context baseline과 snapshot을 byte-stable하게 저장해 context 변경을 명시적으로
reconcile한다 [A].</p>

<p>이는 두 문제를 함께 개선하는 올바른 방향이다.</p>

<ol>
  <li>과거 compaction 이전 row를 매 step 읽지 않아도 된다.</li>
  <li>live instruction이 바뀌었을 때 같은 session의 resume context가 조용히 변하는 대신 baseline
change를 기록할 수 있다.</li>
</ol>

<p>다만 retained context window 자체는 여전히 materialize되며, V2 runner의 durable status/owner와
restart recovery는 남아 있다. 따라서 “10k 문제가 이미 해결됐다”가 아니라 “해결 가능한 schema와 query path가
생겼다”가 정확하다 [D].</p>

<h2 id="10-permission-model과-실제-security-boundary">10. permission model과 실제 security boundary</h2>

<h3 id="101-정책-평가-자체는-이해하기-쉽다">10.1 정책 평가 자체는 이해하기 쉽다</h3>

<p>공식 <a href="https://opencode.ai/docs/permissions/">Permissions 문서</a>의 기본 모델은 <code class="language-plaintext highlighter-rouge">allow</code>, <code class="language-plaintext highlighter-rouge">ask</code>, <code class="language-plaintext highlighter-rouge">deny</code>와
pattern rule이다. 여러 rule 중 마지막으로 match한 rule이 이기므로 broad default 뒤에 narrow
exception을 둘 수 있다. tool은 실행 전 <code class="language-plaintext highlighter-rouge">ctx.ask()</code>를 통해 permission service에 요청하고, 사용자는
once, always, reject로 답한다 [B].</p>

<p>소스도 이 구조를 그대로 구현한다.
<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/permission/index.ts#L28-L37"><code class="language-plaintext highlighter-rouge">permission/index.ts#L28-L37</code></a>는
마지막 matching rule을 선택한다. 이 V1 service에서 pending request와 <code class="language-plaintext highlighter-rouge">always</code> approval은
<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/permission/index.ts#L42-L174"><code class="language-plaintext highlighter-rouge">permission/index.ts#L42-L174</code></a>의
per-directory <code class="language-plaintext highlighter-rouge">InstanceState</code> map/array에만 저장된다. 같은 instance의 여러 session이 approval을
공유하지만 reload/dispose/process restart 때 소멸한다. reject하면 같은 session의 pending request도
함께 reject하고, instance finalizer는 남은 deferred를 실패시킨다 [A].</p>

<p>V2 permission은 의미가 다르다. pending request는 여전히 local <code class="language-plaintext highlighter-rouge">Map</code>이지만, <code class="language-plaintext highlighter-rouge">always</code> reply에
<code class="language-plaintext highlighter-rouge">request.save</code> resource가 있으면 project-scoped rule을 SQLite <code class="language-plaintext highlighter-rouge">permission</code> table에 저장하고 이후
평가에 병합한다. saved rule의 list/remove API도 있다
(<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/core/src/permission.ts"><code class="language-plaintext highlighter-rouge">core/src/permission.ts</code></a>,
<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/core/src/permission/saved.ts"><code class="language-plaintext highlighter-rouge">permission/saved.ts</code></a>) [A].</p>

<p>장점:</p>

<ul>
  <li>permission name과 path/command pattern을 분리해 세밀한 policy를 만들 수 있다.</li>
  <li>deny는 prompt가 아니라 tool execution 앞의 deterministic gate다.</li>
  <li>once/always의 UX가 단순하며 동일 session의 중복 ask를 해소한다.</li>
  <li>subagent session에도 parent <strong>session row</strong>의 explicit deny와 external-directory rule을 전달한다.</li>
</ul>

<p>한계:</p>

<ul>
  <li>V1 <code class="language-plaintext highlighter-rouge">always</code>는 이름과 달리 disk policy가 아니며 해당 directory instance 수명에만 남는다.</li>
  <li>V2 saved <code class="language-plaintext highlighter-rouge">always</code>와 달리 V1 approval memory는 reload/dispose/restart 후 복구되지 않는다. V2에서도
pending interaction과 <code class="language-plaintext highlighter-rouge">save</code> resource가 없는 reply는 process-local이다.</li>
  <li>rule match는 authorization decision일 뿐 OS-level isolation이 아니다.</li>
</ul>

<h3 id="102-기본-권한은-deny-by-default가-아니다">10.2 기본 권한은 “deny by default”가 아니다</h3>

<p>현재 native agent 기본값은
<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/agent/agent.ts#L119-L136"><code class="language-plaintext highlighter-rouge">agent.ts#L119-L136</code></a>에
명확하다 [A].</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">"*": "allow"</code></li>
  <li><code class="language-plaintext highlighter-rouge">doom_loop</code>: ask</li>
  <li>workspace 밖 directory: ask, 일부 temp/skill/reference path allow</li>
  <li>question/plan transition: deny 후 agent별 override</li>
  <li>일반 read: allow</li>
  <li><code class="language-plaintext highlighter-rouge">.env</code>와 <code class="language-plaintext highlighter-rouge">.env.*</code>: <strong>ask</strong></li>
  <li><code class="language-plaintext highlighter-rouge">.env.example</code>: allow</li>
</ul>

<p>즉 OpenCode의 안전성 기본은 “모든 위험 tool을 먼저 묻는다”가 아니라 <strong>대부분 허용하고 몇 개의
민감 action/pattern만 ask/deny</strong>다. 조직 환경에서는 project/global/managed config로 permission을
명시적으로 강화해야 한다.</p>

<h3 id="103-plan과-explore는-기술적으로-read-only가-아니다">10.3 <code class="language-plaintext highlighter-rouge">plan</code>과 <code class="language-plaintext highlighter-rouge">explore</code>는 기술적으로 read-only가 아니다</h3>

<p>현재 source의 중요한 security drift다.</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">plan</code> agent는 edit/write/apply-patch 계열을 deny하고 plan Markdown 경로만 예외로 둔다. 그러나
<code class="language-plaintext highlighter-rouge">bash</code>를 ask/deny로 override하지 않으므로 global <code class="language-plaintext highlighter-rouge">"*": allow</code>를 상속한다
(<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/agent/agent.ts#L156-L180"><code class="language-plaintext highlighter-rouge">agent.ts#L156-L180</code></a>).
shell로 <code class="language-plaintext highlighter-rouge">rm</code>, redirect, script를 실행하면 filesystem을 바꿀 수 있다.</li>
  <li><code class="language-plaintext highlighter-rouge">explore</code> agent는 먼저 <code class="language-plaintext highlighter-rouge">"*": deny</code>를 두지만 <code class="language-plaintext highlighter-rouge">bash</code>를 명시적으로 allow한다
(<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/agent/agent.ts#L196-L217"><code class="language-plaintext highlighter-rouge">agent.ts#L196-L217</code></a>).
설명과 prompt가 탐색 전용이어도 shell capability는 임의 side effect를 낼 수 있다.</li>
</ul>

<p>따라서 문서의 “plan은 command 전에 묻는다”, “explore는 read-only”를 security invariant로 사용할
수 없다. 모델이 지시를 따를 것이라는 behavioral expectation과 tool-level effect safety는 다르다.
read-only agent를 보장하려면 shell을 deny하거나 read-only command broker/OS sandbox를 따로 둬야
한다 [D].</p>

<h3 id="104-shell-permission-parser는-containment가-아니다">10.4 shell permission parser는 containment가 아니다</h3>

<p><a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/tool/shell.ts#L257-L310"><code class="language-plaintext highlighter-rouge">tool/shell.ts#L257-L310</code></a>는
tree-sitter Bash/PowerShell AST를 parse하고 알려진 command argument와 path를 scan해 shell pattern 및
<code class="language-plaintext highlighter-rouge">external_directory</code> permission을 요청한 뒤 host shell을 직접 spawn한다 [A].</p>

<p>이 방식은 raw string prefix match보다 낫다. <code class="language-plaintext highlighter-rouge">git -C</code>, file argument 등 정적 command 구조를 더
잘 분류하고 workspace 밖의 명시적 path를 별도로 물을 수 있다. 그러나 다음은 막지 못한다.</p>

<ul>
  <li>shell expansion, command substitution, script 내부의 동적 path</li>
  <li>parser가 의미를 모르는 executable 자체의 side effect</li>
  <li>network, process, credential, device 접근</li>
  <li>workspace 안에서의 destructive command</li>
  <li>allow된 interpreter가 실행하는 다음 단계 code</li>
</ul>

<p>OpenCode의 shell은 container, chroot, seccomp, macOS Seatbelt 같은 OS sandbox 안에서 실행되지 않는다.
permission은 <strong>사용자 승인과 audit UX</strong>이지 <strong>악성/오작동 code의 capability containment</strong>가 아니다
[D].</p>

<h3 id="105-plugincustom-tool도-같은-host-authority를-쓴다">10.5 plugin·custom tool도 같은 host authority를 쓴다</h3>

<p>local JS/TS plugin과 NPM plugin은 runtime process 안에서 import된다. plugin context에는 SDK client,
directory/worktree와 Bun shell <code class="language-plaintext highlighter-rouge">$</code>가 주어지며 hook/custom tool은 사용자 계정 권한으로 실행된다.
custom tool도 arbitrary language를 shell로 감쌀 수 있고, 같은 이름이면 built-in을 override할 수 있다
(<a href="https://opencode.ai/docs/plugins/">Plugins 문서</a>, <a href="https://opencode.ai/docs/custom-tools/">Custom Tools 문서</a>,
<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/tool/registry.ts"><code class="language-plaintext highlighter-rouge">tool/registry.ts</code></a>) [A/B].</p>

<p>따라서 <code class="language-plaintext highlighter-rouge">.opencode</code>와 config plugin 목록은 단순 prompt data가 아니라 executable supply-chain
boundary다. NPM plugin auto-install, Bun dependency installation, local plugin import에 signing이나
separate process sandbox는 없다. 신뢰하지 않는 repository를 열 때는 config/plugin/tool/command
파일을 code execution surface로 봐야 한다 [D].</p>

<h3 id="106-subagent-권한-전파">10.6 subagent 권한 전파</h3>

<p><a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/agent/subagent-permissions.ts#L4-L26"><code class="language-plaintext highlighter-rouge">subagent-permissions.ts#L4-L26</code></a>는
child session에 <strong>parent session row에 저장된</strong> explicit deny와 external-directory rule을 전달하고,
child가 별도로 허용하지 않으면 nested <code class="language-plaintext highlighter-rouge">task</code>와 <code class="language-plaintext highlighter-rouge">todowrite</code>를 deny한다 [A].</p>

<p>이 범위에는 parent agent의 native/default rule과 agent-level deny가 포함되지 않는다. 해당 restriction은
parent agent 자신만 지배하고 child의 주 capability는 child agent 자체 permission이 결정한다. 따라서
이는 일반적인 “parent hard deny 보존”이나 capability-subset lattice가 아니라 <strong>session policy의 deny
일부 보존 + child-specific policy</strong>다. Parent agent의 shell deny만 믿고 delegation의 side effect를
막을 수는 없다 [A/D].</p>

<h2 id="11-agents-subagents와-background-work">11. agents, subagents와 background work</h2>

<p>OpenCode agent config는 primary/subagent/all mode, prompt, model/variant, temperature/top-p, provider
options, step limit, permission rules를 지정한다. native source에는 <code class="language-plaintext highlighter-rouge">build</code>, <code class="language-plaintext highlighter-rouge">plan</code>, <code class="language-plaintext highlighter-rouge">general</code>, <code class="language-plaintext highlighter-rouge">explore</code>와
hidden <code class="language-plaintext highlighter-rouge">compaction</code>, <code class="language-plaintext highlighter-rouge">title</code>, <code class="language-plaintext highlighter-rouge">summary</code>가 있다 [A]. 공식 문서가 언급하는 <code class="language-plaintext highlighter-rouge">Scout</code>는 고정 commit의
native registry에 존재하지 않는다.</p>

<h3 id="111-foreground-subagent">11.1 foreground subagent</h3>

<p><a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/tool/task.ts"><code class="language-plaintext highlighter-rouge">tool/task.ts</code></a>는
parent ID를 가진 child session을 만들고 선택한 subagent, title, permission을 설정한다. <code class="language-plaintext highlighter-rouge">task_id</code>를
다시 주면 기존 child session을 resume할 수 있다 [A].</p>

<p>이 설계의 좋은 점:</p>

<ul>
  <li>child 대화를 별도 session/transcript로 유지해 parent context를 무한히 오염시키지 않는다.</li>
  <li>parent-child relation이 DB에 남아 UI에서 tree로 탐색할 수 있다.</li>
  <li>task result만 parent tool output으로 돌아오므로 orchestration boundary가 분명하다.</li>
  <li>동일 child resume를 protocol로 지원한다.</li>
</ul>

<p>기본 task는 foreground에서 child가 끝날 때까지 기다린다. 서로 다른 child session과 V1 tool call은
동시에 실행될 수 있지만, 한 session의 active run은 local <code class="language-plaintext highlighter-rouge">SessionRunState</code>가 직렬화한다.</p>

<h3 id="112-background-subagent는-experimentalprocess-local이다">11.2 background subagent는 experimental·process-local이다</h3>

<p>background mode는 <code class="language-plaintext highlighter-rouge">OPENCODE_EXPERIMENTAL_BACKGROUND_SUBAGENTS</code> flag 뒤에 있다. core의
<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/core/src/background-job.ts#L114-L123"><code class="language-plaintext highlighter-rouge">background-job.ts#L114-L123</code></a>는
registry가 scoped, process-local이고 intentionally non-durable라고 주석으로 명시한다 [A].</p>

<p>완료 후 synthetic parent prompt로 결과를 주입하고 parent loop를 다시 깨울 수 있으나:</p>

<ul>
  <li>process restart나 scope close 시 running job과 status를 잃는다.</li>
  <li>active execution은 interrupted된다.</li>
  <li>completed/error/cancelled entry도 map에 남고 instance disposal 전 자동 retention bound가 없다.</li>
  <li>durable session row가 있어도 scheduler/job ownership 자체가 복구되는 것은 아니다.</li>
</ul>

<p>따라서 이는 편리한 local concurrency이지 durable workflow engine이 아니다. 장시간 task, 예약 실행,
다중 process recovery에는 Farad scheduler처럼 별도의 persisted run state와 lease/recovery policy가
필요하다 [D].</p>

<h3 id="113-v2-concurrency-의미">11.3 V2 concurrency 의미</h3>

<p>V2 local execution service는 기록된 tool call들을 eager start해 병렬 settlement하고, 같은 session의
runner는 하나로 조정한다. 서로 다른 session은 concurrent하다. 그러나 runner TODO가 multi-node
ownership을 남겨 둔 만큼 두 server process가 같은 DB/session을 실행하는 clustered correctness는
현재 보장으로 간주하면 안 된다 [A/D].</p>

<h2 id="12-provider와-model-abstraction">12. provider와 model abstraction</h2>

<p>공식 <a href="https://opencode.ai/docs/providers/">Providers</a>와 <a href="https://opencode.ai/docs/models/">Models</a> 문서는
AI SDK 및 Models.dev를 통한 75개 이상의 provider와 local model 지원을 설명한다 [B]. 소스의
<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/provider/provider.ts"><code class="language-plaintext highlighter-rouge">provider/provider.ts</code></a>는
다음 source를 merge한다 [A].</p>

<ul>
  <li>Models.dev catalog</li>
  <li>config에 선언한 provider/model</li>
  <li>auth/environment credential</li>
  <li>internal provider plugin과 provider별 option transform</li>
  <li>custom provider SDK</li>
  <li>whitelist/blacklist, variant, capability normalization</li>
</ul>

<p>SDK module은 필요할 때 dynamic import한다. model family에 따라 reasoning, message, tool schema,
cache/usage metadata를 normalize하고, GPT 계열에는 <code class="language-plaintext highlighter-rouge">apply_patch</code>, 다른 계열에는 edit/write 같은 tool
구성을 선택한다. 이 덕분에 사용자는 provider를 바꿔도 session/tool UX를 유지할 수 있다.</p>

<p>trade-off는 명확하다. provider-specific quirk와 AI SDK version 변화가 <code class="language-plaintext highlighter-rouge">Provider</code>/transform layer에
계속 축적된다. “provider-neutral”은 복잡성이 없다는 뜻이 아니라 <strong>복잡성을 adapter 중앙으로
옮겼다</strong>는 뜻이다. 이 중앙화는 테스트 가능성에는 유리하지만 변경 반경이 크다 [D].</p>

<p>Models.dev catalog는 disk snapshot/cache/network source를 병합하고 cross-process file lock으로
갱신한다. process 안에서는 장기 cache하며 주기적으로 약 60분마다 refresh하고 disk freshness는 약
5분을 본다 (<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/core/src/models-dev.ts"><code class="language-plaintext highlighter-rouge">models-dev.ts</code></a>) [A].</p>

<p>좋은 점:</p>

<ul>
  <li>provider package를 모두 startup에 import하지 않는다.</li>
  <li>model capability와 variant를 catalog data로 관리한다.</li>
  <li>auth/env/config의 우선순위를 한 service가 해석한다.</li>
  <li>AI SDK retry를 끄고 OpenCode가 status/event와 함께 retry를 제어한다.</li>
</ul>

<p>주의할 점:</p>

<ul>
  <li>provider adapter는 핵심 변경 hotspot이다.</li>
  <li>catalog freshness와 runtime config freshness의 정책이 서로 다르다.</li>
  <li>모델별 tool 선택·prompt transform이 늘면 behavior matrix가 폭발할 수 있다.</li>
  <li>OpenCode의 일반 목적 portability와 달리 Farad는 GLM/Kimi 주력 모델의 품질을 먼저 보장하면서
adapter contract만 개방하는 편이 운영상 단순하다 [D].</li>
</ul>

<h2 id="13-tools와-execution-semantics">13. tools와 execution semantics</h2>

<h3 id="131-registry">13.1 registry</h3>

<p><a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/tool/registry.ts"><code class="language-plaintext highlighter-rouge">tool/registry.ts</code></a>는
built-in, custom/plugin, MCP tool을 model/agent/config/permission에 맞춰 resolve한다. permission이
전역 <code class="language-plaintext highlighter-rouge">deny</code>인 tool은 model-visible schema에서도 숨길 수 있고, model family에 맞춰 edit primitive를
선택한다 [A].</p>

<p>OpenCode tool model의 장점은 schema 노출과 execution context가 한 registry에서 결합된다는 것이다.
tool context는 session/message/call ID, abort signal, metadata update, permission ask를 받는다. processor는
tool lifecycle을 part로 저장하므로 UI, resume, usage accounting이 일관된다.</p>

<h3 id="132-output-bound와-cancellation">13.2 output bound와 cancellation</h3>

<p>shell은 기본 timeout을 두고 abort/timeout 때 process group을 종료한 뒤 3초 후 force kill한다.
출력이 line/byte limit을 넘으면 bounded preview만 model에 보내고 전체 output을 XDG data directory의
<code class="language-plaintext highlighter-rouge">tool-output</code>에 저장한다. 기본 limit은 2,000 line/50 KiB이고 <code class="language-plaintext highlighter-rouge">tool_output</code> config로 조정할 수 있다
(<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/tool/shell.ts#L533-L594"><code class="language-plaintext highlighter-rouge">shell.ts#L533-L594</code></a>,
<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/tool/truncate.ts"><code class="language-plaintext highlighter-rouge">truncate.ts</code></a>) [A].</p>

<p>이는 중요한 production discipline이다.</p>

<ul>
  <li>stdout 폭주가 context와 UI를 무한히 점유하지 않는다.</li>
  <li>전체 artifact를 잃지 않고 model-visible result만 제한한다.</li>
  <li>abort가 단순 flag가 아니라 child process kill까지 연결된다.</li>
  <li>tool metadata가 preview/exit/truncation path를 보존한다.</li>
</ul>

<p>full artifact에는 고정 7일 retention과 약 1분 뒤 시작해 매시간 반복하는 cleanup이 구현돼 있다.
남는 운영 쟁점은 XDG data가 local인지 NFS인지, secret-bearing output의 접근권한, retention 설정
가능성, cleanup 전 crash residue다. 또한 child가 detached descendant를 별도로 만들거나 remote side
effect를 낸 경우 local process kill만으로 transaction rollback은 되지 않는다.</p>

<h3 id="133-revert는-tool-side-effect-전체의-transaction이-아니다">13.3 revert는 tool side effect 전체의 transaction이 아니다</h3>

<p>OpenCode는 session snapshot/diff와 undo/revert UX를 제공하지만 이것은 Git/worktree file change에
초점을 둔다. 이미 실행한 network call, package publish, database mutation, process side effect까지
일괄 보상하는 distributed transaction이 아니다. “세션을 revert했다”와 “모든 tool side effect가
없어졌다”를 구분해야 한다 [B/D].</p>

<h2 id="14-config-instruction과-refresh-semantics">14. config, instruction과 refresh semantics</h2>

<h3 id="141-config-precedence">14.1 config precedence</h3>

<p>공식 <a href="https://opencode.ai/docs/config/">Config 문서</a>와 source의 merge path를 합치면 precedence는
낮은 쪽에서 높은 쪽으로 대략 다음과 같다 [A/B].</p>

<ol>
  <li>remote <code class="language-plaintext highlighter-rouge">.well-known</code> organization config</li>
  <li>global user config</li>
  <li><code class="language-plaintext highlighter-rouge">OPENCODE_CONFIG</code>가 가리키는 config</li>
  <li>project config</li>
  <li><code class="language-plaintext highlighter-rouge">.opencode</code> directory의 구성</li>
  <li><code class="language-plaintext highlighter-rouge">OPENCODE_CONFIG_CONTENT</code> inline config</li>
  <li>managed config file</li>
  <li>macOS MDM managed preference</li>
</ol>

<p>일반 remote org config는 default 성격이라 local/project가 override할 수 있고, managed layer가 실제
enforcement 위치다. enterprise policy라면 이 둘을 혼동하면 안 된다.</p>

<p>config는 per-instance service에서 cache된다. explicit invalidation/reload는 가능하지만 runtime hot
path에서 매번 full discovery하지는 않는다. 반대로 reload signal은 config 한 항목의 dependency
closure만 갱신하기보다 instance들을 dispose/reboot하는 넓은 경계다.</p>

<h3 id="142-instruction은-provider-turn마다-io를-다시-할-수-있다">14.2 instruction은 provider turn마다 I/O를 다시 할 수 있다</h3>

<p><a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/session/instruction.ts"><code class="language-plaintext highlighter-rouge">session/instruction.ts</code></a>는
global/project <code class="language-plaintext highlighter-rouge">AGENTS.md</code>, Claude-compatible fallback, configured glob/path, remote URL instruction을
조립한다. file read는 제한된 concurrency로, remote URL은 timeout과 제한된 concurrency로 가져온다.
V1 <code class="language-plaintext highlighter-rouge">runLoop</code>는 provider step의 context assembly에서 <code class="language-plaintext highlighter-rouge">Instruction.system()</code>을 다시 호출한다 [A].</p>

<p>로컬 developer UX에서는 외부 편집을 즉시 반영하는 장점이 있다. 그러나 다음 비용이 있다.</p>

<ul>
  <li>한 user turn 안의 여러 provider/tool step에서도 path discovery와 content read가 반복될 수 있다.</li>
  <li>작은 instruction file이 여러 개인 NFS에서는 file별 read round-trip이 hot path가 된다.</li>
  <li>remote URL latency/failure가 prompt assembly에 들어온다.</li>
  <li>resume 결과가 같은 transcript라도 live file/URL 변화에 따라 달라질 수 있다.</li>
</ul>

<p>V2 context epoch가 byte-stable baseline을 저장하는 이유가 이 문제와 연결된다. Farad의 explicit
refresh + transcript marker contract와 비교하면 V1 OpenCode는 <strong>freshness를 더 높이고 reproducibility와
NFS predictability를 더 적게 보장</strong>한다 [D].</p>

<h2 id="15-skills-commands와-plugins">15. skills, commands와 plugins</h2>

<h3 id="151-skills">15.1 skills</h3>

<p>OpenCode는 project/global <code class="language-plaintext highlighter-rouge">.opencode</code>, Claude-compatible <code class="language-plaintext highlighter-rouge">.claude</code>, agent-compatible <code class="language-plaintext highlighter-rouge">.agents</code>, custom
path와 remote index에서 skill을 발견한다. frontmatter를 parse하고 description을 model에 광고하며,
model이 <code class="language-plaintext highlighter-rouge">skill</code> tool로 선택하면 본문과 일부 연관 file을 context에 넣는다
(<a href="https://opencode.ai/docs/skills/">Agent Skills 문서</a>,
<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/skill/discovery.ts"><code class="language-plaintext highlighter-rouge">skill/discovery.ts</code></a>,
<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/skill/index.ts"><code class="language-plaintext highlighter-rouge">skill/index.ts</code></a>) [A/B].</p>

<p>여기서 “on demand”의 정확한 의미가 중요하다. model context에는 description만 먼저 노출되고 본문은
tool 선택 후 들어가므로 token 측면에서는 on demand다. 그러나 discovery 시점에는 여러 directory를
scan하고 <code class="language-plaintext highlighter-rouge">SKILL.md</code> frontmatter/본문을 읽어 object로 보유한다. 즉 <strong>disk I/O까지 lazy인 것은 아니다</strong>
[A]. shared NFS에서 수백 개 skill을 두면 startup/instance boot 비용이 커질 수 있다 [D].</p>

<p>중복 이름은 warning 후 우선순위에 따라 하나가 이긴다. remote skill index는 cache version을 staging한
뒤 atomic swap해 partial update를 줄인다. 다만 versionless file의 freshness와 cache invalidation은
명시적 운영 고려가 필요하다.</p>

<h3 id="152-commands">15.2 commands</h3>

<p>custom command는 Markdown/JSON으로 prompt template, argument, agent/model, subtask 여부를 정의한다.
<code class="language-plaintext highlighter-rouge">$ARGUMENTS</code>, positional argument, <code class="language-plaintext highlighter-rouge">@file</code>, <code class="language-plaintext highlighter-rouge">!</code> shell interpolation을 지원한다
(<a href="https://opencode.ai/docs/commands/">Commands 문서</a>) [B].</p>

<p>강력하지만 <code class="language-plaintext highlighter-rouge">!</code> command는 prompt macro expansion 전에 host command를 실행한다. command file도
단순 문서가 아니라 executable trusted config다. repository clone 후 command를 실행할 때 review가
필요하다.</p>

<h3 id="153-plugins와-hook-failure-boundary">15.3 plugins와 hook failure boundary</h3>

<p><a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/plugin/index.ts"><code class="language-plaintext highlighter-rouge">plugin/index.ts</code></a>는
local/NPM plugin을 instance init에서 deterministic order로 import하고 hook을 등록한다. config, auth,
event, dispose, tool-before/after, chat params/system/messages, shell env, compaction 등 넓은 interception
surface를 제공한다 [A].</p>

<p>평가:</p>

<ul>
  <li>확장성: 매우 높다. tool 추가뿐 아니라 core flow를 좁은 hook으로 바꿀 수 있다.</li>
  <li>변경 용이성: built-in core를 fork하지 않고 organization behavior를 넣을 수 있다.</li>
  <li>failure isolation: 낮다. 일반 hook은 같은 process에서 await되며 exception이 host flow를 실패시킬
수 있다. event hook은 fire-and-forget 성격이라 ordering/completion을 보장하기 어렵다.</li>
  <li>보안: 낮다. arbitrary code이며 별도 capability manifest나 sandbox가 없다.</li>
  <li>성능: hook 수와 I/O가 provider/tool hot path에 직접 추가될 수 있다.</li>
</ul>

<p><code class="language-plaintext highlighter-rouge">--pure</code>로 외부 plugin을 끌 수 있는 escape hatch는 유용하지만, 안전한 extension architecture는
process isolation, declared capability, timeout/circuit breaker, deterministic ordering, versioned API를
더 요구한다 [D].</p>

<h2 id="16-mcp-architecture">16. MCP architecture</h2>

<p>OpenCode는 local stdio와 remote Streamable HTTP를 지원하고, remote가 필요하면 SSE fallback을 쓴다.
OAuth와 RFC 7591 dynamic registration을 지원하며 token은 <code class="language-plaintext highlighter-rouge">mcp-auth.json</code>에 제한된 file mode로
저장한다 (<a href="https://opencode.ai/docs/mcp-servers/">MCP 문서</a>) [A/B].</p>

<h3 id="161-lifecycle과-catalog">16.1 lifecycle과 catalog</h3>

<p><a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/mcp/index.ts"><code class="language-plaintext highlighter-rouge">mcp/index.ts</code></a>는
per-instance service다. enabled server들을 instance initialization에서 연결하고 client/status를
보유한다. close notification과 tool-list-change를 감시하며 finalizer가 client와 descendant process를
정리한다 [A].</p>

<p>tool definition은 catalog에 cache하고 tool-list-change notification으로 좁게 다시 가져온다. prompt,
resource, resource template는 별도 OpenCode tool로 list/read한다. MCP instruction이 있으면 permission을
통과한 MCP tool이 하나 이상 노출될 때 system context에 넣는다.</p>

<p><a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/mcp/catalog.ts"><code class="language-plaintext highlighter-rouge">mcp/catalog.ts</code></a>는
pagination을 최대 1,000 page로 제한하고 duplicate cursor를 감지한다 [A]. 무한/악성 pagination을
막는 좋은 bound다. tool name은 <code class="language-plaintext highlighter-rouge">server_tool</code> 형태로 sanitize하고 tool schema의 추가 property를
막으며 progress가 오면 timeout을 갱신한다.</p>

<h3 id="162-capability와-제한">16.2 capability와 제한</h3>

<p>현재 구현은 tools, prompts, resources, resource templates를 client 관점에서 지원한다. roots capability는
있지만 sampling, elicitation, MCP task는 source에 TODO/commented 상태다 [A]. resource attachment는
지원 MIME과 약 10 MiB size로 제한한다.</p>

<p>운영상 주의:</p>

<ul>
  <li>instance boot 시 여러 enabled server connect를 시작하므로 server 수가 많으면 startup burst가 난다.</li>
  <li>많은 MCP tool schema는 model context 자체를 크게 만든다. 공식 문서도 이를 경고한다.</li>
  <li>config 변경의 delta reconcile보다 instance reload가 넓은 경계다. tool-list-change는 좁지만 server
config refresh는 별개다.</li>
  <li>remote MCP는 제3자 server와 data/tool authority를 공유하는 trust boundary다.</li>
  <li>문서/schema의 기본 timeout 5초와 current source의 30초가 어긋난다. 운영 SLO는 source 기준으로
검증해야 한다.</li>
</ul>

<p>Farad가 가져올 가치가 큰 부분은 bounded pagination, duplicate-cursor guard, progress-aware timeout,
resource MIME/size bound, tool-list-change의 narrow invalidation이다. 시작 때 모든 MCP를 병렬 연결하고
whole instance reload로 바꾸는 방식은 Farad의 explicit delta reconcile과 맞지 않는다 [D].</p>

<h3 id="163-lsp는-file-triggered-process-service다">16.3 LSP는 file-triggered process service다</h3>

<p>OpenCode의 LSP는 MCP와 별개의 per-instance process subsystem이다. 고정 source의
<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/lsp/lsp.ts#L112-L136"><code class="language-plaintext highlighter-rouge">lsp.ts#L112-L136</code></a>는
client, server definition, broken root/server pair, spawning promise를 instance state로 소유하고,
hover/definition/reference/implementation/symbol/call-hierarchy/diagnostic API를 제공한다 [A].</p>

<p>공식 <a href="https://opencode.ai/docs/lsp/">LSP 문서</a> 기준 built-in LSP는 기본 disabled이며 <code class="language-plaintext highlighter-rouge">lsp: true</code>로
명시적으로 켠다. 자동 server download도 <code class="language-plaintext highlighter-rouge">OPENCODE_DISABLE_LSP_DOWNLOAD=true</code>로 끌 수 있다. 문서가
language server의 높은 메모리, stale state, version/project 편차를 직접 경고하고 lint/typecheck CLI가
더 나은 경우를 인정하는 점은 운영적으로 정직하다 [B].</p>

<p>모든 language server를 무조건 startup에 띄우지는 않는다. file extension과 root detector가 맞는 file을
처음 touch할 때 process를 spawn하고, 같은 root/server의 concurrent spawn은 promise로 합친다. 실패한
root/server key는 instance 수명 동안 <code class="language-plaintext highlighter-rouge">broken</code>으로 기억하고, finalizer는 열린 client들을 shutdown한다
(<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/lsp/lsp.ts#L198-L297"><code class="language-plaintext highlighter-rouge">lsp.ts#L198-L297</code></a>) [A].
read는 warm-up 실패를 본래 read 실패로 승격하지 않고, edit/write 뒤에는 diagnostic을 model output에
붙인다. 이는 optional enrichment의 failure를 core file operation과 분리한 좋은 경계다.</p>

<p>반면 <code class="language-plaintext highlighter-rouge">broken</code>은 자동 backoff/retry state가 아니라 process-local suppression이고, root별 LSP client 수에
capacity bound가 없다. monorepo에서 root가 매우 많이 생기거나 오래 사는 headless instance에서는
process 수가 누적될 수 있다. Farad가 유사 기능을 도입한다면 lazy spawn과 finalizer는 채택하되,
root/client LRU, explicit retry, resource budget, crash status를 함께 계약해야 한다 [D].</p>

<h2 id="17-ui와-human-in-the-loop">17. UI와 human-in-the-loop</h2>

<p>OpenCode의 UX를 “TUI 하나”로 평가하면 현재 제품을 놓친다. 실제로는 OpenTUI 기반 terminal surface와
SolidJS 기반 shared app surface가 같은 server event/message authority를 서로 다른 projection으로
표현한다. shared app은 browser와 Electron desktop에서 재사용되고, <code class="language-plaintext highlighter-rouge">session-ui</code>와 <code class="language-plaintext highlighter-rouge">ui</code> package가
message part·diff·dock·primitive를 제공한다 [A].</p>

<h3 id="171-client는-authority가-아니라-projection이다">17.1 client는 authority가 아니라 projection이다</h3>

<p>웹/데스크톱 app의 session store는 server projection을 page로 읽고 SSE delta를 병합한다. initial message
page는 2개, 과거 history prepend는 200개이며, cursor가 있으면 사용자가 위로 올라갈 때 더 가져온다
(<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/app/src/context/server-session.ts#L21-L26"><code class="language-plaintext highlighter-rouge">server-session.ts#L21-L26</code></a>,
<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/app/src/context/server-session.ts#L599-L703"><code class="language-plaintext highlighter-rouge">server-session.ts#L599-L703</code></a>) [A].
HTTP page를 가져오는 동안 도착한 live delta, optimistic message, 제거 event를 잃지 않도록 touched/retained/
carried-delta marker를 병합한다. 이 부분은 단순 REST fetch보다 훨씬 성숙하다.</p>

<p>SSE disconnect가 exactly-once transport를 제공하지 않으므로 client correctness의 실제 형태는 다음과
같다.</p>

<pre class="mermaid">
flowchart LR
    DB[(Server SQLite projection)] --&gt;|keyset page / item fetch| STORE[Client normalized store]
    SSE[SSE best-effort deltas] --&gt; MERGE[Generation-aware merge]
    OPT[Optimistic local item] --&gt; MERGE
    MERGE --&gt; STORE
    STORE --&gt; PROJ[Timeline projection]
    PROJ --&gt; VIRT[Virtual visible rows]
    GAP[Reconnect / stale cache] --&gt;|bounded refetch| DB

</pre>

<p>즉 현재 shared app의 legacy path에서는 event가 invalidation·low-latency update이고 REST projection이
복구 기준이다. 이는 합리적이지만 legacy server event queue에는 overflow signal이나 replay cursor가
없으므로 reconnect code가 누락을 감지할 수 있다는 전제가 중요하다. Typed V2 API에는 durable
<code class="language-plaintext highlighter-rouge">after</code> replay stream이 있으나 이 app path의 복구 계약으로 아직 통합되지 않았다 [A/D].</p>

<h3 id="172-긴-timeline의-화면-비용은-잘-다뤘다">17.2 긴 timeline의 화면 비용은 잘 다뤘다</h3>

<p><a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/app/src/pages/session/timeline/message-timeline.tsx#L401-L445"><code class="language-plaintext highlighter-rouge">message-timeline.tsx#L401-L445</code></a>는
TanStack virtualizer로 visible range와 작은 overscan만 DOM에 두고, active row와 resize 중 visible row만
pin한다. measurement/tool-open cache도 session 16개로 제한한다
(<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/app/src/pages/session/timeline/message-timeline.tsx#L531-L535"><code class="language-plaintext highlighter-rouge">message-timeline.tsx#L531-L535</code></a>) [A].
scroll anchor, prepend history, variable-height reasoning/tool/diff row까지 별도 logic으로 다룬다.</p>

<p>다만 DOM virtualization과 data-model 복잡도는 별개다. timeline projection은 현재 client에 적재된
message를 순회해 <code class="language-plaintext highlighter-rouge">messageByID</code>, assistant-parent map, row/index map을 만든다
(<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/app/src/pages/session/timeline/projection.ts#L19-L96"><code class="language-plaintext highlighter-rouge">projection.ts#L19-L96</code></a>) [A].
따라서 현재 loaded window에서는 대략 O(N) projection이고, 10,000 turn을 전부 prepend한 뒤의 live
delta가 항상 O(visible)이라고 증명되지는 않는다. 초기 page와 virtual DOM 덕분에 일반 open path는
bounded지만 “사용자가 전체 history를 로드한 장시간 tab”은 별도 stress 대상이다 [D].</p>

<h3 id="173-permission과-question은-독립-interaction-protocol이다">17.3 permission과 question은 독립 interaction protocol이다</h3>

<p>permission request는 단순 modal이 아니라 session 실행을 멈추는 typed HITL interaction이다. shared app의
<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/app/src/pages/session/composer/session-permission-dock.tsx"><code class="language-plaintext highlighter-rouge">session-permission-dock.tsx</code></a>는
permission 설명과 exact pattern을 보여주고 <code class="language-plaintext highlighter-rouge">Deny / Allow always / Allow once</code>를 구분한다 [A]. TUI는
tool별 상세 preview를 제공하며, 현재 V1 interaction의 “always”가 <strong>OpenCode restart까지만</strong>
유효하다는 문구도 명시한다
(<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/tui/src/routes/session/permission.tsx#L138-L170"><code class="language-plaintext highlighter-rouge">permission.tsx#L138-L170</code></a>) [A].
이는 project-scoped saved rule을 제공하는 V2 permission API와 구별해야 한다.</p>

<p>질문은 single/multi select, 여러 tab, custom answer, reject를 지원한다. web app은 답변 도중 component가
unmount되어도 process-local cache로 draft를 유지하고, failed mutation은 toast로 되돌린다
(<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/app/src/pages/session/composer/session-question-dock.tsx#L64-L129"><code class="language-plaintext highlighter-rouge">session-question-dock.tsx#L64-L129</code></a>,
<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/app/src/pages/session/composer/session-question-dock.tsx#L209-L247"><code class="language-plaintext highlighter-rouge">session-question-dock.tsx#L209-L247</code></a>) [A].</p>

<p>HITL UX의 장점:</p>

<ul>
  <li>질문과 permission이 transcript text parsing이 아니라 typed request/reply다.</li>
  <li>child session의 pending request도 parent surface가 모아 보여 줄 수 있다.</li>
  <li>keyboard/TUI와 mouse/web 양쪽에 동등한 승인·질문 경로가 있다.</li>
  <li>permission pattern과 tool context를 사용자에게 노출한다.</li>
  <li>retry status, countdown, provider-specific action도 별도 typed UI로 표현한다.</li>
</ul>

<p>주의할 점:</p>

<ul>
  <li>app과 TUI 모두 auto-approve mode를 제공한다. 이것은 containment가 아니라 prompt를 자동으로 <code class="language-plaintext highlighter-rouge">once</code>
승인하는 client policy다.</li>
  <li>web app의 auto-respond 중복 방지 map은 1,000개/1시간으로 잘 제한되지만, 승인 authority는 server의
process-local pending request다. V2의 saved rule도 pending interaction 자체를 durable하게 만들지는 않는다
(<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/app/src/context/permission.tsx#L104-L139"><code class="language-plaintext highlighter-rouge">permission.tsx#L104-L139</code></a>) [A].</li>
  <li>client가 끊긴 채 permission이 pending이면 run은 기다린다. 별도 unattended timeout/attendance policy는
이 permission UX 자체에는 없다.</li>
  <li>V1 “always”가 reload/dispose/restart 뒤 사라지는 것은 안전한 쪽으로 fail하지만 durable automation에는
예측 불가능성을 만든다. V2에서는 <code class="language-plaintext highlighter-rouge">save</code> resource가 있는 project rule만 restart 뒤 유지된다.</li>
</ul>

<h3 id="174-ui-cache에는-실제-bound가-있다">17.4 UI cache에는 실제 bound가 있다</h3>

<p>server cache와 달리 shared app에는 명시적인 retention guard가 여러 개 있다 [A].</p>

<table>
  <thead>
    <tr>
      <th>client state</th>
      <th style="text-align: right">bound/정책</th>
      <th>source</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>workspace child stores</td>
      <td style="text-align: right">최대 30, idle 20분, active/pinned/booting 보호</td>
      <td><a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/app/src/context/global-sync/types.ts#L143-L146"><code class="language-plaintext highlighter-rouge">types.ts#L143-L146</code></a></td>
    </tr>
    <tr>
      <td>hydrated session caches</td>
      <td style="text-align: right">LRU 성격 40, active/inflight/HITL 보호</td>
      <td><a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/app/src/context/global-sync/session-cache.ts#L11-L65"><code class="language-plaintext highlighter-rouge">session-cache.ts#L11-L65</code></a></td>
    </tr>
    <tr>
      <td>session info seen set</td>
      <td style="text-align: right">2,048</td>
      <td><a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/app/src/context/server-session.ts#L24-L27"><code class="language-plaintext highlighter-rouge">server-session.ts#L24-L27</code></a></td>
    </tr>
    <tr>
      <td>recent root overflow</td>
      <td style="text-align: right">최근 4시간 중 최대 50 추가 유지</td>
      <td><a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/app/src/context/global-sync/types.ts#L145-L146"><code class="language-plaintext highlighter-rouge">types.ts#L145-L146</code></a></td>
    </tr>
    <tr>
      <td>timeline measurement cache</td>
      <td style="text-align: right">16 session</td>
      <td><a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/app/src/pages/session/timeline/message-timeline.tsx#L531-L535"><code class="language-plaintext highlighter-rouge">message-timeline.tsx#L531-L535</code></a></td>
    </tr>
    <tr>
      <td>permission auto-response dedup</td>
      <td style="text-align: right">1,000개, 1시간 TTL</td>
      <td><a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/app/src/context/permission.tsx#L104-L119"><code class="language-plaintext highlighter-rouge">permission.tsx#L104-L119</code></a></td>
    </tr>
  </tbody>
</table>

<p>이것은 OpenCode를 단순히 “전부 unbounded”라고 부르면 틀린 이유다. 정확한 판정은 <strong>현재 app projection은
상당히 bounded하고 virtualized되어 있지만, server instance/event/background registry와 V1 model hot
path는 같은 수준의 bound를 아직 갖지 못했다</strong>이다.</p>

<h2 id="18-failure-recovery와-observability">18. failure, recovery와 observability</h2>

<h3 id="181-failure-matrix">18.1 failure matrix</h3>

<table>
  <thead>
    <tr>
      <th>실패</th>
      <th>현재 처리</th>
      <th>복구 기준</th>
      <th>남는 위험</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>provider 429/5xx/transient error</td>
      <td>OpenCode-owned exponential retry, retry status/event</td>
      <td>같은 active process/run</td>
      <td>retry 횟수·전체 deadline이 명시적으로 bounded되지 않음</td>
    </tr>
    <tr>
      <td>context overflow</td>
      <td>auto compaction 또는 terminal error</td>
      <td>transcript + summary</td>
      <td>compaction 자체 실패·두 번째 overflow 처리 제한</td>
    </tr>
    <tr>
      <td>user abort</td>
      <td>stream interrupt, tool을 interrupted error로 settlement</td>
      <td>persisted message/part</td>
      <td>외부 side effect rollback 불가</td>
    </tr>
    <tr>
      <td>shell timeout</td>
      <td>process group terminate 후 force kill</td>
      <td>tool error + truncated artifact</td>
      <td>detached/remote side effect는 남음</td>
    </tr>
    <tr>
      <td>plugin hook exception</td>
      <td>caller flow 실패 가능</td>
      <td>log/restart/config 수정</td>
      <td>process isolation/circuit breaker 없음</td>
    </tr>
    <tr>
      <td>MCP/LSP connect failure</td>
      <td>status/broken state, instance finalizer</td>
      <td>reconnect 또는 reload</td>
      <td>durable retry state 없음</td>
    </tr>
    <tr>
      <td>legacy client SSE disconnect</td>
      <td>reconnect + projection refetch</td>
      <td>SQLite/REST projection</td>
      <td>legacy route에는 replay cursor/overflow indicator 없음; V2 replay API는 별도</td>
    </tr>
    <tr>
      <td>server crash 중 stream</td>
      <td>DB에 마지막까지 저장된 part</td>
      <td>session reopen/manual continuation</td>
      <td>V1 active ownership·retry status는 비영속</td>
    </tr>
    <tr>
      <td>SQLite crash</td>
      <td>transaction/WAL recovery</td>
      <td>local DB</td>
      <td>WAL을 NFS에 놓는 topology는 부적합</td>
    </tr>
    <tr>
      <td>background subagent 중 crash</td>
      <td>child session 일부는 남을 수 있음</td>
      <td>수동 inspection</td>
      <td>job registry·continuation ownership 유실</td>
    </tr>
  </tbody>
</table>

<h3 id="182-provider-retry는-visible하지만-총량-bound가-없다">18.2 provider retry는 visible하지만 총량 bound가 없다</h3>

<p><a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/session/retry.ts#L26-L66"><code class="language-plaintext highlighter-rouge">retry.ts#L26-L66</code></a>는
<code class="language-plaintext highlighter-rouge">Retry-After</code>/<code class="language-plaintext highlighter-rouge">retry-after-ms</code>를 존중하고, header가 없으면 2초부터 배가해 30초에서 cap한다. processor는
retry 때 session status에 attempt, message, next timestamp, action을 넣어 TUI/app이 countdown과 조치
링크를 보여준다
(<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/session/processor.ts#L627-L681"><code class="language-plaintext highlighter-rouge">processor.ts#L627-L681</code></a>) [A].</p>

<p>그러나 schedule에는 maximum attempt나 total elapsed deadline이 없다. retryable 조건이 계속 유지되면
사용자가 abort하거나 process가 종료될 때까지 기다릴 수 있다. interactive local UX에는 친절하지만,
CI·scheduler·unattended task에는 retry budget이 있어야 한다 [D].</p>

<h3 id="183-cancellation-cleanup은-세심하지만-transaction은-아니다">18.3 cancellation cleanup은 세심하지만 transaction은 아니다</h3>

<p>processor cleanup은 열려 있는 text/reasoning을 종료하고, tool completion을 짧게 기다린 후 남은 call을
<code class="language-plaintext highlighter-rouge">interrupted: true</code> error로 저장하며 assistant 완료 시각까지 기록한다
(<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/session/processor.ts#L540-L597"><code class="language-plaintext highlighter-rouge">processor.ts#L540-L597</code></a>) [A].
session cancel은 연결된 running background job tree도 찾아 취소한다. 이 덕분에 UI에는 영원한 pending
tool보다 명시적인 interrupted state가 남는다.</p>

<p>하지만 이는 runtime record settlement다. file snapshot/revert가 가능한 영역 밖의 network, database,
package publish, credential 사용은 보상되지 않는다. security·workflow 설계에서 “abort가 성공했다”를
“side effect가 원자적으로 취소됐다”로 번역하면 안 된다 [D].</p>

<h3 id="184-restart-recovery의-현재-한계">18.4 restart recovery의 현재 한계</h3>

<p>V1 <code class="language-plaintext highlighter-rouge">SessionStatus</code>와 <code class="language-plaintext highlighter-rouge">SessionRunState</code>는 instance-local map이며 finalizer가 active runner를 cancel한다
(<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/session/status.ts#L21-L48"><code class="language-plaintext highlighter-rouge">status.ts#L21-L48</code></a>,
<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/session/run-state.ts#L35-L68"><code class="language-plaintext highlighter-rouge">run-state.ts#L35-L68</code></a>) [A].
message/part는 durable하지만 “이 session은 retry 중이며 어떤 continuation을 누가 재개해야 하는가”는
durable lease/state machine이 아니다. process가 죽으면 새 process는 transcript를 보여 줄 수 있어도
active run을 자동으로 정확히 이어서 실행한다고 보장할 수 없다.</p>

<p>V2는 durable event replay consumer, admitted input, context epoch, tool/compaction continuation까지
구현해 in-process recovery 기반을 강화했다. 그러나 durable status, multi-node owner lease,
interruption replacement, bounded retry와 <strong>process restart 뒤 continuation ownership</strong>은 여전히
미완이다. 따라서 현재 recovery grade는 <strong>durable transcript/event replay: 강함 / durable execution
restart: 약함</strong>으로 둘로 나눠야 한다 [A/D].</p>

<h3 id="185-관측성">18.5 관측성</h3>

<p>OpenCode server는 기본적으로 XDG log directory의 <code class="language-plaintext highlighter-rouge">opencode.log</code>에 구조화된 key/value line을 append하고,
각 process에 짧은 run ID를 붙인다. log level과 stderr mirror를 환경변수로 제어한다
(<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/core/src/observability/logging.ts"><code class="language-plaintext highlighter-rouge">logging.ts</code></a>) [A].
OTLP endpoint가 있으면 logs와 traces를 내보내고 service version, channel, client, run/instance ID를
resource attribute로 설정한다
(<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/core/src/observability/otlp.ts#L36-L76"><code class="language-plaintext highlighter-rouge">otlp.ts#L36-L76</code></a>) [A].
app error boundary는 build에서 활성화된 경우 Sentry report를 보낼 수 있다.</p>

<p>좋은 점:</p>

<ul>
  <li>Effect span과 structured annotation이 service call에 일관되게 붙는다.</li>
  <li>run ID로 한 process의 file log/OTLP를 묶을 수 있다.</li>
  <li>model usage, tool part, retry status, message error가 사용자 projection에도 남는다.</li>
  <li>debug command와 HTTP log endpoint가 현장 진단 surface를 제공한다.</li>
</ul>

<p>한계:</p>

<ul>
  <li>session/turn/tool 전반에 강제되는 단일 trace/correlation ID 계약은 report에서 확인되지 않았다.</li>
  <li>permission decision과 arbitrary plugin/shell side effect를 immutable audit log로 보존하는 별도 보안
ledger는 없다.</li>
  <li>file log rotation/retention/size bound는 이 logging layer에 드러나지 않는다.</li>
  <li>OTLP export failure와 backpressure가 제품 SLO로 문서화되어 있지 않다.</li>
  <li>V1 status는 durable하지 않으므로 “마지막 관측 상태”와 crash 이후 authority가 갈릴 수 있다.</li>
</ul>

<h3 id="186-server-sharing과-credential-trust-boundary">18.6 server, sharing과 credential trust boundary</h3>

<p>headless API는 session/message만이 아니라 file, shell, PTY, config, MCP/LSP까지 넓게 노출한다. default
loopback bind는 합리적이지만 <code class="language-plaintext highlighter-rouge">0.0.0.0</code> bind와 password 부재도 warning만 하고 계속 실행하며, Basic auth
자체는 TLS가 아니다. directory selector에 per-project ACL/root allowlist가 보이지 않으므로 remote
credential은 사실상 <strong>해당 OS user의 file/shell workspace capability</strong>다 [A/D]. 외부 network에서는
사용자별 daemon, TLS reverse proxy/VPN, 강한 secret, OS user 격리, workspace allowlist가 필요하다.</p>

<p>공식 <a href="https://opencode.ai/docs/share/">Share 문서</a>의 URL sharing도 별도 exfiltration boundary다 [B].
첫 share는 session, full message/part, diff와 사용 model metadata를 읽어 외부 service에 sync하고, 이후
event는 entity key별로 1초 coalesce한다
(<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/share/share-next.ts#L124-L145"><code class="language-plaintext highlighter-rouge">share-next.ts#L124-L145</code></a>,
<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/share/share-next.ts#L274-L299"><code class="language-plaintext highlighter-rouge">share-next.ts#L274-L299</code></a>) [A].
민감 조직은 managed config로 share를 disable해야 하며, 최초 full sync는 큰 session에서 별도 O(N)
DB/CPU/network spike다.</p>

<p>provider credential과 MCP OAuth token은 일반 config와 분리하고 <code class="language-plaintext highlighter-rouge">0600</code>으로 쓴다
(<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/auth/index.ts#L79-L88"><code class="language-plaintext highlighter-rouge">auth/index.ts#L79-L88</code></a>,
<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/mcp/auth.ts#L70-L81"><code class="language-plaintext highlighter-rouge">mcp/auth.ts#L70-L81</code></a>) [A].
좋은 기본이지만 OS keychain/encrypted vault가 아니라 filesystem bearer secret이므로 shared account,
backup, home ACL은 여전히 trust boundary다.</p>

<h2 id="19-성능-nfs와-10000개-규모">19. 성능, NFS와 10,000개 규모</h2>

<p>이 절은 microbenchmark 결과가 아니라 source-level cost model이다. 숫자로 확인하지 않은 부분은 D등급
추론이며, 반증 benchmark는 §24에 명시한다.</p>

<h3 id="191-반복-경로-비용표">19.1 반복 경로 비용표</h3>

<table>
  <thead>
    <tr>
      <th>경로</th>
      <th style="text-align: right">누적 N에 대한 현재 비용</th>
      <th>bound/완화</th>
      <th>판정</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>session 단건 get</td>
      <td style="text-align: right">index lookup에 가까움</td>
      <td>PK</td>
      <td>좋음</td>
    </tr>
    <tr>
      <td>session 목록 첫 화면</td>
      <td style="text-align: right">candidate scan/sort 후 <code class="language-plaintext highlighter-rouge">ORDER BY time_updated LIMIT 100</code> 가능</td>
      <td><code class="language-plaintext highlighter-rouge">time_updated</code>/compound index 없음</td>
      <td>조건부·10k 미검증</td>
    </tr>
    <tr>
      <td>app session open</td>
      <td style="text-align: right">message 2개 + 필요 parent fetch</td>
      <td>cursor page</td>
      <td>좋음</td>
    </tr>
    <tr>
      <td>app 과거 history</td>
      <td style="text-align: right">한 번에 200개 prepend</td>
      <td>explicit user scroll</td>
      <td>합리적</td>
    </tr>
    <tr>
      <td>app DOM render</td>
      <td style="text-align: right">visible rows + overscan</td>
      <td>virtualization</td>
      <td>좋음</td>
    </tr>
    <tr>
      <td>app loaded-window projection</td>
      <td style="text-align: right">O(loaded messages/rows)</td>
      <td>page load가 window를 제한</td>
      <td>조건부</td>
    </tr>
    <tr>
      <td>V1 provider/tool step context</td>
      <td style="text-align: right"><strong>O(full transcript)</strong> hydrate 후 compact filter</td>
      <td>없음</td>
      <td>나쁨</td>
    </tr>
    <tr>
      <td>K-step user turn</td>
      <td style="text-align: right">대략 <strong>O(KN)</strong> history assembly</td>
      <td>compaction도 DB read 전 filter</td>
      <td>나쁨</td>
    </tr>
    <tr>
      <td>V2 post-baseline history</td>
      <td style="text-align: right">O(retained epoch/window)</td>
      <td>sequence query</td>
      <td>좋은 방향, migration 중</td>
    </tr>
    <tr>
      <td>fork</td>
      <td style="text-align: right">prefix message/part 전체 copy</td>
      <td>explicit 1회성</td>
      <td>큰 session에서 비쌈</td>
    </tr>
    <tr>
      <td>initial share sync</td>
      <td style="text-align: right">full transcript 가능</td>
      <td>1회성</td>
      <td>큰 session에서 비쌈</td>
    </tr>
    <tr>
      <td>instance cache</td>
      <td style="text-align: right">visited directory 수에 비례</td>
      <td>explicit dispose만</td>
      <td>long-lived server 위험</td>
    </tr>
    <tr>
      <td>client workspace/session cache</td>
      <td style="text-align: right">30 directory / 40 session 등</td>
      <td>LRU/TTL/protection</td>
      <td>좋음</td>
    </tr>
    <tr>
      <td>legacy SSE buffering</td>
      <td style="text-align: right">slow connection당 event 수</td>
      <td>shipping queue unbounded; <code class="language-plaintext highlighter-rouge">allBounded</code> 미배선</td>
      <td>위험</td>
    </tr>
    <tr>
      <td>background job registry</td>
      <td style="text-align: right">생성 job 수</td>
      <td>instance dispose</td>
      <td>위험</td>
    </tr>
    <tr>
      <td>skill discovery</td>
      <td style="text-align: right">file 수와 content read</td>
      <td>instance init</td>
      <td>startup/NFS 비용</td>
    </tr>
    <tr>
      <td>instruction assembly</td>
      <td style="text-align: right">provider step마다 file/URL I/O 가능</td>
      <td>concurrency/timeout</td>
      <td>hot-path 위험</td>
    </tr>
  </tbody>
</table>

<p>Session list 평가는 특히 보수적으로 봐야 한다. <code class="language-plaintext highlighter-rouge">SessionTable</code> index는 <code class="language-plaintext highlighter-rouge">project_id</code>, <code class="language-plaintext highlighter-rouge">workspace_id</code>,
<code class="language-plaintext highlighter-rouge">parent_id</code>뿐이고 <code class="language-plaintext highlighter-rouge">time_updated</code> 또는 project+updated compound index가 없다. 실제 list query는
<code class="language-plaintext highlighter-rouge">ORDER BY time_updated DESC, id DESC LIMIT 100</code>이므로 반환 크기는 bounded지만 candidate scan/sort는
누적 session 수에 비례할 수 있다
(<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/core/src/session/sql.ts"><code class="language-plaintext highlighter-rouge">session/sql.ts</code></a>,
<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/session/session.ts"><code class="language-plaintext highlighter-rouge">session/session.ts</code></a>) [A/D].</p>

<h3 id="192-local-ssd에서는-sqlite-선택이-타당하다">19.2 local SSD에서는 SQLite 선택이 타당하다</h3>

<p>WAL, <code class="language-plaintext highlighter-rouge">synchronous=NORMAL</code>, indexes, batch part hydration, keyset message cursor는 single-user local tool에
실용적인 조합이다. JSON file 수천 개보다 transaction, search, concurrent UI read가 쉽고, current app의
page loading과 잘 맞는다. V2가 event와 projection을 같은 SQLite transaction에 넣는 것도 local crash
consistency를 개선한다 [A/D].</p>

<h3 id="193-db를-nfs에-두는-것은-별도-문제다">19.3 DB를 NFS에 두는 것은 별도 문제다</h3>

<p>OpenCode의 기본 topology는 XDG data directory의 SQLite DB를 authority로 삼고 WAL을 강제한다. SQLite 공식
<a href="https://sqlite.org/wal.html#overview">WAL 문서</a>는 WAL이 shared-memory 때문에 network filesystem에서
작동하지 않는다고 명시한다 [B]. 따라서 <code class="language-plaintext highlighter-rouge">$XDG_DATA_HOME</code>이나 home이 NFS인 HPC에서 이 DB를 그대로
공유 배치하는 것은 지원 topology로 간주하면 안 된다.</p>

<p><code class="language-plaintext highlighter-rouge">OPENCODE_DB</code> absolute path로 DB 자체를 node-local disk에 둘 수는 있다. 다만 이 flag가 HPC 배포
지원계약으로 문서화되거나 자동 강제되는 것은 확인하지 못했다. 또한 DB를 옮겨도 V1
instruction/skill/config discovery가 NFS workspace를 반복 읽을 수
있다. 특히 provider step마다 instruction glob/file/URL을 재조립하는 path는 file 수 × network round trip
비용을 turn latency에 넣는다. OpenCode의 default freshness는 Farad의 explicit refresh/NFS index 계약과
비용 모델이 다르다 [D].</p>

<h3 id="194-10000-session과-10000-message를-분리해야-한다">19.4 10,000 session과 10,000 message를 분리해야 한다</h3>

<ul>
  <li><strong>10,000 session:</strong> 반환 행은 기본 100으로 bounded지만 <code class="language-plaintext highlighter-rouge">SessionTable</code>에는 <code class="language-plaintext highlighter-rouge">time_updated</code> 또는
<code class="language-plaintext highlighter-rouge">(project_id, time_updated)</code> 계열 index가 없다. <code class="language-plaintext highlighter-rouge">LIMIT</code>는 candidate 전량 scan/sort 비용을 막지 않으므로
첫 화면 latency는 조건부·미검증이다. title <code class="language-plaintext highlighter-rouge">%search%</code>, child 전량 조회, server instance cache와
client 전역 상태 merge까지 <code class="language-plaintext highlighter-rouge">EXPLAIN QUERY PLAN</code>과 heap benchmark가 필요하다.</li>
  <li><strong>한 session에 10,000 message/part:</strong> app은 처음 2개만 가져와 화면 open을 방어한다. 그러나 user가
history를 모두 load한 뒤 projection은 누적 window를 다루고, 더 중요하게 V1 agent step은 처음부터
full transcript를 읽는다. 현재 primary runtime은 이 규모 계약을 만족한다고 볼 수 없다.</li>
  <li><strong>10,000 directory:</strong> app child store는 30개로 제한되지만 server <code class="language-plaintext highlighter-rouge">InstanceStore</code>와 Effect scoped cache는
capacity/idle TTL이 없다. long-lived headless server는 별도 eviction 없이는 부적합하다.</li>
  <li><strong>10,000 events/slow consumer:</strong> legacy SSE queue와 default core PubSub가 unbounded이고 bounded helper가
shipping route에 연결되지 않아 backpressure 증명이 없다.</li>
  <li><strong>10,000 background jobs:</strong> registry가 non-durable이고 retention bound도 없으므로 scheduler 용도가 아니다.</li>
</ul>

<h3 id="195-performance-tests가-증명하는-것과-못-하는-것">19.5 performance tests가 증명하는 것과 못 하는 것</h3>

<p>app에는 별도의 high-volume Playwright suite가 있고 timeline virtualization, streaming delta, tab switch,
review pane, geometry/scroll stability를 진단한다. default streaming scenario는 320 history turn, 160 delta,
30× CPU throttle를 사용한다
(<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/app/e2e/performance/README.md">performance README</a>,
<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/app/e2e/performance/timeline/session-timeline-benchmark.spec.ts"><code class="language-plaintext highlighter-rouge">session-timeline-benchmark.spec.ts</code></a>) [A].</p>

<p>그러나 README가 직접 밝히듯 suite는 normal CI discovery에서 제외되고, machine-dependent performance
budget을 assert하지 않는다. 320-turn renderer stress는 좋은 관측 도구지만 server V1의 10,000-message
turn latency, NFS cold start, slow SSE consumer heap, 10,000 directory instance retention을 증명하지 않는다.</p>

<h2 id="20-테스트-전략과-module-boundary">20. 테스트 전략과 module boundary</h2>

<h3 id="201-monorepo의-유효한-경계">20.1 monorepo의 유효한 경계</h3>

<pre class="mermaid">
flowchart LR
    SCHEMA[schema / protocol] --&gt; CORE[core V2 services]
    CORE --&gt; SERVER[typed server API]
    SERVER --&gt; CLIENT[generated client / sdk-next]
    CLIENT --&gt; SURF[app / TUI / CLI]

    LEG[packages/opencode V1 runtime] --&gt; API[legacy + V2 HTTP mount]
    CORE --&gt; LEG
    SERVER --&gt; LEG
    SDK[legacy generated SDK] --&gt; SURF
    UI[ui + session-ui] --&gt; APP[shared Solid app]
    APP --&gt; DESK[Electron]
    APP --&gt; WEB[Web]

</pre>

<p>목표 architecture의 좋은 경계는 분명하다.</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">schema</code>/<code class="language-plaintext highlighter-rouge">protocol</code>: transport-visible type과 event vocabulary</li>
  <li><code class="language-plaintext highlighter-rouge">core</code>: Effect service, database, V1/V2 공용 event/projector와 V2 session/runner primitives</li>
  <li><code class="language-plaintext highlighter-rouge">server</code>: typed HTTP API definition</li>
  <li><code class="language-plaintext highlighter-rouge">client</code>/<code class="language-plaintext highlighter-rouge">sdk-next</code>: server contract의 client projection</li>
  <li><code class="language-plaintext highlighter-rouge">ui</code>/<code class="language-plaintext highlighter-rouge">session-ui</code>: product-independent-ish primitive와 message rendering</li>
  <li><code class="language-plaintext highlighter-rouge">app</code>: shared browser/Electron product state와 screens</li>
  <li><code class="language-plaintext highlighter-rouge">tui</code>: terminal-specific interaction projection</li>
  <li><code class="language-plaintext highlighter-rouge">opencode</code>: shipping binary와 현재 V1 runtime/adapters</li>
</ul>

<p>특히 server schema에서 generated client를 검사하는 방식은 client/server drift를 CI에서 잡을 수 있다.
Effect <code class="language-plaintext highlighter-rouge">LayerNode</code> graph는 service dependency와 scope/finalizer를 central composition root에 드러낸다
(<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/effect/app-runtime.ts#L58-L109"><code class="language-plaintext highlighter-rouge">app-runtime.ts#L58-L109</code></a>) [A].</p>

<p>현재 약점은 이 그림이 아직 target과 current를 동시에 담는다는 것이다. session/model/provider/tool의
상당한 V1 구현은 여전히 <code class="language-plaintext highlighter-rouge">packages/opencode</code>에 있고 V2는 <code class="language-plaintext highlighter-rouge">core/server/client/sdk-next</code>에 중복 진화한다.
legacy SDK와 next client, legacy live event transport와 shared durable replay API, JSON message/part와
typed projection이 동시에 존재한다. 공용 EventV2 substrate가 storage commit path 일부를 통합했어도
runtime/API/serving model은 이중이다. migration 중에는 불가피하지만, 두 path의 behavior parity와 제거 기준이 없으면
“새 layer를 추가했지만 old authority는 계속 남는” 장기 이중화가 된다 [D].</p>

<h3 id="202-테스트ci-evidence">20.2 테스트·CI evidence</h3>

<p>고정 commit의 CI는 다음을 실행한다 [A].</p>

<ul>
  <li>Linux와 Windows에서 workspace unit tests</li>
  <li>Linux와 Windows에서 shared app Playwright E2E</li>
  <li>Linux에서 generated client drift check</li>
  <li>Linux에서 HTTP API coverage/auth/Effect exerciser</li>
  <li>별도 workflow에서 monorepo TypeScript typecheck</li>
</ul>

<p>근거는 <a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/.github/workflows/test.yml"><code class="language-plaintext highlighter-rouge">.github/workflows/test.yml</code></a>과
<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/.github/workflows/typecheck.yml"><code class="language-plaintext highlighter-rouge">typecheck.yml</code></a>이다.
test path/name inventory를 기계적으로 센 C등급 snapshot에서는 <code class="language-plaintext highlighter-rouge">core</code> 158, <code class="language-plaintext highlighter-rouge">opencode</code> 311, <code class="language-plaintext highlighter-rouge">app</code> 202,
<code class="language-plaintext highlighter-rouge">tui</code> 52개의 test/spec 관련 file이 잡혔다. 이는 실행 성공 수가 아니라 fixture·helper를 포함한 source
inventory이므로 품질 점수로 직접 환산하지 않는다.</p>

<p>강한 부분:</p>

<ul>
  <li>DB/event/projector, permission, tool, provider adapter에 unit coverage가 넓다.</li>
  <li>client optimistic merge와 event-during-fetch race를 세밀하게 test한다.</li>
  <li>app E2E가 Linux/Windows 양쪽에서 돈다.</li>
  <li>HTTP surface가 schema에만 있고 handler가 빠진 경우를 exerciser가 잡는다.</li>
  <li>timeline에는 smoke/regression/stability/manual benchmark가 별도로 있다.</li>
</ul>

<p>빈칸 또는 확인되지 않은 gate:</p>

<ul>
  <li>root에 <code class="language-plaintext highlighter-rouge">oxlint</code> script는 있지만 고정 commit의 workflow에서 lint 실행은 찾지 못했다 [C].</li>
  <li>manual performance suite는 default CI가 아니고 threshold도 없다.</li>
  <li>NFS, 10k session/message, slow SSE consumer, multi-process ownership fault injection gate는 확인하지 못했다.</li>
  <li>arbitrary plugin crash/CPU/memory isolation을 검증할 구조 자체가 없다.</li>
  <li>V1/V2 semantic conformance를 하나의 contract suite로 강제하는지 명확하지 않다.</li>
  <li>macOS app E2E가 이 workflow matrix에는 없다.</li>
</ul>

<h2 id="21-구조적-강점">21. 구조적 강점</h2>

<ol>
  <li><strong>다중 surface를 가능하게 한 server spine.</strong> TUI와 desktop을 따로 구현하면서도 agent loop를
복제하지 않는다. OpenAPI/SDK/ACP가 외부 embedding 경계를 만든다.</li>
  <li><strong>provider portability를 adapter 문제로 중앙화.</strong> 인증, model capability, reasoning, cache usage,
tool variant를 한 layer에서 normalize한다.</li>
  <li><strong>typed resource lifecycle.</strong> Effect scope/finalizer가 MCP, LSP, instance, DB, runner, process cleanup을
composition graph에 드러낸다.</li>
  <li><strong>transcript와 UI tool lifecycle의 높은 관측 가능성.</strong> text/reasoning/tool/patch/retry를 part로
증분 보존해 client가 final answer만 보는 black box가 아니다.</li>
  <li><strong>HITL을 protocol로 승격.</strong> permission과 question이 surface마다 재해석 가능한 typed interaction이다.</li>
  <li><strong>client-side large-history engineering.</strong> keyset page, optimistic/live merge, virtual timeline, cache bound,
scroll-anchor regression test가 실제 제품 품질을 만든다.</li>
  <li><strong>extension breadth.</strong> MCP, LSP, skill, command, plugin, custom tool, ACP가 모두 실제 surface다.</li>
  <li><strong>공용 durable substrate와 V2가 현재 부채를 정확히 겨냥.</strong> V1 lifecycle까지 포함한 durable
sequence와 event+projection transaction, V2 input admission, context epoch, post-baseline history,
compaction continuation과 replay API는 복구·성능 문제에 맞는 방향이다.</li>
  <li><strong>local-first inspectability.</strong> MIT source, SQLite, file logs, documented HTTP API라 운영자가 내부 상태를
조사할 수 있다.</li>
  <li><strong>실용적인 output/cancellation guard.</strong> shell timeout, process-group kill, configurable output
truncation, full artifact 보존과 7일 cleanup은 production coding agent에 필요한 기본기다.</li>
</ol>

<h2 id="22-구조적-약점과-risk-register">22. 구조적 약점과 risk register</h2>

<table>
  <thead>
    <tr>
      <th style="text-align: right">우선도</th>
      <th>위험</th>
      <th>근거</th>
      <th>영향</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td style="text-align: right">P0</td>
      <td>permission을 sandbox로 오인</td>
      <td>host shell/plugin direct execution</td>
      <td>untrusted repo/tool이 OS user 권한 사용</td>
    </tr>
    <tr>
      <td style="text-align: right">P0</td>
      <td><code class="language-plaintext highlighter-rouge">plan</code>/<code class="language-plaintext highlighter-rouge">explore</code> read-only drift</td>
      <td>shell allow 상속/명시 allow</td>
      <td>UI mode가 security invariant가 아님</td>
    </tr>
    <tr>
      <td style="text-align: right">P0</td>
      <td>remote server credential 권한 과대</td>
      <td>directory routing + shell/file/PTY API</td>
      <td>password 유출이 넓은 host capability 유출</td>
    </tr>
    <tr>
      <td style="text-align: right">P1</td>
      <td>V1 turn당 full transcript read</td>
      <td><code class="language-plaintext highlighter-rouge">filterCompactedEffect</code> before filtering</td>
      <td>long session latency/heap이 N에 비례</td>
    </tr>
    <tr>
      <td style="text-align: right">P1</td>
      <td>V1/V2 dual runtime 장기화</td>
      <td>공용 durable substrate 위에도 schema/runtime/client/API path가 병존</td>
      <td>parity bug, migration complexity</td>
    </tr>
    <tr>
      <td style="text-align: right">P1</td>
      <td>durable transcript ≠ durable execution</td>
      <td>status/runner/job process-local</td>
      <td>crash 후 exact continuation 불가</td>
    </tr>
    <tr>
      <td style="text-align: right">P1</td>
      <td>unbounded server resources</td>
      <td>instance cache, SSE/PubSub, background registry</td>
      <td>long-lived server memory/process 누적</td>
    </tr>
    <tr>
      <td style="text-align: right">P1</td>
      <td>SQLite WAL + NFS 부적합</td>
      <td>local data DB topology</td>
      <td>HPC shared-home reliability risk</td>
    </tr>
    <tr>
      <td style="text-align: right">P1</td>
      <td>provider retry total budget 없음</td>
      <td>retry schedule에 terminal bound 없음</td>
      <td>unattended run 무기한 점유</td>
    </tr>
    <tr>
      <td style="text-align: right">P2</td>
      <td>plugin failure/trust isolation 없음</td>
      <td>in-process arbitrary JS/TS hooks</td>
      <td>crash, secret access, hot-path 지연</td>
    </tr>
    <tr>
      <td style="text-align: right">P2</td>
      <td>instruction freshness가 reproducibility를 침식</td>
      <td>provider step마다 live file/URL read</td>
      <td>동일 transcript resume 결과 변화</td>
    </tr>
    <tr>
      <td style="text-align: right">P2</td>
      <td>legacy SSE replay 계약 없음</td>
      <td>legacy에는 cursor/Last-Event-ID 없음; V2 replay API는 별도</td>
      <td>current client reconnect reconciliation 복잡</td>
    </tr>
    <tr>
      <td style="text-align: right">P2</td>
      <td>session list ordering index 부재</td>
      <td><code class="language-plaintext highlighter-rouge">time_updated</code>/project+updated index 없이 <code class="language-plaintext highlighter-rouge">ORDER BY ... LIMIT</code></td>
      <td>10k session 첫 화면 scan/sort 가능</td>
    </tr>
    <tr>
      <td style="text-align: right">P2</td>
      <td>docs/source drift</td>
      <td>TUI topology, agent permission, timeout</td>
      <td>운영자가 잘못된 threat/SLO model 채택</td>
    </tr>
    <tr>
      <td style="text-align: right">P3</td>
      <td>root/client process retention</td>
      <td>LSP/MCP/instance capacity 부족</td>
      <td>monorepo·다중 directory 장기 사용 비용</td>
    </tr>
  </tbody>
</table>

<h2 id="23-farad에-대한-논리적-함의">23. Farad에 대한 논리적 함의</h2>

<p>이 절은 “OpenCode처럼 만들자”가 아니라, Farad의 authority·thread·flow·hot path·persistence·failure
계약에 어떤 선택이 맞는지 분리한다. Farad의 기존
<code class="language-plaintext highlighter-rouge">layers.md</code>, <code class="language-plaintext highlighter-rouge">architecture.md</code>,
<code class="language-plaintext highlighter-rouge">agent.md</code>, <code class="language-plaintext highlighter-rouge">session.md</code>의 계약을 우선한다.</p>

<h3 id="231-transfer-matrix">23.1 transfer matrix</h3>

<table>
  <thead>
    <tr>
      <th>OpenCode pattern</th>
      <th>Farad 판단</th>
      <th>이유와 적용 경계</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>headless server + typed client</td>
      <td><strong>Adapt</strong></td>
      <td>즉시 remote daemon으로 바꾸지 말고, 먼저 in-process application protocol/event DTO를 UI와 runtime 사이 anti-corruption layer로 사용</td>
    </tr>
    <tr>
      <td>generated OpenAPI client</td>
      <td><strong>Defer/Adapt</strong></td>
      <td>외부 automation/remote surface가 실제 roadmap이면 가치 큼. 현재 PySide app만 위해 generator stack을 추가할 필요는 없음</td>
    </tr>
    <tr>
      <td>V1/V2 공용 durable event + projection transaction</td>
      <td><strong>Adapt</strong></td>
      <td>모든 live UI delta를 event sourcing하지 말고 session/run/permission처럼 replay·audit 가치가 큰 lifecycle부터. canonical transcript와 derived index authority를 명시</td>
    </tr>
    <tr>
      <td>V2 context epoch/baseline</td>
      <td><strong>Adopt concept</strong></td>
      <td>Farad marker twin·append-only transcript와 잘 맞음. instruction/settings 변화의 byte-stable provenance 강화</td>
    </tr>
    <tr>
      <td>message keyset pagination</td>
      <td><strong>Adopt</strong></td>
      <td>session/timeline의 10k 계약에 직접 유효. cursor와 stable tie-breaker, bound test까지 포함</td>
    </tr>
    <tr>
      <td>virtual timeline</td>
      <td><strong>Already aligned</strong></td>
      <td>Farad QPainter <code class="language-plaintext highlighter-rouge">TimelineView</code>의 visible-only contract를 유지. OpenCode DOM 구현을 이식하지 않음</td>
    </tr>
    <tr>
      <td>client cache limit/TTL/pin</td>
      <td><strong>Adopt concept</strong></td>
      <td>folder/session registry LRU에 active/inflight/HITL pin과 fail-if-expensive-fallback test를 명시</td>
    </tr>
    <tr>
      <td>provider adapter/catalog</td>
      <td><strong>Adapt</strong></td>
      <td>GLM/Kimi 품질을 first-class로 두고 provider quirk를 adapter에 격리. 75-provider breadth 자체는 목표가 아님</td>
    </tr>
    <tr>
      <td>tool lifecycle part model</td>
      <td><strong>Adopt/strengthen</strong></td>
      <td>pending/running/completed/error/interrupted를 transcript/event와 UI가 같은 ID로 보게 함</td>
    </tr>
    <tr>
      <td>typed question/permission protocol</td>
      <td><strong>Adopt/retain</strong></td>
      <td>callback/event 기반 Farad engine과 잘 맞음. attendance와 permission posture를 계속 직교시킴</td>
    </tr>
    <tr>
      <td>shell output truncation + artifact</td>
      <td><strong>Adopt</strong></td>
      <td>byte/line/token bound, full artifact 위치·ACL, configurable retention, cleanup과 abort process-tree test 포함</td>
    </tr>
    <tr>
      <td>MCP bounded pagination/progress timeout</td>
      <td><strong>Adopt</strong></td>
      <td>duplicate cursor, max page, MIME/size guard를 Farad MCP contract에 반영</td>
    </tr>
    <tr>
      <td>LSP file-triggered lazy spawn</td>
      <td><strong>Defer/Adapt</strong></td>
      <td>실제 제품 요구가 생기면 useful. root process LRU와 explicit refresh 없이는 추가하지 않음</td>
    </tr>
    <tr>
      <td>per-turn instruction file/URL reread</td>
      <td><strong>Reject</strong></td>
      <td>Farad explicit refresh, NFS, transcript reproducibility 계약 위반</td>
    </tr>
    <tr>
      <td>in-process arbitrary plugin</td>
      <td><strong>Reject as trust default</strong></td>
      <td>plugin manifest/exposure와 code execution trust를 분리하고 최소한 capability/timeout/process boundary 필요</td>
    </tr>
    <tr>
      <td>V1 ephemeral <code class="language-plaintext highlighter-rouge">always</code> + V2 saved project rule</td>
      <td><strong>Adapt semantics</strong></td>
      <td>interactive once/instance cache와 durable project policy를 이름·scope·저장소로 명확히 분리하고, pending interaction은 별도 비영속 상태로 둠</td>
    </tr>
    <tr>
      <td>legacy unbounded SSE/PubSub</td>
      <td><strong>Reject default</strong></td>
      <td>core의 opt-in bounded helper만으로 부족하다. shipping consumer에 bounded/coalesced queue + overflow→snapshot/refetch 또는 durable replay를 연결</td>
    </tr>
    <tr>
      <td>SQLite WAL on shared NFS</td>
      <td><strong>Reject default topology</strong></td>
      <td><code class="language-plaintext highlighter-rouge">OPENCODE_DB</code> node-local override는 가능하지만 Farad canonical append/chunk/index 계약을 유지. SQLite를 쓰면 node-local derived cache와 강제 topology를 명시</td>
    </tr>
    <tr>
      <td>whole-instance reload</td>
      <td><strong>Reject</strong></td>
      <td>Farad의 항목별 invalidation/delta reconcile/explicit refresh를 유지</td>
    </tr>
    <tr>
      <td>V1/V2 장기 병행</td>
      <td><strong>Avoid</strong></td>
      <td>새 runtime path를 만들면 entry criteria, conformance, cutover, deletion milestone을 같은 job에 둠</td>
    </tr>
  </tbody>
</table>

<h3 id="232-farad-decision-frame">23.2 Farad decision frame</h3>

<table>
  <thead>
    <tr>
      <th>질문</th>
      <th>Farad 권고 authority/owner</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>session/transcript의 진실은 누구인가?</td>
      <td>append-only transcript/store; UI와 search index는 projection</td>
    </tr>
    <tr>
      <td>한 turn의 execution authority는 누구인가?</td>
      <td>asyncio runtime의 단일 turn owner; Qt thread는 command/event proxy</td>
    </tr>
    <tr>
      <td>permission을 누가 결정하는가?</td>
      <td>policy engine + explicit attendance; UI는 응답 surface이지 보안 authority가 아님</td>
    </tr>
    <tr>
      <td>tool side effect는 어디서 막는가?</td>
      <td>permission parser 위가 아니라 execution runner/OS capability boundary</td>
    </tr>
    <tr>
      <td>10k에서 반복되는 것은 무엇인가?</td>
      <td>active window, hinted lookup, visible timeline만; 전량 scan은 explicit bounded maintenance</td>
    </tr>
    <tr>
      <td>NFS에 남겨야 하는 것은 무엇인가?</td>
      <td>sequential canonical transcript/index와 user config; chat hot cache/DB는 topology를 명시</td>
    </tr>
    <tr>
      <td>crash 후 무엇을 자동 재개하는가?</td>
      <td>durable run state에 idempotency/lease가 있는 action만; transcript 존재만으로 tool을 재실행하지 않음</td>
    </tr>
    <tr>
      <td>refresh는 누가 시작하는가?</td>
      <td>user/app mutation의 targeted invalidation; 외부 file은 explicit reload</td>
    </tr>
  </tbody>
</table>

<h3 id="233-farad-우선순위-제안">23.3 Farad 우선순위 제안</h3>

<ol>
  <li><strong>P0: permission과 containment를 분리해 명세화.</strong> shell/tool runner의 실제 OS boundary, network,
workspace outside, child process, plugin trust를 capability matrix와 platform별 test로 만든다. OpenCode의
가장 큰 교훈은 승인 UX가 좋아도 containment는 자동으로 생기지 않는다는 점이다.</li>
  <li><strong>P0: runtime/UI protocol conformance.</strong> command, event, question, permission, tool lifecycle ID를 typed
application protocol로 고정하고 Qt가 runtime object를 직접 만지지 못하게 한다.</li>
  <li><strong>P1: active-window session query.</strong> compaction 이전 transcript를 turn마다 읽지 않는 index/cursor/hint
contract와 “비싼 fallback 호출 시 fail” test를 강화한다.</li>
  <li><strong>P1: bounded registries.</strong> folder/session/MCP/subagent/scheduler/notification queue마다 capacity, TTL,
pin, overflow semantics를 문서와 test에 둔다.</li>
  <li><strong>P1: crash semantics.</strong> <code class="language-plaintext highlighter-rouge">persisted transcript</code>, <code class="language-plaintext highlighter-rouge">persisted run record</code>, <code class="language-plaintext highlighter-rouge">restartable operation</code>을 분리하고,
terminal/interrupted/orphan settlement와 수동 resume UX를 정의한다.</li>
  <li><strong>P1: provider adapter contract.</strong> GLM/Kimi streaming, reasoning, usage, tool call, retry metadata를 같은
conformance suite로 검증한다.</li>
  <li><strong>P2: external surface는 증거가 생길 때.</strong> CLI/automation/remote UI 요구가 구체화되면 OpenCode처럼
stable application protocol에서 server/SDK를 파생한다. 지금부터 network daemon을 authority로 둘
필요는 없다.</li>
</ol>

<h2 id="24-반증-가능성-미확인-질문과-재현-실험">24. 반증 가능성, 미확인 질문과 재현 실험</h2>

<h3 id="241-공식-문서와-고정-source의-drift">24.1 공식 문서와 고정 source의 drift</h3>

<p>빠르게 변하는 제품에서는 prose와 implementation이 갈릴 수 있다. 아래 차이는 단순 문구 문제가 아니라
topology, permission 또는 timeout 해석을 바꾸므로 고정 SHA source를 우선했다 [A/B/C].</p>

<table>
  <thead>
    <tr>
      <th>주제</th>
      <th>공식 문서 표현</th>
      <th>고정 source <code class="language-plaintext highlighter-rouge">b3a012c…</code></th>
      <th>판정</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>기본 TUI server</td>
      <td>hostname/port를 할당하는 설명</td>
      <td>기본은 Worker RPC + <code class="language-plaintext highlighter-rouge">opencode.internal</code> direct fetch; network flag 때 listener</td>
      <td>topology drift</td>
    </tr>
    <tr>
      <td>native agent</td>
      <td>문서에 언급된 agent set</td>
      <td>고정 native registry에는 Scout가 없고 <code class="language-plaintext highlighter-rouge">general</code>/<code class="language-plaintext highlighter-rouge">explore</code>가 있음</td>
      <td>feature drift</td>
    </tr>
    <tr>
      <td>Plan shell</td>
      <td>command 전에 묻는/read-only 취지</td>
      <td>edit 계열만 deny, broad <code class="language-plaintext highlighter-rouge">bash</code> allow 상속</td>
      <td><strong>security drift</strong></td>
    </tr>
    <tr>
      <td>Explore</td>
      <td>read-only exploration</td>
      <td><code class="language-plaintext highlighter-rouge">bash: allow</code></td>
      <td><strong>security drift</strong></td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">.env</code> read</td>
      <td>deny로 읽힐 수 있는 설명</td>
      <td><code class="language-plaintext highlighter-rouge">.env</code>/<code class="language-plaintext highlighter-rouge">.env.*</code> ask, example allow</td>
      <td>policy drift</td>
    </tr>
    <tr>
      <td>MCP timeout</td>
      <td>docs/schema의 5초</td>
      <td>current catalog/tool path는 30초</td>
      <td>operation drift</td>
    </tr>
  </tbody>
</table>

<p>따라서 보안·retry·timeout·topology invariant는 문서 prose만 믿지 말고 generated contract와 executable
regression test에 고정해야 한다. agent prompt가 “read-only”라고 부르는 것과 registry가 side-effect
capability를 deny하는 것은 별도 test다 [D].</p>

<h3 id="242-결론을-바꿀-수-있는-반증-실험">24.2 결론을 바꿀 수 있는 반증 실험</h3>

<p>다음 결과가 나오면 이 보고서의 일부 결론을 수정해야 한다.</p>

<table>
  <thead>
    <tr>
      <th>현재 결론</th>
      <th>반증/수정 조건</th>
      <th>재현 방법</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>V1 turn history가 O(KN)</td>
      <td>primary handler가 V2 post-baseline query로 완전히 cutover되고 V1 path가 제거됨</td>
      <td>endpoint call graph + query trace</td>
    </tr>
    <tr>
      <td>10k long session 부적합</td>
      <td>10k message/part, 20-step turn에서 history query/heap/latency가 N과 무관한 bound를 통과</td>
      <td>SQLite query counter + heap + p50/p95 benchmark</td>
    </tr>
    <tr>
      <td>server instance cache unbounded</td>
      <td>fixed commit 이후 idle TTL/capacity/eviction이 source와 test에 추가됨</td>
      <td>10k directory visit 후 live service/process count</td>
    </tr>
    <tr>
      <td>legacy shipping SSE backpressure 없음</td>
      <td><code class="language-plaintext highlighter-rouge">allBounded</code> 또는 동등한 bounded queue가 route에 연결되고 coalescing/overflow snapshot signal + reconnect test가 생김</td>
      <td>느린 consumer에 event flood</td>
    </tr>
    <tr>
      <td>durable execution recovery 약함</td>
      <td>durable owner lease/status/continuation과 crash-mid-tool integration test가 shipping path에 들어감</td>
      <td>kill -9 at provider/tool phases, exact recovery audit</td>
    </tr>
    <tr>
      <td>permission은 containment가 아님</td>
      <td>shell/plugin/tool이 verified OS sandbox/capability broker 밖으로 못 나감</td>
      <td>filesystem/network/process escape suite</td>
    </tr>
    <tr>
      <td>plan/explore가 read-only가 아님</td>
      <td>shell deny/read-only broker가 agent policy에 강제되고 regression test가 추가됨</td>
      <td>destructive shell attempt</td>
    </tr>
    <tr>
      <td>HPC/NFS fit이 낮음</td>
      <td>DB가 node-local로 강제되고 workspace I/O가 explicit cached/indexed되며 NFS fault test를 통과</td>
      <td>NFS latency/loss injection</td>
    </tr>
    <tr>
      <td>UI 전체-history projection이 O(N)</td>
      <td>incremental row/index projection이 live delta에서 bounded임을 profiler와 test가 증명</td>
      <td>10k loaded turn 후 1-token delta CPU/alloc</td>
    </tr>
    <tr>
      <td>V1/V2 dual architecture가 risk</td>
      <td>cutover checklist, semantic conformance, old schema deletion milestone이 공개되고 완료됨</td>
      <td>package/API dependency graph diff</td>
    </tr>
  </tbody>
</table>

<h3 id="243-미확인-운영-질문">24.3 미확인 운영 질문</h3>

<p>추가로 답이 필요한 운영 질문:</p>

<ol>
  <li>구현상 <code class="language-plaintext highlighter-rouge">OPENCODE_DB</code> absolute path를 쓸 수 있는데, 이를 node-local로 강제·검증하는 공식 HPC 배포
계약과 migration/backup 지침이 있는가?</li>
  <li>remote <code class="language-plaintext highlighter-rouge">serve</code>를 multi-user/tenant 용도로 지원하는가, 아니면 trusted single-user remote access만
의도하는가?</li>
  <li>이미 존재하는 V2 <code class="language-plaintext highlighter-rouge">after</code> durable replay API로 shared app/TUI를 언제 전환하며, legacy SSE의
overflow/reconnect 계약은 어떻게 종료할 것인가?</li>
  <li>provider retry의 maximum elapsed/attempt budget을 command/session별로 둘 계획이 있는가?</li>
  <li>plugin capability manifest, signature, isolation process roadmap이 있는가?</li>
  <li>V1/V2 cutover의 endpoint별 완료 정의와 old table/API 제거 시점은 무엇인가?</li>
  <li>performance suite를 thresholded CI gate로 올릴 hardware-normalized metric이 있는가?</li>
  <li>crash 후 pending permission/question을 expire/reject/orphan 처리하는 durable policy는 무엇인가?</li>
</ol>

<h2 id="25-재현-가능한-source-index">25. 재현 가능한 source index</h2>

<h3 id="251-공식-문서-b">25.1 공식 문서 [B]</h3>

<ul>
  <li><a href="https://opencode.ai/docs/">Introduction</a></li>
  <li><a href="https://opencode.ai/docs/server/">Server / OpenAPI</a></li>
  <li><a href="https://opencode.ai/docs/sdk/">SDK</a></li>
  <li><a href="https://opencode.ai/docs/agents/">Agents</a></li>
  <li><a href="https://opencode.ai/docs/permissions/">Permissions</a></li>
  <li><a href="https://opencode.ai/docs/providers/">Providers</a></li>
  <li><a href="https://opencode.ai/docs/models/">Models</a></li>
  <li><a href="https://opencode.ai/docs/config/">Config</a></li>
  <li><a href="https://opencode.ai/docs/mcp-servers/">MCP servers</a></li>
  <li><a href="https://opencode.ai/docs/lsp/">LSP</a></li>
  <li><a href="https://opencode.ai/docs/skills/">Agent skills</a></li>
  <li><a href="https://opencode.ai/docs/plugins/">Plugins</a></li>
  <li><a href="https://opencode.ai/docs/custom-tools/">Custom tools</a></li>
  <li><a href="https://opencode.ai/docs/commands/">Commands</a></li>
  <li><a href="https://opencode.ai/docs/acp/">ACP</a></li>
  <li><a href="https://opencode.ai/docs/ide/">IDE</a></li>
  <li><a href="https://opencode.ai/docs/share/">Share</a></li>
  <li><a href="https://sqlite.org/wal.html#overview">SQLite WAL</a></li>
</ul>

<h3 id="252-고정-implementation-evidence-a">25.2 고정 implementation evidence [A]</h3>

<ul>
  <li>shipping entry/surfaces: <a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/index.ts"><code class="language-plaintext highlighter-rouge">index.ts</code></a></li>
  <li>TUI worker/server topology: <a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/cli/cmd/tui.ts"><code class="language-plaintext highlighter-rouge">tui.ts</code></a>,
<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/cli/tui/worker.ts"><code class="language-plaintext highlighter-rouge">worker.ts</code></a></li>
  <li>instance ownership: <a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/project/instance-store.ts"><code class="language-plaintext highlighter-rouge">instance-store.ts</code></a>,
<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/effect/instance-state.ts"><code class="language-plaintext highlighter-rouge">instance-state.ts</code></a></li>
  <li>V1 loop/runtime: <a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/session/prompt.ts"><code class="language-plaintext highlighter-rouge">prompt.ts</code></a>,
<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/session/processor.ts"><code class="language-plaintext highlighter-rouge">processor.ts</code></a>,
<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/session/run-state.ts"><code class="language-plaintext highlighter-rouge">run-state.ts</code></a>,
<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/session/session.ts"><code class="language-plaintext highlighter-rouge">session.ts</code></a></li>
  <li>V1 durable event definitions/bridge: <a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/schema/src/v1/session.ts"><code class="language-plaintext highlighter-rouge">schema/v1/session.ts</code></a>,
<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/event-v2-bridge.ts"><code class="language-plaintext highlighter-rouge">event-v2-bridge.ts</code></a></li>
  <li>V2 runner/compaction: <a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/core/src/session/runner/llm.ts"><code class="language-plaintext highlighter-rouge">runner/llm.ts</code></a>,
<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/core/src/session/compaction.ts"><code class="language-plaintext highlighter-rouge">compaction.ts</code></a></li>
  <li>V2 durable history/replay API: <a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/protocol/src/groups/session.ts"><code class="language-plaintext highlighter-rouge">protocol/groups/session.ts</code></a>,
<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/server/src/handlers/session.ts"><code class="language-plaintext highlighter-rouge">server/handlers/session.ts</code></a></li>
  <li>persistence/event/projector: <a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/core/src/database/database.ts"><code class="language-plaintext highlighter-rouge">database.ts</code></a>,
<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/core/src/event.ts"><code class="language-plaintext highlighter-rouge">event.ts</code></a>,
<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/core/src/session/sql.ts"><code class="language-plaintext highlighter-rouge">session/sql.ts</code></a>,
<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/core/src/session/projector.ts"><code class="language-plaintext highlighter-rouge">projector.ts</code></a></li>
  <li>message page/full-history distinction: <a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/session/message-v2.ts"><code class="language-plaintext highlighter-rouge">message-v2.ts</code></a></li>
  <li>context epoch/history: <a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/core/src/session/context-epoch.ts"><code class="language-plaintext highlighter-rouge">context-epoch.ts</code></a>,
<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/core/src/session/history.ts"><code class="language-plaintext highlighter-rouge">history.ts</code></a></li>
  <li>permission/agent policy: <a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/permission/index.ts"><code class="language-plaintext highlighter-rouge">permission/index.ts</code></a>,
<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/core/src/permission.ts"><code class="language-plaintext highlighter-rouge">core/permission.ts</code></a>,
<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/core/src/permission/saved.ts"><code class="language-plaintext highlighter-rouge">permission/saved.ts</code></a>,
<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/agent/agent.ts"><code class="language-plaintext highlighter-rouge">agent.ts</code></a>,
<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/agent/subagent-permissions.ts"><code class="language-plaintext highlighter-rouge">subagent-permissions.ts</code></a></li>
  <li>shell/tool boundary: <a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/tool/shell.ts"><code class="language-plaintext highlighter-rouge">shell.ts</code></a>,
<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/tool/truncate.ts"><code class="language-plaintext highlighter-rouge">truncate.ts</code></a>,
<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/tool/registry.ts"><code class="language-plaintext highlighter-rouge">tool/registry.ts</code></a></li>
  <li>provider adapter: <a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/provider/provider.ts"><code class="language-plaintext highlighter-rouge">provider.ts</code></a></li>
  <li>extension services: <a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/mcp/index.ts"><code class="language-plaintext highlighter-rouge">mcp/index.ts</code></a>,
<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/lsp/lsp.ts"><code class="language-plaintext highlighter-rouge">lsp.ts</code></a>,
<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/skill/discovery.ts"><code class="language-plaintext highlighter-rouge">skill/discovery.ts</code></a>,
<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/plugin/index.ts"><code class="language-plaintext highlighter-rouge">plugin/index.ts</code></a></li>
  <li>shared app paging/merge/cache: <a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/app/src/context/server-session.ts"><code class="language-plaintext highlighter-rouge">server-session.ts</code></a>,
<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/app/src/context/global-sync/session-cache.ts"><code class="language-plaintext highlighter-rouge">session-cache.ts</code></a></li>
  <li>timeline projection/virtualization: <a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/app/src/pages/session/timeline/projection.ts"><code class="language-plaintext highlighter-rouge">projection.ts</code></a>,
<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/app/src/pages/session/timeline/message-timeline.tsx"><code class="language-plaintext highlighter-rouge">message-timeline.tsx</code></a></li>
  <li>HITL surfaces: <a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/app/src/pages/session/composer/session-permission-dock.tsx"><code class="language-plaintext highlighter-rouge">session-permission-dock.tsx</code></a>,
<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/app/src/pages/session/composer/session-question-dock.tsx"><code class="language-plaintext highlighter-rouge">session-question-dock.tsx</code></a></li>
  <li>share/credentials: <a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/share/share-next.ts"><code class="language-plaintext highlighter-rouge">share-next.ts</code></a>,
<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/auth/index.ts"><code class="language-plaintext highlighter-rouge">auth/index.ts</code></a>,
<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/opencode/src/mcp/auth.ts"><code class="language-plaintext highlighter-rouge">mcp/auth.ts</code></a></li>
  <li>observability: <a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/core/src/observability/logging.ts"><code class="language-plaintext highlighter-rouge">logging.ts</code></a>,
<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/packages/core/src/observability/otlp.ts"><code class="language-plaintext highlighter-rouge">otlp.ts</code></a></li>
  <li>CI gates: <a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/.github/workflows/test.yml"><code class="language-plaintext highlighter-rouge">test.yml</code></a>,
<a href="https://github.com/anomalyco/opencode/blob/b3a012cbdde854b0fe14f95b1c48543bf6517c0a/.github/workflows/typecheck.yml"><code class="language-plaintext highlighter-rouge">typecheck.yml</code></a></li>
</ul>

<h3 id="253-local-reproduction-commands-c">25.3 local reproduction commands [C]</h3>

<div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>git clone https://github.com/anomalyco/opencode.git
git <span class="nt">-C</span> opencode checkout b3a012cbdde854b0fe14f95b1c48543bf6517c0a
git <span class="nt">-C</span> opencode show <span class="nt">-s</span> <span class="nt">--format</span><span class="o">=</span><span class="s1">'%H%n%cI%n%s'</span>
<span class="nb">sed</span> <span class="nt">-n</span> <span class="s1">'44,90p'</span> opencode/packages/core/src/session/runner/llm.ts
rg <span class="nt">-n</span> <span class="s1">'durable:|PartDelta'</span> opencode/packages/schema/src/v1/session.ts
rg <span class="nt">-n</span> <span class="s1">'EventV2Bridge|MessageUpdated|PartUpdated'</span> <span class="se">\</span>
  opencode/packages/opencode/src/<span class="o">{</span>event-v2-bridge.ts,session/session.ts<span class="o">}</span>
rg <span class="nt">-n</span> <span class="s1">'ContinueAfterCompaction|ContinueAfterOverflowCompaction|compactIfNeeded|compactAfterOverflow'</span> <span class="se">\</span>
  opencode/packages/core/src/session/<span class="o">{</span>runner/llm.ts,compaction.ts<span class="o">}</span>
rg <span class="nt">-n</span> <span class="s1">'session.history|session.events|Replay durable events'</span> <span class="se">\</span>
  opencode/packages/<span class="o">{</span>protocol/src/groups,server/src/handlers<span class="o">}</span>/session.ts
rg <span class="nt">-n</span> <span class="s1">'POSITIVE_INFINITY|Queue.unbounded|PubSub.unbounded'</span> opencode/packages
rg <span class="nt">-n</span> <span class="s1">'allBounded|SubscriberOverflowError'</span> opencode/packages/core/src/event.ts
rg <span class="nt">-n</span> <span class="s1">'filterCompactedEffect|Instruction.system'</span> opencode/packages/opencode/src/session
rg <span class="nt">-n</span> <span class="s1">'OPENCODE_DB|opencode-.*channel|time_updated|session_project_idx'</span> <span class="se">\</span>
  opencode/packages/<span class="o">{</span>core/src/database/database.ts,core/src/session/sql.ts,opencode/src/session/session.ts<span class="o">}</span>
rg <span class="nt">-n</span> <span class="s1">'PermissionSaved|request.save|parentSessionPermission|RETENTION|tool-output'</span> <span class="se">\</span>
  opencode/packages/<span class="o">{</span>core/src/permission.ts,core/src/permission/saved.ts,opencode/src/agent/subagent-permissions.ts,opencode/src/tool<span class="o">}</span>
rg <span class="nt">-n</span> <span class="s1">'MAX_DIR_STORES|SESSION_CACHE_LIMIT|initialMessagePageSize|historyMessagePageSize'</span> opencode/packages/app/src
rg <span class="nt">-n</span> <span class="s1">'"\*": "allow"|explore|plan:'</span> opencode/packages/opencode/src/agent/agent.ts
rg <span class="nt">-n</span> <span class="s1">'OTEL_EXPORTER_OTLP|OPENCODE_LOG_LEVEL'</span> opencode/packages/core/src/observability
</code></pre></div></div>

<h2 id="26-최종-판정">26. 최종 판정</h2>

<h3 id="261-축별-평가">26.1 축별 평가</h3>

<p>아래 5단계 평가는 절대 성능 점수가 아니라 고정 commit의 architecture maturity를 압축한 D등급
판단이다. <code class="language-plaintext highlighter-rouge">5</code>가 “모든 환경에서 최고”라는 뜻은 아니며, 0.5 단위도 측정 오차가 있는 정량값이 아니다.
다른 제품 보고서의 판정과 산술 비교하거나 합산하지 않는다.</p>

<table>
  <thead>
    <tr>
      <th>축</th>
      <th style="text-align: right">평가</th>
      <th>이유</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>client/server 경계·임베딩</td>
      <td style="text-align: right">5/5</td>
      <td>HTTP/OpenAPI/SDK/ACP와 여러 surface가 같은 core를 공유</td>
    </tr>
    <tr>
      <td>provider portability</td>
      <td style="text-align: right">5/5</td>
      <td>광범위 adapter와 model capability normalization</td>
    </tr>
    <tr>
      <td>extension breadth</td>
      <td style="text-align: right">5/5</td>
      <td>MCP/LSP/skill/plugin/custom tool/command가 실제 통합</td>
    </tr>
    <tr>
      <td>interactive UX/HITL</td>
      <td style="text-align: right">4.5/5</td>
      <td>TUI+desktop/web, typed permission/question, virtual timeline</td>
    </tr>
    <tr>
      <td>local transcript persistence</td>
      <td style="text-align: right">4.5/5</td>
      <td>V1 lifecycle부터 durable append+transactional projection, V2 typed history/replay; live delta는 비영속</td>
    </tr>
    <tr>
      <td>durable execution/recovery</td>
      <td style="text-align: right">3/5</td>
      <td>V2 compaction/tool continuation과 replay는 구현, active owner/status/restart continuation은 미완</td>
    </tr>
    <tr>
      <td>security containment</td>
      <td style="text-align: right">2/5</td>
      <td>approval policy는 좋지만 host sandbox가 아니며 agent policy drift</td>
    </tr>
    <tr>
      <td>long-session hot path</td>
      <td style="text-align: right">2/5</td>
      <td>V1 step당 full transcript, V2 해결 방향은 아직 migration 중</td>
    </tr>
    <tr>
      <td>long-lived server boundedness</td>
      <td style="text-align: right">2.5/5</td>
      <td>client bounds와 core helper는 있으나 shipping cache/legacy queue/job은 unbounded</td>
    </tr>
    <tr>
      <td>observability</td>
      <td style="text-align: right">3.5/5</td>
      <td>structured log + OTLP + rich parts, audit/retention/correlation 빈칸</td>
    </tr>
    <tr>
      <td>HPC/NFS 적합성</td>
      <td style="text-align: right">1.5/5</td>
      <td>SQLite WAL/NFS 및 repeated workspace reads가 Farad 비용 모델과 충돌</td>
    </tr>
    <tr>
      <td>architecture 일관성</td>
      <td style="text-align: right">3/5</td>
      <td>목표 구조는 좋지만 V1/V2 dual path 전환기</td>
    </tr>
  </tbody>
</table>

<h3 id="262-use-case별-결론">26.2 use-case별 결론</h3>

<table>
  <thead>
    <tr>
      <th>use case</th>
      <th>적합도</th>
      <th>조건</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>개인 개발자의 local poly-provider coding agent</td>
      <td><strong>매우 높음</strong></td>
      <td>trusted account/repo, local DB</td>
    </tr>
    <tr>
      <td>TUI·desktop·web·IDE를 함께 쓰는 개발 workflow</td>
      <td><strong>매우 높음</strong></td>
      <td>server auth와 directory trust 이해</td>
    </tr>
    <tr>
      <td>coding agent를 SDK/HTTP로 임베드</td>
      <td><strong>높음</strong></td>
      <td>API drift와 current V1/V2 상태 고정 필요</td>
    </tr>
    <tr>
      <td>untrusted repository를 zero-trust로 실행</td>
      <td><strong>낮음</strong></td>
      <td>외부 container/sandbox를 별도로 강제해야 함</td>
    </tr>
    <tr>
      <td>다중 tenant 장기 headless service</td>
      <td><strong>낮음~조건부</strong></td>
      <td>ACL, isolation, bounds, backpressure, durable owner 보강 필요</td>
    </tr>
    <tr>
      <td>crash-safe durable workflow/scheduler</td>
      <td><strong>낮음~조건부</strong></td>
      <td>current background/runner를 workflow engine으로 간주하면 안 됨</td>
    </tr>
    <tr>
      <td>shared-home HPC/NFS desktop assistant</td>
      <td><strong>낮음</strong></td>
      <td>DB node-local 분리와 workspace I/O redesign 없이는 부적합</td>
    </tr>
  </tbody>
</table>

<p>OpenCode를 Farad의 직접 blueprint로 삼는 것은 맞지 않는다. 제품 authority와 배포 비용 모델이 다르기
때문이다. 하지만 <strong>server/API anti-corruption layer, generated typed contract, keyset history, virtual UI,
typed HITL, provider adapter, bounded output+retention, Effect-style resource finalizer, V1/V2 공용 durable
event projection과 V2 replay API</strong>는 매우
좋은 설계 참고점이다.</p>

<p>최종 결론은 §1과 일치한다. OpenCode는 현재 가장 개방적이고 surface가 넓은 coding-agent platform 중
하나이며, UI/client engineering은 이미 상당히 강하다. 동시에 primary V1 agent hot path, host-level
security boundary, durable execution recovery, long-lived server resource bound는 그 야심에 아직 못 미친다.
V2는 compaction continuation과 durable replay까지 이미 실체가 있지만, stale checklist의 문구만으로
구현을 과소평가하거나 반대로 owner lease·restart continuation까지 완성됐다고 과대평가해서는 안 된다.
Farad는 OpenCode의 경계와 관측성을 배우되, <strong>explicit refresh, NFS-aware persistence, 10k hot-path
bound, UI/async thread ownership, attendance와 permission의 분리</strong>라는 자체 강점을 버리면 안 된다.</p>

<style>pre.mermaid{overflow-x:auto;background:transparent;border:0;padding:0}.mermaid svg{max-width:100%;height:auto}</style>

<script type="module">
import mermaid from "https://cdn.jsdelivr.net/npm/mermaid@11.16.0/dist/mermaid.esm.min.mjs";
mermaid.initialize({startOnLoad:true,securityLevel:"strict"});
</script>]]></content><author><name>hvppycoding</name></author><category term="research" /><summary type="html"><![CDATA[OpenCode 제품·런타임·아키텍처 심층 분석]]></summary></entry><entry><title type="html">OpenCode · Claude Code · Codex · Farad 아키텍처 비교 조사 방법론</title><link href="https://hvppycoding.github.io/research/coding-agent-comparison-methodology/" rel="alternate" type="text/html" title="OpenCode · Claude Code · Codex · Farad 아키텍처 비교 조사 방법론" /><published>2026-07-13T23:50:00+09:00</published><updated>2026-07-13T23:50:00+09:00</updated><id>https://hvppycoding.github.io/research/coding-agent-comparison-methodology</id><content type="html" xml:base="https://hvppycoding.github.io/research/coding-agent-comparison-methodology/"><![CDATA[<h1 id="opencode--claude-code--codex--farad-아키텍처-비교-조사-방법론">OpenCode · Claude Code · Codex · Farad 아키텍처 비교 조사 방법론</h1>

<blockquote>
  <p><a href="https://hvppycoding.github.io/research/coding-agent-architecture-comparison/">전체 보고서 인덱스</a></p>
</blockquote>

<h2 id="목표">목표</h2>

<p>네 제품을 기능 체크리스트가 아니라 다음 질문으로 비교한다.</p>

<ol>
  <li>제품이 최적화하는 사용 환경과 핵심 루프는 무엇인가?</li>
  <li>상태와 변경 권위는 어느 계층·프로세스·스레드가 소유하는가?</li>
  <li>모델 호출, 컨텍스트 조립, 도구 실행, 권한, 세션 영속화의 데이터 흐름은 무엇인가?</li>
  <li>MCP·skills·plugins·agents 등 확장은 어느 경계에서 발견·로딩·실행되는가?</li>
  <li>실패·취소·재개·압축·동시성·격리·관측성을 어떻게 다루는가?</li>
  <li>규모가 커질 때 hot path와 운영 비용은 어떤 구조적 한계를 갖는가?</li>
  <li>Farad가 다른 제품에서 배워야 할 것과 의도적으로 달라야 할 것은 무엇인가?</li>
</ol>

<h2 id="공통-평가축">공통 평가축</h2>

<ul>
  <li>제품 경계와 배포 형태</li>
  <li>프로세스·스레드·클라이언트/서버 토폴로지</li>
  <li>에이전트 루프와 상태 머신</li>
  <li>컨텍스트·transcript·compaction</li>
  <li>도구·MCP·권한·sandbox</li>
  <li>세션·resume·branch/fork</li>
  <li>멀티에이전트·background work·동시성</li>
  <li>configuration·instruction·memory</li>
  <li>확장 모델(skills, plugins, hooks, custom agents)</li>
  <li>UI/UX와 human-in-the-loop</li>
  <li>모델/provider 결합도</li>
  <li>장애 경계·취소·재시도·관측성</li>
  <li>보안·신뢰 경계</li>
  <li>성능·대규모 상태·원격/NFS 운영</li>
  <li>테스트 가능성·모듈 경계·변경 용이성</li>
</ul>

<h2 id="증거-규칙">증거 규칙</h2>

<ul>
  <li><strong>A — 1차 구현 증거:</strong> 공개 소스의 특정 파일·타입·함수, Farad 현재 소스와 테스트.</li>
  <li><strong>B — 1차 문서 증거:</strong> 제품 공식 문서, 공식 저장소 문서, 공식 변경 이력.</li>
  <li><strong>C — 관찰 증거:</strong> 저장된 요청/응답 캡처나 재현 가능한 실행 관찰. 구현 세부의 확정 근거로는 제한한다.</li>
  <li><strong>D — 추론:</strong> A~C에서 도출한 해석. 보고서에서 사실과 분리하고 반증 가능성을 적는다.</li>
</ul>

<p>문서의 마케팅 표현은 런타임 보장으로 승격하지 않는다. 비공개 구현은 관찰 가능한 계약까지만
서술한다. 공개 소스 제품도 현재 브랜치의 구현과 안정된 사용자 계약을 구분한다.</p>

<h2 id="시간버전-기준">시간·버전 기준</h2>

<ul>
  <li>조사 기준일: 2026-07-13 (Asia/Seoul)</li>
  <li>웹 자료는 가능한 한 공식 도메인과 공식 GitHub 저장소를 사용한다.</li>
  <li>공개 저장소 분석에는 확인한 commit/tag를 기록한다.</li>
  <li>Farad는 사용자 변경을 포함한 현재 worktree를 기준으로 하되, 기존 변경을 수정하지 않는다.</li>
</ul>

<h2 id="판정-원칙">판정 원칙</h2>

<ul>
  <li>기능 수보다 핵심 use case에서의 응집도와 실패 시 예측 가능성을 높게 평가한다.</li>
  <li>서로 다른 제품 형태(CLI, client/server TUI, 데스크톱 앱)를 동일 UX 잣대로 억지 환산하지 않는다.</li>
  <li>Farad 권고는 <code class="language-plaintext highlighter-rouge">authority → thread/process → flow → hot path → persistence → failure boundary</code> 순서로
검토한다.</li>
  <li>Farad의 explicit refresh, NFS 비용, 누적 10,000개, transcript 재현성, UI/async 경계 계약은
편의 기능보다 우선하는 설계 제약으로 취급한다.</li>
</ul>]]></content><author><name>hvppycoding</name></author><category term="research" /><summary type="html"><![CDATA[OpenCode · Claude Code · Codex · Farad 아키텍처 비교 조사 방법론]]></summary></entry><entry><title type="html">Claude Code Workflow 툴과 Farad orchestrate-workflow 스킬: 한영 원문 대조와 종합 비교</title><link href="https://hvppycoding.github.io/farad/claude-code-workflow-vs-farad-orchestrate-workflow/" rel="alternate" type="text/html" title="Claude Code Workflow 툴과 Farad orchestrate-workflow 스킬: 한영 원문 대조와 종합 비교" /><published>2026-07-13T16:34:20+09:00</published><updated>2026-07-13T16:34:20+09:00</updated><id>https://hvppycoding.github.io/farad/claude-code-workflow-vs-farad-orchestrate-workflow</id><content type="html" xml:base="https://hvppycoding.github.io/farad/claude-code-workflow-vs-farad-orchestrate-workflow/"><![CDATA[<h1 id="claude-code-workflow-툴과-farad-orchestrate-workflow-스킬-한영-원문-대조와-종합-비교">Claude Code Workflow 툴과 Farad orchestrate-workflow 스킬: 한영 원문 대조와 종합 비교</h1>

<p>Claude Code의 <code class="language-plaintext highlighter-rouge">Workflow</code> 툴 설명과 Farad의 <code class="language-plaintext highlighter-rouge">orchestrate-workflow</code> 스킬 설명을 한국어 번역 한 단락, 영어 원문 한 단락 순서로 수록한다.</p>

<blockquote>
  <p>This document presents the Claude Code <code class="language-plaintext highlighter-rouge">Workflow</code> tool description and Farad's <code class="language-plaintext highlighter-rouge">orchestrate-workflow</code> skill description as paired Korean translations and English originals.</p>
</blockquote>

<p>설명 문장은 요약하지 않고 가능한 한 원문의 문장·목록·순서를 유지했다. 실행 가능한 JavaScript, JSON, Markdown 예시는 의미가 변하지 않도록 원문 그대로 둔다.</p>

<blockquote>
  <p>Prose is not summarized; the original sentence, list, and section order is preserved as closely as possible. Executable JavaScript, JSON, and Markdown examples remain unchanged so their meaning is not altered.</p>
</blockquote>

<h2 id="1-claude-code-workflow-툴-설명">1. Claude Code Workflow 툴 설명</h2>

<p>출처는 2026-05-30 ultracode 세션의 실제 LLM 요청에 포함된 <code class="language-plaintext highlighter-rouge">Workflow</code> 도구 description이다.</p>

<blockquote>
  <p>The source is the <code class="language-plaintext highlighter-rouge">Workflow</code> tool description included in an actual LLM request from the 2026-05-30 ultracode session.</p>
</blockquote>

<p>추출된 영문 원문의 SHA-256은 <code class="language-plaintext highlighter-rouge">824f5e29d5007bff5f2c28fd1283d371c5dd671a43bb542ace97028da4a076fe</code>이고, 줄 수는 159줄, 크기는 18,465바이트다.</p>

<blockquote>
  <p>The extracted English original has SHA-256 <code class="language-plaintext highlighter-rouge">824f5e29d5007bff5f2c28fd1283d371c5dd671a43bb542ace97028da4a076fe</code>, 159 lines, and 18,465 bytes.</p>
</blockquote>

<p>여러 서브에이전트를 결정적으로 오케스트레이션하는 워크플로 스크립트를 실행한다. 워크플로는 백그라운드에서 실행된다. 이 도구는 task ID와 함께 즉시 반환하고, 워크플로가 완료되면 <code class="language-plaintext highlighter-rouge">&lt;task-notification&gt;</code>이 도착한다. 실시간 진행 상황은 <code class="language-plaintext highlighter-rouge">/workflows</code>로 본다.</p>

<blockquote>
  <p>Execute a workflow script that orchestrates multiple subagents deterministically. Workflows run in the background — this tool returns immediately with a task ID, and a <task-notification> arrives when the workflow completes. Use /workflows to watch live progress.</task-notification></p>
</blockquote>

<p>워크플로는 많은 에이전트에 걸쳐 작업을 구조화한다. 포괄성을 위해 병렬로 분해·커버하고, 신뢰도를 위해 커밋 전에 독립 관점과 적대적 검증을 수행하며, 마이그레이션·감사·광범위한 탐색처럼 한 컨텍스트가 감당할 수 없는 규모를 처리한다. 무엇을 fan-out하고, 무엇을 검증하고, 무엇을 종합할지는 스크립트에 인코딩한다.</p>

<blockquote>
  <p>A workflow structures work across many agents — to be comprehensive (decompose and cover in parallel), to be confident (independent perspectives and adversarial checks before committing), or to take on scale one context can't hold (migrations, audits, broad sweeps). The script is where you encode that structure: what fans out, what verifies, what synthesizes.</p>
</blockquote>

<p>사용자가 멀티에이전트 오케스트레이션을 명시적으로 선택한 경우에만 이 도구를 호출한다. 워크플로는 수십 개의 에이전트를 생성하고 많은 토큰을 소비할 수 있으므로, 이 규모를 추론해서는 안 되고 사용자가 요청해야 한다. 명시적 opt-in은 다음 중 하나다.</p>

<blockquote>
  <p>ONLY call this tool when the user has explicitly opted into multi-agent orchestration. Workflows can spawn dozens of agents and consume a large amount of tokens; the user must request that scale, not have it inferred. Explicit opt-in means one of:</p>
</blockquote>

<ul>
  <li>사용자가 <code class="language-plaintext highlighter-rouge">workflow</code> 또는 <code class="language-plaintext highlighter-rouge">workflows</code> 키워드를 포함했다. 이를 확인하는 system-reminder가 보인다.</li>
  <li>The user included the "workflow" or "workflows" keyword (you'll see a system-reminder confirming it).</li>
  <li>Ultracode가 켜져 있고 system-reminder가 이를 확인한다. 아래 <strong>Ultracode</strong> 절을 참고한다.</li>
  <li>Ultracode is on (a system-reminder confirms it) — see <strong>Ultracode</strong> below.</li>
  <li>사용자가 자신의 말로 워크플로 실행 또는 멀티에이전트 오케스트레이션을 직접 요청했다. 요청은 사용자의 말이어야 하며, 단지 워크플로가 도움이 될 만한 작업이라는 이유는 해당하지 않는다.</li>
  <li>The user directly asked you to run a workflow or use multi-agent orchestration in their own words ("run a workflow", "fan out agents", "orchestrate this with subagents"). The ask must be in the user's words — a task that would merely benefit from a workflow does not count.</li>
  <li>사용자가 Workflow 호출을 지시하는 skill 또는 slash command를 실행했다.</li>
  <li>The user invoked a skill or slash command whose instructions tell you to call Workflow.</li>
  <li>사용자가 특정 named workflow 또는 saved workflow 실행을 요청했다.</li>
  <li>The user asked you to run a specific named or saved workflow.</li>
</ul>

<p>그 외 작업에서는 병렬화의 이점이 명백해도 이 도구를 호출하지 않는다. 개별 서브에이전트에는 Agent 도구를 쓰거나, 멀티에이전트 워크플로가 무엇을 하고 비용이 대략 얼마나 될지 짧게 설명한 뒤 실행 여부를 묻는다. 다음 메시지에 <code class="language-plaintext highlighter-rouge">workflow</code>를 포함하면 질문을 생략할 수 있다고 알려준다.</p>

<blockquote>
  <p>For any other task — even one that would clearly benefit from parallelism — do NOT call this tool. Use the Agent tool for individual subagents, or briefly describe what a multi-agent workflow could do and how much it would roughly cost, and ask the user whether to run it. Mention they can include "workflow" in a future message to skip the ask.</p>
</blockquote>

<p>호출할 때는 흔히 <strong>하이브리드</strong>가 적절하다. 먼저 인라인으로 정찰해 파일을 나열하고, 채널을 찾고, diff 범위를 확정하여 작업 목록을 발견한 다음, Workflow를 호출해 그 목록을 pipeline으로 처리한다. 작업 전에 모양을 알 필요는 없고 오케스트레이션 단계 전에만 알면 된다.</p>

<blockquote>
  <p>When you do call it, the right move is often <strong>hybrid</strong>: scout inline first (list the files, find the channels, scope the diff) to discover the work-list, then call Workflow to pipeline over it. You don't need to know the shape before the <em>task</em> — only before the <em>orchestration step</em>.</p>
</blockquote>

<p>턴을 가로질러 연결할 수 있는 흔한 단일 단계 워크플로는 다음과 같다.</p>

<blockquote>
  <p>Common single-phase workflows you can chain across turns:</p>
</blockquote>

<ul>
  <li><strong>이해</strong> — 관련 서브시스템을 병렬로 읽고 구조화된 맵을 만든다.</li>
  <li><strong>Understand</strong> — parallel readers over relevant subsystems → structured map</li>
  <li><strong>설계</strong> — N개의 독립 접근법을 judge panel로 평가하여 점수화된 종합안을 만든다.</li>
  <li><strong>Design</strong> — judge panel of N independent approaches → scored synthesis</li>
  <li><strong>리뷰</strong> — 차원별 탐색 후 발견하고 적대적으로 검증한다.</li>
  <li><strong>Review</strong> — dimensions → find → adversarially verify (example below)</li>
  <li><strong>리서치</strong> — 다중 방식으로 탐색하고 깊이 읽은 뒤 종합한다.</li>
  <li><strong>Research</strong> — multi-modal sweep → deep-read → synthesize</li>
  <li><strong>마이그레이션</strong> — 대상 위치를 찾고 worktree 격리 상태에서 각각 변환한 뒤 검증한다.</li>
  <li><strong>Migrate</strong> — discover sites → transform each (worktree isolation) → verify</li>
</ul>

<p>더 큰 작업은 여러 워크플로를 순서대로 실행한다. 다음 phase를 결정하기 전에 각 결과를 읽는다. 메인 에이전트는 루프 안에 머물고, 각 워크플로는 범위가 명확한 하나의 fan-out이다.</p>

<blockquote>
  <p>For larger work, run several in sequence — read each result before deciding the next phase. You stay in the loop; each workflow is one well-scoped fan-out.</p>
</blockquote>

<p><strong>Ultracode.</strong> system-reminder가 ultracode 활성 상태를 확인하면 그 opt-in은 계속 유효하다. 기본적으로 모든 실질적인 작업에 워크플로를 작성하고 실행한다. 목표는 가장 빠르거나 저렴한 답이 아니라 만들 수 있는 가장 포괄적이고 정확한 답이며, 토큰 비용은 제약이 아니다. 이해 → 설계 → 구현 → 리뷰 같은 다단계 작업에서는 루프 안에 머물기 위해 phase마다 하나씩 여러 워크플로를 순서대로 실행하는 경우가 많다. 아래의 품질 패턴, 즉 적대적 검증, 다중 방식 탐색, 완전성 비평가, 새로운 결과가 없을 때까지 반복하기를 작업에 맞게 고른다. 작업이 사소하거나 이미 검증된 경우가 아니면 워크플로 오케스트레이션과 적대적 검증 쪽으로 기운다. 대화성 턴이나 사소한 기계적 수정만 단독으로 수행한다. reminder가 ultracode 비활성화를 알리면 위의 표준 opt-in 규칙으로 돌아간다.</p>

<blockquote>
  <p><strong>Ultracode.</strong> When a system-reminder confirms ultracode is on, that opt-in is standing: author and run a workflow for every substantive task by default. The goal is the most exhaustive, correct answer you can produce — token cost is not a constraint. For multi-phase work (understand → design → implement → review), that often means several workflows in sequence — one per phase — so you stay in the loop between them. The quality patterns below (adversarial verify, multi-modal sweep, completeness critic, loop-until-dry) are the tools; pick what fits the task. Lean toward orchestrating with workflows and adversarially verifying your findings — unless the work is trivial or already verified. Solo only on conversational turns or trivial mechanical edits. When a reminder says ultracode is off, revert to the opt-in rule above.</p>
</blockquote>

<p>스크립트는 <code class="language-plaintext highlighter-rouge">script</code>로 인라인 전달하며 먼저 파일로 쓰지 않는다. 호출할 때마다 세션 디렉터리 아래에 스크립트가 자동 저장되고 도구 결과로 그 경로가 반환된다. 워크플로를 수정할 때는 전체 스크립트를 다시 보내지 말고 그 파일을 Write/Edit한 다음 <code class="language-plaintext highlighter-rouge">{scriptPath: "&lt;path&gt;"}</code>로 Workflow를 다시 호출한다.</p>

<blockquote>
  <p>Pass the script inline via <code class="language-plaintext highlighter-rouge">script</code> — do not Write it to a file first. Every invocation automatically persists its script to a file under the session directory and returns the path in the tool result. To iterate on a workflow, edit that file with Write/Edit and re-invoke Workflow with <code class="language-plaintext highlighter-rouge">{scriptPath: "&lt;path&gt;"}</code> instead of resending the full script.</p>
</blockquote>

<p>모든 스크립트는 <code class="language-plaintext highlighter-rouge">export const meta = {...}</code>로 시작해야 한다.</p>

<blockquote>
  <p>Every script must begin with <code class="language-plaintext highlighter-rouge">export const meta = {...}</code>:</p>
</blockquote>

<div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">export</span> <span class="kd">const</span> <span class="nx">meta</span> <span class="o">=</span> <span class="p">{</span>
  <span class="na">name</span><span class="p">:</span> <span class="dl">'</span><span class="s1">find-flaky-tests</span><span class="dl">'</span><span class="p">,</span>
  <span class="na">description</span><span class="p">:</span> <span class="dl">'</span><span class="s1">Find flaky tests and propose fixes</span><span class="dl">'</span><span class="p">,</span>   <span class="c1">// one-line, shown in permission dialog</span>
  <span class="na">phases</span><span class="p">:</span> <span class="p">[</span>
    <span class="p">{</span> <span class="na">title</span><span class="p">:</span> <span class="dl">'</span><span class="s1">Scan</span><span class="dl">'</span><span class="p">,</span> <span class="na">detail</span><span class="p">:</span> <span class="dl">'</span><span class="s1">grep test logs for retries</span><span class="dl">'</span> <span class="p">},</span>
    <span class="p">{</span> <span class="na">title</span><span class="p">:</span> <span class="dl">'</span><span class="s1">Fix</span><span class="dl">'</span><span class="p">,</span> <span class="na">detail</span><span class="p">:</span> <span class="dl">'</span><span class="s1">one agent per flaky test</span><span class="dl">'</span> <span class="p">},</span>
  <span class="p">],</span>
<span class="p">}</span>
<span class="c1">// script body starts here — use agent()/parallel()/pipeline()/phase()/log()</span>
<span class="nx">phase</span><span class="p">(</span><span class="dl">'</span><span class="s1">Scan</span><span class="dl">'</span><span class="p">)</span>
<span class="kd">const</span> <span class="nx">flaky</span> <span class="o">=</span> <span class="k">await</span> <span class="nx">agent</span><span class="p">(</span><span class="dl">'</span><span class="s1">grep CI logs for retry markers</span><span class="dl">'</span><span class="p">,</span> <span class="p">{</span><span class="na">schema</span><span class="p">:</span> <span class="nx">FLAKY_SCHEMA</span><span class="p">})</span>
<span class="p">...</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">meta</code> 객체는 변수, 함수 호출, spread, template interpolation이 없는 <strong>순수 리터럴</strong>이어야 한다. 필수 필드는 <code class="language-plaintext highlighter-rouge">name</code>과 <code class="language-plaintext highlighter-rouge">description</code>이고, <code class="language-plaintext highlighter-rouge">whenToUse</code>와 <code class="language-plaintext highlighter-rouge">phases</code>는 선택이다. <code class="language-plaintext highlighter-rouge">meta.phases</code>와 <code class="language-plaintext highlighter-rouge">phase()</code> 호출에 같은 phase title을 사용한다. 제목은 정확히 일치시킨다. meta 항목이 없는 <code class="language-plaintext highlighter-rouge">phase()</code> 호출은 자체 progress group을 만든다. 특정 모델 override를 사용하는 phase 항목에는 <code class="language-plaintext highlighter-rouge">model</code>을 추가한다.</p>

<blockquote>
  <p>The <code class="language-plaintext highlighter-rouge">meta</code> object must be a PURE LITERAL — no variables, function calls, spreads, or template interpolation. Required fields: <code class="language-plaintext highlighter-rouge">name</code>, <code class="language-plaintext highlighter-rouge">description</code>. Optional: <code class="language-plaintext highlighter-rouge">whenToUse</code> (shown in the workflow list), <code class="language-plaintext highlighter-rouge">phases</code>. Use the SAME phase titles in meta.phases as in phase() calls — titles are matched exactly; a phase() call with no matching meta entry just gets its own progress group. Add <code class="language-plaintext highlighter-rouge">model</code> to a phase entry when that phase uses a specific model override.</p>
</blockquote>

<p>스크립트 본문 hook은 다음과 같다.</p>

<blockquote>
  <p>Script body hooks:</p>
</blockquote>

<ul>
  <li><code class="language-plaintext highlighter-rouge">agent(prompt, opts)</code>는 서브에이전트를 생성한다. schema가 없으면 최종 텍스트 문자열을 반환하고, JSON Schema가 있으면 StructuredOutput 도구 호출을 강제하여 검증된 객체를 반환하므로 별도 파싱이 필요 없다. 사용자가 실행 중 agent를 skip하면 <code class="language-plaintext highlighter-rouge">null</code>을 반환하므로 <code class="language-plaintext highlighter-rouge">.filter(Boolean)</code>으로 거른다. <code class="language-plaintext highlighter-rouge">label</code>은 표시명을, <code class="language-plaintext highlighter-rouge">phase</code>는 progress group을, <code class="language-plaintext highlighter-rouge">model</code>은 해당 agent 모델을 override한다. 모델은 기본적으로 생략해 main-loop 모델을 상속한다. 확신할 때만 다른 tier를 지정한다. <code class="language-plaintext highlighter-rouge">isolation: 'worktree'</code>는 병렬 수정 충돌을 막아야 할 때만 새 git worktree에서 실행한다. agent마다 약 200–500ms와 디스크 비용이 드는 비싼 옵션이다. <code class="language-plaintext highlighter-rouge">agentType</code>은 기본 workflow subagent 대신 Explore나 code-reviewer 같은 custom subagent type을 사용하며 schema와 조합할 수 있다.</li>
  <li>
    <p>agent(prompt: string, opts?: {label?: string, phase?: string, schema?: object, model?: string, isolation?: 'worktree', agentType?: string}): Promise<any> — spawn a subagent. Without schema, returns its final text as a string. With schema (a JSON Schema), the subagent is forced to call a StructuredOutput tool and agent() returns the validated object — no parsing needed. Returns null if the user skips the agent mid-run (filter with .filter(Boolean)). opts.label overrides the display label. opts.phase explicitly assigns this agent to a progress group (use this inside pipeline()/parallel() stages to avoid races on the global phase() state — same phase string → same group box). opts.model overrides the model for this agent call. Default to omitting it — the agent inherits the main-loop model (the resolved session model), which is almost always correct. Only set it when you're highly confident a different tier fits the task; when unsure, omit. opts.isolation: 'worktree' runs the agent in a fresh git worktree — EXPENSIVE (~200-500ms setup + disk per agent), use ONLY when agents mutate files in parallel and would otherwise conflict; the worktree is auto-removed if unchanged. opts.agentType uses a custom subagent type (e.g. 'Explore', 'code-reviewer') instead of the default workflow subagent — resolved from the same registry as the Agent tool; composes with schema (the custom agent's system prompt gets a StructuredOutput instruction appended).</any></p>
  </li>
  <li><code class="language-plaintext highlighter-rouge">pipeline(items, stage1, stage2, ...)</code>은 각 item을 모든 stage에 독립적으로 통과시키며 stage 사이에 barrier가 없다. A가 stage 3에 있을 때 B는 stage 1에 있을 수 있다. 이것이 다단계 작업의 기본값이다. wall-clock은 각 stage의 최장 시간 합이 아니라 가장 느린 단일 item chain이다. 모든 callback은 이전 결과, 원본 item, index를 받는다. stage가 throw하면 해당 item은 <code class="language-plaintext highlighter-rouge">null</code>로 떨어지고 남은 stage를 건너뛴다.</li>
  <li>
    <p>pipeline(items, stage1, stage2, …): Promise&lt;any[]&gt; — run each item through all stages independently, NO barrier between stages. Item A can be in stage 3 while Item B is still in stage 1. This is the DEFAULT for multi-stage work. Wall-clock = slowest single-item chain, not sum-of-slowest-per-stage. Every stage callback receives (prevResult, originalItem, index) — use originalItem/index in later stages to label work without threading context through stage 1's return value. A stage that throws drops that item to <code class="language-plaintext highlighter-rouge">null</code> and skips its remaining stages.</p>
  </li>
  <li><code class="language-plaintext highlighter-rouge">parallel(thunks)</code>는 task를 동시에 실행하는 <strong>barrier</strong>로 모든 thunk를 기다린 뒤 반환한다. throw 또는 agent error가 난 thunk는 <code class="language-plaintext highlighter-rouge">null</code>로 resolve되고 호출 자체는 reject되지 않는다. 전체 결과가 정말 함께 필요할 때만 사용한다.</li>
  <li>
    <p>parallel(thunks: Array&lt;() =&gt; Promise<any>&gt;): Promise&lt;any[]&gt; — run tasks concurrently. This is a BARRIER: awaits all thunks before returning. A thunk that throws (or whose agent errors) resolves to `null` in the result array — the call itself never rejects, so `.filter(Boolean)` before using the results. Use ONLY when you genuinely need all results together.</any></p>
  </li>
  <li><code class="language-plaintext highlighter-rouge">log(message)</code>는 progress tree 위 narrator line으로 사용자에게 진행 메시지를 보낸다.</li>
  <li>
    <p>log(message: string): void — emit a progress message to the user (shown as a narrator line above the progress tree)</p>
  </li>
  <li><code class="language-plaintext highlighter-rouge">phase(title)</code>은 새 phase를 시작하고 이후 agent 호출을 해당 progress group에 넣는다.</li>
  <li>
    <p>phase(title: string): void — start a new phase; subsequent agent() calls are grouped under this title in the progress display</p>
  </li>
  <li><code class="language-plaintext highlighter-rouge">args</code>는 Workflow 호출의 <code class="language-plaintext highlighter-rouge">args</code> 입력값을 그대로 받는다. 배열과 객체는 JSON 값 자체로 전달하고 JSON 문자열로 다시 인코딩하지 않는다. named workflow에 리서치 질문, 대상 경로, 설정 객체 등을 side-channel 파일 없이 전달할 때 쓴다.</li>
  <li>
    <p>args: any — the value passed as Workflow's <code class="language-plaintext highlighter-rouge">args</code> input, verbatim (undefined if not provided). Pass arrays/objects as actual JSON values in the tool call, NOT as a JSON-encoded string — <code class="language-plaintext highlighter-rouge">args: ["a.ts", "b.ts"]</code>, not <code class="language-plaintext highlighter-rouge">args: "[\"a.ts\", ...]"</code> (a stringified list reaches the script as one string, so <code class="language-plaintext highlighter-rouge">args.filter</code>/<code class="language-plaintext highlighter-rouge">args.map</code> throw). Use this to parameterize named workflows — e.g. pass a research question, target path, or config object directly instead of via a side-channel file.</p>
  </li>
  <li><code class="language-plaintext highlighter-rouge">budget</code>은 사용자의 <code class="language-plaintext highlighter-rouge">+500k</code> 스타일 지시에서 온 turn token target이다. target이 없으면 <code class="language-plaintext highlighter-rouge">total</code>은 null이다. <code class="language-plaintext highlighter-rouge">spent()</code>는 main loop와 모든 workflow의 이번 turn 출력 토큰을 합산하고, <code class="language-plaintext highlighter-rouge">remaining()</code>은 남은 양 또는 target이 없을 때 Infinity를 반환한다. target은 권고가 아닌 hard ceiling이다. 도달하면 이후 <code class="language-plaintext highlighter-rouge">agent()</code>가 throw한다.</li>
  <li>
    <table>
      <tbody>
        <tr>
          <td>budget: {total: number</td>
          <td>null, spent(): number, remaining(): number} — the turn's token target from the user's "+500k"-style directive. <code class="language-plaintext highlighter-rouge">budget.total</code> is null if no target was set. <code class="language-plaintext highlighter-rouge">budget.spent()</code> returns output tokens spent this turn across the main loop and all workflows — the pool is shared, not per-workflow. <code class="language-plaintext highlighter-rouge">budget.remaining()</code> returns <code class="language-plaintext highlighter-rouge">max(0, total - spent())</code>, or <code class="language-plaintext highlighter-rouge">Infinity</code> if no target. The target is a HARD ceiling, not advisory: once <code class="language-plaintext highlighter-rouge">spent()</code> reaches <code class="language-plaintext highlighter-rouge">total</code>, further <code class="language-plaintext highlighter-rouge">agent()</code> calls throw. Use for dynamic loops: <code class="language-plaintext highlighter-rouge">while (budget.total &amp;&amp; budget.remaining() &gt; 50_000) { ... }</code>, or static scaling: <code class="language-plaintext highlighter-rouge">const FLEET = budget.total ? Math.floor(budget.total / 100_000) : 5</code>.</td>
        </tr>
      </tbody>
    </table>
  </li>
  <li><code class="language-plaintext highlighter-rouge">workflow(nameOrRef, args)</code>는 다른 workflow를 inline sub-step으로 실행하여 결과를 반환한다. name으로 saved workflow를 실행하거나 <code class="language-plaintext highlighter-rouge">scriptPath</code>로 앞서 저장한 스크립트를 실행한다. 자식은 부모의 concurrency cap, agent counter, abort signal, token budget을 공유한다. nesting은 한 단계만 허용되며 자식 안의 <code class="language-plaintext highlighter-rouge">workflow()</code>는 throw한다. 알 수 없는 이름, 읽을 수 없는 경로, syntax error도 throw한다.</li>
  <li>
    <table>
      <tbody>
        <tr>
          <td>workflow(nameOrRef: string</td>
          <td>{scriptPath: string}, args?: any): Promise<any> — run another workflow inline as a sub-step and return whatever it returns. Pass a name to invoke a saved workflow (same registry as {name: "..."}), or {scriptPath} to run a script file you Wrote earlier. The child shares this run's concurrency cap, agent counter, abort signal, and token budget — its agents appear under a "▸ name" group in /workflows and its tokens count toward budget.spent(). The args param becomes the child's `args` global. Nesting is one level only: workflow() inside a child throws. Throws on unknown name / unreadable scriptPath / child syntax error; catch to handle gracefully.</any></td>
        </tr>
      </tbody>
    </table>
  </li>
</ul>

<p>서브에이전트는 최종 텍스트가 곧 반환값이라는 지시를 받으므로 사람에게 보내는 메시지가 아닌 raw data를 반환한다. structured output에는 schema를 사용한다. validation은 tool-call layer에서 이루어져 불일치 시 모델이 재시도한다.</p>

<blockquote>
  <p>Subagents are told their final text IS the return value (not a human-facing message), so they return raw data. For structured output, use the schema option — validation happens at the tool-call layer so the model retries on mismatch.</p>
</blockquote>

<p>Workflow agent는 ToolSearch를 통해 세션에 연결된 모든 MCP 도구에 접근하며 schema는 agent마다 on-demand로 로드된다. 대화형 인증을 거친 MCP 서버는 headless 또는 cron 실행에서 없을 수 있다.</p>

<blockquote>
  <p>Workflow agents can reach all session-connected MCP tools via ToolSearch — schemas load on demand per agent. Caveat: interactively-authenticated MCP servers (e.g. claude.ai) may be absent in headless/cron runs.</p>
</blockquote>

<p>스크립트는 TypeScript가 아닌 일반 JavaScript다. type annotation, interface, generic은 parsing에 실패한다. 본문은 async context에서 실행되므로 await를 직접 쓴다. 표준 JavaScript built-in은 사용할 수 있지만 <code class="language-plaintext highlighter-rouge">Date.now()</code>, <code class="language-plaintext highlighter-rouge">Math.random()</code>, 인자 없는 <code class="language-plaintext highlighter-rouge">new Date()</code>는 resume을 깨므로 throw한다. timestamp는 <code class="language-plaintext highlighter-rouge">args</code>로 넣고 결과가 반환된 뒤 찍으며, randomness는 index별 prompt나 label을 달리해 만든다. filesystem과 Node.js API는 사용할 수 없다.</p>

<blockquote>
  <p>Scripts are plain JavaScript, NOT TypeScript — type annotations (<code class="language-plaintext highlighter-rouge">: string[]</code>), interfaces, and generics fail to parse. The script body runs in an async context — use await directly. Standard JS built-ins (JSON, Math, Array, etc.) are available — EXCEPT <code class="language-plaintext highlighter-rouge">Date.now()</code>/<code class="language-plaintext highlighter-rouge">Math.random()</code>/argless <code class="language-plaintext highlighter-rouge">new Date()</code>, which throw (they would break resume); pass timestamps in via <code class="language-plaintext highlighter-rouge">args</code>, stamp results after the workflow returns, and for randomness vary the agent prompt/label by index. No filesystem or Node.js API access.</p>
</blockquote>

<p>기본은 pipeline이다. stage 사이 barrier는 다음 stage가 모든 이전 stage 결과를 정말로 함께 필요로 할 때만 사용한다.</p>

<blockquote>
  <p>DEFAULT TO pipeline(). Only reach for a barrier (parallel between stages) when you genuinely need ALL prior-stage results together.</p>
</blockquote>

<p>barrier가 옳은 경우는 stage N이 stage N-1 전체 item의 교차 context를 필요로 할 때뿐이다.</p>

<blockquote>
  <p>A barrier is correct ONLY when stage N needs cross-item context from all of stage N-1:</p>
</blockquote>

<ul>
  <li>비싼 downstream 작업 전에 전체 결과를 dedup/merge한다.</li>
  <li>Dedup/merge across the full result set before expensive downstream work</li>
  <li>총 개수가 0이면 일찍 종료한다.</li>
  <li>Early-exit if the total count is zero ("0 bugs found → skip verification entirely")</li>
  <li>stage N prompt가 비교를 위해 다른 finding을 참조한다.</li>
  <li>Stage N's prompt references "the other findings" for comparison</li>
</ul>

<p>다음 이유는 barrier를 정당화하지 않는다.</p>

<blockquote>
  <p>A barrier is NOT justified by:</p>
</blockquote>

<ul>
  <li>먼저 flatten/map/filter가 필요하다는 이유. pipeline stage 안에서 한다.</li>
  <li>"I need to flatten/map/filter first" — do it inside a pipeline stage: pipeline(items, stageA, r =&gt; transform([r]).flat(), stageB)</li>
  <li>stage가 개념적으로 분리돼 있다는 이유. pipeline이 바로 그것을 모델링한다. 분리된 stage가 동기화된 stage를 뜻하지 않는다.</li>
  <li>"The stages are conceptually separate" — that's what pipeline() models. Separate stages ≠ synchronized stages.</li>
  <li>코드가 더 깔끔하다는 이유. barrier latency는 실제다. finder 5개 중 가장 느린 것이 가장 빠른 것보다 세 배 걸리면 빠른 finder 유휴시간의 3분의 2가 낭비된다.</li>
  <li>"It's cleaner code" — barrier latency is real. If 5 finders run and the slowest takes 3× the fastest, a barrier wastes 2/3 of the fast finders' idle time.</li>
</ul>

<p>다음과 같은 smell이 있다면 중간 변환은 barrier가 필요하지 않다. 변환을 stage 안에 넣은 pipeline으로 다시 쓴다. 의심스러우면 pipeline을 선택한다.</p>

<blockquote>
  <p>Smell test: if you wrote</p>
</blockquote>

<div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">const</span> <span class="nx">a</span> <span class="o">=</span> <span class="k">await</span> <span class="nx">parallel</span><span class="p">(...)</span>
<span class="kd">const</span> <span class="nx">b</span> <span class="o">=</span> <span class="nx">transform</span><span class="p">(</span><span class="nx">a</span><span class="p">)</span>        <span class="c1">// flatten, map, filter — no cross-item dependency</span>
<span class="kd">const</span> <span class="nx">c</span> <span class="o">=</span> <span class="k">await</span> <span class="nx">parallel</span><span class="p">(</span><span class="nx">b</span><span class="p">.</span><span class="nx">map</span><span class="p">(...))</span>
</code></pre></div></div>

<blockquote>
  <p>that middle transform doesn't need the barrier. Rewrite as a pipeline with the transform inside a stage. When in doubt: pipeline.</p>
</blockquote>

<p>동시 <code class="language-plaintext highlighter-rouge">agent()</code> 호출은 workflow마다 <code class="language-plaintext highlighter-rouge">min(16, cpu cores - 2)</code>로 제한되고 초과 호출은 slot이 빌 때까지 queue된다. <code class="language-plaintext highlighter-rouge">parallel()</code>이나 <code class="language-plaintext highlighter-rouge">pipeline()</code>에 100개 item을 넣어도 모두 완료되며 한 순간에는 약 10개만 실행된다. workflow 수명 전체의 agent 수는 runaway loop 방지용으로 1000개까지다.</p>

<blockquote>
  <p>Concurrent agent() calls are capped at min(16, cpu cores - 2) per workflow — excess calls queue and run as slots free up. You can still pass 100 items to parallel()/pipeline() and they all complete; only ~10 run at any moment. Total agent count across a workflow's lifetime is capped at 1000 — a runaway-loop backstop set far above any real workflow.</p>
</blockquote>

<p>정본 다단계 패턴은 pipeline을 기본으로 하고, 각 dimension의 review가 끝나는 즉시 그 dimension을 검증한다.</p>

<blockquote>
  <p>The canonical multi-stage pattern — pipeline by default, each dimension verifies as soon as its review completes:</p>
</blockquote>

<div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">export</span> <span class="kd">const</span> <span class="nx">meta</span> <span class="o">=</span> <span class="p">{</span>
  <span class="na">name</span><span class="p">:</span> <span class="dl">'</span><span class="s1">review-changes</span><span class="dl">'</span><span class="p">,</span>
  <span class="na">description</span><span class="p">:</span> <span class="dl">'</span><span class="s1">Review changed files across dimensions, verify each finding</span><span class="dl">'</span><span class="p">,</span>
  <span class="na">phases</span><span class="p">:</span> <span class="p">[{</span> <span class="na">title</span><span class="p">:</span> <span class="dl">'</span><span class="s1">Review</span><span class="dl">'</span> <span class="p">},</span> <span class="p">{</span> <span class="na">title</span><span class="p">:</span> <span class="dl">'</span><span class="s1">Verify</span><span class="dl">'</span> <span class="p">}],</span>
<span class="p">}</span>
<span class="kd">const</span> <span class="nx">DIMENSIONS</span> <span class="o">=</span> <span class="p">[{</span><span class="na">key</span><span class="p">:</span> <span class="dl">'</span><span class="s1">bugs</span><span class="dl">'</span><span class="p">,</span> <span class="na">prompt</span><span class="p">:</span> <span class="dl">'</span><span class="s1">...</span><span class="dl">'</span><span class="p">},</span> <span class="p">{</span><span class="na">key</span><span class="p">:</span> <span class="dl">'</span><span class="s1">perf</span><span class="dl">'</span><span class="p">,</span> <span class="na">prompt</span><span class="p">:</span> <span class="dl">'</span><span class="s1">...</span><span class="dl">'</span><span class="p">}]</span>
<span class="kd">const</span> <span class="nx">results</span> <span class="o">=</span> <span class="k">await</span> <span class="nx">pipeline</span><span class="p">(</span>
  <span class="nx">DIMENSIONS</span><span class="p">,</span>
  <span class="nx">d</span> <span class="o">=&gt;</span> <span class="nx">agent</span><span class="p">(</span><span class="nx">d</span><span class="p">.</span><span class="nx">prompt</span><span class="p">,</span> <span class="p">{</span><span class="na">label</span><span class="p">:</span> <span class="s2">`review:</span><span class="p">${</span><span class="nx">d</span><span class="p">.</span><span class="nx">key</span><span class="p">}</span><span class="s2">`</span><span class="p">,</span> <span class="na">phase</span><span class="p">:</span> <span class="dl">'</span><span class="s1">Review</span><span class="dl">'</span><span class="p">,</span> <span class="na">schema</span><span class="p">:</span> <span class="nx">FINDINGS_SCHEMA</span><span class="p">}),</span>
  <span class="nx">review</span> <span class="o">=&gt;</span> <span class="nx">parallel</span><span class="p">(</span><span class="nx">review</span><span class="p">.</span><span class="nx">findings</span><span class="p">.</span><span class="nx">map</span><span class="p">(</span><span class="nx">f</span> <span class="o">=&gt;</span> <span class="p">()</span> <span class="o">=&gt;</span>
    <span class="nx">agent</span><span class="p">(</span><span class="s2">`Adversarially verify: </span><span class="p">${</span><span class="nx">f</span><span class="p">.</span><span class="nx">title</span><span class="p">}</span><span class="s2">`</span><span class="p">,</span> <span class="p">{</span><span class="na">label</span><span class="p">:</span> <span class="s2">`verify:</span><span class="p">${</span><span class="nx">f</span><span class="p">.</span><span class="nx">file</span><span class="p">}</span><span class="s2">`</span><span class="p">,</span> <span class="na">phase</span><span class="p">:</span> <span class="dl">'</span><span class="s1">Verify</span><span class="dl">'</span><span class="p">,</span> <span class="na">schema</span><span class="p">:</span> <span class="nx">VERDICT_SCHEMA</span><span class="p">})</span>
      <span class="p">.</span><span class="nx">then</span><span class="p">(</span><span class="nx">v</span> <span class="o">=&gt;</span> <span class="p">({...</span><span class="nx">f</span><span class="p">,</span> <span class="na">verdict</span><span class="p">:</span> <span class="nx">v</span><span class="p">}))</span>
  <span class="p">))</span>
<span class="p">)</span>
<span class="kd">const</span> <span class="nx">confirmed</span> <span class="o">=</span> <span class="nx">results</span><span class="p">.</span><span class="nx">flat</span><span class="p">().</span><span class="nx">filter</span><span class="p">(</span><span class="nb">Boolean</span><span class="p">).</span><span class="nx">filter</span><span class="p">(</span><span class="nx">f</span> <span class="o">=&gt;</span> <span class="nx">f</span><span class="p">.</span><span class="nx">verdict</span><span class="p">?.</span><span class="nx">isReal</span><span class="p">)</span>
<span class="k">return</span> <span class="p">{</span> <span class="nx">confirmed</span> <span class="p">}</span>
<span class="c1">// Dimension 'bugs' findings verify while dimension 'perf' is still reviewing. No wasted wall-clock.</span>
</code></pre></div></div>

<p>비싼 검증 전에 모든 finding을 dedup해야 할 때는 barrier가 올바르다.</p>

<blockquote>
  <p>When a barrier IS correct — dedup across all findings before expensive verification:</p>
</blockquote>

<div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">const</span> <span class="nx">all</span> <span class="o">=</span> <span class="k">await</span> <span class="nx">parallel</span><span class="p">(</span><span class="nx">DIMENSIONS</span><span class="p">.</span><span class="nx">map</span><span class="p">(</span><span class="nx">d</span> <span class="o">=&gt;</span> <span class="p">()</span> <span class="o">=&gt;</span> <span class="nx">agent</span><span class="p">(</span><span class="nx">d</span><span class="p">.</span><span class="nx">prompt</span><span class="p">,</span> <span class="p">{</span><span class="na">schema</span><span class="p">:</span> <span class="nx">FINDINGS_SCHEMA</span><span class="p">})))</span>
<span class="kd">const</span> <span class="nx">deduped</span> <span class="o">=</span> <span class="nx">dedupeByFileAndLine</span><span class="p">(</span><span class="nx">all</span><span class="p">.</span><span class="nx">filter</span><span class="p">(</span><span class="nb">Boolean</span><span class="p">).</span><span class="nx">flatMap</span><span class="p">(</span><span class="nx">r</span> <span class="o">=&gt;</span> <span class="nx">r</span><span class="p">.</span><span class="nx">findings</span><span class="p">))</span>  <span class="c1">// &lt;-- genuinely needs ALL at once</span>
<span class="kd">const</span> <span class="nx">verified</span> <span class="o">=</span> <span class="k">await</span> <span class="nx">parallel</span><span class="p">(</span><span class="nx">deduped</span><span class="p">.</span><span class="nx">map</span><span class="p">(</span><span class="nx">f</span> <span class="o">=&gt;</span> <span class="p">()</span> <span class="o">=&gt;</span> <span class="nx">agent</span><span class="p">(</span><span class="nx">verifyPrompt</span><span class="p">(</span><span class="nx">f</span><span class="p">),</span> <span class="p">{</span><span class="na">schema</span><span class="p">:</span> <span class="nx">VERDICT_SCHEMA</span><span class="p">})))</span>
</code></pre></div></div>

<p>목표 개수에 도달할 때까지 누적하는 loop-until-count 패턴은 다음과 같다.</p>

<blockquote>
  <p>Loop-until-count pattern — accumulate to a target:</p>
</blockquote>

<div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">const</span> <span class="nx">bugs</span> <span class="o">=</span> <span class="p">[]</span>
<span class="k">while</span> <span class="p">(</span><span class="nx">bugs</span><span class="p">.</span><span class="nx">length</span> <span class="o">&lt;</span> <span class="mi">10</span><span class="p">)</span> <span class="p">{</span>
  <span class="kd">const</span> <span class="nx">result</span> <span class="o">=</span> <span class="k">await</span> <span class="nx">agent</span><span class="p">(</span><span class="dl">"</span><span class="s2">Find bugs in this codebase.</span><span class="dl">"</span><span class="p">,</span> <span class="p">{</span><span class="na">schema</span><span class="p">:</span> <span class="nx">BUGS_SCHEMA</span><span class="p">})</span>
  <span class="nx">bugs</span><span class="p">.</span><span class="nx">push</span><span class="p">(...</span><span class="nx">result</span><span class="p">.</span><span class="nx">bugs</span><span class="p">)</span>
  <span class="nx">log</span><span class="p">(</span><span class="s2">`</span><span class="p">${</span><span class="nx">bugs</span><span class="p">.</span><span class="nx">length</span><span class="p">}</span><span class="s2">/10 found`</span><span class="p">)</span>
<span class="p">}</span>
</code></pre></div></div>

<p>사용자의 <code class="language-plaintext highlighter-rouge">+500k</code> 지시에 맞춰 깊이를 조절하는 loop-until-budget 패턴은 <code class="language-plaintext highlighter-rouge">budget.total</code>을 guard해야 한다. target이 없으면 remaining은 Infinity라서 guard가 없을 경우 agent 1000개 cap까지 곧장 실행된다.</p>

<blockquote>
  <p>Loop-until-budget pattern — scale depth to the user's "+500k" directive. Guard on budget.total: with no target set, remaining() is Infinity and the loop would run straight to the 1000-agent cap.</p>
</blockquote>

<div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">const</span> <span class="nx">bugs</span> <span class="o">=</span> <span class="p">[]</span>
<span class="k">while</span> <span class="p">(</span><span class="nx">budget</span><span class="p">.</span><span class="nx">total</span> <span class="o">&amp;&amp;</span> <span class="nx">budget</span><span class="p">.</span><span class="nx">remaining</span><span class="p">()</span> <span class="o">&gt;</span> <span class="mi">50</span><span class="nx">_000</span><span class="p">)</span> <span class="p">{</span>
  <span class="kd">const</span> <span class="nx">result</span> <span class="o">=</span> <span class="k">await</span> <span class="nx">agent</span><span class="p">(</span><span class="dl">"</span><span class="s2">Find bugs in this codebase.</span><span class="dl">"</span><span class="p">,</span> <span class="p">{</span><span class="na">schema</span><span class="p">:</span> <span class="nx">BUGS_SCHEMA</span><span class="p">})</span>
  <span class="nx">bugs</span><span class="p">.</span><span class="nx">push</span><span class="p">(...</span><span class="nx">result</span><span class="p">.</span><span class="nx">bugs</span><span class="p">)</span>
  <span class="nx">log</span><span class="p">(</span><span class="s2">`</span><span class="p">${</span><span class="nx">bugs</span><span class="p">.</span><span class="nx">length</span><span class="p">}</span><span class="s2"> found, </span><span class="p">${</span><span class="nb">Math</span><span class="p">.</span><span class="nx">round</span><span class="p">(</span><span class="nx">budget</span><span class="p">.</span><span class="nx">remaining</span><span class="p">()</span><span class="o">/</span><span class="mi">1000</span><span class="p">)}</span><span class="s2">k remaining`</span><span class="p">)</span>
<span class="p">}</span>
</code></pre></div></div>

<p>패턴을 조합한 exhaustive review는 찾기, 전체 seen 대비 dedup, 다양한 lens panel, 두 번 연속 새 결과가 없을 때까지 반복하는 순서다.</p>

<blockquote>
  <p>Composing patterns — exhaustive review (find → dedup vs seen → diverse-lens panel → loop-until-dry):</p>
</blockquote>

<div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">const</span> <span class="nx">seen</span> <span class="o">=</span> <span class="k">new</span> <span class="nb">Set</span><span class="p">(),</span> <span class="nx">confirmed</span> <span class="o">=</span> <span class="p">[]</span>
<span class="kd">let</span> <span class="nx">dry</span> <span class="o">=</span> <span class="mi">0</span>
<span class="k">while</span> <span class="p">(</span><span class="nx">dry</span> <span class="o">&lt;</span> <span class="mi">2</span><span class="p">)</span> <span class="p">{</span>
  <span class="kd">const</span> <span class="nx">found</span> <span class="o">=</span> <span class="p">(</span><span class="k">await</span> <span class="nx">parallel</span><span class="p">(</span><span class="nx">FINDERS</span><span class="p">.</span><span class="nx">map</span><span class="p">(</span><span class="nx">f</span> <span class="o">=&gt;</span> <span class="p">()</span> <span class="o">=&gt;</span>
    <span class="nx">agent</span><span class="p">(</span><span class="nx">f</span><span class="p">.</span><span class="nx">prompt</span><span class="p">,</span> <span class="p">{</span><span class="na">phase</span><span class="p">:</span> <span class="dl">'</span><span class="s1">Find</span><span class="dl">'</span><span class="p">,</span> <span class="na">schema</span><span class="p">:</span> <span class="nx">BUGS</span><span class="p">})))).</span><span class="nx">filter</span><span class="p">(</span><span class="nb">Boolean</span><span class="p">).</span><span class="nx">flatMap</span><span class="p">(</span><span class="nx">r</span> <span class="o">=&gt;</span> <span class="nx">r</span><span class="p">.</span><span class="nx">bugs</span><span class="p">)</span>
  <span class="kd">const</span> <span class="nx">fresh</span> <span class="o">=</span> <span class="nx">found</span><span class="p">.</span><span class="nx">filter</span><span class="p">(</span><span class="nx">b</span> <span class="o">=&gt;</span> <span class="o">!</span><span class="nx">seen</span><span class="p">.</span><span class="nx">has</span><span class="p">(</span><span class="nx">key</span><span class="p">(</span><span class="nx">b</span><span class="p">)))</span>
  <span class="k">if</span> <span class="p">(</span><span class="o">!</span><span class="nx">fresh</span><span class="p">.</span><span class="nx">length</span><span class="p">)</span> <span class="p">{</span> <span class="nx">dry</span><span class="o">++</span><span class="p">;</span> <span class="k">continue</span> <span class="p">}</span>
  <span class="nx">dry</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span> <span class="nx">fresh</span><span class="p">.</span><span class="nx">forEach</span><span class="p">(</span><span class="nx">b</span> <span class="o">=&gt;</span> <span class="nx">seen</span><span class="p">.</span><span class="nx">add</span><span class="p">(</span><span class="nx">key</span><span class="p">(</span><span class="nx">b</span><span class="p">)))</span>
  <span class="kd">const</span> <span class="nx">judged</span> <span class="o">=</span> <span class="k">await</span> <span class="nx">parallel</span><span class="p">(</span><span class="nx">fresh</span><span class="p">.</span><span class="nx">map</span><span class="p">(</span><span class="nx">b</span> <span class="o">=&gt;</span> <span class="p">()</span> <span class="o">=&gt;</span>
    <span class="nx">parallel</span><span class="p">([</span><span class="dl">'</span><span class="s1">correctness</span><span class="dl">'</span><span class="p">,</span><span class="dl">'</span><span class="s1">security</span><span class="dl">'</span><span class="p">,</span><span class="dl">'</span><span class="s1">repro</span><span class="dl">'</span><span class="p">].</span><span class="nx">map</span><span class="p">(</span><span class="nx">lens</span> <span class="o">=&gt;</span> <span class="p">()</span> <span class="o">=&gt;</span>
      <span class="nx">agent</span><span class="p">(</span><span class="s2">`Judge "</span><span class="p">${</span><span class="nx">b</span><span class="p">.</span><span class="nx">desc</span><span class="p">}</span><span class="s2">" via the </span><span class="p">${</span><span class="nx">lens</span><span class="p">}</span><span class="s2"> lens — real?`</span><span class="p">,</span> <span class="p">{</span><span class="na">phase</span><span class="p">:</span> <span class="dl">'</span><span class="s1">Verify</span><span class="dl">'</span><span class="p">,</span> <span class="na">schema</span><span class="p">:</span> <span class="nx">VERDICT</span><span class="p">})))</span>
      <span class="p">.</span><span class="nx">then</span><span class="p">(</span><span class="nx">vs</span> <span class="o">=&gt;</span> <span class="p">({</span> <span class="nx">b</span><span class="p">,</span> <span class="na">real</span><span class="p">:</span> <span class="nx">vs</span><span class="p">.</span><span class="nx">filter</span><span class="p">(</span><span class="nb">Boolean</span><span class="p">).</span><span class="nx">filter</span><span class="p">(</span><span class="nx">v</span> <span class="o">=&gt;</span> <span class="nx">v</span><span class="p">.</span><span class="nx">real</span><span class="p">).</span><span class="nx">length</span> <span class="o">&gt;=</span> <span class="mi">2</span> <span class="p">}))))</span>
  <span class="nx">confirmed</span><span class="p">.</span><span class="nx">push</span><span class="p">(...</span><span class="nx">judged</span><span class="p">.</span><span class="nx">filter</span><span class="p">(</span><span class="nx">v</span> <span class="o">=&gt;</span> <span class="nx">v</span><span class="p">.</span><span class="nx">real</span><span class="p">).</span><span class="nx">map</span><span class="p">(</span><span class="nx">v</span> <span class="o">=&gt;</span> <span class="nx">v</span><span class="p">.</span><span class="nx">b</span><span class="p">))</span>
<span class="p">}</span>
<span class="k">return</span> <span class="nx">confirmed</span>
<span class="c1">// dedup vs `seen`, NOT `confirmed` — else judge-rejected findings reappear every round and it never converges.</span>
</code></pre></div></div>

<p>품질 패턴은 흔한 형태이며 작업에 맞게 자유롭게 고르고 조합한다.</p>

<blockquote>
  <p>Quality patterns — common shapes; pick by task and compose freely:</p>
</blockquote>

<ul>
  <li><strong>적대적 검증:</strong> finding마다 N명의 독립 skeptic을 생성하고 각각 REFUTE하도록 지시한다. 과반이 반박하면 제거하여 그럴듯하지만 틀린 finding의 생존을 막는다.</li>
  <li>Adversarial verify: spawn N independent skeptics per finding, each prompted to REFUTE. Kill if ≥majority refute. Prevents plausible-but-wrong findings from surviving.</li>
</ul>

<div class="language-javascript highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">const</span> <span class="nx">votes</span> <span class="o">=</span> <span class="k">await</span> <span class="nx">parallel</span><span class="p">(</span><span class="nb">Array</span><span class="p">.</span><span class="k">from</span><span class="p">({</span><span class="na">length</span><span class="p">:</span> <span class="mi">3</span><span class="p">},</span> <span class="p">()</span> <span class="o">=&gt;</span> <span class="p">()</span> <span class="o">=&gt;</span>
  <span class="nx">agent</span><span class="p">(</span><span class="s2">`Try to refute: </span><span class="p">${</span><span class="nx">claim</span><span class="p">}</span><span class="s2">. Default to refuted=true if uncertain.`</span><span class="p">,</span> <span class="p">{</span><span class="na">schema</span><span class="p">:</span> <span class="nx">VERDICT</span><span class="p">})))</span>
<span class="kd">const</span> <span class="nx">survives</span> <span class="o">=</span> <span class="nx">votes</span><span class="p">.</span><span class="nx">filter</span><span class="p">(</span><span class="nb">Boolean</span><span class="p">).</span><span class="nx">filter</span><span class="p">(</span><span class="nx">v</span> <span class="o">=&gt;</span> <span class="o">!</span><span class="nx">v</span><span class="p">.</span><span class="nx">refuted</span><span class="p">).</span><span class="nx">length</span> <span class="o">&gt;=</span> <span class="mi">2</span>
</code></pre></div></div>

<ul>
  <li><strong>관점 다양화 검증:</strong> 하나의 finding이 여러 방식으로 실패할 수 있으면 동일한 refuter N명 대신 correctness, security, performance, reproduction 여부처럼 서로 다른 lens를 부여한다. 다양성은 중복성이 잡지 못하는 failure mode를 찾는다.</li>
  <li>Perspective-diverse verify: when a finding can fail in more than one way, give each verifier a distinct lens (correctness, security, perf, does-it-reproduce) instead of N identical refuters — diversity catches failure modes redundancy can't.</li>
  <li><strong>Judge panel:</strong> MVP-first, risk-first, user-first처럼 다른 각도에서 N개의 독립 시도를 생성하고 병렬 judge로 채점한 뒤, 승자를 기반으로 종합하되 차점자의 좋은 아이디어를 접목한다. solution space가 넓을 때 하나의 시도를 반복 개선하는 것보다 낫다.</li>
  <li>Judge panel: generate N independent attempts from different angles (e.g. MVP-first, risk-first, user-first), score with parallel judges, synthesize from the winner while grafting the best ideas from runners-up. Beats one-attempt-iterated when the solution space is wide.</li>
  <li><strong>새 결과가 없을 때까지 반복:</strong> 크기를 모르는 버그·이슈·edge case 탐색은 K회 연속 새로운 결과가 없을 때까지 finder를 계속 생성한다. 단순 개수 counter는 tail을 놓친다.</li>
  <li>Loop-until-dry: for unknown-size discovery (bugs, issues, edge cases), keep spawning finders until K consecutive rounds return nothing new. Simple counters (while count &lt; N) miss the tail.</li>
  <li><strong>다중 방식 탐색:</strong> agent마다 container, content, entity, time 등 서로 다른 방식으로 병렬 검색한다. 각 agent는 다른 agent의 결과를 모르므로 한 가지 검색 방식으로 모든 것을 찾기 어려울 때 유용하다.</li>
  <li>Multi-modal sweep: parallel agents each searching a different way (by-container, by-content, by-entity, by-time). Each is blind to what the others surface; useful when one search angle won't find everything.</li>
  <li><strong>완전성 비평가:</strong> 마지막 agent가 실행하지 않은 방식, 검증되지 않은 주장, 읽지 못한 source 등 빠진 것을 묻는다. 발견한 것은 다음 작업 round가 된다.</li>
  <li>Completeness critic: a final agent that asks "what's missing — modality not run, claim unverified, source unread?" What it finds becomes the next round of work.</li>
  <li><strong>조용한 cap 금지:</strong> top-N, no-retry, sampling 등 coverage를 제한했다면 <code class="language-plaintext highlighter-rouge">log()</code>로 무엇을 버렸는지 알린다. 조용한 truncation은 실제로는 그렇지 않은데도 모든 것을 커버한 것처럼 보인다.</li>
  <li>No silent caps: if a workflow bounds coverage (top-N, no-retry, sampling), <code class="language-plaintext highlighter-rouge">log()</code> what was dropped — silent truncation reads as "covered everything" when it didn't.</li>
</ul>

<p>사용자의 요청에 맞춰 규모를 조절한다. <code class="language-plaintext highlighter-rouge">find any bugs</code>에는 소수 finder와 단일 vote 검증을, <code class="language-plaintext highlighter-rouge">thoroughly audit this</code> 또는 <code class="language-plaintext highlighter-rouge">be comprehensive</code>에는 더 큰 finder pool, 3–5 vote adversarial pass, synthesis stage를 쓴다. 확신이 없으면 research/review/audit에는 철저함을, 빠른 확인에는 간결함을 택한다.</p>

<blockquote>
  <p>Scale to what the user asked for. "find any bugs" → a few finders, single-vote verify. "thoroughly audit this" or "be comprehensive" → larger finder pool, 3–5 vote adversarial pass, synthesis stage. When unsure, lean toward thoroughness for research/review/audit requests and toward brevity for quick checks.</p>
</blockquote>

<p>이 패턴이 전부는 아니다. 작업에 맞으면 tournament bracket, self-repair loop, staged escalation 등 새로운 harness를 조합한다.</p>

<blockquote>
  <p>These patterns aren't exhaustive — compose novel harnesses when the task calls for it (tournament brackets, self-repair loops, staged escalation, whatever fits).</p>
</blockquote>

<p>이 도구는 loop, condition, fan-out처럼 control flow가 모델 주도가 아니라 결정적이어야 하는 다단계 오케스트레이션에 사용한다.</p>

<blockquote>
  <p>Use this tool for multi-step orchestration where control flow should be deterministic (loops, conditionals, fan-out) rather than model-driven.</p>
</blockquote>

<h3 id="재개">재개</h3>

<h3 id="resume">Resume</h3>

<p>도구 결과에는 runId가 있다. pause, kill, script edit 후에는 <code class="language-plaintext highlighter-rouge">Workflow({scriptPath, resumeFromRunId})</code>로 다시 실행한다. 변경되지 않은 가장 긴 <code class="language-plaintext highlighter-rouge">agent()</code> 호출 prefix는 즉시 cached result를 반환하고, 처음 수정되거나 추가된 호출부터 이후는 live로 실행한다. 같은 script와 같은 args면 cache hit가 100%다. <code class="language-plaintext highlighter-rouge">Date.now()</code>, <code class="language-plaintext highlighter-rouge">Math.random()</code>, <code class="language-plaintext highlighter-rouge">new Date()</code>는 이를 깨므로 사용할 수 없다. timestamp는 결과 반환 후 찍거나 args로 전달한다. journal이 없을 때는 transcript directory의 <code class="language-plaintext highlighter-rouge">agent-&lt;id&gt;.jsonl</code>을 읽고 continuation script를 수동 작성한다.</p>

<blockquote>
  <p>The tool result includes a runId. To resume after a pause, kill, or script edit, relaunch with Workflow({scriptPath, resumeFromRunId}) — the longest unchanged prefix of agent() calls returns cached results instantly; the first edited/new call and everything after it runs live. Same script + same args → 100% cache hit. Date.now()/Math.random()/new Date() are unavailable in scripts (they would break this) — stamp results after the workflow returns, or pass timestamps via args. Fallback when no journal is available: Read agent-<id>.jsonl files in the transcript directory and hand-author a continuation script.</id></p>
</blockquote>

<h2 id="2-farad-orchestrate-workflow-스킬-설명">2. Farad <code class="language-plaintext highlighter-rouge">orchestrate-workflow</code> 스킬 설명</h2>

<p>출처는 Farad Built-ins marketplace의 현재 <code class="language-plaintext highlighter-rouge">farad.orchestrate-workflow</code> 스킬 v1.6.0이다.</p>

<blockquote>
  <p>The source is the current <code class="language-plaintext highlighter-rouge">farad.orchestrate-workflow</code> skill v1.6.0 in the Farad Built-ins marketplace.</p>
</blockquote>

<p>영문 원본 파일의 SHA-256은 <code class="language-plaintext highlighter-rouge">cda25a97f4fd21112f8a4393f06705a9c1bebf5adbfdbb3f13167c08b30966cd</code>이고, 줄 수는 311줄, 크기는 17,775바이트다.</p>

<blockquote>
  <p>The original English file has SHA-256 <code class="language-plaintext highlighter-rouge">cda25a97f4fd21112f8a4393f06705a9c1bebf5adbfdbb3f13167c08b30966cd</code>, 311 lines, and 17,775 bytes.</p>
</blockquote>

<h3 id="메타데이터">메타데이터</h3>

<h3 id="metadata">Metadata</h3>

<p>ID와 이름은 각각 <code class="language-plaintext highlighter-rouge">farad.orchestrate-workflow</code>와 <code class="language-plaintext highlighter-rouge">orchestrate-workflow</code>이다. 설명은 workflow folder, prompt template, file output, phase barrier로 다단계 병렬 subagent 작업을 오케스트레이션한다는 것이다. subagent fan-out, target별 research, multi-step pipeline, chat context 비대를 피하는 file handoff에 사용한다.</p>

<blockquote>
  <p>The ID and name are <code class="language-plaintext highlighter-rouge">farad.orchestrate-workflow</code> and <code class="language-plaintext highlighter-rouge">orchestrate-workflow</code>. Its description is: "Orchestrate multi-phase parallel subagent work with workflow folders, prompt templates, file outputs, and phase barriers." Its when-to-use text is: "Use for subagent fan-out, per-target research, multi-step pipelines, or file handoffs that avoid chat context bloat."</p>
</blockquote>

<p>icon은 <code class="language-plaintext highlighter-rouge">mdi6.sitemap-outline</code>, tag는 orchestration·subagents·automation, group은 Development, version은 1.6.0, author는 Farad다. 표시 이름은 Workflow이고 subtitle은 file-based handoff를 사용한 multi-phase subagent orchestration이다. alias는 workflow와 orchestrate이며 한국어 alias는 워크플로와 워크플로우다.</p>

<blockquote>
  <p>The icon is <code class="language-plaintext highlighter-rouge">mdi6.sitemap-outline</code>; tags are orchestration, subagents, and automation; the group is Development; the version is 1.6.0; and the author is Farad. The display name is Workflow and the subtitle is "Multi-phase subagent orchestration with file-based handoffs." Aliases are workflow and orchestrate; Korean aliases are 워크플로 and 워크플로우.</p>
</blockquote>

<p>원본 frontmatter는 다음과 같다.</p>

<blockquote>
  <p>The original frontmatter follows.</p>
</blockquote>

<div class="language-yaml highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">---</span>
<span class="na">id</span><span class="pi">:</span> <span class="s">farad.orchestrate-workflow</span>
<span class="na">name</span><span class="pi">:</span> <span class="s">orchestrate-workflow</span>
<span class="na">description</span><span class="pi">:</span> <span class="s">Orchestrate multi-phase parallel subagent work with workflow folders, prompt templates, file outputs, and phase barriers.</span>
<span class="na">argument-hint</span><span class="pi">:</span> <span class="s2">"</span><span class="s">goal</span><span class="nv"> </span><span class="s">to</span><span class="nv"> </span><span class="s">orchestrate"</span>
<span class="na">when_to_use</span><span class="pi">:</span> <span class="s">Use for subagent fan-out, per-target research, multi-step pipelines, or file handoffs that avoid chat context bloat.</span>
<span class="na">farad</span><span class="pi">:</span>
  <span class="na">icon</span><span class="pi">:</span> <span class="s">mdi6.sitemap-outline</span>
  <span class="na">tags</span><span class="pi">:</span> <span class="s">orchestration, subagents, automation</span>
  <span class="na">group</span><span class="pi">:</span> <span class="s">Development</span>
  <span class="na">version</span><span class="pi">:</span> <span class="s2">"</span><span class="s">1.6.0"</span>
  <span class="na">author</span><span class="pi">:</span> <span class="s">Farad</span>
  <span class="na">display-name</span><span class="pi">:</span> <span class="s">Workflow</span>
  <span class="na">subtitle</span><span class="pi">:</span> <span class="s">Multi-phase subagent orchestration with file-based handoffs.</span>
  <span class="na">aliases</span><span class="pi">:</span>
    <span class="pi">-</span> <span class="s">workflow</span>
    <span class="pi">-</span> <span class="s">orchestrate</span>
  <span class="na">locales</span><span class="pi">:</span>
    <span class="na">ko</span><span class="pi">:</span>
      <span class="na">subtitle</span><span class="pi">:</span> <span class="s">파일 기반 핸드오프로 다단계 서브에이전트 오케스트레이션을 수행합니다.</span>
      <span class="na">argument-hint</span><span class="pi">:</span> <span class="s2">"</span><span class="s">오케스트레이션할</span><span class="nv"> </span><span class="s">목표"</span>
      <span class="na">aliases</span><span class="pi">:</span>
        <span class="pi">-</span> <span class="s">워크플로</span>
        <span class="pi">-</span> <span class="s">워크플로우</span>
<span class="nn">---</span>
</code></pre></div></div>

<h3 id="workflow-orchestration">Workflow Orchestration</h3>

<p>당신이 orchestrator다. fan-out마다 하나의 shared prompt template, agent마다 하나의 output file, parallel Agent call로 join되는 phase로 구성된 <strong>workflow folder</strong>를 통해 multi-phase subagent 작업을 실행한다. subagent output은 disk에 저장되고 현재 context에는 path와 짧은 summary만 들어온다.</p>

<blockquote>
  <p>You are the orchestrator. Run multi-phase subagent work through a <strong>workflow folder</strong>: one shared prompt template per fan-out, one output file per agent, phases joined by parallel Agent calls. Subagent outputs land on disk — your context only carries paths and short summaries.</p>
</blockquote>

<p>필요하지 않은 의식을 치르지 않는다. 질문 하나, 파일 하나, one-shot task 하나는 <code class="language-plaintext highlighter-rouge">prompt</code>만 쓰고 <code class="language-plaintext highlighter-rouge">output_file</code>은 쓰지 않는 direct Agent call 하나로 처리한다. 병렬 target이 둘 이상이거나 진짜 multi-phase pipeline일 때 folder protocol을 사용한다.</p>

<blockquote>
  <p>Skip the ceremony when it isn't earned: a single question, file, or one-shot task is one direct Agent call (<code class="language-plaintext highlighter-rouge">prompt</code> only, no <code class="language-plaintext highlighter-rouge">output_file</code>). Reach for the folder protocol at ≥2 parallel targets or a real multi-phase pipeline.</p>
</blockquote>

<h3 id="1-workflow-folder-설정">1. workflow folder 설정</h3>

<h3 id="1-set-up-the-workflow-folder">1. Set up the workflow folder</h3>

<p>workflow root는 <code class="language-plaintext highlighter-rouge">&lt;session_dir&gt;/workflows/</code>이며 <code class="language-plaintext highlighter-rouge">&lt;session_dir&gt;</code>는 system prompt에 표시된 <code class="language-plaintext highlighter-rouge">Session storage</code> path다. 이번 run의 kebab-case slug를 정하고 <code class="language-plaintext highlighter-rouge">&lt;session_dir&gt;/workflows/&lt;slug&gt;/</code> 안에서 작업한다. write 시 parent directory가 생성되므로 mkdir은 필요 없다.</p>

<blockquote>
  <p>Your workflow root is <code class="language-plaintext highlighter-rouge">&lt;session_dir&gt;/workflows/</code> where <code class="language-plaintext highlighter-rouge">&lt;session_dir&gt;</code> is the "Session storage" path shown in your system prompt. Pick a kebab-case slug for this run and work inside <code class="language-plaintext highlighter-rouge">&lt;session_dir&gt;/workflows/&lt;slug&gt;/</code> (parent directories are created on write — no mkdir).</p>
</blockquote>

<p><code class="language-plaintext highlighter-rouge">&lt;session_dir&gt;/workflows/&lt;slug&gt;/state.md</code>를 <strong>먼저</strong> 만들고 모든 phase boundary에서 갱신한다. conversation이 compact되거나 interrupt될 때 이것이 single source of truth다.</p>

<blockquote>
  <p>Create <code class="language-plaintext highlighter-rouge">&lt;session_dir&gt;/workflows/&lt;slug&gt;/state.md</code> FIRST and keep it current at every phase boundary. It is the single source of truth if this conversation is compacted or interrupted:</p>
</blockquote>

<div class="language-markdown highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="gh"># &lt;slug&gt; — workflow state</span>
Goal: <span class="nt">&lt;one</span> <span class="na">line</span><span class="nt">&gt;</span>
<span class="gu">## Plan</span>
<span class="p">-</span> Phase 1: <span class="nt">&lt;what&gt;</span> → outputs: phase1-discover/alpha.md, phase1-discover/beta.md
<span class="p">-</span> Phase 2: <span class="nt">&lt;what&gt;</span> → outputs: phase2-synthesize/report.md
<span class="gu">## Status</span>
<span class="p">-</span> [x] phase1-discover/alpha.md (done)
<span class="p">-</span> [ ] phase1-discover/beta.md (background, agent_id a1b2c3)
</code></pre></div></div>

<p>workflow root 아래의 Write/Edit은 orchestrator와 subagent에게 미리 승인된다. 이 승인은 workflow folder만 포함한다. subagent가 실제 project file을 수정하거나 바깥에서 command를 실행하면 정상 permission을 거친다. unattended 또는 background phase 전에 바깥 작업 permission을 미리 확보하지 않으면 아무도 보고 있지 않은 approval에서 phase가 멈춘다.</p>

<blockquote>
  <p>Write/Edit under the workflow root are pre-authorized for you and your subagents. That covers ONLY the workflow folder: a subagent editing real project files or running commands outside it goes through normal permissions — before an unattended or background phase, make sure that outside work is already permitted, or the phase stalls on an approval nobody is watching.</p>
</blockquote>

<h3 id="2-path-규칙">2. path 규칙</h3>

<h3 id="2-path-rules">2. Path rules</h3>

<ul>
  <li>모든 file path는 <code class="language-plaintext highlighter-rouge">&lt;session_dir&gt;/workflows/&lt;slug&gt;/...</code> 형태의 <strong>absolute path</strong>다. <code class="language-plaintext highlighter-rouge">output_file</code>은 반드시 <code class="language-plaintext highlighter-rouge">&lt;session_dir&gt;/workflows/</code> 내부에 있어야 한다. 최종 산출물이 project folder 같은 다른 위치에 속한다면 subagent가 자신의 도구로 그곳에 Write하게 하거나 deliver 단계에서 직접 복사한다.</li>
  <li>Every file path is ABSOLUTE — <code class="language-plaintext highlighter-rouge">&lt;session_dir&gt;/workflows/&lt;slug&gt;/...</code>, one rule everywhere — and <code class="language-plaintext highlighter-rouge">output_file</code> must additionally stay inside <code class="language-plaintext highlighter-rouge">&lt;session_dir&gt;/workflows/</code>. If a final deliverable belongs elsewhere (e.g. the project folder), have a subagent Write it there with its own tools, or copy it yourself in the deliver step.</li>
  <li>phase output folder는 <code class="language-plaintext highlighter-rouge">phase1-discover/</code>, <code class="language-plaintext highlighter-rouge">phase2-refute/</code>, <code class="language-plaintext highlighter-rouge">phase3-synthesize/</code>처럼 <code class="language-plaintext highlighter-rouge">phaseN-purpose/</code>로 이름 지어 listing만 봐도 순서와 역할을 알 수 있게 한다. 주변 workflow에서 목적이 분명한 경우가 아니면 bare <code class="language-plaintext highlighter-rouge">phase1/</code>은 피한다.</li>
  <li>Name phase output folders as <code class="language-plaintext highlighter-rouge">phaseN-purpose/</code> (for example <code class="language-plaintext highlighter-rouge">phase1-discover/</code>, <code class="language-plaintext highlighter-rouge">phase2-refute/</code>, <code class="language-plaintext highlighter-rouge">phase3-synthesize/</code>) so listings show both order and role. Avoid bare <code class="language-plaintext highlighter-rouge">phase1/</code> unless the phase purpose is obvious from the surrounding workflow.</li>
  <li><code class="language-plaintext highlighter-rouge">prompt</code>와 <code class="language-plaintext highlighter-rouge">template_vars</code> 안의 path는 orchestrator가 resolve하지 않고 subagent가 자신의 도구로 resolve한다. 따라서 같은 absolute path를 전달한다.</li>
  <li>Paths inside <code class="language-plaintext highlighter-rouge">prompt</code> and <code class="language-plaintext highlighter-rouge">template_vars</code> are not resolved by you — the subagent resolves them with its own tools, so pass the same absolute paths there too.</li>
  <li>subagent의 system prompt에는 orchestrator가 아니라 <strong>자기</strong> session storage가 표시된다. workflow folder에 추가 artifact를 쓰게 하려면 absolute folder path를 명시적으로 전달하고 스스로 유도하게 하지 않는다.</li>
  <li>A subagent's own system prompt shows ITS session storage, not yours. If a subagent should write extra artifacts into the workflow folder, pass it the absolute folder path explicitly — never tell it to derive the path itself.</li>
</ul>

<h3 id="3-shared-prompt-template">3. shared prompt template</h3>

<h3 id="3-shared-prompt-templates">3. Shared prompt templates</h3>

<p>여러 agent가 같은 긴 지시를 받으면 Jinja syntax를 사용해 template을 <code class="language-plaintext highlighter-rouge">&lt;session_dir&gt;/workflows/&lt;slug&gt;/templates/&lt;name&gt;.md</code>에 <strong>한 번만</strong> 쓰고 각 agent를 다음 값으로 생성한다.</p>

<blockquote>
  <p>When several agents get the same long instructions, write the template ONCE to <code class="language-plaintext highlighter-rouge">&lt;session_dir&gt;/workflows/&lt;slug&gt;/templates/&lt;name&gt;.md</code> using Jinja syntax (<code class="language-plaintext highlighter-rouge">{{ variable }}</code>, <code class="language-plaintext highlighter-rouge">{% if %}</code>, <code class="language-plaintext highlighter-rouge">{% for %}</code>), then spawn each agent with:</p>
</blockquote>

<ul>
  <li><code class="language-plaintext highlighter-rouge">prompt</code>는 agent별 짧은 지시다. template이 self-contained면 생략할 수 있다. 둘 다 주면 rendered template이 prompt 뒤에 붙는다. <code class="language-plaintext highlighter-rouge">prompt_file</code>만 Jinja render되고 <code class="language-plaintext highlighter-rouge">prompt</code>는 verbatim 전달되므로 prompt에 <code class="language-plaintext highlighter-rouge">{{ }}</code>를 넣지 않고 <code class="language-plaintext highlighter-rouge">template_vars</code>로 차이를 준다.</li>
  <li><code class="language-plaintext highlighter-rouge">prompt</code>: a short per-agent directive. Optional when the template is self-contained; when both are given the rendered template is appended after <code class="language-plaintext highlighter-rouge">prompt</code>. Only <code class="language-plaintext highlighter-rouge">prompt_file</code> is Jinja-rendered — <code class="language-plaintext highlighter-rouge">prompt</code> is passed verbatim, so never put <code class="language-plaintext highlighter-rouge">{{ }}</code> in it; vary each agent through <code class="language-plaintext highlighter-rouge">template_vars</code>.</li>
  <li><code class="language-plaintext highlighter-rouge">prompt_file</code>은 absolute template path다.</li>
  <li><code class="language-plaintext highlighter-rouge">prompt_file</code>: the absolute template path</li>
  <li><code class="language-plaintext highlighter-rouge">template_vars</code>는 <code class="language-plaintext highlighter-rouge">{"target": "alpha"}</code> 같은 agent별 fill 값이다.</li>
  <li><code class="language-plaintext highlighter-rouge">template_vars</code>: the per-agent fill, e.g. <code class="language-plaintext highlighter-rouge">{"target": "alpha"}</code></li>
</ul>

<p>누락 variable이나 render error는 Agent call을 즉시 실패시키며 아무 agent도 생성되지 않는다. template에는 전달한 list를 대상으로 한 variable, conditional, loop만 사용한다. single-agent phase는 template이 선택이며 prompt에 전부 inline해도 된다.</p>

<blockquote>
  <p>A missing variable or render error fails the Agent call immediately — fix the call, nothing was spawned. Keep templates to variables, conditionals, and loops over the lists you pass in. For a single-agent phase a template is optional — inlining everything in <code class="language-plaintext highlighter-rouge">prompt</code> is fine.</p>
</blockquote>

<p>prompt와 template을 어떻게 나누든 모든 fan-out agent에 명시적 objective, expected output format, source/tool guidance, task boundary의 네 가지를 준다. 모호한 brief는 병렬 agent가 중복 작업하거나 lane을 벗어나게 한다.</p>

<blockquote>
  <p>Whatever the split between <code class="language-plaintext highlighter-rouge">prompt</code> and template, give every fan-out agent four things — an explicit objective, the expected output format, source and tool guidance (where to look, what to trust), and task boundaries (out of scope). Vague briefs make parallel agents duplicate work or drift out of their lane.</p>
</blockquote>

<h3 id="4-output을-file로-pipe">4. output을 file로 pipe</h3>

<h3 id="4-pipe-outputs-to-files">4. Pipe outputs to files</h3>

<p>모든 fan-out agent에 <code class="language-plaintext highlighter-rouge">output_file</code>을 준다. phase마다 이름 있는 folder를 쓰면 listing이 읽기 쉽다. harness는 agent 완료 시 최종 메시지를 그 file에 쓰고 orchestrator에는 <code class="language-plaintext highlighter-rouge">path + size + first lines</code>만 보낸다.</p>

<blockquote>
  <p>Give every fan-out agent an <code class="language-plaintext highlighter-rouge">output_file</code> (e.g. <code class="language-plaintext highlighter-rouge">&lt;session_dir&gt;/workflows/&lt;slug&gt;/phase1-discover/alpha.md</code> — a named folder per phase keeps listings legible). The harness writes the agent's final message to that file on completion; you receive only <code class="language-plaintext highlighter-rouge">path + size + first lines</code>. Contract:</p>
</blockquote>

<ul>
  <li><strong>file 존재는 성공이고 <code class="language-plaintext highlighter-rouge">&lt;file&gt;.error.md</code> sibling은 failure record다. 둘은 동시에 존재하지 않는다.</strong> harness 자체가 disk error로 file을 쓰지 못하면 run은 여전히 성공하며 output은 <code class="language-plaintext highlighter-rouge">[output_file capture failed: ...]</code> prefix와 함께 inline으로 온다. resume할 때 이미 inline output을 받은 neither-file agent를 다시 생성하지 않는다.</li>
  <li><strong>File exists = success; <code class="language-plaintext highlighter-rouge">&lt;file&gt;.error.md</code> sibling = failure record</strong> (status, reason, last assistant text) — never both. Third case: if the harness itself cannot write the file (disk error), the run still SUCCEEDED and its output arrives inline prefixed <code class="language-plaintext highlighter-rouge">[output_file capture failed: ...]</code> — record that result; on resume, don't respawn a neither-file agent whose inline output you already have.</li>
  <li>모든 live agent에는 서로 다른 <code class="language-plaintext highlighter-rouge">output_file</code>이 필요하다. 같은 path를 두 번째로 동시에 claim한 Agent call은 실패한다. 이전 run이 끝난 뒤 respawn할 때만 path를 재사용한다.</li>
  <li>Every live agent needs a DISTINCT <code class="language-plaintext highlighter-rouge">output_file</code> — a second concurrent claim on the same path fails that Agent call. Reuse a path only when respawning after the prior run ended.</li>
  <li>각 subagent에게 최종 메시지가 곧 deliverable이며 2–3줄 TL;DR로 시작해야 한다고 지시한다. agent가 artifact를 직접 Write하면 최종 메시지는 TL;DR만 담아도 된다. 어느 쪽이든 file이 작업 내용을 가진다. 빈 최종 메시지도 0-byte success file을 쓰므로 envelope의 <code class="language-plaintext highlighter-rouge">0 bytes</code>는 red flag다.</li>
  <li>Instruct each subagent (in the template or prompt) that its final message IS the deliverable — full content, starting with a 2–3 line TL;DR (the preview you see). If the agent instead Writes its artifacts itself, its final message is the TL;DR alone; either way the files carry the work. An empty final message still writes a 0-byte "success" file; treat <code class="language-plaintext highlighter-rouge">0 bytes</code> in the envelope as a red flag.</li>
  <li><code class="language-plaintext highlighter-rouge">.error.md</code>로 끝나는 <code class="language-plaintext highlighter-rouge">output_file</code> 이름은 failure record용 reserved suffix이므로 거부된다.</li>
  <li>Don't name an <code class="language-plaintext highlighter-rouge">output_file</code> ending in <code class="language-plaintext highlighter-rouge">.error.md</code> — that suffix is reserved for failure records and is rejected.</li>
</ul>

<h3 id="5-phase-실행-parallel과-barrier">5. phase 실행: parallel과 barrier</h3>

<h3 id="5-run-a-phase-parallel--barrier">5. Run a phase (parallel + barrier)</h3>

<p>한 phase의 <strong>모든</strong> Agent call을 다른 내용 없이 <strong>하나의 assistant message</strong>에 넣는다. 동시에 실행되며 foreground call은 batch 전체가 완료될 때 turn이 재개된다. 이것이 automatic barrier다. phase template을 쓰고 <code class="language-plaintext highlighter-rouge">state.md</code>를 갱신하는 일은 fan-out <strong>이전</strong> 메시지에서 끝낸다. Agent call 사이의 Write/Edit는 batch를 분리한다. concurrency cap과 LLM admission이 자동으로 queue하므로 넓은 phase도 error 대신 대기하며 직접 throttle하지 않는다. 모든 fan-out은 orchestrator가 담당한다. depth cap 때문에 subagent는 다시 spawn할 수 없으므로 worker에게 delegate하라는 template을 쓰지 않는다.</p>

<blockquote>
  <p>Issue ALL of a phase's Agent calls in ONE assistant message with nothing else in it — they run concurrently, and for foreground calls your turn resumes when the whole batch is done (the automatic barrier). Write the phase's template(s) and update <code class="language-plaintext highlighter-rouge">state.md</code> in messages BEFORE the fan-out, never alongside it (a Write/Edit interposed among the Agent calls splits the batch). Queuing is automatic — concurrency caps and LLM admission pace execution, so a wide phase queues rather than erroring; do not throttle the phase size yourself. All fan-out stays with you: subagents cannot spawn their own (depth cap), so never write a template that tells a worker to delegate.</p>
</blockquote>

<p>각 phase의 전후를 사용자에게 알린다. chat이 유일한 progress view다. fan-out 직전에 어떤 phase인지, 어떤 agent를 몇 개 시작하는지, 각각 무엇을 만드는지 말한다. barrier와 file verification 뒤에는 N-of-N 완료, failure 처리, 다음 작업을 한두 줄로 정리한다.</p>

<blockquote>
  <p>Bookend each phase for the user — the chat is their only progress view. Right before the fan-out, say in your visible text what the phase does: which agents you are launching, the total count, and what each produces. After the barrier (files verified), wrap up in a line or two: N-of-N done, any failures and how you handled them, and what comes next.</p>
</blockquote>

<ul>
  <li>foreground wait 기본값은 agent마다 1시간이며 늘리면 turn 전체를 block한다. 장시간 build에는 <code class="language-plaintext highlighter-rouge">run_in_background: true</code>를 쓴다.</li>
  <li>Foreground wait defaults to 1 hour per agent and blocks your whole turn if raised; for known-long work (builds, hours-scale jobs) use <code class="language-plaintext highlighter-rouge">run_in_background: true</code>.</li>
  <li>automatic barrier는 foreground call만 포함한다. 명시적으로 background로 보냈거나 foreground batch에 섞였거나 wait deadline에 자동 승격된 agent는 notification으로 완료된다. 이때 barrier는 orchestrator 책임이며 첫 notification 뒤가 아니라 N-of-N output file을 확인한 뒤 다음 phase를 시작한다. background launch나 promotion notice에서 task/agent id를 받으면 <code class="language-plaintext highlighter-rouge">state.md</code>에 즉시 기록한다. TaskStatus와 TaskStop은 그 id만 받으며 compaction 뒤에는 남지 않는다.</li>
  <li>The automatic barrier covers foreground calls only. Agents running in background — sent there explicitly, mixed into a foreground batch, or promoted (auto-converted at the wait deadline) — complete via notifications instead, and the barrier is YOURS: confirm N-of-N output files exist before starting the next phase, never after the first notification. When a call returns a task/agent id (background launch, promotion notice), record it in <code class="language-plaintext highlighter-rouge">state.md</code> immediately — TaskStatus and TaskStop take only that id, and it does not survive compaction.</li>
  <li>promoted agent는 여전히 실행 중이며 완료 시 output file을 만든다. 다시 spawn하지 않고 live 여부가 불확실하면 TaskStatus부터 확인한다.</li>
  <li>A promoted agent is still running and its output file still appears on completion — do NOT respawn it. If unsure whether a run is live, check TaskStatus first.</li>
  <li>subagent tool call에는 별도 timeout이 있다. multi-hour command는 하나의 blocking call로 실행하지 말고 subagent 자신의 tool로 detach/background 실행한 뒤 poll하게 한다.</li>
  <li>A subagent's own tool calls have their own timeouts: for a multi-hour command, tell the subagent to run it detached/backgrounded with its own tools and poll, not as one blocking call.</li>
</ul>

<h3 id="6-다음-phase에-입력-전달">6. 다음 phase에 입력 전달</h3>

<h3 id="6-feed-the-next-phase">6. Feed the next phase</h3>

<p>이전 phase의 output <strong>path</strong>를 content 대신 <code class="language-plaintext highlighter-rouge">template_vars</code>로 다음 agent에 전달하고 template에서 그 file을 Read하게 한다. 각 agent는 fresh context에서 직접 읽는 비용을 부담한다. 그것이 목적이다.</p>

<blockquote>
  <p>Pass the previous phase's output <strong>paths</strong> (not contents) to the next phase's agents via <code class="language-plaintext highlighter-rouge">template_vars</code> — absolute paths, per the path rules — and have the template tell them to Read those files. Each agent pays for reading in its own fresh context — that is the point.</p>
</blockquote>

<h3 id="7-failure와-restart">7. failure와 restart</h3>

<h3 id="7-failures-and-restart">7. Failures and restart</h3>

<ul>
  <li>각 phase 뒤 live Agent tool result 또는 durable <code class="language-plaintext highlighter-rouge">&lt;file&gt;.error.md</code>를 확인한다. error record를 읽고 원인을 고친 뒤 <strong>그 agent만</strong> 같은 <code class="language-plaintext highlighter-rouge">output_file</code>로 respawn한다. 수정 후 다시 실패하면 loop하지 말고 멈춰서 blocker를 사용자에게 알린다.</li>
  <li>After each phase, check results: a failed Agent tool result (seen live) or a <code class="language-plaintext highlighter-rouge">&lt;file&gt;.error.md</code> on disk (the durable record) means that agent failed. Read the error record, fix the cause, respawn ONLY that agent (same <code class="language-plaintext highlighter-rouge">output_file</code> — reclaimable once the run ended). If it fails again after a fix, stop and surface the blocker to the user instead of looping.</li>
  <li>완료한 작업을 수정하거나 확장하려면 existing output을 먼저 Read하라는 self-contained prompt의 fresh Agent를 만들고 같은 <code class="language-plaintext highlighter-rouge">output_file</code>을 재사용한다. completed run은 path를 release했으므로 새 run이 revised deliverable로 덮어쓴다. follow-up이 실패하면 old file은 남고 <code class="language-plaintext highlighter-rouge">.error.md</code> sibling이 생긴다. sibling 존재가 마지막 run의 authoritative status다.</li>
  <li>To revise or extend finished work, launch a fresh Agent with a self-contained prompt that tells it to Read the existing output first, then reuse the same <code class="language-plaintext highlighter-rouge">output_file</code>. The completed run has released that path, and the fresh run overwrites it with the revised deliverable. If the follow-up fails, the older file stays AND an <code class="language-plaintext highlighter-rouge">.error.md</code> sibling appears — a present sibling is the authoritative status of the last run.</li>
  <li>interruption/compaction 뒤 resume할 때는 <code class="language-plaintext highlighter-rouge">state.md</code>를 읽고 workflow folder를 list한다. output file이 있으면 skip, error sibling이면 위 방식으로 처리, 둘 다 없으면 respawn한다. run이 live일 수 있으면 TaskStatus부터 확인한다.</li>
  <li>Resuming after interruption/compaction: read <code class="language-plaintext highlighter-rouge">state.md</code>, then list the workflow folder. Output file present → skip that agent. Error sibling → handle as above. Neither → respawn (check TaskStatus first if a run might still be live).</li>
  <li>agent name이 아니라 <code class="language-plaintext highlighter-rouge">output_file</code>로 추적한다. respawn agent는 새 identity를 얻고 이름은 <code class="language-plaintext highlighter-rouge">name-2</code>처럼 drift할 수 있다.</li>
  <li>Track work by <code class="language-plaintext highlighter-rouge">output_file</code>, not by agent name — a respawned agent gets a new identity (names can drift to <code class="language-plaintext highlighter-rouge">name-2</code>).</li>
</ul>

<h3 id="8-deliver">8. deliver</h3>

<h3 id="8-deliver-1">8. Deliver</h3>

<p>마지막 phase 뒤 최종 artifact를 직접 Read하고 실제 summary와 file path를 사용자에게 준다. envelope의 첫 줄은 preview일 뿐 deliverable이 아니므로 결과로 전달하지 않는다. coverage가 top-N target, sample subset, time-box 등으로 제한됐으면 summary와 <code class="language-plaintext highlighter-rouge">state.md</code>에 밝힌다. 조용한 cap은 실제로 완전하지 않은 작업을 완전하게 보이게 한다.</p>

<blockquote>
  <p>After the last phase, Read the final artifact(s) yourself and give the user a real summary plus the file path. The envelope's first lines are a preview, not the deliverable — never forward them as the result. If coverage was bounded (top-N targets, sampled subset, time-boxed), say so in the summary and <code class="language-plaintext highlighter-rouge">state.md</code> — a silent cap reads as completeness it isn't.</p>
</blockquote>

<h3 id="9-흔한-형태">9. 흔한 형태</h3>

<h3 id="9-common-shapes">9. Common shapes</h3>

<p>작업에 맞게 형태를 선택하고 자유롭게 조합한다. lookup은 agent 하나, comparison은 2–4개, 분해 가능한 research/audit/migration만 10개 이상을 쓴다. 보통 2–3 phase, fan-out phase마다 3–8 agent이며 target·dimension·finding 같은 실제 독립 unit 수로 정하고 quota로 정하지 않는다. 각 agent는 context를 처음부터 다시 읽으므로 width가 cost를 곱한다. 한 phase가 약 15 agent를 넘거나 4 phase 이상이면 over-orchestration을 의심한다. 좁은 target list나 잘 정의된 agent 하나가 더 넓은 net보다 나을 수 있다.</p>

<blockquote>
  <p>Pick the shape by task and compose freely; scale to the ask — a lookup is one agent, a comparison 2–4, only decomposable research/audit/migration work 10+. Typical: 2–3 phases, 3–8 agents per fan-out phase, sized by the number of real independent units (targets, dimensions, findings) — never a quota. Width multiplies cost (each agent re-reads context from scratch); past ~15 agents in a phase or 4+ phases, suspect over-orchestration — a narrower target list or one well-scoped agent may beat a wider net.</p>
</blockquote>

<ul>
  <li><strong>조사 → 종합:</strong> phase 1은 dependency, subsystem, file, question 등 target마다 agent 하나를 쓴다. coverage보다 discovery가 중요하면 서로 다른 search angle로 나눈다. phase 2 synthesizer 하나가 모든 phase 1 output file을 읽고 report를 쓴다.</li>
  <li>
    <p><strong>Investigate → synthesize</strong> — the worked example below. Phase 1 (<code class="language-plaintext highlighter-rouge">phase1-investigate/</code>): one agent per target (dependency, subsystem, file, question); when discovery matters more than coverage, split by search angle instead — each agent hunts a different way. Phase 2 (<code class="language-plaintext highlighter-rouge">phase2-synthesize/</code>): one synthesizer reads every Phase 1 output file and writes the report.</p>
  </li>
  <li><strong>Code review:</strong> diff, branch, PR과 audit·bug hunt에 같은 skeleton을 쓴다. phase 1에서 exact diff command, changed-file list, change summary, governing convention doc를 <code class="language-plaintext highlighter-rouge">scope.md</code>에 고정하고 empty diff면 멈춘다. phase 2는 correctness lens별 finder로 line-by-line hunk, removed behavior, cross-file caller/callee, language pitfall을 검사하며 cleanup lens는 하나의 finder가 맡는다. 모든 candidate는 <code class="language-plaintext highlighter-rouge">file:line</code>과 이름 붙일 수 있는 failure scenario를 포함하고 반신반의하는 candidate도 다음 독립 검증으로 넘긴다. phase 3는 location별 fresh verifier가 각 claim을 <strong>CONFIRMED</strong>, <strong>PLAUSIBLE</strong>, <strong>REFUTED</strong>로 판정한다. realistic하지만 불확실한 race·rare error·boundary는 PLAUSIBLE로 남기고 코드가 반증할 때만 REFUTED로 버린다. thorough 요청만 phase 4 gap sweep을 수행하고 새 결과가 없으면 빈 결과를 낸다. phase 5는 같은 root finding을 merge하고 correctness와 CONFIRMED를 우선해 severity순으로 종합한다. exhaustive hunt는 두 round 연속 새 finding이 없을 때까지 반복하며 high-stakes finding은 서로 다른 lens의 verifier 2–3명과 majority vote를 쓴다.</li>
  <li>
    <p><strong>Code review</strong> — a diff, branch, or PR; the same skeleton serves audits and bug hunts. Phase 1 (<code class="language-plaintext highlighter-rouge">phase1-scope/</code>): one agent fixes scope into <code class="language-plaintext highlighter-rouge">scope.md</code> — the exact diff command, changed-file list, change summary, and which convention docs govern it; empty diff → stop and report, no fan-out. Phase 2 (<code class="language-plaintext highlighter-rouge">phase2-find/</code>): parallel finders. One finder per correctness lens (pick per review size) — line-by-line hunk scan (what input, state, or timing makes this line wrong), removed-behavior audit (every guard or invariant the diff deletes must be re-established somewhere, else it's a candidate), cross-file tracer (callers and callees of each changed function: new preconditions, changed return shapes, ordering), language-pitfall scan; the cleanup lenses (reuse, simplification, efficiency, conventions) share ONE finder. Each candidate is <code class="language-plaintext highlighter-rouge">file:line</code> plus a nameable failure scenario — pass each through, never silently drop half-believed ones (independent verification is the next phase's job), cap per finder. Phase 3 (<code class="language-plaintext highlighter-rouge">phase3-verify/</code>): group candidates by location (same file, overlapping lines), one fresh verifier per group judging each on its own claim — <strong>CONFIRMED</strong> (names the triggering input/state and wrong output, quotes the line), <strong>PLAUSIBLE</strong> (mechanism real, trigger uncertain on timing/env/config, says what would confirm it), <strong>REFUTED</strong> (factually wrong or already guarded, quotes the disproving line). Reviews are recall-biased: realistic-but-uncertain findings (races, rare error paths, boundaries) stay PLAUSIBLE — refute only what the code disproves; drop REFUTED. Phase 4, thorough asks only (<code class="language-plaintext highlighter-rouge">phase4-sweep/</code>): one fresh finder sees the verified list and hunts ONLY gaps — no re-deriving or re-confirming what's listed; nothing new → return empty, don't pad — then verify the same way. Phase 5 (<code class="language-plaintext highlighter-rouge">phase5-synthesize/</code>): merge same-root findings, rank most-severe first (correctness over cleanup, CONFIRMED over PLAUSIBLE), cap the report and note what dropped. Exhaustive bug hunts repeat find/verify until TWO consecutive rounds add nothing new (one dry round can be noise) — track seen in <code class="language-plaintext highlighter-rouge">state.md</code>; high stakes, 2–3 verifiers per finding with different lenses, majority wins.</p>
  </li>
  <li><strong>Deep research:</strong> breadth와 verification이 필요한 source-backed report이며 local-first로 읽는다. phase 1은 질문을 code, doc, log, test data, user file 같은 서로 다른 local evidence lane 3–6개로 나눈다. web은 환경이 제공할 때만 bonus lane이다. phase 2는 lane별 agent가 path/URL을 normalize하고 budget 때문에 skip한 것을 기록한다. cross-lane duplicate는 synthesis에서 merge한다. phase 3는 source path와 evidence가 있는 falsifiable claim을 뽑는다. phase 4는 high-value claim마다 2–3 skeptic이 cited source를 다시 읽고 REFUTE한다. 두 반박이면 claim을 제거하고 uncertain도 refuted다. verifier 실행 실패는 refutation이 아니라 unverified로 보고한다. phase 5는 semantic duplicate를 merge하고 source strength와 vote에 따라 high/medium/low confidence를 부여하며 citation, caveat, unanswered open question 2–4개를 포함한 <code class="language-plaintext highlighter-rouge">report.md</code>를 쓴다.</li>
  <li>
    <p><strong>Deep research</strong> — source-backed reports needing breadth plus verification; reads local-first. Phase 1 (<code class="language-plaintext highlighter-rouge">phase1-scope/</code>): decompose the question into 3–6 evidence angles, each a different LOCAL lane (code, docs, logs, test data, user-provided files) — web search is a bonus lane only when the environment provides it, never the assumed default; write <code class="language-plaintext highlighter-rouge">scope.md</code>. Phase 2 (<code class="language-plaintext highlighter-rouge">phase2-discover/</code>): one agent per lane gathers its evidence — lanes are disjoint by Phase 1 design, so dedup is within-lane: normalize each path (and URL, when web is available) once before reading, and log anything skipped for budget; cross-lane duplicates merge later, in synthesis. Phase 3 (<code class="language-plaintext highlighter-rouge">phase3-extract/</code>): pull concrete, falsifiable claims with source paths and evidence. Phase 4 (<code class="language-plaintext highlighter-rouge">phase4-verify/</code>): 2–3 independent skeptics per high-value claim, each prompted to REFUTE by re-reading the cited source — is the claim actually supported by the quoted evidence, or an overreach? — two refutations kill a claim, uncertain = refuted; a skeptic that fails to RUN is not a refutation, so a claim whose panel errored is <em>unverified</em> and the report says "unverified (verifier failed)", never counted confirmed or refuted. Phase 5 (<code class="language-plaintext highlighter-rouge">phase5-synthesize/</code>): merge semantic duplicates, assign per-finding confidence (high = multiple strong sources + unanimous votes, medium = secondary or split votes, low = single/weak source), cite sources, state caveats and 2–4 open questions that emerged unanswered, write <code class="language-plaintext highlighter-rouge">report.md</code>.</p>
  </li>
  <li><strong>Design panel:</strong> spawn 전에 scoring criteria를 <code class="language-plaintext highlighter-rouge">state.md</code>에 써서 designer와 judge가 같은 rubric을 보게 한다. phase 1은 MVP-first, risk-first, user-first 등 다른 angle의 N designer가 서로의 draft를 보지 않고 같은 brief를 푼다. fresh context로 anchoring을 막고 disagreement를 보존한다. phase 2 judge는 rubric으로만 모든 design을 채점한다. phase 3은 winner를 기반으로 종합하고 점수가 좋은 runner-up idea를 접목한다.</li>
  <li>
    <p><strong>Design panel</strong> — wide solution spaces. Write the scoring criteria into <code class="language-plaintext highlighter-rouge">state.md</code> BEFORE spawning, so designs and judges target one rubric. Phase 1 (<code class="language-plaintext highlighter-rouge">phase1-design/</code>): N designers attack the same brief from deliberately different angles (MVP-first, risk-first, user-first), working blind to one another — the fresh contexts are the point: they block anchoring and preserve genuine disagreement, so never feed one designer another's draft. Phase 2 (<code class="language-plaintext highlighter-rouge">phase2-judge/</code>): judges score every design against the rubric only. Phase 3 (<code class="language-plaintext highlighter-rouge">phase3-synthesize/</code>): synthesize from the winner, grafting runner-up ideas that scored well.</p>
  </li>
  <li><strong>Sweep / migrate:</strong> 먼저 inline으로 work-list를 scout하고 item마다 break risk와 test를 tag한다. 위험 item은 개별 agent에, trivial item은 batch에 준다. phase 1은 item 또는 small batch별 agent가 같은 template으로 적용한다. phase 2 verifier 하나가 실제 test, lint, leftover grep을 실행하고 worker self-report가 아닌 fresh command output을 신뢰하며 miss만 respawn한다.</li>
  <li>
    <p><strong>Sweep / migrate</strong> — many similar mechanical changes. Scout the work-list yourself first (cheap, inline), tagging each item with a risk note (what could break, what to test) so risky items get their own agent and trivial ones batch. Phase 1 (<code class="language-plaintext highlighter-rouge">phase1-apply/</code>): one agent per item or small batch, same template. Phase 2 (<code class="language-plaintext highlighter-rouge">phase2-verify/</code>): one verifier runs the real gates (tests, lint, grep for leftovers) and trusts fresh command output, never the workers' self-reports; respawn only the misses.</p>
  </li>
  <li><strong>Completeness critic:</strong> deliver 전에 critic 하나가 synthesis와 phase output을 대조해 unverified claim, unread source, unswept dimension을 찾는다. 완료 주장은 disk output이나 rerun gate 같은 fresh evidence가 필요하며 worker의 self-reported done은 근거가 아니다. 실제 gap만 targeted round 하나로 처리하고 전체를 다시 하지 않는다.</li>
  <li><strong>Completeness critic</strong> — a finisher for any shape. Before delivering, one critic reads the synthesis against the phase outputs and asks what is missing: unverified claim, unread source, dimension never swept. Completion claims need fresh evidence — an output file on disk, a gate rerun — a worker's self-reported "done" counts for nothing. Real gaps become one more targeted round, not a redo.</li>
</ul>

<h3 id="worked-example">worked example</h3>

<h3 id="worked-example-1">Worked example</h3>

<p>phase 1은 target 세 개와 template 하나를 사용한다. 아래 <code class="language-plaintext highlighter-rouge">&lt;wf&gt;</code>는 absolute <code class="language-plaintext highlighter-rouge">&lt;session_dir&gt;/workflows/audit-deps</code>를 뜻하며 실제 call에서는 전부 적는다.</p>

<blockquote>
  <p>Phase 1 (<code class="language-plaintext highlighter-rouge">phase1-investigate/</code>), three targets, one template. <code class="language-plaintext highlighter-rouge">&lt;wf&gt;</code> below stands for the absolute <code class="language-plaintext highlighter-rouge">&lt;session_dir&gt;/workflows/audit-deps</code> — spell it out in real calls:</p>
</blockquote>

<ol>
  <li><code class="language-plaintext highlighter-rouge">&lt;wf&gt;/templates/investigate.md</code>에 정확히 dependency 하나를 조사하고 최종 메시지를 deliverable로 쓰며 2–3줄 TL;DR로 시작하라는 template을 쓴다.</li>
  <li>한 메시지에 세 Agent call을 넣고 <code class="language-plaintext highlighter-rouge">template_vars</code>와 <code class="language-plaintext highlighter-rouge">output_file</code>만 바꾼다. prompt는 appended instruction에 따라 dependency 하나를 조사하되 research만 하고 file은 수정하지 말라는 내용이다. prompt_file은 shared template, target은 예를 들어 httpx, output은 phase 1의 httpx file이다.</li>
  <li>barrier가 반환되면 세 file을 확인하고 <code class="language-plaintext highlighter-rouge">state.md</code>를 갱신한 뒤 세 phase 1 output path를 <code class="language-plaintext highlighter-rouge">template_vars</code>로 받은 phase 2 synthesizer를 생성해 <code class="language-plaintext highlighter-rouge">phase2-synthesize/report.md</code>에 쓴다.</li>
  <li>최종 report를 읽고 path와 함께 사용자에게 요약한다.</li>
</ol>

<blockquote>
  <ol>
    <li>
      <p>Write <code class="language-plaintext highlighter-rouge">&lt;wf&gt;/templates/investigate.md</code>: <code class="language-plaintext highlighter-rouge">You are investigating exactly one dependency: {{ target }}. ... Your final message is the deliverable — start it with a 2-3 line TL;DR.</code></p>
    </li>
    <li>
      <p>One message, three Agent calls, varying only <code class="language-plaintext highlighter-rouge">template_vars</code> and <code class="language-plaintext highlighter-rouge">output_file</code>: <code class="language-plaintext highlighter-rouge">prompt: "Investigate one dependency per the appended instructions. Research only — do not edit files."</code>, <code class="language-plaintext highlighter-rouge">prompt_file: "&lt;wf&gt;/templates/investigate.md"</code>, <code class="language-plaintext highlighter-rouge">template_vars: {"target": "httpx"}</code>, <code class="language-plaintext highlighter-rouge">output_file: "&lt;wf&gt;/phase1-investigate/httpx.md"</code>.</p>
    </li>
    <li>
      <p>When the barrier returns, verify the three files, update <code class="language-plaintext highlighter-rouge">state.md</code>, then spawn the Phase 2 synthesizer with the three <code class="language-plaintext highlighter-rouge">phase1-investigate/</code> output paths in <code class="language-plaintext highlighter-rouge">template_vars</code> and <code class="language-plaintext highlighter-rouge">output_file: "&lt;wf&gt;/phase2-synthesize/report.md"</code>.</p>
    </li>
    <li>
      <p>Read <code class="language-plaintext highlighter-rouge">&lt;wf&gt;/phase2-synthesize/report.md</code> and summarize it for the user with the path.</p>
    </li>
  </ol>
</blockquote>

<h2 id="3-종합-비교">3. 종합 비교</h2>

<h2 id="3-comparative-synthesis">3. Comparative synthesis</h2>

<h3 id="같은-목표">같은 목표</h3>

<h3 id="shared-objective">Shared objective</h3>

<p>두 문서는 모두 큰 작업을 독립 unit으로 분해하고, 병렬 agent가 fresh context에서 작업하며, 별도 검증과 synthesis를 거쳐 main context의 비대를 막는 것을 목표로 한다. adversarial verification, diverse lens, judge panel, loop-until-dry, completeness critic, no silent caps 같은 품질 패턴도 공유한다.</p>

<blockquote>
  <p>Both documents aim to decompose large work into independent units, let parallel agents work in fresh contexts, add separate verification and synthesis, and prevent main-context bloat. They also share quality patterns such as adversarial verification, diverse lenses, judge panels, loop-until-dry, completeness critics, and no silent caps.</p>
</blockquote>

<h3 id="가장-큰-차이-control-flow의-권위">가장 큰 차이: control flow의 권위</h3>

<h3 id="primary-difference-authority-over-control-flow">Primary difference: authority over control flow</h3>

<p>Claude Workflow는 JavaScript script가 control flow의 권위다. <code class="language-plaintext highlighter-rouge">pipeline</code>, <code class="language-plaintext highlighter-rouge">parallel</code>, loop, conditional, budget, nested workflow가 VM 안에서 결정적으로 실행된다. Farad 스킬은 main agent가 reasoning loop로 phase 전환, barrier 확인, retry, synthesis를 직접 결정한다.</p>

<blockquote>
  <p>Claude Workflow makes the JavaScript script authoritative over control flow. <code class="language-plaintext highlighter-rouge">pipeline</code>, <code class="language-plaintext highlighter-rouge">parallel</code>, loops, conditionals, budgets, and nested workflows execute deterministically inside a VM. The Farad skill makes the main agent decide phase transitions, barrier completion, retries, and synthesis through its reasoning loop.</p>
</blockquote>

<h3 id="실행-substrate">실행 substrate</h3>

<h3 id="execution-substrate">Execution substrate</h3>

<p>Claude Workflow는 dedicated Workflow tool, JavaScript VM, progress event model, journal/cache/resume, named workflow registry를 가진 별도 runtime이다. Farad는 새 runtime 없이 기존 Agent call의 same-message concurrency, foreground barrier, background notification, session storage를 조합한다.</p>

<blockquote>
  <p>Claude Workflow has a dedicated Workflow tool, JavaScript VM, progress event model, journal/cache/resume machinery, and named-workflow registry. Farad composes existing same-message Agent concurrency, foreground barriers, background notifications, and session storage without adding a new runtime.</p>
</blockquote>

<h3 id="데이터-전달과-context-보호">데이터 전달과 context 보호</h3>

<h3 id="data-transfer-and-context-protection">Data transfer and context protection</h3>

<p>Claude Workflow의 <code class="language-plaintext highlighter-rouge">agent()</code>는 final text 또는 schema-validated object를 script에 반환한다. 결과는 runtime의 journal과 transcript에 연결된다. Farad는 agent final message를 <code class="language-plaintext highlighter-rouge">output_file</code>에 pipe하고 parent에는 path, size, first lines만 돌려준다. 다음 phase는 이전 content를 inline하지 않고 path를 전달해 fresh agent가 직접 Read한다.</p>

<blockquote>
  <p>Claude Workflow's <code class="language-plaintext highlighter-rouge">agent()</code> returns final text or a schema-validated object to the script, with results tied to the runtime journal and transcript. Farad pipes each agent's final message to an <code class="language-plaintext highlighter-rouge">output_file</code> and returns only the path, size, and first lines to the parent. The next phase passes paths rather than inlining prior content, and fresh agents Read those files themselves.</p>
</blockquote>

<h3 id="병렬성과-barrier">병렬성과 barrier</h3>

<h3 id="concurrency-and-barriers">Concurrency and barriers</h3>

<p>Claude Workflow는 item별 stage가 서로 앞서갈 수 있는 true pipeline과 모든 결과를 기다리는 explicit parallel barrier를 구분한다. Farad는 한 assistant message의 Agent call batch를 foreground barrier로 사용한다. background나 promoted agent가 섞이면 자동 barrier가 아니므로 orchestrator가 N-of-N file을 직접 확인한다.</p>

<blockquote>
  <p>Claude Workflow distinguishes true per-item pipelines, where items can advance independently, from explicit parallel barriers that wait for all results. Farad uses a batch of Agent calls in one assistant message as a foreground barrier. If background or promoted agents are involved, the orchestrator must manually confirm N-of-N files because the automatic barrier no longer covers them.</p>
</blockquote>

<h3 id="progress-ux">progress UX</h3>

<h3 id="progress-ux-1">Progress UX</h3>

<p>Claude Workflow는 <code class="language-plaintext highlighter-rouge">phase()</code>와 <code class="language-plaintext highlighter-rouge">log()</code> event를 progress tree 및 <code class="language-plaintext highlighter-rouge">/workflows</code> UI에 보낸다. Farad에는 dedicated progress tree가 없으므로 phase 직전 launch 계획과 phase 직후 N-of-N 결과를 chat에서 bookend하고, durable 상태는 <code class="language-plaintext highlighter-rouge">state.md</code>에 기록한다.</p>

<blockquote>
  <p>Claude Workflow emits <code class="language-plaintext highlighter-rouge">phase()</code> and <code class="language-plaintext highlighter-rouge">log()</code> events into a progress tree and the <code class="language-plaintext highlighter-rouge">/workflows</code> UI. Farad has no dedicated progress tree, so it bookends each phase in chat with a pre-launch plan and a post-barrier N-of-N result, while recording durable state in <code class="language-plaintext highlighter-rouge">state.md</code>.</p>
</blockquote>

<h3 id="failure와-resume">failure와 resume</h3>

<h3 id="failure-and-resume">Failure and resume</h3>

<p>Claude Workflow는 동일 script와 args의 unchanged agent-call prefix를 journal/cache에서 재사용하고 <code class="language-plaintext highlighter-rouge">resumeFromRunId</code> 이후 처음 변경된 call부터 live 실행한다. Farad는 <code class="language-plaintext highlighter-rouge">state.md</code>와 file 존재 여부를 completion marker로 사용한다. output file은 success, <code class="language-plaintext highlighter-rouge">.error.md</code> sibling은 failure이며 neither 상태는 live status 확인 후 respawn 대상이다. 따라서 Claude 방식은 replay-aware resume이고 Farad 방식은 artifact-aware restart다.</p>

<blockquote>
  <p>Claude Workflow reuses the unchanged prefix of agent calls from its journal/cache for the same script and args, then runs live from the first changed call after <code class="language-plaintext highlighter-rouge">resumeFromRunId</code>. Farad uses <code class="language-plaintext highlighter-rouge">state.md</code> and file existence as completion markers: an output file means success, an <code class="language-plaintext highlighter-rouge">.error.md</code> sibling means failure, and neither means the agent may need respawning after checking live status. Claude therefore provides replay-aware resume, while Farad provides artifact-aware restart.</p>
</blockquote>

<h3 id="structured-output">structured output</h3>

<h3 id="structured-output-1">Structured output</h3>

<p>Claude Workflow는 JSON Schema를 agent option으로 받아 StructuredOutput tool을 강제하고 검증된 object를 script로 반환한다. Farad 스킬의 중심 계약은 Markdown/text artifact이며 <code class="language-plaintext highlighter-rouge">output_file</code>이 raw final message를 저장한다. 구조가 필요하면 template과 output format 지시로 만들지만 Workflow VM 수준의 schema return contract는 아니다.</p>

<blockquote>
  <p>Claude Workflow accepts a JSON Schema as an agent option, forces the StructuredOutput tool, and returns a validated object to the script. The Farad skill centers on Markdown/text artifacts, with <code class="language-plaintext highlighter-rouge">output_file</code> storing the raw final message. Templates and output-format instructions can impose structure, but this is not a VM-level schema-return contract.</p>
</blockquote>

<h3 id="permission과-isolation">permission과 isolation</h3>

<h3 id="permissions-and-isolation">Permissions and isolation</h3>

<p>Claude Workflow는 inline script 승인, named workflow rule, worktree isolation, subagent tool permission을 runtime contract로 다룬다. Farad는 harness write를 session workflow folder 안으로 제한하고 그 안의 Write/Edit만 pre-authorize한다. project file 수정과 외부 command는 정상 permission을 그대로 거친다. 즉 Claude는 execution isolation을, Farad는 artifact containment를 더 강하게 전면에 둔다.</p>

<blockquote>
  <p>Claude Workflow treats inline-script approval, named-workflow rules, worktree isolation, and subagent tool permissions as runtime contracts. Farad confines harness writes to the session workflow folder and pre-authorizes only Write/Edit operations inside it; project-file edits and outside commands still use normal permissions. Claude foregrounds execution isolation, while Farad foregrounds artifact containment.</p>
</blockquote>

<h3 id="규모와-비용">규모와 비용</h3>

<h3 id="scale-and-cost">Scale and cost</h3>

<p>Claude Workflow는 workflow당 동시 agent cap, 전체 1000 agent cap, hard token budget, budget-scaled loop를 제공하므로 매우 큰 deterministic fleet을 표현할 수 있다. Farad 스킬은 일반적으로 2–3 phase, phase마다 3–8 agent를 권장하고 약 15 agent 또는 4 phase를 넘으면 over-orchestration을 의심한다. 이것은 기능 부족을 숨기는 제한이 아니라 main-agent-driven orchestration의 신뢰성과 비용에 맞춘 의도적 operating envelope다.</p>

<blockquote>
  <p>Claude Workflow exposes per-workflow concurrency limits, a 1,000-agent lifetime cap, hard token budgets, and budget-scaled loops, allowing very large deterministic fleets. The Farad skill normally recommends two to three phases with three to eight agents per fan-out and treats roughly 15 agents in one phase or four-plus phases as an over-orchestration signal. This is not a hidden limitation but a deliberate operating envelope matched to the reliability and cost of main-agent-driven orchestration.</p>
</blockquote>

<h3 id="ultracode와의-관계">Ultracode와의 관계</h3>

<h3 id="relationship-to-ultracode">Relationship to Ultracode</h3>

<p>Claude Workflow description에서 Ultracode는 모든 substantive task에 workflow를 기본 사용하고 token cost를 constraint로 보지 않는 standing opt-in이다. Farad <code class="language-plaintext highlighter-rouge">orchestrate-workflow</code>에는 세션 전체를 강제하는 ultracode mode가 없다. 대신 단일 작업이 folder protocol을 정당화하는지 먼저 판단하고, 작은 작업은 direct Agent call 하나로 끝낸다.</p>

<blockquote>
  <p>In the Claude Workflow description, Ultracode is a standing opt-in that defaults every substantive task to a workflow and does not treat token cost as a constraint. Farad's <code class="language-plaintext highlighter-rouge">orchestrate-workflow</code> has no session-wide ultracode mode; it first asks whether a single task earns the folder protocol and keeps small work to one direct Agent call.</p>
</blockquote>

<h3 id="계보와-의도적-편차">계보와 의도적 편차</h3>

<h3 id="lineage-and-deliberate-divergence">Lineage and deliberate divergence</h3>

<p>Farad 스킬의 investigate/synthesize, code review, deep research, design panel, sweep/migrate, completeness critic은 Claude Workflow의 quality pattern과 뚜렷한 계보를 공유한다. 그러나 Claude의 deterministic JS runtime을 복사하지 않고, 같은 품질 전략을 file handoff와 기존 Agent runtime 위의 모델 주도 protocol로 다시 표현한다. 따라서 Farad 구현은 Workflow tool의 축소 복제본이 아니라, 운영 지침은 계승하고 execution engine은 교체한 재설계다.</p>

<blockquote>
  <p>Farad's investigate/synthesize, code review, deep research, design panel, sweep/migrate, and completeness-critic shapes clearly share lineage with Claude Workflow's quality patterns. However, Farad does not copy the deterministic JavaScript runtime; it re-expresses the same quality strategy as a model-driven protocol built on file handoffs and the existing Agent runtime. The Farad implementation is therefore not a reduced clone of the Workflow tool, but a redesign that inherits the operating guidance while replacing the execution engine.</p>
</blockquote>

<h3 id="결론">결론</h3>

<h3 id="conclusion">Conclusion</h3>

<p>Claude Workflow가 강한 곳은 결정적 control flow, true pipeline, schema return, budget, journal/cache resume, 대규모 fleet이다. Farad <code class="language-plaintext highlighter-rouge">orchestrate-workflow</code>가 강한 곳은 낮은 구현 복잡도, 기존 runtime 재사용, disk artifact의 직접 inspectability, session lifecycle에 묶인 containment와 GC, main agent가 phase 사이에서 판단을 바꿀 수 있는 유연성이다.</p>

<blockquote>
  <p>Claude Workflow is strongest in deterministic control flow, true pipelines, schema returns, budgets, journal/cache resume, and large fleets. Farad's <code class="language-plaintext highlighter-rouge">orchestrate-workflow</code> is strongest in low implementation complexity, reuse of the existing runtime, directly inspectable disk artifacts, containment and garbage collection tied to the session lifecycle, and flexibility for the main agent to revise its judgment between phases.</p>
</blockquote>

<p>둘의 핵심 trade-off는 기능 개수가 아니라 <strong>결정적 script가 control을 소유하는가, reasoning agent가 control을 소유하는가</strong>다. Farad는 현재 규모에서 후자를 선택하고, 전자의 품질 패턴을 가능한 범위까지 흡수했다.</p>

<blockquote>
  <p>The central trade-off is not feature count but <strong>whether deterministic scripts own control or reasoning agents own control</strong>. Farad currently chooses the latter for its operating scale while absorbing as many of the former's quality patterns as practical.</p>
</blockquote>]]></content><author><name>hvppycoding</name></author><category term="farad" /><summary type="html"><![CDATA[Claude Code Workflow 툴과 Farad orchestrate-workflow 스킬: 한영 원문 대조와 종합 비교]]></summary></entry><entry><title type="html">GPT-5.6 API 모델 가이드: 핵심 기능과 마이그레이션 전략</title><link href="https://hvppycoding.github.io/ai/gpt-5-6-api-guide/" rel="alternate" type="text/html" title="GPT-5.6 API 모델 가이드: 핵심 기능과 마이그레이션 전략" /><published>2026-07-10T16:37:24+09:00</published><updated>2026-07-10T16:37:24+09:00</updated><id>https://hvppycoding.github.io/ai/gpt-5-6-api-guide</id><content type="html" xml:base="https://hvppycoding.github.io/ai/gpt-5-6-api-guide/"><![CDATA[<h1 id="gpt-56-api-모델-가이드-핵심-기능과-마이그레이션-전략">GPT-5.6 API 모델 가이드: 핵심 기능과 마이그레이션 전략</h1>

<blockquote>
  <p>이 글은 OpenAI의 <a href="https://developers.openai.com/api/docs/guides/latest-model">GPT-5.6 모델 가이드</a>를 바탕으로, API를 운영하는 개발자가 빠르게 적용 판단을 내릴 수 있도록 핵심을 한국어로 재구성한 실무 요약입니다. 원문은 수시로 변경될 수 있으므로 실제 도입 전에는 공식 문서를 확인하세요.</p>
</blockquote>

<h2 id="한눈에-보기">한눈에 보기</h2>

<p>GPT-5.6은 복잡한 프로덕션 워크플로를 겨냥한 최신 모델 계열이다. OpenAI는 고난도 작업에서의 품질과 토큰 효율을 함께 높였으며, 특히 웹·앱 UI를 만들 때 레이아웃, 정보 위계, 시각적 판단이 좋아졌다고 안내한다.</p>

<p>API에서 <code class="language-plaintext highlighter-rouge">gpt-5.6</code> 별칭은 최상위 성능의 <code class="language-plaintext highlighter-rouge">gpt-5.6-sol</code>로 연결된다. 비용과 처리량 요구가 다르면 다음 계열 중 하나를 고르면 된다.</p>

<table>
  <thead>
    <tr>
      <th>모델</th>
      <th>권장 용도</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">gpt-5.6-sol</code></td>
      <td>가장 높은 품질이 필요한 복잡한 작업</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">gpt-5.6-terra</code></td>
      <td>지능과 비용의 균형이 필요한 일반 프로덕션 작업</td>
    </tr>
    <tr>
      <td><code class="language-plaintext highlighter-rouge">gpt-5.6-luna</code></td>
      <td>대량 처리와 효율이 중요한 워크로드</td>
    </tr>
  </tbody>
</table>

<p>모델 문자열만 바꾸는 방식은 충분하지 않다. 기존 GPT-5.5 또는 GPT-5.4 설정을 출발점으로 삼되, 대표 업무에서 같은 추론 수준과 한 단계 낮은 수준을 함께 평가하는 것이 좋다. 더 적은 토큰으로 같은 품질을 내는 경우가 많지만, 최적점은 업무마다 다르다.</p>

<h2 id="새로-검토할-기능">새로 검토할 기능</h2>

<h3 id="프로그램-방식-도구-호출">프로그램 방식 도구 호출</h3>

<p>Programmatic Tool Calling(PTC)은 모델이 호스팅된 실행 환경에서 JavaScript를 작성해, 허용된 도구를 호출하고 결과를 가공·전달하도록 한다. 많은 도구 결과를 필터링, 조인, 정렬, 중복 제거, 집계하거나 검증한 뒤 작은 구조화 결과만 돌려줘야 할 때 특히 적합하다.</p>

<p>다만 여러 번의 도구 호출이 있다는 이유만으로 PTC를 선택할 필요는 없다. 호출 결과가 작거나, 매 결과마다 모델의 다음 판단이 달라지거나, 승인 절차가 포함되거나, 최종 결과에 원본 인용·아티팩트를 보존해야 한다면 일반적인 직접 도구 호출이 더 명확하다. PTC를 쓴다면 허용 도구, 출력 스키마, 재시도·중단 한계, 직접 호출로 남길 단계를 프롬프트에 구체적으로 적는다.</p>

<h3 id="멀티에이전트-베타">멀티에이전트 베타</h3>

<p>Responses API의 멀티에이전트 기능은 하나의 GPT-5.6 인스턴스가 독립적인 하위 작업을 병렬로 조율하고 결과를 합치도록 돕는다. 조사, 비교, 코드·문서 검토처럼 분리 가능한 작업 흐름에서 응답 시간을 줄이고 결과 품질을 높일 여지가 있다. 베타 기능이므로 실제 서비스에 적용하기 전에는 실패 처리, 비용, 관측성을 포함해 충분히 검증해야 한다.</p>

<h3 id="프롬프트-캐시와-추론-문맥">프롬프트 캐시와 추론 문맥</h3>

<p>프롬프트 캐시는 자동 모드로도 계속 사용할 수 있다. GPT-5.6에서는 재사용할 프롬프트 접두부를 명시적으로 표시하는 방식도 지원한다. 캐시 읽기는 할인되지만 캐시 쓰기는 일반 입력보다 높은 단가가 적용될 수 있으므로, <code class="language-plaintext highlighter-rouge">cached_tokens</code>와 <code class="language-plaintext highlighter-rouge">cache_write_tokens</code>를 함께 관찰해 전체 비용이 실제로 줄었는지 확인한다. 기존 <code class="language-plaintext highlighter-rouge">prompt_cache_retention</code>을 사용했다면 <code class="language-plaintext highlighter-rouge">prompt_cache_options.ttl</code>로 옮기는 계획도 세운다.</p>

<p>여러 턴에 걸친 작업이라면 <code class="language-plaintext highlighter-rouge">reasoning.context</code>를 검토한다. 목표와 가정, 우선순위가 계속 유지될 때는 <code class="language-plaintext highlighter-rouge">all_turns</code>와 <code class="language-plaintext highlighter-rouge">previous_response_id</code> 조합이 이전 추론을 활용하는 데 유용하다. 이전 판단이 더 이상 맞지 않는 업무라면 <code class="language-plaintext highlighter-rouge">current_turn</code>이 더 안전하다. 수동으로 히스토리를 관리하거나 저장을 끈 환경에서는, 필요한 입력과 응답 항목 및 암호화된 추론 항목을 빠짐없이 다시 보내는 설계가 필요하다.</p>

<h3 id="추론-수준과-pro-모드">추론 수준과 Pro 모드</h3>

<p>GPT-5.6은 <code class="language-plaintext highlighter-rouge">none</code>, <code class="language-plaintext highlighter-rouge">low</code>, <code class="language-plaintext highlighter-rouge">medium</code>, <code class="language-plaintext highlighter-rouge">high</code>, <code class="language-plaintext highlighter-rouge">xhigh</code>, <code class="language-plaintext highlighter-rouge">max</code> 추론 수준을 지원한다. 일반적으로 <code class="language-plaintext highlighter-rouge">medium</code>은 균형 잡힌 시작점이고, 지연 시간이 중요한 경우 <code class="language-plaintext highlighter-rouge">low</code>, 검증과 탐색이 실질적인 품질 향상을 만드는 경우 <code class="language-plaintext highlighter-rouge">high</code> 또는 <code class="language-plaintext highlighter-rouge">xhigh</code>를 평가한다. <code class="language-plaintext highlighter-rouge">max</code>는 가장 어려운 품질 우선 업무에만 두고 <code class="language-plaintext highlighter-rouge">xhigh</code>와 직접 비교하는 편이 좋다.</p>

<p><code class="language-plaintext highlighter-rouge">reasoning.mode: "pro"</code>는 별도 모델명이 아니라 Responses API의 실행 모드다. 어려운 최적화, 고가치 코드 작업, 심층 분석처럼 품질 향상이 결과에 크게 영향을 주는 경우에 고려한다. 응답은 하나의 최종 답으로 돌아오며 지연 시간과 사용 토큰이 늘어날 수 있다. Pro 모드와 추론 수준은 독립적이므로, 표준 모드의 기준 구성과 같은 모델·수준부터 비교한다. 프롬프트에서 “더 오래 생각하라”고 요구하기보다 API 요청에서 모드를 설정하고 목표·근거·성공 기준을 명확히 쓰는 편이 낫다.</p>

<h2 id="프롬프트를-가볍게-만드는-방법">프롬프트를 가볍게 만드는 방법</h2>

<p>GPT-5.6은 장황한 시스템 프롬프트보다 필요한 제약만 남긴 짧은 프롬프트에서 좋은 결과를 낼 수 있다. 오래된 지시와 예시를 계속 누적하면, 불필요한 탐색과 반복 검증으로 지연·비용이 오히려 커질 수 있다.</p>

<ul>
  <li>작업을 안정적으로 끝내는 최소 프롬프트와 최소 도구 집합에서 시작한다.</li>
  <li>평가에서 드러난 구체적 실패를 해결할 때만 지시, 도구, 예시를 추가한다.</li>
  <li>도구 설명은 반환 형식과 오류 처리를 알 수 있을 만큼만 간결하고 정확하게 유지한다.</li>
  <li>프론트엔드·형식·문체 지시는 제품 고유의 요구일 때만 둔다.</li>
  <li>시작 문맥뿐 아니라 턴이 쌓인 뒤의 문맥 크기도 측정한다.</li>
</ul>

<p>특히 “짧게 답하라” 같은 일반적인 축약 지시는 필요한 산출물까지 줄일 수 있다. 분량을 강제하기보다, 결론을 먼저 제시하고 근거·중요한 주의점·다음 행동은 유지하되 인사말, 반복, 선택적 배경 설명부터 덜어 내도록 지시하는 편이 안전하다.</p>

<h2 id="자율성과-권한을-명확히-정하기">자율성과 권한을 명확히 정하기</h2>

<p>GPT-5.6은 적극적으로 작업을 이어갈 수 있으므로, 요청이 허용하는 행동 범위를 간단히 선언하는 것이 중요하다. 예를 들어 답변·분석·검토·계획 요청에서는 자료를 조사하고 결과만 보고하며, 변경·구현·수정 요청에서는 범위 안의 로컬 변경과 비파괴 검증을 수행하도록 구분할 수 있다. 외부 쓰기, 파괴적 작업, 구매, 범위의 큰 확장은 확인을 요구한다.</p>

<p>파일 읽기, 로그 확인, 검색, 범위 안 코드 편집, 비파괴 테스트처럼 별도 승인 없이 가능한 행동도 분명히 적어 둔다. 반대로 “먼저 물어봐”나 “변경하지 마” 같은 문구를 여러 곳에서 되풀이하면, 안전한 일반 작업까지 불필요하게 멈출 수 있다.</p>

<h2 id="운영안전성-고려-사항">운영·안전성 고려 사항</h2>

<p>실시간 사이버·생물학 오용 분류기가 응답 생성 중 작동할 수 있다. 이 때문에 일부 요청은 거절되거나, 생성 중 검토 시간만큼 지연될 수 있다. 방어적 보안 연구나 디버깅처럼 정당한 작업에도 드물게 영향을 줄 수 있으므로, 제품의 오류 안내와 재시도 경험을 설계할 때 이를 고려한다.</p>

<p>개별 최종 사용자를 상대하는 서비스라면 요청마다 안정적이고 개인정보를 직접 드러내지 않는 <code class="language-plaintext highlighter-rouge">safety_identifier</code>를 보내는 방식을 검토한다.</p>

<h2 id="마이그레이션-체크리스트">마이그레이션 체크리스트</h2>

<ol>
  <li>대표적인 실제 업무와 성공 기준을 정한다.</li>
  <li><code class="language-plaintext highlighter-rouge">sol</code>·<code class="language-plaintext highlighter-rouge">terra</code>·<code class="language-plaintext highlighter-rouge">luna</code> 중 목표 품질, 비용, 처리량에 맞는 모델을 고른다.</li>
  <li>기존 추론 수준을 기준선으로 두고, 한 단계 낮은 수준과 함께 비교한다.</li>
  <li>추론·도구·다중 턴 흐름에는 Responses API를 사용한다.</li>
  <li>캐시 읽기·쓰기 토큰, 전체 토큰, 지연 시간, 비용을 함께 기록한다.</li>
  <li>작업 성공률, 최종 답의 완결성, 요구된 근거 충족 여부를 측정한다.</li>
  <li>필요한 경우에만 Pro 모드, PTC, 멀티에이전트를 단계적으로 켠다.</li>
  <li>줄어든 호출 수나 중간 결과가 사용자에게 보이는 품질을 낮추지 않았는지 마지막으로 확인한다.</li>
</ol>

<h2 id="참고-자료">참고 자료</h2>

<ul>
  <li><a href="https://developers.openai.com/api/docs/guides/latest-model">OpenAI 공식 GPT-5.6 모델 가이드</a></li>
  <li><a href="https://developers.openai.com/api/docs/guides/migrate-to-responses">Responses API 소개</a></li>
  <li><a href="https://developers.openai.com/api/docs/guides/reasoning">추론 가이드</a></li>
  <li><a href="https://developers.openai.com/api/docs/guides/prompt-caching">프롬프트 캐싱 가이드</a></li>
  <li><a href="https://developers.openai.com/api/docs/guides/tools-programmatic-tool-calling">Programmatic Tool Calling 가이드</a></li>
</ul>]]></content><author><name>hvppycoding</name></author><category term="ai" /><summary type="html"><![CDATA[GPT-5.6 API 모델 가이드: 핵심 기능과 마이그레이션 전략]]></summary></entry><entry><title type="html">경량 HTML 뷰어 검토 — 에이전트 HTML 산출물용 별도 프로세스 뷰어 (가칭 `hview`)</title><link href="https://hvppycoding.github.io/farad/lightweight-html-viewer/" rel="alternate" type="text/html" title="경량 HTML 뷰어 검토 — 에이전트 HTML 산출물용 별도 프로세스 뷰어 (가칭 `hview`)" /><published>2026-07-04T13:21:02+09:00</published><updated>2026-07-04T13:21:02+09:00</updated><id>https://hvppycoding.github.io/farad/lightweight-html-viewer</id><content type="html" xml:base="https://hvppycoding.github.io/farad/lightweight-html-viewer/"><![CDATA[<h1 id="경량-html-뷰어-검토--에이전트-html-산출물용-별도-프로세스-뷰어-가칭-hview">경량 HTML 뷰어 검토 — 에이전트 HTML 산출물용 별도 프로세스 뷰어 (가칭 <code class="language-plaintext highlighter-rouge">hview</code>)</h1>

<blockquote>
  <p>2026-07-04. 발단: <a href="https://news.hada.io/topic?id=29347">GeekNews 29347</a> — "에이전트 산출물은
Markdown보다 HTML이 낫다" (표·SVG·JS 인터랙션·일회용 도구). Farad가 이 방향을 쓰려면 HTML을
여는 경로가 필요한데, <strong>Farad 내장(QWebEngineView in-process)은 성능 부담으로 배제</strong>(사용자 결정).
별도 프로세스로 뜨는 경량 뷰어를 만들어 <code class="language-plaintext highlighter-rouge">hrun</code> 커맨드(<code class="language-plaintext highlighter-rouge">hrag</code> 패턴)로 배포하는 안을 검토한다.</p>
</blockquote>

<hr />

<h2 id="tldr">TL;DR</h2>

<ul>
  <li><strong>엔진을 새로 만드는 선택지는 없다.</strong> HTML 산출물의 핵심 가치가 JS 인터랙션(탭·슬라이더·캔버스)
이므로 진짜 엔진이 필요하고, no-JS 경량 렌더러(litehtml 등)는 목적 자체를 잃는다.
"가벼운 브라우저를 만든다" = <strong>기존 엔진 + 미니멀 뷰어 셸 + 상태(프로필) 정책</strong>이 작업의 본체다.
<strong>"가벼움"은 엔진이 아니라 셸·상태·수명주기에서 온다</strong> — 엔진 프로세스 비용은 어차피
크롬급이다 (§4 "크롬급의 범위").</li>
  <li><strong>권장: PySide6 + QtWebEngine 단일창 뷰어 <code class="language-plaintext highlighter-rouge">hview</code>.</strong> 결정적 이유는 배포 채널 적합성 —
<strong>pip wheel만으로 완결되는 유일한 실엔진</strong>이라 hrun python toolkit + wheelhouse 모델에 그대로
실리고, PySide6(Addons 포함) wheel은 farad 때문에 이미 망내에 반입돼 있어 새 외부 패키지
승인·반입이 없다. 단 별도 venv 디스크(수백 MB)는 실비용 — farad 설치 머신에선 farad venv
공유로 줄일 수 있다(최적화이지 전제는 아님). 스택이 같아 유지보수 지식도 재사용된다.</li>
  <li><strong>Chrome/Firefox의 NFS 프로필 락 문제는 "상태를 없애서" 구조적으로 제거한다.</strong>
off-the-record 프로필(디스크 무기록)이 기본 — 상태가 없으면 락도 없다. 상태가 꼭 필요해지면
host-local(<code class="language-plaintext highlighter-rouge">$XDG_RUNTIME_DIR</code>, <code class="language-plaintext highlighter-rouge">/tmp</code>)에만 두고 NFS 홈에는 절대 두지 않는다.</li>
  <li><strong>소스는 <code class="language-plaintext highlighter-rouge">hrag</code>처럼 독립 pip 패키지</strong>(farad·hrun 의존 없음), hrun 저장소에는
<code class="language-plaintext highlighter-rouge">toolkits/hview/</code> 래퍼만 추가. farad는 detached 프로세스로 spawn.</li>
  <li><strong>차별화 기능: <code class="language-plaintext highlighter-rouge">--watch</code> live-reload</strong> — 에이전트가 HTML을 고치는 동안 뷰어가 자동 갱신.
기사에서 말하는 "spec 목업 반복", "일회용 도구" 워크플로우가 이걸로 완성된다.</li>
  <li>Plan B: ① firefox 임시 프로필 래퍼(zero-install 폴백), ② pywebview(WebKitGTK) — 각각
한계가 명확해 후순위 (§6).</li>
</ul>

<hr />

<h2 id="1-문제-정의--기존-브라우저가-안-맞는-이유">1. 문제 정의 — 기존 브라우저가 안 맞는 이유</h2>

<p>주 환경: 여러 사용자가 공유하는 저사양 HPC(RHEL 8.10, NFS 공유 홈, 원격 X11/VNC) + Windows.</p>

<ol>
  <li><strong>무게.</strong> Chrome/Firefox는 뷰어 용도 대비 과체중 — 다만 정확히 하면, 페이지 렌더링
자체의 비용이 아니라 <strong>브라우저 인프라의 비용</strong>이다: 백그라운드 서비스·업데이터·확장·
세션 복원·탭 프로세스 트리·프로필 초기화. 뷰어는 이 중 아무것도 필요 없는데 전부 딸려온다.
공유 장비에서 사용자당 이 상주 비용이 크다. (렌더링 엔진 자체의 RAM은 어떤 선택을 해도
비슷하다 — §4 "크롬급의 범위" 참고.)</li>
  <li><strong>프로필 관리.</strong> 사용자의 "진짜" 브라우징 프로필과 에이전트 산출물 뷰잉이 섞인다.
산출물 뷰잉은 세션성(열고-보고-닫기)인데 브라우저는 영속 프로필을 전제로 설계돼 있다.</li>
  <li><strong>멀티호스트 프로필 락 (실제 겪은 pain).</strong> NFS 공유 홈에서 호스트 A에 firefox가 떠 있으면
호스트 B에서 열 때 프로필 락(<code class="language-plaintext highlighter-rouge">~/.mozilla/.../lock</code>·<code class="language-plaintext highlighter-rouge">.parentlock</code>, Chrome은 <code class="language-plaintext highlighter-rouge">SingletonLock</code>)에
걸려 "already running" 에러. 단일 머신 가정의 설계가 HPC 멀티호스트 사용과 충돌한다.</li>
  <li><strong>Farad 내장 배제.</strong> QtWebEngine을 farad 프로세스에 올리면 상주 RAM·기동 시간이 늘고
메인 UI 응답성 리스크가 생긴다. 뷰잉은 간헐적 작업이므로 별도 프로세스가 맞다 —
닫으면 메모리가 온전히 반환된다.</li>
</ol>

<h2 id="2-요구사항">2. 요구사항</h2>

<p><strong>기능</strong></p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">file://</code> HTML 렌더 (self-contained 파일이 기본 대상), 내부망 <code class="language-plaintext highlighter-rouge">http(s)</code> URL도 허용</li>
  <li>최신 CSS + JS 실행 (인터랙션이 핵심 가치)</li>
  <li>뷰어 UX: 명령 한 번에 창 하나, ESC/Ctrl+W로 닫기, zoom(Ctrl±), find(Ctrl+F)</li>
  <li><code class="language-plaintext highlighter-rouge">--watch</code>: 파일 변경 감지 자동 reload (에이전트 반복 루프 지원)</li>
</ul>

<p><strong>비기능</strong></p>

<ul>
  <li>RHEL 8.10(glibc 2.28) + Windows, 동일 코드</li>
  <li>저사양 공유 HPC — 상주 데몬 없음, 닫으면 자원 반환, 원격 X11/VNC에서 소프트웨어 렌더 동작</li>
  <li>NFS 공유 홈 + 멀티호스트 동시 실행 안전 (락 파일 금지)</li>
  <li>폐쇄망 — 외부 CDN 접근 불가 전제 (→ §5.5 self-contained 규약)</li>
  <li>배포: hrun toolkit 커맨드 + pip 패키지 (Windows/개발 환경)</li>
</ul>

<h2 id="3-접근-재정의--만드는-것은-셸--상태-정책">3. 접근 재정의 — 만드는 것은 "셸 + 상태 정책"</h2>

<p>렌더링 엔진(Chromium/WebKit) 개발·포크는 논외다. 실제로 만들 것은:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>┌───────────────────────────────────────────────┐
│ hview 셸 (수백 줄 규모)                          │
│  CLI 파싱 / 단일창 / 단축키 / watch-reload       │
├───────────────────────────────────────────────┤
│ 상태 정책 ← 여기가 기존 브라우저와의 진짜 차별점    │
│  off-the-record 기본 (디스크 무기록 → 락 없음)    │
│  잔여 상태는 host-local만, NFS 홈 금지            │
├───────────────────────────────────────────────┤
│ 렌더링 엔진 (기성품 선택)                         │
│  QtWebEngine(Chromium) / WebKitGTK / 기성 브라우저│
└───────────────────────────────────────────────┘
</code></pre></div></div>

<p>기존 브라우저의 문제(§1의 2·3번)는 엔진 문제가 아니라 <strong>영속 프로필 전제</strong>의 문제다.
뷰어는 프로필이 필요 없으므로, 상태를 갖지 않는 셸을 만들면 문제가 소멸한다.</p>

<h2 id="4-스택-옵션-비교">4. 스택 옵션 비교</h2>

<table>
  <thead>
    <tr>
      <th>옵션</th>
      <th>렌더 충실도</th>
      <th>RAM(추정)*</th>
      <th>추가 배포 비용</th>
      <th>멀티호스트</th>
      <th>Windows</th>
      <th>비고</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><strong>A. PySide6 QtWebEngine</strong> ✅</td>
      <td>Chromium 그대로</td>
      <td>~200–350MB/창</td>
      <td>pip wheel만 (이미 망내 반입); 전용 venv 디스크는 실비용</td>
      <td>off-the-record로 해결</td>
      <td>동일 코드</td>
      <td>LGPL-3</td>
    </tr>
    <tr>
      <td>B. pywebview (WebKitGTK/WebView2)</td>
      <td>OS마다 다른 엔진</td>
      <td>~150–300MB</td>
      <td>GTK+PyGObject rpm 의존</td>
      <td>해결 가능</td>
      <td>엔진이 달라짐</td>
      <td>렌더 결과 diverge</td>
    </tr>
    <tr>
      <td>C. 기성 브라우저 래퍼 (firefox <code class="language-plaintext highlighter-rouge">-no-remote -profile &lt;tmp&gt;</code>)</td>
      <td>최상</td>
      <td>~400MB+</td>
      <td>0 (설치돼 있으면)</td>
      <td>임시 프로필로 우회</td>
      <td>별도 처리</td>
      <td>뷰어 UX 아님, 기동 느림</td>
    </tr>
    <tr>
      <td>D. no-JS 렌더러 (QTextBrowser/litehtml)</td>
      <td>CSS 부분집합, <strong>JS 없음</strong></td>
      <td>&lt;50MB</td>
      <td>0</td>
      <td>해당 없음</td>
      <td>동일</td>
      <td><strong>목적 상실 — 탈락</strong></td>
    </tr>
    <tr>
      <td>E. Electron / CEF / Tauri</td>
      <td>Chromium/WebView</td>
      <td>200MB+</td>
      <td>별도 런타임 반입</td>
      <td>해결 가능</td>
      <td>가능</td>
      <td>폐쇄망 배포·스택 이질성 — 탈락</td>
    </tr>
  </tbody>
</table>

<p>* 실측 필요 항목 (§7). 다중 프로세스 합산 RSS 기준 추정치.</p>

<p><strong>A를 권장하는 결정적 이유 — 선택의 주체는 PySide6가 아니라 엔진이다.</strong> hview는 별도
프로세스이므로 farad와 UI 스택을 맞추는 것 자체에는 아무 가치가 없다(공유 위젯·테마 없음).
결정 순서는 이렇다: ① 인터랙티브 HTML에는 실엔진(Chromium/WebKit)이 필요하고, ② 폐쇄망에서
<strong>pip wheel만으로 반입되는 실엔진은 QtWebEngine이 유일</strong>하며(PySide6-Addons wheel이 Chromium을
통째로 담고 있다), ③ QtWebEngine을 고르는 순간 셸은 Qt가 된다 — <strong>PySide6는 엔진의 배송 수단</strong>
이다. 나머지 실엔진은 전부 pip 밖 반입 경로가 필요하다: WebKitGTK는 시스템 rpm(wheelhouse로
못 나름), CEF 파이썬 바인딩(cefpython3)은 사실상 unmaintained, Electron/Tauri는 별도 런타임
바이너리 반입. 부수 이점 — 같은 노드에서 farad가 같은 Qt 기반으로 이미 동작(플랫폼 리스크
기검증), PySide6 wheel이 이미 망내 반입돼 신규 승인 불요, 팀 숙련도 — 은 보너스지 근거의
본체가 아니다.</p>

<p>B는 "가볍다"는 장점이 있지만 Linux(WebKitGTK)/Windows(WebView2) 엔진이 달라 에이전트가 만든
HTML이 환경마다 다르게 깨질 수 있고, RHEL 8의 webkit2gtk3 버전 리스크 + GTK/PyGObject 의존이
Qt 단일 스택에 이질 계층을 더한다. C는 폴백으로만 가치 있다(§6).</p>

<p><strong>반입 vs 설치 — 진짜 제약의 위치.</strong> 폐쇄망 반입 자체는 일회성 작업이라 결정 게이트가 아니다
(사용자 확인). 진짜 제약은 <strong>배포 모델</strong>: hrun은 NFS 공유 스토리지에 파일을 놓는 방식이라
노드별 root 설치가 필요 없는데, pip wheel은 이 모델에 그대로 맞고 시스템 rpm(webkit2gtk3,
gtk3 등)은 전 노드 설치가 필요하다(이미 깔려 있지 않는 한 — §7 확인 항목). 반입이 자유로워도
이 구도는 유지되고, OS 간 엔진 일관성 논거도 그대로다.</p>

<p><strong>"크롬급"의 범위 — 충실도와 무게 양쪽 다 크롬급이 맞다.</strong> QtWebEngine은 Chromium
(Blink+V8) 그 자체다. 충실도: 렌더링·JS·Canvas·WebGL(원격에선 SwiftShader 소프트웨어 폴백)
모두 Chrome과 동일 계열. 차이는 ① Chromium 버전이 Qt 릴리스에 고정된 스냅샷 + 보안 백포트라
최신 Chrome 대비 1년 안팎 랙(표준 HTML/CSS/JS 리포트에는 실질 무영향), ② Google 서비스·확장·
Widevine 없음, ③ 공식 바이너리에 독점 코덱(H.264/AAC) 미포함 — mp4 재생만 안 되고 WebM은
된다. LLM이 생성하는 HTML은 사실상 Chrome 기준으로 만들어지므로 호환성 면에선 오히려 최적.
무게: <strong>엔진 프로세스 비용도 크롬급이다</strong> — 창 하나가 Chrome 탭 몇 개 수준의 RAM(추정
200–350MB)을 쓴다. 즉 hview의 "가벼움"은 더 가벼운 엔진에서 오는 게 아니라 <strong>브라우저
인프라 제거</strong>(서비스·업데이터·확장·탭 트리·프로필 없음), <strong>수명주기</strong>(열고-보고-닫으면 0으로
복귀), <strong>상태 정책</strong>(무기록)에서 온다. 엔진 자체까지 가볍길 원하면 선택지는 WebKitGTK(소폭)
아니면 no-JS 렌더러(대폭, 목적 상실)뿐이라는 트레이드오프를 §4 표가 보여준다.</p>

<h2 id="5-권장안--hview-설계">5. 권장안 — <code class="language-plaintext highlighter-rouge">hview</code> 설계</h2>

<h3 id="51-프로세스상태-모델">5.1 프로세스·상태 모델</h3>

<ul>
  <li><strong>창 하나 = 프로세스 하나</strong> (MVP). 뷰어 시맨틱(evince처럼) — 닫으면 전부 반환.</li>
  <li><strong>off-the-record 프로필 기본</strong>: <code class="language-plaintext highlighter-rouge">QWebEngineProfile()</code> 기본 생성자가 off-the-record —
쿠키·캐시·스토리지 전부 메모리, 디스크 무기록. <strong>락 파일이 생길 곳이 없다.</strong></li>
  <li>영속 상태(창 geometry, zoom 기억 등)는 MVP에서 넣지 않는다(YAGNI). 필요해지면
<code class="language-plaintext highlighter-rouge">$XDG_RUNTIME_DIR</code>(host-local) 아래에만 — NFS 홈(<code class="language-plaintext highlighter-rouge">~/.hview</code> 같은 것) 금지.</li>
  <li>상주 데몬·single-instance 서버 없음 (MVP). RAM 절약용 per-host 단일 인스턴스는 Phase 2(§8).</li>
</ul>

<h3 id="52-ux-최소-스펙">5.2 UX 최소 스펙</h3>

<ul>
  <li>창: 슬림 헤더 1줄(문서 <code class="language-plaintext highlighter-rouge">&lt;title&gt;</code> 또는 파일명, reload 버튼, watch 상태 표시) + WebView.
탭·주소창·북마크·히스토리 <strong>없음</strong> — 브라우저가 아니라 뷰어다.</li>
  <li>단축키: <code class="language-plaintext highlighter-rouge">ESC</code>/<code class="language-plaintext highlighter-rouge">Ctrl+W</code> 닫기, <code class="language-plaintext highlighter-rouge">Ctrl+R</code> reload, <code class="language-plaintext highlighter-rouge">Ctrl+F</code> find(QWebEnginePage.findText),
<code class="language-plaintext highlighter-rouge">Ctrl+±/0</code> zoom, <code class="language-plaintext highlighter-rouge">Ctrl+P</code> PDF 저장(Phase 2, printToPdf).</li>
  <li>링크: 같은 창에서 이동(문서 내 앵커·상대 경로), 외부 스킴은 무시 또는 명시 허용 목록.</li>
</ul>

<h3 id="53-cli">5.3 CLI</h3>

<div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code>hview report.html                 <span class="c"># 파일 열기</span>
hview <span class="nt">--watch</span> report.html         <span class="c"># 변경 감지 자동 reload (mtime, QFileSystemWatcher)</span>
hview http://internal-host/dash   <span class="c"># 내부망 URL</span>
hview <span class="nt">--title</span> <span class="s2">"Review"</span> a.html     <span class="c"># 창 제목 오버라이드</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">--watch</code> reload 시 스크롤 위치 보존(reload 전 scrollY 저장 → 복원). 에이전트가 파일을
수십 번 고쳐 쓰는 동안 사용자는 창을 띄워두기만 하면 된다 — 기사 활용처 5가지(스펙 목업,
코드 리뷰 시각화, 프로토타입, 리포트, 일회용 편집기)를 그대로 커버.</p>

<h3 id="54-배포--소스-위치와-hrun-통합">5.4 배포 — 소스 위치와 hrun 통합</h3>

<ul>
  <li><strong>소스: 독립 pip 패키지</strong> (hrag 패턴 — 자체 repo, <code class="language-plaintext highlighter-rouge">[project.scripts] hview = ...</code>,
의존은 <code class="language-plaintext highlighter-rouge">PySide6&gt;=6.6</code> 하나). farad·hrun 어디에도 의존하지 않는다.
    <ul>
      <li>hrun repo에 소스를 넣지 않는 이유: hrun은 C++ 라우터 + POSIX 셸 인프라 저장소라
Python GUI 앱이 들어가면 이질적이고, python·hrag도 toolkit 래퍼만 두는 기존 패턴과 어긋난다.</li>
    </ul>
  </li>
  <li><strong>hrun 통합: <code class="language-plaintext highlighter-rouge">toolkits/hview/</code></strong> — <code class="language-plaintext highlighter-rouge">commands/hview</code> 래퍼가 <code class="language-plaintext highlighter-rouge">hrun_enter_tool hview</code> 후
python toolkit 런타임 + wheelhouse로 설치/실행. env 오버라이드는
<code class="language-plaintext highlighter-rouge">$HYPERRUN_DATA_DIR/env/tools/hview.env</code>.</li>
  <li>Windows/개발 환경: farad venv에 pip 설치 → <code class="language-plaintext highlighter-rouge">hview</code> 콘솔 스크립트 또는 <code class="language-plaintext highlighter-rouge">python -m hview</code>.</li>
</ul>

<h3 id="55-farad-통합">5.5 farad 통합</h3>

<ul>
  <li><strong>실행 해석 순서</strong>: ① 같은 인터프리터에서 <code class="language-plaintext highlighter-rouge">hview</code> importable하면 <code class="language-plaintext highlighter-rouge">[sys.executable, "-m", "hview"]</code>
(farad venv 재사용 — QtWebEngine 이미 있음) → ② PATH의 <code class="language-plaintext highlighter-rouge">hview</code> → ③ <code class="language-plaintext highlighter-rouge">hrun hview</code>.</li>
  <li><strong>detached spawn</strong>: <code class="language-plaintext highlighter-rouge">QProcess.startDetached</code> — farad 수명과 무관, 종료 대기 없음.</li>
  <li><strong>에이전트 산출물 계약 — self-contained HTML</strong>: 폐쇄망이라 CDN 참조는 그냥 깨진다.
HTML 산출 스킬/프롬프트에 "인라인 CSS/JS, 외부 호스트 참조 금지, 이미지는 data: URI" 규약을
명시한다 (Claude Code Artifact의 CSP 정책과 같은 원리). 산출 위치는 세션 아티팩트 폴더.</li>
  <li>UI 아이디어(후속): 타임라인의 <code class="language-plaintext highlighter-rouge">.html</code> 파일 경로에 "Open in hview" 액션.</li>
</ul>

<h3 id="56-저사양원격-x11-대응">5.6 저사양·원격 X11 대응</h3>

<ul>
  <li><code class="language-plaintext highlighter-rouge">QTWEBENGINE_CHROMIUM_FLAGS="--disable-gpu"</code> — 원격 X11/VNC에서 GPU 경로 차단,
소프트웨어 래스터라이저 사용. 래퍼(<code class="language-plaintext highlighter-rouge">toolkits/hview/env/hview.env</code>)에 기본값으로 굽는다.</li>
  <li>Chromium 샌드박스는 user namespaces 필요 — HPC 커널 설정에 따라 막혀 있으면
<code class="language-plaintext highlighter-rouge">QTWEBENGINE_DISABLE_SANDBOX=1</code> 폴백 (내부 신뢰 콘텐츠 전용 뷰어이므로 수용 가능). §7 확인.</li>
  <li>신뢰 모델: 에이전트가 만든 HTML의 JS를 로컬 실행하는 것은 에이전트가 만든 코드를 실행하는
것과 동일한 신뢰 수준. off-the-record라 영속 흔적도 없다.</li>
</ul>

<h2 id="6-plan-b-상세">6. Plan B 상세</h2>

<p><strong>B1. firefox 임시 프로필 래퍼 (zero-install 폴백)</strong></p>

<div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">profile</span><span class="o">=</span><span class="si">$(</span><span class="nb">mktemp</span> <span class="nt">-d</span><span class="si">)</span> <span class="o">&amp;&amp;</span> firefox <span class="nt">-no-remote</span> <span class="nt">-profile</span> <span class="s2">"</span><span class="nv">$profile</span><span class="s2">"</span> <span class="s2">"file://</span><span class="nv">$PWD</span><span class="s2">/report.html"</span>
</code></pre></div></div>

<ul>
  <li>장점: 설치할 것이 없다 (RHEL에 firefox ESR 기본 존재). 락 문제도 임시 프로필로 우회.</li>
  <li>단점: 기동 느림(프로필 초기화 포함), RAM 큼, 브라우저 UI 그대로(뷰어 UX 아님),
임시 프로필 정리 책임, Windows 별도 처리. → hview 배포 전 임시 브리지 스크립트로만 가치.</li>
</ul>

<p><strong>B2. pywebview</strong> — §4 참조. WebKitGTK(RHEL 8은 2.4x대 백포트)로 A보다 가볍지만,
OS별 엔진 분화 + GTK 의존이 결정적 감점. QtWebEngine 배포가 불가능해질 때만 재검토.</p>

<h2 id="7-리스크--확인-필요">7. 리스크 / 확인 필요</h2>

<ul class="task-list">
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" /><strong>farad 실배포 환경에서 QtWebEngine import 확인</strong>:
    <code class="language-plaintext highlighter-rouge">./.venv/bin/python -c "from PySide6.QtWebEngineWidgets import QWebEngineView"</code> —
    메타패키지가 Addons를 실제로 끌고 왔는지, wheelhouse에 Addons wheel이 있는지.</li>
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" />HPC 원격 X11/VNC에서 <code class="language-plaintext highlighter-rouge">--disable-gpu</code> 렌더 확인 (검은 화면·크래시 여부).</li>
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" /><code class="language-plaintext highlighter-rouge">sysctl user.max_user_namespaces</code> — Chromium 샌드박스 동작 여부, 폴백 필요성.</li>
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" />off-the-record 실행 중 NFS 홈에 잔여 쓰기 없는지 확인 (fontconfig/dconf 캐시 등,
    <code class="language-plaintext highlighter-rouge">strace -e trace=file</code> 샘플링).</li>
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" />RAM 실측: 창 1개/3개 동시, 콜드 스타트 시간 (NFS 위 Python+PySide6 import 비용 포함 —
    farad와 같은 조건이므로 대략 예측 가능하나 뷰어는 lazy import로 최소화).</li>
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" />wheelhouse에 PySide6-Addons wheel 존재 확인 (farad용으로 이미 반입돼 있으면 신규 반입 0).</li>
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" />QtWebEngine wheel의 시스템 라이브러리 전제(nss, libxkbcommon, xcb 계열) 노드 존재 확인 —
    firefox가 깔린 노드면 대부분 충족. (참고: webkit2gtk3/gtk3 존재 여부도 확인해두면
    Plan B 실현 가능성 판단에 유용.)</li>
  <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" />hview 전용 venv 디스크 실측(수백 MB 예상) + farad 설치 머신에서 farad venv 공유 설치로
    갈지 결정 (공유 시 디스크 절약 vs 버전 커플링).</li>
</ul>

<h2 id="8-로드맵">8. 로드맵</h2>

<ul>
  <li><strong>MVP</strong>: 단일창 뷰어 + off-the-record + <code class="language-plaintext highlighter-rouge">--watch</code> + 단축키(zoom/find/close) + hrun toolkit.</li>
  <li><strong>Phase 2</strong>: per-host 단일 인스턴스(로컬 소켓 <code class="language-plaintext highlighter-rouge">$XDG_RUNTIME_DIR/hview.sock</code> — host-local이라
멀티호스트 안전; 창 N개가 엔진 1개 공유 → RAM 절약), <code class="language-plaintext highlighter-rouge">Ctrl+P</code> PDF 내보내기(printToPdf),
farad 타임라인 "Open in hview" 액션.</li>
  <li><strong>Phase 3 (아이디어만)</strong>: QWebChannel 브릿지로 "회신 가능한 HTML" — 에이전트가 만든 폼에서
사용자가 선택하면 JSON으로 저장 → 에이전트가 읽는 interactive AskUserQuestion식 워크플로우.</li>
  <li><strong>Non-goals</strong>: 탭, 주소창, 북마크, 히스토리, 확장, 일반 웹 브라우징. 뷰어의 선을 지킨다.</li>
</ul>

<p><strong>유지보수 모델 — 일회성 빌드에 가깝다.</strong> 셸은 안정 API 위의 수백 줄이라 만들고 나면 손댈
일이 거의 없다. 엔진 업데이트는 farad의 PySide6 범프에 편승하므로 별도 케이던스가 없고,
폐쇄망에서 자기 에이전트 산출물만 여는 뷰어라 Chromium 보안 패치를 급하게 쫓을 이유도 없다.
이 "일회성"이 유지되는 조건은 Non-goals를 지키는 것 — Phase 3 브릿지처럼 프로토콜 표면이
생기는 기능은 필요가 증명될 때 별도 결정으로.</p>

<h2 id="9-결론">9. 결론</h2>

<p>"가벼운 브라우저"의 실체는 <strong>엔진 선택이 아니라 상태 정책</strong>이다. 엔진은 폐쇄망 pip 배포
채널에 실리는 유일한 실엔진인 QtWebEngine으로 사실상 결정되고(PySide6는 그 배송 수단),
off-the-record 셸로 감싸면 Chrome/Firefox의 무게·프로필·NFS 락 문제가 전부 설계 수준에서
소거된다. <code class="language-plaintext highlighter-rouge">hrag</code> 패턴의 독립 pip 패키지 + hrun toolkit 래퍼로
배포하면 HPC(<code class="language-plaintext highlighter-rouge">hrun hview</code>)와 Windows(farad venv) 양쪽을 같은 코드로 커버한다.
MVP는 수백 줄 규모 — 가장 큰 선행 작업은 §7의 배포 환경 검증이다.</p>]]></content><author><name>hvppycoding</name></author><category term="farad" /><summary type="html"><![CDATA[경량 HTML 뷰어 검토 — 에이전트 HTML 산출물용 별도 프로세스 뷰어 (가칭 hview)]]></summary></entry><entry><title type="html">Farad 스케줄 실행에 허용 도구 알려주기 — Claude Code 벤치마크와 구현 계획</title><link href="https://hvppycoding.github.io/farad/farad-scheduled-run-permission-context/" rel="alternate" type="text/html" title="Farad 스케줄 실행에 허용 도구 알려주기 — Claude Code 벤치마크와 구현 계획" /><published>2026-07-03T13:32:58+09:00</published><updated>2026-07-03T13:32:58+09:00</updated><id>https://hvppycoding.github.io/farad/farad-scheduled-run-permission-context</id><content type="html" xml:base="https://hvppycoding.github.io/farad/farad-scheduled-run-permission-context/"><![CDATA[<h1 id="farad-스케줄-실행에-허용-도구-알려주기--claude-code-벤치마크와-구현-계획">Farad 스케줄 실행에 허용 도구 알려주기 — Claude Code 벤치마크와 구현 계획</h1>

<p>Farad의 스케줄 실행(예약 작업)은 사람이 지켜보지 않는 <strong>무인 실행</strong>이다. 그래서 도구
승인 프롬프트를 띄울 수 없고, 미리 승인해 둔 패턴(<code class="language-plaintext highlighter-rouge">permissions.allow</code>)에 맞는 도구만
자동으로 실행된다. 문제는 <strong>모델이 이 사실을 전혀 모른 채 실행된다</strong>는 것. 이 글은 그
문제를 정리하고, Claude Code(CC) 원본 소스를 벤치마크한 결과와 Farad에 넣을 변경 두
가지를 설명한다.</p>

<h2 id="1-지금-무슨-문제가-있나">1. 지금 무슨 문제가 있나</h2>

<p>스케줄 태스크가 실행되면 Farad는 이렇게 동작한다.</p>

<ol>
  <li>격리된 SCHEDULED 세션을 만들고 권한 모드를 <code class="language-plaintext highlighter-rouge">dont_ask</code>로 연다
(전체 권한 태스크면 <code class="language-plaintext highlighter-rouge">bypass</code>).</li>
  <li>태스크의 <code class="language-plaintext highlighter-rouge">settings.json</code>에 미리 승인해 둔 <code class="language-plaintext highlighter-rouge">permissions.allow</code> 패턴
(예: <code class="language-plaintext highlighter-rouge">Bash(git status:*)</code>)에 맞는 도구 호출만 자동 허용. 나머지 승인 필요 도구는
<strong>자동 거부</strong>.</li>
  <li>모델에게 가는 첫 메시지는 "You are executing a scheduled task" + 태스크
이름/설명뿐. <strong>무인 실행이라는 것도, 어떤 도구가 허용돼 있는지도 알려주지 않는다.</strong></li>
</ol>

<p>그리고 거부당했을 때 모델이 받는 문구가 이게 전부다:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>DONT_ASK mode blocked tool requiring user approval.
</code></pre></div></div>

<p>이 한 줄에는 (a) 뭐가 허용돼 있는지, (b) 재시도해 봐야 소용없다는 것, (c) 그럼 뭘
해야 하는지가 전부 빠져 있다. 그 결과 모델은 이런 삽질을 한다:</p>

<ul>
  <li><code class="language-plaintext highlighter-rouge">git push</code> 거부 → <code class="language-plaintext highlighter-rouge">git push origin main</code>으로 바꿔서 재시도 → 또 거부 → 또 변형…</li>
  <li>승인이 필요 없는 순수 read-only 작업으로 끝낼 수 있는데도 엉뚱한 우회를 시도</li>
  <li>매 거부가 LLM 왕복 하나씩을 태움 — 30분 hard timeout 예산과 토큰을 낭비</li>
</ul>

<p>아이러니하게 이 낭비는 이미 <strong>측정까지 되고 있다</strong>. 실행 기록에 거부된 도구 호출
수(<code class="language-plaintext highlighter-rouge">denied_tool_count</code>)가 amber 칩으로 표시된다. 측정만 하고 예방은 안 하고 있는 셈.</p>

<h2 id="2-claude-code는-어떻게-하나-벤치마크">2. Claude Code는 어떻게 하나 (벤치마크)</h2>

<p>Farad는 도구 이름·스키마·동작을 CC 스펙에 맞추는 걸 원칙으로 하므로, 구현 전에 CC
원본 소스(<code class="language-plaintext highlighter-rouge">ref_src/claude-code-sourcemap/restored-src/</code>)를 확인했다. 요약:</p>

<table>
  <thead>
    <tr>
      <th>질문</th>
      <th>CC의 답</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>허용 규칙을 프롬프트에 미리 열거하나?</td>
      <td><strong>안 한다</strong></td>
    </tr>
    <tr>
      <td>거부 메시지는?</td>
      <td>도구명 + 이유 + 행동 지침이 담긴 <strong>정교한 문구</strong></td>
    </tr>
    <tr>
      <td>권한 모드를 프롬프트로 알려주는 선례는?</td>
      <td>있다 (plan/auto 모드 지시 블록)</td>
    </tr>
    <tr>
      <td>"부족한 권한을 마지막에 보고하라"는 지시?</td>
      <td>있다 (auto 모드 거부 메시지)</td>
    </tr>
  </tbody>
</table>

<h3 id="사전-열거는-안-한다">사전 열거는 안 한다</h3>

<p><code class="language-plaintext highlighter-rouge">getAllowRules()</code>가 모으는 허용 규칙은 권한 판정과 설정 UI에서만 쓰이고, 시스템
프롬프트나 리마인더 텍스트 어디에도 들어가지 않는다. CC의 headless 실행(<code class="language-plaintext highlighter-rouge">-p</code>/SDK)은
호출자가 <code class="language-plaintext highlighter-rouge">--allowedTools</code>로 도구를 직접 큐레이션하는 사용 사례라, 모델에게는 거부
메시지로만 알려준다.</p>

<h3 id="대신-거부-메시지에-전부-건다">대신 거부 메시지에 전부 건다</h3>

<p>CC의 don't ask 모드 거부 문구 (<code class="language-plaintext highlighter-rouge">utils/messages.ts:237</code>):</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Permission to use ${toolName} has been denied because Claude Code is running
in don't ask mode. IMPORTANT: You *may* attempt to accomplish this action
using other tools that might naturally be used to accomplish this goal, e.g.
using head instead of cat. But you *should not* attempt to work around this
denial in malicious ways ... If you believe this capability is essential to
complete the user's request, STOP and explain to the user what you were
trying to do and why you need this permission.
</code></pre></div></div>

<p>포인트가 세 개다.</p>

<ul>
  <li><strong>어떤 도구가, 왜</strong> 거부됐는지 명시한다.</li>
  <li><strong>합리적 우회는 허용, 의도를 뚫는 우회는 금지</strong>를 구분해 준다.</li>
  <li>사용자가 없는 컨텍스트(서브에이전트)용 변형은 어미가 바뀐다: "Try a different
approach or <strong>report the limitation</strong> to complete your task" — 멈추고 기다리는
대신 보고하고 계속하라는 것.</li>
</ul>

<p>auto 모드 거부 메시지에는 이런 지시까지 있다 (<code class="language-plaintext highlighter-rouge">messages.ts:273</code>):</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>At the end of your session, recommend what permission rules to add
so you don't get blocked again.
</code></pre></div></div>

<p>그리고 "read-only 작업은 여전히 쓸 수 있다"고 명시해 주는 선례도 있다
(<code class="language-plaintext highlighter-rouge">messages.ts:296</code>): "reading files, searching code, and other read-only
operations … can still be used."</p>

<h2 id="3-그래서-farad에는-이렇게-넣는다">3. 그래서 Farad에는 이렇게 넣는다</h2>

<p>변경은 두 곳이다.</p>

<h3 id="변경-1--스케줄-시작-프롬프트에-무인-실행--권한-안내-확장">변경 1 — 스케줄 시작 프롬프트에 무인 실행 + 권한 안내 (확장)</h3>

<p><code class="language-plaintext highlighter-rouge">_compose_prompt()</code>가 만드는 첫 메시지에 이런 섹션을 붙인다 (시안):</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>This is an unattended scheduled run. No user is available to approve
tool calls or answer questions (interactive tools like AskUserQuestion
will fail immediately).

Tool permissions for this run:
- Read-only tools (Read, Grep, ...) are always available.
- These patterns are pre-approved and run automatically:
    Bash(git status:*)
    Bash(pytest:*)
- Any other tool call that would need approval is denied automatically.
  Do not retry a denied call with variations. If a missing permission
  blocks the task, note it in your final summary so the user can
  pre-approve it for future runs.
</code></pre></div></div>

<p>전체 권한(bypass) 태스크면 목록 대신 "모든 도구가 사전 승인됨" 한 줄만.</p>

<p>이건 CC에는 없는 <strong>의도적 확장</strong>이다. 근거:</p>

<ul>
  <li>CC headless는 호출자가 도구를 직접 고르는 SDK 사용 사례지만, Farad 스케줄은 최종
사용자가 만든 태스크 + 태스크별로 누적된 패턴이라 fire 시점에 정확한 목록이 공짜로
나온다.</li>
  <li>Farad의 주 사용 모델(GLM-5.1, Kimi K2.6)은 Claude보다 암묵적 제약 추론이 약해서
명시적 열거의 이득이 크다.</li>
  <li>문구 재료(read-only 안내, "pre-approved" 뉘앙스)는 전부 CC 선례에서 조달했다.</li>
</ul>

<p>주의해서 지킬 것: "목록에 있는 것<strong>만</strong> 쓸 수 있다"고 쓰면 안 된다. <code class="language-plaintext highlighter-rouge">dont_ask</code>에서도
read-only 도구는 패턴 없이 항상 허용이라, 잘못 쓰면 모델이 멀쩡한 Read/Grep까지
피하는 반대 방향 삽질이 생긴다. 그래서 3단(read-only 자유 / 패턴 사전 승인 / 나머지
자동 거부) 구조가 필수다.</p>

<h3 id="변경-2--거부-메시지를-cc-수준으로-매칭">변경 2 — 거부 메시지를 CC 수준으로 (매칭)</h3>

<p><code class="language-plaintext highlighter-rouge">core/permission.py</code>의 두 문구를 CC 패턴으로 교체한다. Before:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>DONT_ASK mode blocked tool requiring user approval.
</code></pre></div></div>

<p>After (시안 — 도구명 포함 + 이유 + 행동 지침):</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Permission to use Bash has been denied: this is an unattended scheduled
run and no user is available to approve it. You may accomplish the goal
with other permitted tools in reasonable ways, but do not retry the same
call or bypass the intent of this restriction. If this capability is
essential, note it in your final summary and recommend what permission
rules the user should pre-approve for future runs.
</code></pre></div></div>

<p>어미는 CC의 STOP-and-wait("멈추고 사용자에게 물어라")가 아니라 서브에이전트/auto
모드 계열("보고하고 계속하라")을 쓴다 — 스케줄 실행에는 기다릴 사용자가 없기
때문이다. 이 "요약에 보고 + 규칙 추천" 지시는 기존 UX와 정확히 맞물린다: 사용자는
실행 기록에서 거부 칩을 보고 → 요약에서 뭐가 필요했는지 읽고 → "Run now to
pre-approve"로 그 패턴을 승인해 주면 다음 실행부터 안 막힌다.</p>

<h3 id="안-하기로-한-것">안 하기로 한 것</h3>

<ul>
  <li><strong>거부 N회 누적 시 run abort</strong>: CC도 don't ask 모드 거부에는 한도를 안 건다
(auto 모드 classifier 거부에만 연속 3회/총 20회). Farad에는 이미 30분 watchdog +
거부 수 집계 + 연속 실패 자동 비활성화가 있어서 지금은 불필요.</li>
  <li><strong>매 턴 리마인더 재주입</strong>: CC의 plan/auto 모드는 매 턴 full/sparse 지시 블록을
주입하지만, 스케줄 run은 단일 submit이라 시작 프롬프트 1회로 충분하다.</li>
</ul>

<h2 id="4-변경-지점-요약">4. 변경 지점 요약</h2>

<table>
  <thead>
    <tr>
      <th>항목</th>
      <th>위치</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>시작 프롬프트 preamble</td>
      <td><code class="language-plaintext highlighter-rouge">farad/scheduler/executor.py</code> — <code class="language-plaintext highlighter-rouge">_compose_prompt()</code></td>
    </tr>
    <tr>
      <td>사전 승인 패턴 조회</td>
      <td><code class="language-plaintext highlighter-rouge">ScheduleStore.permission_allow(task_id)</code></td>
    </tr>
    <tr>
      <td>거부 메시지 2종</td>
      <td><code class="language-plaintext highlighter-rouge">farad/core/permission.py</code> (ask→deny 변환부)</td>
    </tr>
    <tr>
      <td>CC 근거</td>
      <td><code class="language-plaintext highlighter-rouge">restored-src/src/utils/messages.ts:216-298</code>, <code class="language-plaintext highlighter-rouge">utils/permissions/permissions.ts:503-517, 944-951</code></td>
    </tr>
  </tbody>
</table>]]></content><author><name>hvppycoding</name></author><category term="farad" /><summary type="html"><![CDATA[Farad 스케줄 실행에 허용 도구 알려주기 — Claude Code 벤치마크와 구현 계획]]></summary></entry></feed>